# 複製成 .env 並填入。.env 已在 .gitignore,不會被 commit。
# 只需要填你實際要用的那一家。

ANTHROPIC_API_KEY=your-key-here

# 用 Grok 的話:
# XAI_API_KEY=your-key-here
# EVERLIVING_PROVIDER=grok

# 覆蓋預設模型(可選)。各家的 model ID 會變,404 時去對應 console 查。
# EVERLIVING_MODEL=claude-sonnet-5
