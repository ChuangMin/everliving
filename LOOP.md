# LOOP.md — 想 → 寫 → 查

> **怎麼讓它動起來**——開一個新 session,打這一句(不要多講):
>
> ```
> 讀 LOOP.md,照它說的做這一輪。做完更新「現在輪到」交棒。
> ```
>
> Claude Code 可以改打 `/turn`(同一句話,存在 `.claude/commands/turn.md`)。
> 別家 agent(Codex / Qwen)照上面那句原樣貼。
> **要它自己一直跑**:`/loop 30m 讀 LOOP.md,照它說的做這一輪。做完更新「現在輪到」交棒。`
> ——但它每 5 輪會被人類心跳擋下來,那是故意的(見設計文件第十九節補丁 1)。
>
> **這句話刻意寫得很短。** 交棒給誰、能碰什麼、怎樣算做完,全部該由 `LOOP.md` 跟 `AGENTS.md` 講清楚;
> 要靠 prompt 補充說明,就表示那兩份文件沒寫好——**該修的是文件,不是這句話**。
>
> ---
>
> **這份是開發循環的交接板。** 角色契約在 `AGENTS.md`,理由在設計文件第十九節。
> **每個角色只准寫自己那一區**,寫別人的區就是違約。
> 動之前跑 `python tools/loop_check.py`,收尾再跑一次。
>
> **這份檔案不會長大。** 排隊中上限 3 則、進行中上限 1 則,滿了就是重排跟砍,不是再加。
> 歷史往 `PROGRESS.md` 走,不留在這裡。

**現在輪到**:查(auditor)   **上一棒**:反思 @2026-08-07
**本輪次**:29   **距離下次反思**:3 輪   **距離人類心跳**:⛔ **逾期 24 輪。人類 2026-08-07 明示照跑,
但這個數字本身現在是一則發現**——見〈反思〉第四點:整段都在同一個 session 裡跑,沒有外部錨

> **「暫停」沒有自己的欄位,這是刻意的。** 一開始這裡寫「⏸ 暫停中」,`loop_check` 立刻報違規——
> 「現在輪到」只接受四種角色。與其為暫停多開一個狀態,不如**讓它停在下一棒,
> 由心跳違規本身當暫停訊號**:少一個狀態、少一條規則,而且忘不掉——
> 因為 `loop_check` 每次都會叫。
>
> **loop 停在這裡是設計,不是故障**(設計文件第十九節補丁 1)。
> auditor 驗收的是自己隊伍的產出、對著自己隊伍寫的判準,沒有外部錨,標準會隨輪次軟化。
> 唯一的錨是**人類真的打開一次看它做了什麼**。
>
> **要解開兩步**:
> 1. 真的開一次畫面 —— `python -m everliving.web --provider ollama --db play.db`
>    (五分鐘一輪;想快就用 Groq,但**先看 `NOW.md`〈等人類決定〉那題**)
> 2. 在下面「輪次記錄」補一行含 `人類心跳:已開`
>
> 補完之後下一棒是**寫(builder)**,撿「排隊中」最上面那則。

## 退回重做

<!-- auditor 放,builder 優先撿。格式同「排隊中」,外加退回理由 -->

(空 —— 上一則已於第 13 輪修完,見「待驗收」)

<!-- 修完的原始退回內容保留在下面,縮排避免被 parser 數成一則:

  **[退回] `scene`/`action` 持久化 —— 存得對,但漏到玩家畫面上了。**
      **退回理由(有證據,不是感覺)**:
      - `src/everliving/web.py:156-159` 把 `get_assets()` 的**每一筆**原封不動放進 `assets` payload
      - `src/everliving/static/index.html:988`:`li.textContent = a.kind+' · '+a.ref`
        ——**每一筆 asset 都會被渲染成一個文字標籤**
      - 合起來:從這個 commit 之後,**每一則敘事旁邊都會多出 `scene · 工作間`、`action · 焊接`
        兩個像 debug 訊息的小標籤**
      - `index.html:185` 那段註解寫著「Nothing has assets yet, so this stays absent」
        ——**那個 UI 本來是空的**,現在每一拍都會亮兩個
      - 而且 **`scene` 早就以獨立欄位送出去了**(payload 頂層本來就有 `"scene"`),
        所以那兩筆是**重複的、而且有害的重複**

      **不是資料層錯了,是它漏進了展示層。** 存下來是對的(這一則的目的就是讓它可觀測),
      但可觀測**不等於要給玩家看**。

      **判準(修完要達到)**:
      1. 玩家畫面上不再出現 `scene ·` / `action ·` 標籤
      2. `story_assets` 裡照樣存得到(不要為了畫面乾淨就不存了,那等於退回原點)
      3. **加一個測試釘住這件事**——`tests/test_web.py` 那類,斷言 `assets` payload 裡
         不含 `scene`/`action`。**沒有測試的話下次還會漏**
      4. **改完要去問瀏覽器**(`node tests/page_check.js` 而且 `python tools/shot.py` 真的看一眼)
         ——`NOW.md` 收尾檢查第 4 條。**我這次是讀 code 讀出來的,還沒有真的渲染過**

      **怎麼修留給 builder**,但方向提示:`scene` 是**元資料**不是 asset,
      比較可能該在 `web.py` 那層就不要送,而不是叫前端過濾。
-->

<!-- ⚠️ 這裡原本有一個 `## 待驗收(第 13 輪 builder 交)` 標題,由第 14 輪 auditor 驗收通過後撤掉。
     **撤掉不只是因為驗完了,是因為它擺錯位置而且檢查器看不見**——見「驗收結果」倒數第二則。
     待驗收只有下面那一個,標題不要加後綴。

  **[退回重做] `scene`/`action` 不再漏到玩家畫面上 —— 修好了。** **196 passed(原 194,+2)。**
      **動過的檔案**:`src/everliving/db.py`(新增 `SCENE_KINDS` 與 `get_media_assets`)、
      `src/everliving/web.py`(三處 `get_assets` → `get_media_assets`)、
      `tests/test_web.py`(新增 2 個測試)。**沒動前端一個字。**

      **照 auditor 的方向修的**:在 `web.py` 那層就不送,不是叫前端過濾。
      `get_assets` 保留原樣給「要看完整記錄」的人用——**那正是這整件事的目的**;
      `get_media_assets` 濾掉 `SCENE_KINDS`,展示端一律走它。

      **判準四條的對帳,包含沒做到的那一條**:
      1. ✅ 玩家畫面不再有 `scene ·` / `action ·` —— payload 層由測試釘死
      2. ✅ `story_assets` 照樣存得到 —— 兩個新測試都**同時斷言 DB 裡還在**
         (`kinds == {"scene","action"}`),不是靠不存來讓畫面乾淨
      3. ✅ 加了測試:`test_the_scene_a_beat_was_drawn_in_never_reaches_the_player`
         與 `test_the_offline_beat_hides_its_scene_the_same_way`——**兩條路都測**,
         兩個都先看到紅(漏出來的 `{'kind':'scene','ref':'配電所'}` 就在失敗訊息裡)
      4. ⚠️ **這一條只做到一半,請 auditor 自己判斷夠不夠**:
         `node tests/page_check.js` 全過、`python tools/shot.py --size 1440x820` 也跑了、
         版面沒被改壞。**但那張截圖證明不了漏洞修好了**——沒有伺服器就沒有 beat,
         沒有 beat 就沒有 asset 可渲染,所以畫面上本來就不會有標籤。
         **真正的證據是 payload 層的兩個測試 + 渲染那段程式我一個字都沒動。**
         要百分之百的視覺證據,得起一個有 beat 的伺服器再截一次——**我沒做,請你判斷要不要退回。**
-->

## 排隊中

<!-- planner 唯一能寫的區。每則:- [階N] 做什麼 判準:怎樣算做完 -->

> **[已由循環外做掉,2026-08-06]** 原本排在最上面的「繁體中文那條規則沒有執行者」已完成:
> `src/everliving/script_check.py` + `tests/test_script_check.py`,186 passed(原 178)。
> 拿真的那份 `playtests/2026-08-06-ollama.txt` 跑過,**只抓到 `没见过` 三個字,
> 其餘八十行繁體中文零假陽性**。人類說「繼續開發」,而 loop 卡在心跳,
> 所以是用單獨一輪的一般 session 做的(`AGENTS.md` 工作流程)。
> **這裡把它劃掉是為了讓下一個 builder 不要重做**,不是 planner 在動這一區。

> **[第 9 輪砍掉]** 原本有一則 `[階6]` 要做「`--agents-append-only`,用 `git diff` 抓 `AGENTS.md` 被刪的行」。
> **砍掉,因為第 8 輪當場證明那個判準是錯的**:把兩行插進既有那一行的中間,
> `git diff --numstat` 顯示 `5 1`——有 1 行「刪除」,但**一個字都沒被移除**。
> 照那個判準做出來的檢查,第一次跑就會抓到一個不存在的違規。
> **這正是〈第四題〉那節說的 proxy 跟本意分家**,而它自己就是實例。
> 真要做,判準得改成**比對內容有沒有被拿掉**,不是數行。**但那不是這一輪該塞的,先砍。**

<!-- 以下已於第 10 輪做完,移到「待驗收」。
     注意這幾行**故意不用 `- ` 開頭**:`loop_check` 的 parser 只看行首,**不懂 HTML 註解**,
     第一版留成 `- ` 開頭時它照樣數成一則,佇列變 3 則、2 則階 6,配額當場誤報。
     要劃掉一則就整則刪、或縮排,不要以為註解掉就不算數。

  [階4] **把 `scene` / `action` 跟敘事一起存下來。** 第 7 輪 auditor 查證的結論:
      這兩個欄位只活在一次 HTTP 回應裡——`memory_events` 沒這欄、`story_assets` 0 筆、
      `llm_calls` 只有 token 數(三個 DB 含備份都查了)、`everliving.log` 零命中,
      連 `--debug` 都只記 prompt 跟玩家訊息、**不記模型的回覆**(`llm.py:168-169`)。
      **所以「場景跟台詞對不上」連被觀察的資格都沒有,更談不上驗證修好了沒。**
      判準:`memory_events` 或 `story_assets` 存得下 `scene`/`action` 並跟敘事對得起來;
      **先寫測試看它紅**;`python -m pytest -q` 不減少通過數(現況 188)。
      **舊資料不用回填**(填不出來,那是憑空捏造);**不要順手做一致性檢查**——
      這一則只負責讓它「看得見」,看得見之後才輪到判斷對不對

      判準:`memory_events` 或 `story_assets` 存得下 `scene`/`action` 並跟敘事對得起來 ✅
-->

<!-- 第 16 輪 builder 撿走,見「進行中」。原文保留在那裡,不在這裡重複。

  [階1] **世界時鐘:一個不管玩家在不在都會走的數字。** 人類 2026-08-07 答了「單調」,
      四個軸全中,而**「玩十次跟玩一次一樣」是根**——沒有東西在累積,場景就沒有理由變、
      按鈕就沒有意義、他的故事就沒有新素材。
      設計文件第十一節已經有現成的料:**汙染是「單向惡化的背景壓力,一個不管玩家做什麼都在走的時鐘」**。
      不用發明世界觀,讓它真的在跑就好。
      判準:(1) 存得下來,而且**只會往一個方向走**;(2) **餵進離線 prompt**,
      敘事讀得出壓力有變(不是硬塞一句「汙染是 37」);(3) 先寫測試看它紅;
      (4) `python -m pytest -q` 不減少通過數(現況 194)。
      **不要做資源管理介面、不要做建造、不要讓玩家能直接改它**——那是後面子專案的事
-->

<!-- 第 20 輪 builder 做掉,見「待驗收」。原文留著給 auditor 對判準:

  [階5] **打開來是一片空白的那一半,要有下場。** **服務的是卡住的「AUTO ROUTER」那題,
      而且兩條路都需要**——路由決定的是**打給誰**,而任何一家模型都會偶爾吐出解析不出來的東西。
      今天量到的:**四次真呼叫兩次產不出可用 JSON**(一次截斷、一次全空),
      證據 `playtests/2026-08-07-world-clock.txt`。
      全空那次 `parse_offline_response` 會回傳 `narrative=""`,
      **然後那個空敘事被當成一則記憶寫進永久歷史,玩家看到的是一片空白**。
      **這是里程碑 0 的成敗判準本身在失敗**:「打開來讀到那段敘事」讀到的是空的。
      判準:(1) 拿不到可用敘事時**重試一次**;(2) 仍然失敗就丟 `LLMRefusal`——
      `web.py:312` **已經在接這個例外並顯示給玩家**,所以玩家看到的是理由不是空白;
      (3) **空敘事永遠不准寫進 `memory_events`**(寫進去就是永久的,而歷史是 append-only);
      (4) 先寫測試看它紅;(5) `python -m pytest -q` 不減少通過數(現況 210)。
      **不要順手改 `MAX_TOKENS`、不要換模型、不要做 provider 選擇**——那才是 AUTO ROUTER 的題
-->

<!-- 第 22 輪 builder 做掉,見「待驗收」。原文留著給 auditor 對判準:

  [階1] **開場不要永遠是工作間。** `offline.py:47-51` 刻意把開場定在工作間
      (「不用一句台詞就說明他是誰」),那個理由在第一次成立、在第十次變成「畫面都一樣」。
      判準:(1) 開場場景由**世界時鐘 + 既有狀態**決定,不是隨機(隨機會變成沒有意義的閃爍);
      (2) **連開三次至少看到兩種不同的開場**;(3) 先寫測試看它紅;
      (4) **改完要去問瀏覽器**(`node tests/page_check.js` + `python tools/shot.py` 真的看一眼)。
      **依賴上面那則**,時鐘沒做完不要開始
-->

<!-- 第 26 輪 builder 做掉,見「待驗收」。原文留著給 auditor 對判準:

  [階1] **把預設 provider 換成 `auto`,而且先拆掉擋路的那顆地雷。**
      `--provider auto` 已經能用,但**預設**還是 `anthropic`,所以沒有帳號的人 clone 下來
      仍然是啟動就結束。**換預設值不是改一行**:第 24 輪試過一次,整套測試當場卡死——
      `tests/test_provider_selection.py:76` 設了一把假的 `XAI_API_KEY`,router 於是**真的把
      Grok client 建起來並發出網路呼叫**。
      **地雷本身比預設值重要:這套測試在 provider 建得起來的時候會打真的 API。**
      判準:(1) **先加一道機制讓測試不可能打出真的網路呼叫**(conftest 層級,不是靠每個測試自律)
      ——先寫一個「會打出去就失敗」的測試看它紅;(2) 再把 `DEFAULT_PROVIDER` 換成 `auto`
      並更新 `test_defaults_to_anthropic`;(3) `python -m pytest -q` 不減少通過數(現況 228)
      **而且跑得完**(卡死不算通過);(4) 拿一台**沒有任何金鑰**的環境變數狀態驗一次落到 ollama
-->

- [階6] **`loop_check` 的配額規則量錯對象。** 「階 5-7 不得超過半數」的用意是限制 planner
      **這一輪加了什麼**,但它量的是佇列**此刻剩下什麼**。佇列在「寫」耗盡、要到兩棒後的「想」才補,
      所以**整個「查」都坐在紅燈上,而沒有人做過任何決定**——第 6、7 兩輪都是這樣紅的。
      判準:先寫一個測試重現「佇列被撿到剩 1 則階 6 → 現在會誤報」(要先看到它紅),
      改完轉綠,`python -m pytest -q` 不減少通過數。
      **怎麼改由 builder 判斷**(候選:佇列 <2 則時不套配額 / 改成量「本輪新增」),
      但**改完要能解釋新的量法量的是不是規則講的那件事**——`AGENTS.md`〈第四題〉

## 進行中

<!-- builder,最多一則 -->

(空 —— 第 16 輪做完,移到「待驗收」。)

<!-- 第 16 輪撿的那則,做完了,原文留著給 auditor 對判準:

  [階1] **世界時鐘:一個不管玩家在不在都會走的數字。**(第 16 輪 builder 撿,原文照抄自「排隊中」)
      人類 2026-08-07 答了「單調」,四個軸全中,而**「玩十次跟玩一次一樣」是根**——
      沒有東西在累積,場景就沒有理由變、按鈕就沒有意義、他的故事就沒有新素材。
      設計文件第十一節已經有現成的料:**汙染是「單向惡化的背景壓力,一個不管玩家做什麼都在走的時鐘」**。
      不用發明世界觀,讓它真的在跑就好。
      判準:(1) 存得下來,而且**只會往一個方向走**;(2) **餵進離線 prompt**,
      敘事讀得出壓力有變(不是硬塞一句「汙染是 37」);(3) 先寫測試看它紅;
      (4) `python -m pytest -q` 不減少通過數(現況 **196**,判準寫的 194 是排它那一輪的數字)。
      **不要做資源管理介面、不要做建造、不要讓玩家能直接改它**——那是後面子專案的事
-->

## 待驗收

<!-- builder 交棒。寫清楚動了哪些檔案、怎麼驗 -->

- **[階7] H-1 現在答得出來了,而且答案是「還沒過」。** **234 passed(原 230,+4;retention 另 6 個)。**
      **動過的檔案**:`src/everliving/db.py`(新增 `visits` 表、`record_visit`、`get_visits`)、
      `src/everliving/web.py`(`open()` 記一次來訪)、`tools/retention.py`(**新工具**)、
      `tests/test_visits.py` + `tests/test_retention.py`(新增 4 + 6 個測試)。

      **階 7 的正當性(把決定變便宜)**:人類設了目標「最多人玩」但沒答「要量哪一個」。
      **這一則不替他答**——它把候選項目印在真實數字旁邊,**讓他打開就能指**。

      **⚠️ 做之前先發現了一件更重要的事,而且它是同一個形狀的第三次:**
      `player_state` 是**一列、每次來訪覆蓋掉上一次**(`db.py` 的 `ON CONFLICT DO UPDATE`)。
      **所以系統記得「他在」,從來沒記過「他來了」**——兩次跟二十次是同一列。
      **H-1「他隔天有沒有再打開」在結構上無法回答。**
      前兩次是:`scene`/`action` 只活在一次 HTTP 回應裡;`memory_events` 記玩家說什麼、
      不記陌洲回覆的原始形式。**狀態便宜而且答得了「現在怎樣」;歷史才答得了「有沒有變」,
      而這裡每一個值得問的問題都是第二種。**

      **兩個我自己抓到並修正的設計錯誤**:
      1. **`record_visit` 一開始順手也去動 `last_seen`**,我把它拆開了——
         **抵達跟離開是兩件事**。離開的時間才是離線間隔該量的起點,
         用抵達去量會把每次間隔都少掉「他待了多久」。**那是我圖方便,不是領域要求的。**
      2. **記在 `open()` 不是 `leave()`**。`leave` 只有按按鈕才會觸發,
         **關掉分頁就等於沒來過**——那種靜悄悄的規則正是會讓留存數字說謊的東西

      **報表刻意做到的三件事**:
      - **空的時候說「沒有被記錄過」而不是「他沒回來」**。`visits` 比它要讀的存檔還新,
         **把沒記錄過的期間報成「從來沒回來」就是第 10 輪拒絕回填時劃的同一條線**
      - **H-1 沒過就直說「H-1 還沒過」**,不修飾
      - **候選指標列出來但不選**,而且點出 (d)「有沒有人講給別人聽」是唯一跟「最多人」
         直接有關的,**而它現在一筆資料都沒有**

      **拿人類真實存檔跑過**:`python tools/retention.py everliving.db` →
      「還沒有任何一次來訪被記下來」,**而且它自己解釋了那是因為表比存檔新**。

- **[階1] 預設換成 `auto`,而且先擋掉那顆地雷 —— 做完了。** **230 passed(原 228,+2)。**
      **動過的檔案**:`tests/conftest.py`(新增 `_no_real_network`)、`tests/test_no_network.py`(新增 2 個測試)、
      `src/everliving/llm.py`(`DEFAULT_PROVIDER` → `auto`)、
      `tests/test_cli_auth.py` 與 `tests/test_provider_selection.py`(**改了既有測試,理由見下**)。

      **一、先做機制,再改那一行。判準是這樣寫的,我照著做。**
      `_no_real_network` 擋在 **socket 層**——在每一個 SDK、每一個 provider、
      每一條金鑰可能到達的路徑**底下**。理由寫在 code 裡:
      **設定層的防護會一直輸,因為金鑰永遠有下一條路進來。**
      證據就在隔壁:`_hide_the_developers_dotenv` 是**上一次**踩到時加的
      (「`.env` 裡一個 `EVERLIVING_PROVIDER=groq` 把 8 個測試變成真實 API 呼叫、整套跑 200 秒」),
      而它**擋不住這一次**——因為金鑰是測試自己設的,根本不經過 `.env`。**同一個形狀,第二次。**
      **loopback 保持開放**(有測試會自己起 HTTP server),**但本機模型那個 port 例外**,
      而且 port 是從 `OPENAI_COMPATIBLE` **讀出來的不是打字進去的**——
      免得規則跟設定各走各的(`AGENTS.md`〈第五題〉)。

      **二、⚠️ 我改了 4 個既有測試,請 auditor 逐一判斷有沒有掩蓋回歸:**
      - `test_cli_auth.py` 三則:原本靠「預設是 anthropic」,改成**明寫 `--provider anthropic`**。
        **它們失敗是因為對的理由**——在 router 底下,一個死掉的 provider **本來就該被繞過而不是結束程式**。
        明寫 provider 保住它們真正在保護的東西:**指名一個你沒有金鑰的 provider,
        要拿到一句能照著做的話,不是 stack trace。**
      - `test_defaults_to_anthropic` → `test_defaults_to_the_router_so_no_key_is_needed_to_start`,
        **而且不是改個名字了事**:新的斷言查 `_candidates` 的順序,並且釘住
        **`DEFAULT_ORDER[-1] == "ollama"`**——「不會用完的那一個必須排最後」

      **三、🔴 判準第 4 條的結果跟我預期的不一樣,照實寫:**
      清掉所有金鑰之後跑,**`AnthropicLLMClient` 照樣建得起來**——Anthropic SDK
      **不在建構時驗證憑證**(`test_cli_auth.py` 開頭那段 docstring 早就寫過這件事)。
      所以順序是 groq 跳過 → grok 跳過 → **anthropic 建得起來** → ollama。
      **一個沒有金鑰的人仍然玩得到,但走的是「呼叫時失敗再往下掉」那條路,不是「建不起來就跳過」。**
      功能上成立(401 很快,而且 `test_an_outage_falls_through_to_the_next_one` 釘住了掉落路徑),
      **但它會先浪費一次失敗的呼叫,我沒有假裝那是免費的。**
      **我沒有順手去修 `AnthropicLLMClient` 讓它在建構時檢查金鑰**——
      那會影響用其他 Anthropic 驗證方式(token / bedrock)的人,是另一則的題。

- **[階1] AUTO ROUTER —— 做完了(`--provider auto` 可用;預設值另排一則)。** **228 passed(原 221,+7)。**
      **動過的檔案**:`src/everliving/router.py`(**新模組**)、`src/everliving/llm.py`
      (`PROVIDERS` 多一個 `auto`、`make_client` 接它)、`tests/test_router.py`(新增 7 個測試)。
      **先看到紅**:`ModuleNotFoundError: everliving.router`。

      **它答的是人類 2026-08-07 那句「AUTO ROUTER」**:不選一個,**每一次呼叫**才選,
      順序是**快的先、本機最後**(`DEFAULT_ORDER = groq, grok, anthropic, ollama`)。
      本機放最後不是備胎心態——**它是唯一不會用完的**,所以它才是「沒有帳號的人也玩得到」的那一格。

      **⚠️ 它把 `TASKS.md` H-1 那條衝突拆掉了,而且理由可查證**:
      那條寫「中途換 provider 會讓歸因失效」。**不會**——`llm.py:197-211` 的 `log_usage`
      **每一次呼叫都把 `usage["model"]` 寫進 `llm_calls`**。
      真正衝突的是「換」跟「**事後用猜的**」,而沒有人需要猜。
      有測試釘住這件事(`test_the_provider_that_answered_is_the_one_recorded`)。

      **三個決定**:
      1. **拒絕不換人。** `LLMRefusal` 直接往上丟,不會問下一家。
         換人等於花第二次呼叫去換一個「他已經答過」的答案,而且會把
         **「他說不要」偷偷變成「問到有人肯為止」**——那正好是設計文件第十二節那套拒絕規則要擋的
      2. **建不起來的直接跳過,而且只試一次。** 沒金鑰、SDK 沒裝,對這個 process 是永久事實
      3. **全部失敗時丟最後那個真實錯誤**,不是「全都失敗了」——
         provider 自己的訊息通常才是那句有用的指示

      🔴 **一件我試了、失敗了、然後退回來的事,請 auditor 特別看**:
      我本來**連預設值一起換成 `auto`**,結果**整套測試卡死**——
      `tests/test_provider_selection.py:76` 設了假的 `XAI_API_KEY`,router 真的把 Grok
      建起來並**發出真實網路呼叫**。**我把預設值改回 `anthropic` 並另排一則**,
      理由是「寧可小而完整」。
      **但那顆地雷比預設值重要:這套測試在 provider 建得起來的時候會打真的 API。**
      已寫進排隊中那一則的判準第 1 條。

- **[階1] 開場不要永遠是工作間 —— 做完了。** **221 passed(原 215,+6)。**
      **動過的檔案**:`src/everliving/offline.py`(新增 `OPENING_BY_STAGE`、`_BODY_MARKS`、
      `opening_scene`)、`src/everliving/web.py`(開場那一行改走它,並**移掉已經沒人用的
      `OPENING_SCENE` import**)、`tests/test_opening_scene.py`(新增 6 個測試)。
      **先看到紅**:第一次是 collection error(`OPENING_BY_STAGE` 不存在)。

      **規則讀起來是一句話**:`OPENING_BY_STAGE = ("工作間","工作間","回收場","配電所","潮線")`——
      **他從自己的工作檯開始,世界越壞就被推得越往外**:先是去撿鏽穿的零件,
      再是一直跳電的配電所,最後是水本身。
      **而他的身體贏過世界**:狀態裡出現手/體/傷/腕/肩,不管潮水跟限電在幹嘛,他都在工作檯前。

      **判準四條全中**:
      1. ✅ **由世界時鐘 + 既有狀態決定,不是隨機** —— `test_the_opening_is_decided_rather_than_rolled`
         連問 20 次斷言只有一個答案
      2. ✅ **連開三次至少兩種** —— `test_three_visits_far_enough_apart_do_not_all_look_the_same`
         取世界第 0 / 20 / 200 天,斷言 `len(seen) >= 2`
      3. ✅ 先寫測試看它紅
      4. ✅ **真的去問了瀏覽器**:`node tests/page_check.js` **exit 0**,
         而且 `python tools/shot.py` 把**四個開場各渲染一張** 1440x820 我**逐張看過**——
         `回收場` / `配電所` / `潮線` 這三個**從來沒當過開場**,所以不看不算數。
         三張都畫得出來、構圖在 SAFE 帶內、場景標籤正確、彼此明顯不同

      **兩個防呆測試,擋的是我自己可能犯的錯**:
      - `test_every_opening_is_a_scene_the_page_can_draw` —— 斷言 `OPENING_BY_STAGE ⊆ SCENES`
        **而且長度等於 `world.STAGES`**。少一格就會 IndexError,多一個畫不出來的值就是空畫面
      - `test_an_unrelated_state_change_does_not_move_the_opening` —— 「他的身體贏過世界」
        如果寫得太貪心,會變成「有狀態就回工作間」,而他幾乎永遠有狀態,
        **那等於把這一輪剛拆掉的單一畫面又裝回去**

      ⚠️ **`_BODY_MARKS` 是子字串比對,我知道它脆**。理由寫在 code 裡:
      那些鍵名是模型自己寫的,真實跑過的就有「右手腕機能」「體能狀況」「雙手狀態」「手部狀態」
      四種寫法,**列舉法會在它換一種說法的那天靜靜失效**。
      **但子字串會誤中**(例如某天出現「工具體積」)。**兩種都不完美,我選了會誤中而不是會漏掉,請 auditor 判。**

- **[階5] 打開來是一片空白的那一半 —— 做完了。** **215 passed(原 210,+5)。**
      **動過的檔案**:`src/everliving/offline.py`(新增 `_ask_until_there_is_a_night`,
      `simulate_offline_period` 改走它)、`tests/test_offline_blank.py`(新增 5 個測試)。
      **怎麼驗**:`python -m pytest tests/test_offline_blank.py -q` → 5 passed;全套 215。
      **先看到紅**:5 個裡 **3 個先紅**,另 2 個是「既有行為不准壞」的守門測試,本來就綠。

      **判準五條全中**:
      1. ✅ 拿不到可用敘事**重試一次**(`test_an_empty_answer_is_asked_again_rather_than_shown`
         斷言 `llm.calls == 2`),而且 ✅ **好的回覆不會被問第二次**(`calls == 1`)——
         **重試不准在已經能用的路徑上加錢**
      2. ✅ 仍然失敗丟 `LLMRefusal`,`web.py:312` 早就在接並顯示給玩家
      3. ✅ **空敘事永遠不進 `memory_events`**——測試直接查表斷言 `== []`
      4. ✅ 先寫測試看它紅
      5. ✅ 215 passed,一則都沒少

      **三個決定**:
      1. **只重試一次,不是迴圈。** 兩次真實失敗都來自**幾分鐘前才用同一個 prompt 答對過**的模型,
         所以這是硬幣擲壞了,不是 prompt 答不出來。無限重試會把一個壞掉的夜晚,
         變成本來就最慢的那個 provider 上一張沒有上限的帳單
      2. **丟例外是刻意的結局,不是放棄。** 玩家看到的是一句「這一夜沒有被記下來」,
         不是一片空白——**空白正是人類上次回報的「代打沒甚麼反應」**
      3. **沒碰 `MAX_TOKENS`、沒換模型、沒做 provider 選擇**(判準明寫)——那是 AUTO ROUTER 的題

      ⚠️ **這一則的判準是 planner 這一輪才排的,而排它的理由是我上一輪寫的觀測。**
      同一個 session 連著扮演,**auditor 要知道這件事再判**。

- **[階1] 世界時鐘 —— 做完了。** **215 passed(世界時鐘那一輪交的是 210,原 196)。**
      **動過的檔案**:`src/everliving/world.py`(**新模組**)、`src/everliving/db.py`(新增 `world` 表)、
      `src/everliving/offline.py`(`_build_prompts` 多一個 `pressure` 參數 + `simulate_offline_period` 接一行)、
      `tests/test_world_clock.py`(新增 14 個測試)、`playtests/2026-08-07-world-clock.txt`(真模型)。
      **沒動前端、沒動 `web.py`、沒動 `agent_loop.py`。**

      **怎麼驗**:`python -m pytest tests/test_world_clock.py -q` → 14 passed;全套 210。
      **先看到紅**:第一次跑是 collection error(`world` 模組不存在);
      後補的兩則(既有存檔的世界年齡)是先看到 `assert 0 == 200` 才修的。

      **三個決定,請 auditor 特別看**:

      1. **時鐘是「算出來的」不是「跳出來的」。** 存的只有 `world.started_at` 一個時間戳,
         汙染值 = 世界存在了幾天。**所以沒有東西要 tick**,也沒有「程式沒開的時候誰來推」的問題——
         這跟離線敘事是同一個 lazy simulation 的賭注。
         **單向惡化因此是結構保證不是防呆**:時間的單調函數沒辦法被講低,
         而且這個模組裡**沒有任何一行在寫入等級**
      2. **刻意不放進 `agent_state`。** 那張表是直接拿模型的 `state_changes` 寫的,
         把「只能往上」的東西交給「什麼都能說」的寫入者,一次幻覺的「汙染:好轉」就把累積清掉了。
         所以另開一張 `world` 表,而且**沒有 `agent_id`**——里程碑 1 會有好幾個 agent 站在同一場天氣裡。
         有測試釘住這條(`test_the_clock_is_not_stored_where_the_model_can_write_to_it`)
      3. **既有存檔用第一個 agent 的 `created_at` 當起點。** ⚠️ **這條最接近「回填」,我自己標出來。**
         我的理由是**那個時間戳是真的被記錄過的事實,不是捏造**——
         這正是第 10 輪拒絕回填 scene 時劃的那條線(那裡從來沒有任何場景被記錄過,填什麼都是編的)。
         **拿人類真實存檔的副本驗過**(沒動原檔):`everliving.db` 的 agent 建於 `2026-08-03T15:54`,
         推出來 index=3、stage 0/4——**世界確實才三天大,不是被重設成三天大**。
         **但這是我的判斷,請你自己決定這條線劃得對不對。**

      **判準對帳**:
      1. ✅ 存得下來、只會往一個方向走 —— `test_the_clock_never_goes_backwards`
         掃 0-400 天斷言非遞減,`test_a_clock_read_before_its_world_began_reads_zero` 釘住不會變負
      2. ✅ 餵進離線 prompt,而且**不是硬塞數字** —— `test_the_prompt_never_hands_the_model_a_bare_number`
         斷言原始數值**不在** user_message 裡。五個階段寫的是**他這禮拜摸得到的東西**
         (水要濾幾次、鏽咬多深、限電幾天),照設計文件第十一節「必須局部化」,
         有測試擋掉「人類/文明/滅亡/全世界」那種文明級寫法
      3. ✅ 先寫測試看它紅(見上)
      4. ✅ 210 passed,一則都沒少
      5. ✅ **繁體規則自己先驗過**:拿 `script_check.find_simplified` 掃五段階段文字 + prompt 段,
         **零命中**。(這不是判準要求的,是我自己補的——新增八十幾個中文字不驗等於在賭)

      **🔴 真模型那條沒有過,而且它抓到一個所有測試都看不見的東西。判準 2 目前是「未驗證」不是「通過」。**
      拿 Ollama(`qwen3.6:latest`)跑了同一個 agent、同一段離線窗、只有世界年齡不同的兩輪。
      全文 `playtests/2026-08-07-world-clock.txt`,**原樣輸出,沒挑好的留**:

      - **第一輪(世界 0 天,460 秒)**:JSON **截斷在半路**——
        `"open_thread": "那台挖出來的監護儀脈衝線斷了兩` 就沒了。
        解析失敗 → 退化成純敘事,所以 `narrative` 欄位裡塞的是**整包生 JSON**,
        而 `events` / `state_changes` **全空**、`scene` 掉回預設的 `港城`
      - **第二輪(世界 200 天,361 秒)**:**敘事完全是空的。** 什麼都沒有

      **`src/everliving/llm.py:61-64` 早就寫著這個病**:Qwen 會先推理再回答,
      而 `MAX_TOKENS = 6000` **是推理跟輸出共用的**,註解裡甚至記著上次觀察到的 2048 截斷。

      **✅ 對照組跑完了,結論翻過來:不是我害的,而且判準 2 這次真的被證明了。**
      全文 `playtests/2026-08-07-world-clock-control.txt`(A:`pressure=None`,等於這一輪之前的行為;
      B:stage 4 最長那段)。**兩組都吐出完整合法的 JSON**,而且:

      | | input | output | 結果 |
      |---|---|---|---|
      | A 對照(無壓力段) | 516 | **3174** | 完整 |
      | B 實驗(stage 4) | 585 | **2898** | 完整 |

      **B 的輸出比 A 還短。** 加了壓力段沒有把它推過頂,`MAX_TOKENS = 6000` 兩邊都用不到一半。
      **所以前一份那兩次失敗是這個模型的變異,不是這一輪造成的。**

      **而 B 的敘事讀得出壓力,還沒有講出任何數字或等級**——這正是判準 2 那句話:
      - B:「**濾水器**喉管又卡死了」「**除鏽劑**啃出淺紅的裂口」「工作台角落那堆**別人留下的廢零件**
        被我一掃而空」「備用密封圈存量:**見底**」
      - A(同一個 agent、同一段離線窗、只差壓力段):潮線、冷卻管、導航晶片——**完全沒有這些**
      - **最值得看的一句是「別人留下的廢零件」**:stage 4 寫的是
        「巷子那頭的老人上個月搬進了模擬倉,他的房間現在堆著沒人要的零件」——
        **它把那段折射進他自己的一週,而不是複述**。這就是「局部化」要的東西
      - **全程沒有出現數字、等級、或「汙染」兩個字**

      **auditor 要知道的三件事,包含對我不利的那件**:
      1. **210 個測試全綠,而第一次真模型跑是壞的。** 這正是收尾檢查第 4 條存在的理由,
         **而我第一次的反應是先懷疑自己**——對照組才是把它釘死的東西
      2. 🔴 **今天四次真呼叫裡有兩次產不出可用的 JSON(50%)。** 兩次都在第一份 playtest 裡:
         一次截斷、一次全空。`llm.py:61-64` 記著 Qwen 推理與輸出共用預算。
         **這是既有問題不是這一輪的**,但它對 AUTO ROUTER 跟 H-1 都是硬資料,已寫進 `NOW.md` 收件匣
      3. **B 的敘事裡有一個簡體字**:「相位對不对」的 `对`(`script_check.find_simplified` 確認)。
         **人類 2026-08-07 已裁定這題只記 log 不擋**,所以不是缺陷,是照規矩留痕

      **兩件我刻意沒做,留給 planner**:
      - **對話路徑(`agent_loop.respond`)沒有接壓力**,只有離線接了。判準只寫離線,而且一次只做一則
      - **`web.py` 沒有把壓力送到畫面上**。存下來之後誰會讀,是第 11 輪那個退回的教訓,
        但**這一則的判準明寫「不要做資源管理介面」**,所以我停在 prompt。
        **要不要讓玩家看見,是下一則的題**

<!-- 已於第 14 輪驗收通過,保留原文備查。這一則走完了退回→修好→通過的全程:
     第 10 輪交、第 11 輪退回(漏到畫面上)、第 13 輪修、第 14 輪通過。

  **[階4] `scene`/`action` 持久化 —— 做完了。** **194 passed(原 188,+6)。**
      **動過的檔案**:`src/everliving/db.py`(新增 `attach_scene`)、`offline.py` 與 `agent_loop.py`
      各接一行、`tests/test_scene_recording.py`(新增 6 個測試)、
      `tests/test_offline_consequences.py`(改 1 個既有測試的斷言,理由見下)。
      **怎麼驗**:`python -m pytest tests/test_scene_recording.py -q` → 6 passed;全套 194。
      **先看到紅再修**:6 個測試裡 4 個先紅(另 2 個是「不該記」的,本來就綠)。

      **沒改 schema。** `story_assets` 早就存在(`memory_event_id` + `kind` + `ref`)而且是空的,
      設計本來就想好了——`offline.py:349` 寫「this row is what story assets hang on」、
      `Turn` 的 docstring 寫「story assets need something to hang on」。
      **掛鉤留了三個月,只是沒人掛上去。** 所以零 migration,三個既有 DB 都不用動。

      **三個刻意不做的決定**:
      1. **`None` 不寫任何列**。「沒有特別在發生什麼」是畫面的真實狀態,不是缺資料;
         寫一列進去就分不出「模型選了無」跟「有人忘了寫」
      2. **舊資料不回填**(判準明寫)。回填等於替一段從沒被記錄過的敘事**捏造**一個場景。
         空的答案是真的,像樣的答案是假的
      3. **沒有順手做一致性檢查**。這一則只負責讓它「看得見」——
         而且現在連一筆真實的不一致樣本都還沒有,先寫檢查等於憑空猜規則

      **⚠️ 我改了一個既有測試,要 auditor 特別看這條**:
      `test_an_asset_can_be_hung_on_the_beat_the_simulation_just_wrote` 原本斷言
      `[a["ref"] for a in get_assets(...)] == ["clips/夜.webm"]`——**整個清單只有一筆**。
      真實的一輪現在會在同一個 beat 上多掛一列 scene,所以它紅了。
      我把斷言改成**用 `kind == "video"` 過濾**。
      理由:那個測試的名字和 docstring 說的是「掛得上一支影片」,
      **「這支影片是唯一的 asset」是它從來沒打算主張的東西**。
      但這是 builder 改既有測試,**請自行判斷我有沒有在掩蓋回歸。**
-->

<!-- 以下保留 builder 交棒時寫的內容,驗收已通過(第 7 輪):

  **[階4] 場景/動作跟台詞對不上 —— 重現失敗,建議退回。** 全文 `playtests/2026-08-06-scene-mismatch-repro.txt`。
      **動過的檔案**:只有那份 playtest(新增)。**沒動 `src/`**,判準寫「重現不出來就不要硬修」。
      **三條路都試了,三條都是死的**:
      1. `memory_events` 沒有 scene 欄位(`PRAGMA table_info` → `id, agent_id, kind, content, occurred_at, created_at`)
      2. `story_assets` 也沒有,而且 `SELECT count(*)` = **0 筆**
      3. `everliving.log` 141 行,`grep "scene\|場景\|工作間\|配電所"` **零命中**
      4. `grep -rn "scene" src/everliving/db.py` **零命中**——持久層不知道有這個概念

      **結論不是「這次沒重現」,是結構上無法回頭查**:`scene`/`action` 只活在一次 HTTP 回應裡。
      過去發生幾次、現在還在不在發生、修好了沒——三題都查不到。
      **最後一題最要命:就算硬修了也沒辦法證明修好了。**

      唯一有完整記錄的那次(`playtests/2026-08-06-ollama.txt`)三者是對得上的,樣本數 1。

      **建議給 planner(我沒有自己排)**:先做更小的一件事——**把 `scene`/`action` 跟敘事一起存下來**。
      存了之後這件事才第一次可觀測。**這跟 `AGENTS.md` 那節是同一個形狀:
      「場景要跟敘事一致」是一條規則,而它連被觀察的資格都還沒有。**
-->

## 給反思那一棒的兩件事(第 7 輪 auditor 留)

1. **配額規則量錯對象。** 「階 5-7 不得超過半數」的用意是限制 planner **這一輪加了什麼**,
   但 `loop_check` 量的是 **佇列此刻剩下什麼**。佇列在「寫」耗盡、要到兩棒後的「想」才補,
   所以 想→寫→查 之中**整個「查」都會坐在紅燈上**,而且沒有人做錯任何事。
   現在就是這個狀態:排隊中剩 1 則階 6,`floor(1/2)=0`,必然違規。
2. **紅燈太便宜會被無視。** 這是同一個檢查器第二次因為「規則本身的形狀不對」而叫
   (第一次是「暫停沒有合法狀態」)。**兩次都不是使用者做錯,是規則沒想清楚。**
   這正是 `AGENTS.md` 那節反過來的失敗:規則有人執行了,但**執行的是錯的東西**。

## 驗收結果

<!-- auditor 唯一能寫的區。每則必須附證據:`檔案:行號` 或指令輸出 -->

- **通過** —— [階5] 空白的夜晚有下場了。**215 passed,我自己重跑。**
      **我追的是 builder 沒追的那一步:例外丟出去之後,兩個呼叫端都接得住嗎?**
      (**第 11 輪那次退回就是「沒往下游追一步」,所以這次先追。**)
      - `src/everliving/web.py:312` `except (LLMAuthError, LLMUnavailable, LLMRefusal)` —— 接住並顯示給玩家 ✅
      - `src/everliving/cli.py:124` `except LLMRefusal` —— 而且註解寫著
        「Don't let this abort startup — the conversation loop is still usable」 ✅
      **兩條路都是既有的,不是新開的**。所以這一則沒有製造新的失敗模式,
      它是把一個**本來會靜靜寫進資料庫**的東西,改走既有的「說出來」那條路

      **測試的強度我也查了,而且這次沒有第 17 輪那個毛病**:
      `assert llm.calls == 1` / `== 2` 是**直接觀察呼叫次數**,不是 proxy——
      重試若跑進成功路徑,那個數字當場變 2 而測試變紅。
      查表那則 `assert [row["content"] ...] == []` 同樣是直接觀察。

      **一件我確認過的邊界**:重試發生在**任何 DB 寫入之前**,
      所以 `LLMRefusal` 丟出來的時候不會留下半筆殘缺的記憶。

- **通過** —— [階1] 世界時鐘。第 17 輪已判「行為通過、兩個測試退回」,
      第 18 輪修完後我重驗:拿同一個 `breakstorage` plugin 跑,**那兩個測試現在會紅**;
      正常時全綠。**退回的事項已結清。**

- **通過,但退回兩個測試** —— [階1] 世界時鐘。**行為是對的,而兩個以「記得住」命名的測試,
      在儲存被整個拿掉之後照樣是綠的。**

      **一、行為我自己驗過,是真的**:拿檔案型 DB 寫入 → `close()` → 重開,
      epoch 讀回 `2026-01-01`、`pollution(E+50天)` = 50。**跨連線真的存得住。**

      **二、但釘住它的測試是套套邏輯,我用執行結果證明**:
      寫一個 plugin 把 `world._epoch` 換成「永遠回傳 2000-01-01」,**等於儲存完全失效**,再跑全檔:
      ```
      5 failed, 9 passed
      ```
      再單獨跑那兩個以持久化命名的:
      ```
      python -m pytest tests/test_world_clock.py -q -p breakstorage -k "remembers or twice"
      2 passed
      ```
      - `test_the_world_remembers_when_it_started` —— 它開的是**第二個 `:memory:` DB**,
        而且**把 epoch 用參數餵進去**。兩邊拿到同一個明示值當然相等,
        **這件事在儲存完全壞掉時也成立**
      - `test_starting_a_world_twice_does_not_move_its_epoch` —— 同病:兩次讀都走同一條假路徑

      **這正是這個 repo 的老毛病換一個位置再犯**(`AGENTS.md`〈第四題〉〈第五題〉):
      **測試的名字宣稱在量持久化,實際量的是「同一個常數等於它自己」。**
      **而寫它的人(我)以為那條判準「存得下來」已經有執行者了。**

      **不退回整則的理由**:判準四條的實質全部成立——時鐘單向(掃 0-400 天)、
      壓力進 prompt 且不含數字、先看到紅、210 passed,**真模型對照組還反過來證明了判準 2**。
      **壞的是證據的強度,不是產品。** 所以只退這兩個測試。

      **判準(修完要達到)**:那兩個測試要在 `breakstorage` 那種「儲存失效」下**變紅**。
      **做法提示**:用真的檔案 DB,寫入後 `close()`,重開一個連線再讀,**而且不要把 epoch 餵回去**。

- **通過** —— [退回重做] `scene`/`action` 不再漏到玩家畫面上。**我上一棒退回的那一則,這次修對了。**
      **證據全部是我自己跑的,沒有採信 builder 的宣稱。**

      **一、修復範圍屬實,而且沒有第四個出口。**
      - `git show HEAD --stat -- src/everliving/static/` → **空的**,前端 0 行,屬實
      - `grep get_media_assets` → `web.py:158` / `:191` / `:229` 三處,就是原本漏的那三處
      - `grep get_assets` 掃全 repo:`src/` 裡除了 `db.py` 的定義,**再也沒有別的呼叫者**。
        上一棒我抓到的漏洞是「builder 沒往下游追一步」,這次下游我自己走完了

      **二、「先看到紅」我用不動 repo 的方式重跑,結果比 builder 說的更有力。**
      `git checkout <舊 commit> -- src/` 被權限擋下,改寫一個 pytest plugin
      把 `db.get_media_assets` 在執行期別名回 `db.get_assets`——**那正是這次修復的全部內容**,
      所以等價於還原。跑**全套**:
      ```
      2 failed, 194 passed
      FAILED tests/test_web.py::test_the_scene_a_beat_was_drawn_in_never_reaches_the_player
      FAILED tests/test_web.py::test_the_offline_beat_hides_its_scene_the_same_way
      AssertionError: assert [{'kind': 'sc... 'ref': '淹水'}] == []
        Left contains 2 more items, first extra item: {'kind': 'scene', 'ref': '配電所'}
      ```
      **關掉修復,恰好只有這 2 個測試紅,其餘 194 全綠。** 兩個方向都成立:
      (a) 這兩個測試**是唯一釘住這件事的東西**——在它們之前,整套 194 個測試對這個漏洞完全無感,
      而我上一棒是**讀 code** 讀出來的,不是測出來的;
      (b) **沒有任何別的測試依賴著過濾行為**,所以這個改動沒有順手改掉別人的地基。
      現況 `python -m pytest -q` → **196 passed**

      **三、判準第 4 條(去問瀏覽器):builder 自己標一半並請我判斷,我判定通過,理由不是通融。**
      - `node tests/page_check.js` → **exit 0**,我自己跑的;其中
        `ok   an agent turn renders with its assets beside it` ——
        **該渲染的 asset 照樣渲染得出來**,不是靠把渲染弄壞來讓標籤消失
      - `src/everliving/static/index.html:985` `if(!list || !list.length) return;`
        ——空清單直接不建 `<ul>`。伺服器現在送 `[]`,**所以連元素都不會產生**
      - 前端 diff 是 0 行,渲染路徑跟漏洞發生時**逐字相同**
      **builder 說得對:有 beat 的截圖只會用一個更慢的儀器,把 payload 層那兩個測試再證一次。**
      拒絕它不會多知道任何事

      **四、順帶確認一件容易被誤讀的事:場景仍然看得到,而且應該看得到。**
      `index.html:883` `$('place').textContent = scene + (act ? '・' + act : '')`、
      `:1001-1002` 用 `data.scene` 畫圖。那是**設計好的畫面標題**,走的是 payload 頂層那個獨立欄位。
      判準第 1 條講的是 `scene ·` 那種 debug 標籤,**範圍框對了**

- **一則觀察,不是退回** —— `tests/test_agent_loop.py:139` 還留著同一個形狀的過度斷言:
      `[a["ref"] for a in db.get_assets(conn, turn.event_id)] == ["clips/bench.webm"]`,
      而 `agent_loop.py:160` 明明會呼叫 `attach_scene`。
      **它現在是綠的,而且我查出了原因**:`tests/conftest.py:27` 的 `FakeLLMClient` 預設回覆是
      `"(fake reply)"`,不帶 `場景:` 標籤,所以照 builder 第 10 輪的決定一(`None` 不寫任何列)
      根本沒有 scene 列可以掛。
      **不退回**:它是綠的、是既有的、而且第 11 輪我已經驗過那類改動沒掩蓋回歸。
      但它跟 builder 第 10 輪修掉的那個是同一個地雷,**哪天有人給那個 fixture 一個場景標籤就會踩到**。
      **我沒有自己修,也沒有排成任務**——兩件都不是我的區

- **兩個 `loop_check` 的洞,都是〈第四題〉那個形狀,而且是我這一棒實際踩到的** ——
      **兩個都用執行結果確認,不是讀 code 推測**(`python -c` 直接呼叫 `loop_check._entries`):
      1. **改個標題就能讓一整區從檢查器眼前消失。** builder 上一棒把交棒區寫成
         `## 待驗收(第 13 輪 builder 交)`,而且**擺在「退回重做」跟「排隊中」中間**——順序是錯的。
         檢查器沒叫,因為 `tools/loop_check.py:87`
         `order = [h for h in _HEADING.findall(text) if h in SECTIONS]`
         **先把不認得的標題整個濾掉,再去比對順序**。
         規則講「區塊順序是 load-bearing」(`loop_check.py:30-31` 自己寫的),
         量的卻是「**我認得的那些**標題的順序」。已撤掉那個標題,原文縮排保留在原處
      2. **註解掉的項目仍然被數。** 清空之前 `_entries` 對「待驗收」回報 **2 則**,
         但其中一則躺在第 7 輪 auditor 留的 `<!-- -->` 裡——parser 只看行首是不是 `- `,不懂 HTML 註解。
         **這是第 10 輪 builder 記過的同一個坑,它一直還在檔案裡。** 已把那行改成縮排,現在回報 0 則。
      **這兩個都不是任何人偷懶,是檢查器量的東西跟規則講的東西分家**——
      跟「配額量錯對象」(排隊中那則 [階6])同一個病灶。**留給下一棒反思,我沒有改 `tools/`**

- **`TASKS.md` 這次沒有東西可以翻,而且我確認過不是漏掉的** ——
      這一則的來源是 `NOW.md` 收件匣(第 7 輪 auditor 寫進去的),**不是 T 編號 backlog**。
      `grep "場景\|scene" TASKS.md` 只命中 T1-V3/V5/V6/V7 那些**已經 done** 的舊項目,
      沒有任何一則在講持久化或畫面外洩。
      **硬替已經做完的事回頭發一個 T 編號只會製造雜訊。** `PROGRESS.md` 與 `NOW.md` 照常同步

- **退回** —— [階4] `scene`/`action` 持久化。**資料層做得對,但漏到玩家畫面上了。**
      理由與判準見「退回重做」那一區。**這是這個循環第一次真的退回一則工作。**

      **先講 builder 做對的部分,而且是我自己驗過的**:
      - **測試真的先紅**:`git checkout f9e4b94 -- src/` 之後跑,
        6 個新測試裡 **4 個紅**(另 2 個是「不該記」的,builder 也是這樣說的),還原後 194 passed
      - **改既有測試沒有掩蓋回歸**:那個被改的斷言在**舊 `src/` 下也過**(24 passed)——
        它在改動前後都成立,所以是把過度斷言縮回它本來的主張,不是把紅燈改綠
      - **零 schema 變更**屬實,`story_assets` 三個 DB 都早就在

      **漏掉的那一條也講清楚是什麼性質**:builder 查了資料層、查了測試,
      **沒有往下游追一步問「存進去之後誰會讀」**。`web.py` 把 `get_assets()` 整包往前端送,
      這件事 `grep get_assets` 一次就看得到。
      **不是難,是沒查。**

- **通過(但不是「做完」,是「不該做」)** —— [階4] 場景/動作跟台詞對不上。
      判準本來就寫「重現不出來就不要硬修,把嘗試寫進 `playtests/` 然後退回」,builder 照做了。
      **四條證據我自己重跑,全部屬實**:
      - `PRAGMA table_info(memory_events)` → `id, agent_id, kind, content, occurred_at, created_at`,**無 scene**
      - `SELECT count(*) FROM story_assets` → **0**
      - `grep -c "scene\|場景\|工作間\|配電所" everliving.log` → **0**
      - `grep -c "scene" src/everliving/db.py` → **0**
      - `git show --stat 372f479` → 只有 `LOOP.md` 與那份 playtest,**`src/` 0 行**

- **補兩條 builder 沒查的,結論不但成立而且更強** ——
      1. **`llm_calls` 只存 token 數**:`['id','agent_id','purpose','model','input_tokens','output_tokens','created_at']`。
         **三個 DB 都查了**,含 `everliving.db.bak-20260803`(13 筆)——builder 完全沒提備份那份
      2. **`--debug` 也救不了**:`src/everliving/llm.py:168-169` 只記 system prompt 和 user message,
         **不記模型的回覆**。所以連「事後打開 debug 再看」這條路都沒有

      **合起來是一句更難聽的話**:這個系統會記玩家說了什麼,**不記陌洲說了什麼的原始形式**。
      `memory_events` 存的是 `split_scene_tag` **剝掉場景標籤之後**的文字。
      不是「這次沒抓到」,是**任何 verbosity 下都抓不到**。

- **這一則不放進「退回重做」** —— 退回是給「做壞了要重做」的。
      這裡 builder 沒做壞,**是任務本身不成立**:在 scene 可觀測之前,修它等於賭。
      已把「先把 `scene`/`action` 跟敘事一起存下來」寫進 `NOW.md` 收件匣給 planner。
      **我沒有自己排成任務,那不是我的區。**

- **通過** —— [階1] 用 Ollama 實跑一輪。判準四項全中,而且我自己重跑過,沒採信上一棒的宣稱。
      - **只動了該動的**:`git show --stat --format="" 4859ae9` → 只有 `LOOP.md` 與
        `playtests/2026-08-06-ollama.txt`,`-- src/` 過濾後是 **0 行**
      - **沒動到真的那份記憶**:`everliving.db` mtime `2026-08-06 00:32`(這一輪開工之前),
        `play.db` mtime `21:43`
      - **驗證現在還是綠的**:`python -m pytest -q` → `177 passed`;`python tools/loop_check.py` → exit 0
      - **含失敗的都寫了**:三步輸出在 `playtests/2026-08-06-ollama.txt`,
        cp950 那個坑寫在同檔【4】
      - **沒有自己標 done**,交的是「待驗收」

- **一則更正,更正後比原本嚴重** —— builder 寫「簡體字…現在沒有任何測試在管這件事」。
      沒有測試屬實,但它漏了更重要的一半:**prompt 早就明文要求繁體**——
      `src/everliving/offline.py:259`「所有文字一律用繁體中文,包括 `state_changes` 的鍵名」
      (`src/everliving/visitor.py:34` 也有一條)。
      所以這不是「還沒規定的行為」,是**已經規定、被違反、而且沒有任何東西會發現**。
      違反的證據:`playtests/2026-08-06-ollama.txt:40`「從没见过」。
      已寫進 `NOW.md` 收件匣等 planner 排。**我沒有自己排成任務,那不是我的區。**

## 反思

<!-- reflector,每 3 輪 append 一則。原料是上面三輪的驗收結果與退回重做 -->

### 第 4 次反思(涵蓋第 15-28 輪,逾期三輪)@2026-08-07 by Claude Opus 5

**一、新的模式,而且是這個 repo 目前最貴的一個:把該存成歷史的東西存成了狀態。**

三個實例,全部有證據,全部是「發現當下才知道問題連問都問不了」:

| # | 存了什麼 | 答不了什麼 | 怎麼發現的 |
|---|---|---|---|
| 1 | `scene`/`action` 只活在一次 HTTP 回應裡 | 「場景跟台詞對不上」發生過幾次 | 第 6-7 輪,查了三個 DB 才確定查不到 |
| 2 | `memory_events` 存剝掉標籤後的文字 | 陌洲**原本**說了什麼 | 第 7 輪 auditor 補查 |
| 3 | `player_state` 一列、每次覆蓋 | **他有沒有再回來(H-1)** | 第 28 輪,要做報表時才撞到 |

**共同形狀**:存的是「**現在怎樣**」,而每一個真正想問的問題都是「**有沒有變**」。
**狀態便宜、夠用、而且看起來很合理**——直到有人問了第二種問題,才發現資料從來不在那裡。

**第 3 個最貴**,因為它擋住的是**里程碑 0 唯一的成敗判準**。
H-1 從一開始就寫在 `TASKS.md`,而**能回答它的資料一天都沒有存在過**。
不是沒人去算,是算不出來。

**判準(下次寫任何 schema 前先問)**:
> **這張表要回答的問題,是「現在怎樣」還是「有沒有變」?**
> 如果有任何一個利害關係人會問第二種,**它就必須是 append-only 的,而且現在就要是。**
> 事後補不回來——`visits` 加上去的那天,之前所有的來訪已經永遠不見了。

已 append 進 `AGENTS.md`〈第六題〉。

**二、上一次反思的判準生效了,而且是我自己踩到的。**

第 3 次反思寫:「每加一條規則,先答『文件可以怎麼合法地騙過這個 parser』」。
這一段有兩次同型事件,兩次都被抓到:

1. **第 17 輪**:世界時鐘那兩個以「記得住」命名的測試,在儲存被整個拿掉之後照樣是綠的
   ——它們開第二個 `:memory:` DB 又把 epoch 用參數餵回去
2. **第 26 輪**:我自己寫的 `test_an_outbound_connection...` 名字說 outbound、實際撥 loopback

**兩次都是「名字宣稱的比實際檢查的多」。** 抓到的方法也一樣:
**把被測的東西整個拿掉,看還有幾個測試是綠的**(`breakstorage` plugin,5 failed / 9 passed)。
**這個手法值得固化**,但它是手法不是知識——見下面 skill 帳本。

**三、設定層的防護會一直輸,兩次都是。**

`_hide_the_developers_dotenv`(上一次)擋不住 `test_provider_selection.py` 自己設的
`XAI_API_KEY`(這一次),因為**金鑰永遠有下一條路進來**。
第 26 輪把防護下沉到 **socket 層**——在每個 SDK、每條金鑰路徑底下。
**這跟第一點是同一個道理的另一面**:在錯的抽象層防守,防的是你想像中的那條路。

**四、誠實對帳,而且這次要講的是這座工廠本身的可信度。**

**第 15-28 輪全部在同一個 session 裡跑完**,四個角色都是同一個 agent。
`T2-L4`(冷啟動 session 空跑一輪)**到今天仍然沒做**,所以
**「builder 不能自我驗收」目前是靠這個 agent 記得,不是靠機制。**

要公平地說另一面:**它確實退回了自己寫的東西兩次**(第 11 輪的 payload 外洩、
第 17 輪的套套邏輯測試),而且第 28 輪還拆掉了自己十分鐘前寫的耦合。
**所以紀律有在運作。但那是自律,不是隔離,兩者在報告裡不該長得一樣。**

產出對帳:第 15-28 輪 **196 → 240 passed**,`src/` 真的動了(世界時鐘、開場、
空白夜晚、router、visits)。**這推翻了第 2 次反思那句「這座工廠在真正的阻塞旁邊找事做」**
——但推翻它的是**人類給了目標跟拆了圍籬**,不是循環自己找到方向。

**五、skill 帳本:第一次有東西該寫,但這一輪不寫,理由是 Iron Law。**

「把被測的東西整個拿掉,看還有幾個測試是綠的」出現 ≥2 次(第 17、26 輪),
**符合門檻**。但 `superpowers:writing-skills` 的 Iron Law 要求先跑 baseline
(觀察沒有 skill 時 agent 會不會失敗),**而這一輪沒跑**。
上一次跑 baseline 的結論是「那個失敗不存在」,**這一次形狀不同、不能沿用那個結論**。
**所以帳本仍然空著,但這次的理由是「還沒測」,不是「測過了不該寫」——這個差別要記清楚,
下一次反思要嘛跑 baseline,要嘛說明為什麼不跑。**

### 第 3 次反思(涵蓋第 12-14 輪)@2026-08-07 by Claude Opus 5

**一、同一個病灶第三、四、五次,而 `AGENTS.md` 已經為它寫了兩節。**
**所以這一次不准再寫第三節——該問的是它為什麼一直復發。**

| # | 檢查器以為在量 | 實際在量 | 哪一輪抓到 |
|---|---|---|---|
| 1 | planner 這一輪加了什麼 | 佇列此刻剩下什麼 | 第 6-7 輪 |
| 2 | 「排隊中有幾則」 | 有幾行以 `- ` 開頭(不懂 HTML 註解) | 第 10 輪記過,**第 14 輪發現它還在** |
| 3 | 區塊順序合法 | **我認得的那些**標題的順序 | 第 14 輪 |

**前兩節的診斷是「量錯對象」,而那個診斷不夠深,因為它把三次講成三個獨立的疏忽。**
攤開看,三次共用同一個機制:

> **`LOOP.md` 是一份 Markdown 文件,但被當成資料庫用。**
> `loop_check` 必須從**排版**(行首字元、標題文字)反推**結構**(這是一則任務、這是哪一區),
> 而排版是人手寫的、可以合法地長出各種樣子。**parser 的結構模型是一個猜測,
> 而文件可以在不違反任何 Markdown 規則的情況下悄悄違反那個猜測。**

**這解釋了為什麼寫兩節文字擋不住它**:那兩節教的是「寫規則時要想清楚量什麼」,
但這三次的作者**都想清楚了**——`loop_check.py:30-31` 自己寫著「order is load-bearing, not cosmetic」,
它知道順序重要,**它只是沒料到標題可以被改名**。**這不是思慮不周,是介面選錯了。**

**二、所以這一次的產出不是第三節文字,是一條可以被做的判準。**

**每加一條 `loop_check` 規則,要能回答:「文件可以怎麼合法地騙過這個 parser?」**
——把答案寫成測試。三次裡有兩次(`- ` 在註解裡、標題加後綴)**都是一行測試就抓得到的**,
而且都是**寫規則的當下就想得到**的,不需要等它在真實輪次裡炸。

已 append 進 `AGENTS.md`(**沒改任何已存在的行**)。
**注意這一條跟前兩節的關係是收斂不是疊加**:前兩節問「量的對不對」,這一條問
「**量得到嗎**」——`loop_check.py:87` 那個 `if h in SECTIONS` 就是量不到的地方。

**三、誠實對帳,而且這次的帳跟上一次反過來。**

第 2 次反思說「三輪下來遊戲本身一行都沒動」,並引設計文件〈已知風險〉第 1 條說
「蓋一座四人工廠,很可能是在真正的阻塞旁邊找事做」。**第 12-14 輪推翻了那個觀察:**

- 第 13 輪(寫):`db.get_media_assets` + 2 個測試,**196 passed(原 194)**
- 第 14 輪(查):**關掉修復跑全套 → 恰好 2 紅 194 綠**,證明那兩個測試是唯一釘住它的東西
- 加上第 10 輪的持久層,**這六輪是循環第一次真的動到遊戲**,
  而且**第一次走完「交 → 退回 → 修 → 通過」的全程**

**而且退回那一次是循環唯一能產生的價值**:第 11 輪 auditor 讀 code 抓到漏洞,
**當時 194 個測試全綠**——沒有那一棒,那兩個 debug 標籤會直接上到玩家臉上。
**單一 session 的 agent 不會退回自己剛寫的東西。**

**所以對這座工廠的正確評價要改**:它的產出不是吞吐,是**第二雙眼睛**。
上一次我說「不要因為它很忙就以為它在推進」,那句話仍然對;
**但這三輪它既沒空轉、也不只是紀律——它擋下了一個真的會出貨的缺陷。**

**四、仍然不寫 skill,而且理由沒有變。**

這一次的模式(parser 騙得過)是**這個 repo 專屬的機制細節**,
`superpowers:writing-skills` 明列「專案慣例 → 放進 instructions file」。
而且 Iron Law 要 baseline,**上一次真的跑過的結論是那個失敗不存在**;
這一次的形狀更窄,更沒有理由。**帳本維持空的。**

### 反思補記:baseline 跑了,結論是**不寫 skill**,而且理由跟原本說的不一樣

@2026-08-06,人類授權派 subagent 之後補跑。**這一則推翻我自己前兩次的推論。**

前兩次我說不寫 skill,理由是「專案慣例該進 instructions file」+「Iron Law 要 baseline 但我不能派 agent」。
**授權下來之後我跑了 baseline,答案更乾脆:那個失敗根本不存在。**

**4 個 rep,兩種情境形狀,4/4 都自己指定了執行者**(全部沒有給任何提示):

| rep | 情境 | 它自己加了什麼 |
|---|---|---|
| 1 | 往 CONTRIBUTING.md 加「API 回應要帶 request ID」 | 「不帶就是 bug,**review 時直接擋**」+ review checklist + middleware 統一處理 |
| 2 | 往 conventions.md 加「記錄要寫明模型版本」 | 「**code review 看到就擋下,不進 main**」+ `model: none` 而非留空 |
| 3 | 寫 system prompt(規則只是副產品) | 主動說「LLM 數字數不可靠,**建議在程式端補一道硬檢查**」 |
| 4 | 寫設計文件(限制只是其中兩條) | 2MB → 「讀完 body 前就 413」;格式 → 「**檔頭 magic bytes,不信副檔名**」 |

rep 3、4 是**刻意設計成「規則只是副產品」**的忠實情境,因為 rep 1、2 太簡單
(把「寫規則」當成整個任務,等於逼它認真想)。**忠實情境下一樣沒失敗。**

`superpowers:writing-skills` 的規定是:**「control 沒有出現那個失敗,就沒有東西要修——
停,不要寫那份指引。」** 照做,不寫。

**那為什麼這個 repo 真的發生了五次?** 重新解讀:

**不是 agent 不知道要指定執行者,是那些規則都寫在「一次做五十件事」的長 session 裡。**
`prefers-reduced-motion`、`offline.py:259` 的繁體規則、補丁 1 的心跳、佇列上限——
四條的共同點不是無知,是**它們都只是當時任務的第五十件事**。

**這改變了對策**:知識型的 skill 沒用(它們本來就知道),
**該做的是把那個提問綁在 commit 前的固定動作上**——`AGENTS.md` 那兩節現在扮演的正是這個角色。
**留著它們是對的,但要知道它們是在對抗負載,不是在對抗無知。**

**樣本數 4,`writing-skills` 建議 5+。** 但四次分屬兩種情境形狀、零失敗,足以停手。

**附帶一個現成的示範,而且是踩出來的**:記錄授權時我把那兩行插進 reflector 那一條的**中間**,
`git diff --numstat AGENTS.md` 因此顯示 `5 1`——**有 1 行「刪除」。**
但實際上一個字都沒被移除,只是被插進去的那一行在 git 眼裡算「改過」。

**這就是〈第四題〉那節在講的事,而且是同一天第三次**:
「反思只准 append」的執行者量的是**行的增刪數**,規則講的是**內容有沒有被拿掉**——
proxy 跟本意會分家。真的照這個檢查跑,會抓到一次不存在的違規。
(順帶:這次那個修改本來就不受補丁 3 管——**是人類改契約,不是反思改自己**。)

---

### 第 2 次反思(涵蓋第 5-7 輪)@2026-08-06 by Claude Opus 5

**一、模式:規則有人執行了,但執行的是錯的東西。**(上一次反思的鏡像)

上一次抓到「規則寫了但沒人執行」。這三輪抓到反面,而且同樣是 ≥2 次:

| # | 規則 | 它以為在量什麼 | 實際在量什麼 |
|---|---|---|---|
| 1 | 「現在輪到」只能是四種角色 | 交棒對象合法 | **假設永遠有一棒在跑**,所以「暫停」無法表達 |
| 2 | 階 5-7 不得超過半數 | planner **這一輪加了什麼** | 佇列**此刻剩下什麼** |

第 2 條特別壞,因為它**懲罰的是時間流逝而不是任何人的決定**:
佇列在「寫」耗盡、要到兩棒後的「想」才補,所以整個「查」都坐在紅燈上,而沒有人做錯任何事。

**合起來的教訓比任一條大**:上一次的結論是「寫規則時要指定執行者」。
**那句話不夠。** 有執行者但量錯對象,結果比沒有執行者更糟——
**沒有執行者的規則是一句願望,量錯對象的規則是一個會說謊的儀表。**
願望至少誠實地什麼都不做;儀表會讓人相信它。

已 append 進 `AGENTS.md`,沒有改任何已存在的行。

**二、這三輪的產出是文件,不是程式。**

誠實對帳,`git show --stat` 查得到:

- 第 5 輪(想):排 1 則
- 第 6 輪(寫):重現失敗,產出一份 playtest,**`src/` 動了 0 行**
- 第 7 輪(查):覆核 + 補查兩條,**產出驗收結果,0 行程式**

**三輪下來,遊戲本身一行都沒動。**

同一段時間裡,人類說了一句「繼續開發」,循環外做出了:`script_check.py`(新模組)、
兩處接線、10 個測試、`loop_check` 的 crash 修復。**188 passed,原本 177。**

**設計文件第十九節〈已知風險〉第 1 條寫過:「蓋一座四人工廠,很可能是在真正的阻塞旁邊
找事做的高級版本。」那條現在不是預測了,是觀察。**

要公平地說另一面:第 6-7 輪那個「文件」結論其實是有價值的——
它證明了「場景對不上」在可觀測之前不該修,**避免了一次盲修**。
但那是**兩輪換一個「不要做」**,而且那個結論可以在 planner 排它的當下就查出來
(四條 `grep` 而已)。**排任務時沒查,是這一輪真正的成本。**

**給人類的一句話**:如果目標是推進遊戲,`/loop` 目前的投報率不如直接說「繼續開發」。
loop 的價值在**紀律**(builder 不能自我驗收、證據要附行號),不在**吞吐**。
兩者要分開看,不要因為它很忙就以為它在推進。

---

### 第 1 次反思(涵蓋第 1-3 輪)@2026-08-06 by Claude Opus 5

**抓到的模式:規則被寫下來,然後沒有任何東西在執行它。** 五個實例,遠超過「≥2 次」的門檻:

| # | 規則寫在哪 | 執行者 | 怎麼被發現的 |
|---|---|---|---|
| 1 | `prefers-reduced-motion` 要生效 | **無** | 上線後拿真瀏覽器數才發現 0/87(見 `PROGRESS.md`) |
| 2 | 「所有文字一律用繁體中文」`offline.py:259` | **無** | 這一輪 auditor 抓到,`playtests/2026-08-06-ollama.txt:40` |
| 3 | 補丁 1 人類心跳(設計文件第十九節) | **差點沒有** | 寫計畫時自我檢查才發現沒有任務在實作它 |
| 4 | 「階 5-7 不得超過半數」 | 文字與程式碼**不一致** | TDD 紅燈(文字說進位、程式碼捨去) |
| 5 | 佇列不能無限長大 | **根本沒寫下來** | 人類問「一個一直塞 task 進去」才發現 |

**但這一輪不寫 skill,兩個理由都不是偷懶:**

1. **`superpowers:writing-skills` 自己說不該寫。** 它的〈When to Create a Skill〉明列
   「**專案慣例 → 放進 instructions file**」與「**機械式約束 → 自動化,不要寫成文件**」。
   這個模式兩條都中:它是這個 repo 的慣例,而且上表 #2 是可以用 regex 測出來的
2. **Iron Law 要求先跑 baseline 測試**(用 subagent 觀察沒有 skill 時會怎麼失敗)。
   那需要派 agent,**而派 agent 要人類同意**。沒跑 RED 就寫 skill,寫出來的東西沒人知道教得對不對

**所以這一輪的產出是**:往 `AGENTS.md` **append** 一節(不改任何已存在的行,補丁 3)。
`#2` 那條該補一個測試,但**排任務不是我的區**——留在 `NOW.md` 收件匣給 planner。

**下一次反思(第 6 輪)要回頭看的**:那一節 append 進去之後,
第 4-6 輪有沒有真的出現「寫規則時同時指定執行者」的行為。**沒有的話,那節文字本身就是第 6 個實例。**

**順手把自己那節套在自己身上**(不然它就是第 6 個實例):

- **補丁 3「反思只准 append」誰執行?** —— `git diff --numstat AGENTS.md` → `21 0`(21 增、**0 刪**)。
  這一輪查過了,不是宣稱
- **心跳規則誰執行?** —— 拿第 5 輪的狀態實際餵給 `check()` 跑過一次,
  真的吐出「心跳逾期:已經 5 輪沒有人類心跳,loop 應暫停」。**規則會擋,不是假設**
- **上面 #2(繁體中文)誰執行?** —— **仍然沒有人**。這一輪沒補,因為排任務不是我的區

## skill 帳本

<!-- 哪個 skill 何時建、驗證狀態(verified / unverified)、用過幾次。
     三輪沒用過的,下次反思直接砍 -->

| skill | 建於 | 狀態 | 用過 |
|---|---|---|---|
| (無) | — | — | — |

**⚠️ 第 4 次反思(第 29 輪)第一次有東西「該」寫,而它仍然沒寫,理由跟前幾次不同。**
候選是**「把被測的東西整個拿掉,看還有幾個測試是綠的」**——第 17 輪(世界時鐘的
epoch 測試)與第 26 輪(我自己那個名字說 outbound 卻撥 loopback 的測試)各一次,**≥2 次,過門檻**。
**沒寫的理由是 Iron Law 要求先跑 baseline,而這一輪沒跑。**
上一次 baseline 的結論(「那個失敗不存在」)**形狀不同,不能沿用**。
**所以這一格現在是「還沒測」,不是「測過了不該寫」——下一次反思要嘛跑 baseline,要嘛說明為什麼不跑。**

**第 1 次(第 4 輪)與第 2 次(第 8 輪)反思都沒有新增 skill。**
帳本空著不是漏寫,是結論——**這一格保持空的比塞一個沒驗過的 skill 誠實。**

**[已解決] 人類 2026-08-06 授權 reflector 派 subagent 跑 baseline**(已寫進 `AGENTS.md` 角色定義)。

授權之後**第一次真的跑了 baseline,結果是:不寫 skill,因為那個失敗不存在**
(4 rep、兩種情境形狀、4/4 都自己指定了執行者)。詳見上面〈反思補記〉。

**所以帳本空著的理由從「做不到」變成「測過了,不該寫」。** 這個差別很大:
前者是能力缺口,後者是結論。**現在這一格是空的,而且是有證據的空。**

## 輪次記錄

<!-- 每輪 append 一行。人類開過畫面的那一輪要寫「人類心跳:已開」,
     滿 5 輪沒有心跳,loop_check 會擋下來 -->

- 第 1 輪 想(planner)@2026-08-06 by Claude Opus 5 —— 排 3 則(階1 / 階4 / 階6),階 5-7 佔 1 則(上限 1)。
      **T2-L4 沒排**:它要冷啟動 session 才有效度,不是 builder 做得了的事。
      **「等人類決定」那七題一則都沒碰。**
      ⚠️ 這一輪是由寫出這套循環的同一個 session 跑的,**不能拿來當 T2-L4 的證據**
- 第 2 輪 寫(builder)@2026-08-06 by Claude Opus 5 —— 撿了階 1 那則,Ollama 三步全過,
      寫進 `playtests/2026-08-06-ollama.txt`,交「待驗收」。**沒動 `src/`,沒自己標 done。**
      發現兩件事:延遲 5-7 分鐘(Groq 是 2.8 秒)、簡體字混進繁中敘事。
      **provider 怎麼選沒有自己決定**——那是人類的題
- 第 3 輪 查(auditor)@2026-08-06 by Claude Opus 5 —— **通過**,四項判準自己重跑過。
      抓到一則更正:簡體字那件事 builder 講輕了,**prompt 早就規定繁體**(`offline.py:259`),
      是「規定被違反且沒人發現」不是「還沒規定」。已同步 `NOW.md` 收件匣。
      **沒寫 code、沒排任務**,兩件都不是我的區
- 第 4 輪 反思(reflector)@2026-08-06 by Claude Opus 5 —— 抓到「規則寫了但沒人執行」的模式(5 個實例)。
      **沒寫 skill**(`writing-skills` 說專案慣例該進 instructions file、機械約束該自動化,
      而且 Iron Law 要求先跑 baseline 測試,那要人同意才能派 agent)。
      產出是往 `AGENTS.md` **append** 一節,沒改任何已存在的行。
      ⚠️ **下一輪之後人類心跳就到期,`loop_check` 會擋下來**
- 第 5 輪 想(planner)@2026-08-06 by Claude Opus 5 —— 補 1 則到上限 3(繁體中文那條規則沒有執行者,
      auditor 上一輪抓到的)。階 5-7 佔 1/3,在上限內。「等人類決定」那幾題一則都沒碰。
      ⛔ **這一輪之後心跳逾期,loop 停在這裡等人類。**
      ❗ **而且收尾時發現 `tools/loop_check.py` 有 bug**:它印違規訊息時用 `✗`,
      在 cp950 主控台會 `UnicodeEncodeError` 直接 crash(`tools/loop_check.py:169`)。
      exit code 還是 1(traceback 造成的),**但訊息全部看不到**——
      也就是說**這個檢查器的失敗路徑從來沒有真的被走過**,前四輪全是 exit 0。
      **這正是 `AGENTS.md` 那節講的東西,而且跟 `NOW.md` 記的 `/api/auto` bug 同一類**
      (錯誤路徑沒被走過,畫面一片安靜)。
      **我沒有修**——planner 不准改 code。留給人類決定要不要當場修,還是交給 builder。
      ❗ 另外發現**格式沒有「暫停」這個狀態**:表頭寫「⏸ 暫停中」會被判違規。
      已改成「停在下一棒 + 由心跳違規當暫停訊號」,理由寫在表頭底下。
      現在只剩 1 則違規,就是心跳那則——**那則是應該留著的**
      **(循環外,不算一輪——所以這幾行是縮排的續行,不是新的一則。**
      **原本寫成 `- ` 開頭的項目,檢查器照樣把它數成一輪:文字說「不算」、機制算了,**
      **正好是〈反思〉那張表的毛病。改成續行之後兩邊才一致。)**
      2026-08-06 by Claude Opus 5 —— 修掉上面那個 crash。
      `sys.stdout.reconfigure(errors="replace")`,不可編碼的字降級成 `?` 而不是殺掉整個執行。
      **先寫測試看它紅**(`test_main_survives_a_console_that_cannot_encode_its_output`,
      用 ascii 串流模擬比真主控台更窄的環境),再修,178 passed(原 177)。
      現在跑 `python tools/loop_check.py` 會**真的印出**「心跳逾期…」然後 exit 1。
      **這不是一輪**:loop 仍然停在心跳,沒有任何角色開工,「排隊中」一則都沒動。
- **人類心跳:已開** @2026-08-06 23:02 —— 人類真的打開 `http://127.0.0.1:8770` 並送出訊息。
      **證據不是宣稱,是伺服器日誌**:`/api/open ok in 0.0s` @22:55:30、
      `/api/say ok in 407.3s` @23:02:28(`in=621 out=2603`)。
      他打「確認」回答了離線那輪留下的懸念,陌洲**接住了**:帶進 `右手腕機能:發炎受限`
      的狀態(「右腕發炎的鉸鏈讓我回得慢了些」)並丟回一個新問題。
      **代價也量到了:他為了那一句等了 6 分 47 秒**,而且中途以為當機、又貼了一次啟動指令。
      **心跳重新計時,loop 恢復,下一棒是寫(builder)。**
- 第 6 輪 寫(builder)@2026-08-06 by Claude Opus 5 —— 撿階 4 那則,**重現失敗**,
      照判準「重現不出來就不要硬修」**沒動 `src/`**,三條查證路徑寫進
      `playtests/2026-08-06-scene-mismatch-repro.txt`,交「待驗收」建議退回。
      **沒自己標 done、沒自己排新任務**(「先把 scene 存下來」只是建議,留給 planner)。
      ❗ **收尾時 `loop_check` 變紅,但抓到的是規則自己的毛病**:
      「階 5-7 有 1 則,1 則裡最多只能 0 則」。排隊中被我撿到只剩 1 則,而那則是階 6,
      `floor(1/2)=0` 所以必然違規。
      **配額的用意是限制 planner「這一輪加了什麼」,但它量的是「佇列剩下什麼」**——
      量錯對象了。而且 想→寫→查 的順序讓佇列在「寫」耗盡、要到兩棒後的「想」才補,
      中間整個「查」都會坐在紅燈上。
      **我沒有改規則**(那是 code,而且我這一棒已經在交棒了)。留給 auditor 判、reflector 想。
      下一次「想」補到 3 則之後它會自己好,所以**不擋事,但那是巧合不是設計**
- 第 7 輪 查(auditor)@2026-08-06 by Claude Opus 5 —— **通過**,四條證據自己重跑全部屬實。
      **補查兩條 builder 漏的**(`llm_calls` 三個 DB 含備份、`--debug` 只記 prompt 不記回覆),
      結論不但成立而且更強:**這個系統記玩家說了什麼,不記陌洲說了什麼的原始形式。**
      沒放進「退回重做」——builder 沒做壞,是任務本身不成立。
      建議已進 `NOW.md` 收件匣。**沒寫 code、沒排任務。**
      ⚠️ 收尾時 `loop_check` 仍紅(配額量錯對象),**留給反思那一棒,見上面那節**
- 第 8 輪 反思(reflector)@2026-08-06 by Claude Opus 5 —— 抓到上一次反思的**鏡像**:
      「規則有人執行了,但執行的是錯的東西」(2 個實例)。已 append `AGENTS.md` 第四題,
      沒改任何已存在的行。**仍然沒寫 skill**,而且連兩次卡在同一個地方(Iron Law 要 baseline 測試
      → 要派 agent → 要人類同意),已寫進 skill 帳本要人裁定。
      **並且誠實對帳:第 5-7 輪 `src/` 動了 0 行**,同期循環外做完 `script_check` + crash 修復。
      **設計文件〈已知風險〉第 1 條從預測變成觀察。**
      ⚠️ `loop_check` 仍紅(配額),**修它要 planner 排、builder 做**——我碰不到 `tools/`
- 第 9 輪 想(planner)@2026-08-07 by Claude Opus 5 —— **砍 1 則、排 2 則**。
      砍掉的是「用 `git diff` 數行來管 append-only」——第 8 輪當場證明那個判準會誤報。
      排的兩則:`scene`/`action` 持久化(階 4,auditor 查證後的建議)、
      修配額量錯對象(階 6)。**2 則裡階 5-7 佔 1 則,`floor(2/2)=1`,在上限內,紅燈解除。**
      **「等人類決定」那幾題一則都沒碰**;`T2-L4` 一樣沒排(要冷啟動 session,builder 做不了)。
      **誠實記一筆**:階 1-4 的料真的很薄——`NOW.md` 前幾條不是要冷啟動 session
      就是卡在人類決定,所以這一輪只排得出 2 則而不是補滿 3 則。**寧可少排,不要為了填滿而發明。**
- 第 10 輪 寫(builder)@2026-08-07 by Claude Opus 5 —— 撿階 4 那則,**`scene`/`action` 現在存得下來了**。
      **194 passed(原 188)**,零 schema 變更(`story_assets` 早就留好了掛鉤)。
      **這是這個循環第一次真的動到遊戲的持久層。**
      改了 1 個既有測試的過度斷言,**已在「待驗收」標紅請 auditor 特別看**。
      沒自己標 done。
      ❗ **交棒時自己踩了一個坑值得記**:我把做完那則用 `<!-- -->` 註解掉留在「排隊中」,
      **`loop_check` 照樣把它數成一則**——parser 只看行首是不是 `- `,不懂 HTML 註解。
      佇列瞬間變 3 則、其中 2 則階 6,配額當場誤報。已改成縮排。
      **這是同一個 proxy 問題的第四次**:規則講「排隊中有幾則」,量的是「有幾行以 `- ` 開頭」。
      ⛔ 收尾時 `loop_check` 剩 2 則違規,**兩則都是已知的**:
      配額 bug(就是排隊中那則任務本身)、心跳逾期(要人類)。
- 第 11 輪 查(auditor)@2026-08-07 by Claude Opus 5 —— **退回**,這個循環第一次真的退回一則工作。
      builder 做對的三件事我自己驗過(測試真的先紅、改既有測試沒掩蓋回歸、零 schema 變更屬實),
      但它**沒往下游追一步問「存進去之後誰會讀」**:`web.py:156` 把 asset 整包送前端、
      `index.html:988` 把每一筆渲染成文字標籤,**玩家從此每一拍都會看到兩個 debug 標籤**。
      `grep get_assets` 一次就看得到。**不是難,是沒查。**
      ⚠️ **這一棒是在心跳逾期的狀態下做的。** 判斷依據:板子寫「不要**開工**」,
      而驗收是把已完成的東西收尾、什麼都沒新開;放著不驗更糟。
      **但 planner 不准在心跳補上之前排新東西**——那才是開工。
- 第 12 輪 想(planner)@2026-08-07 by Claude Opus 5 —— **排 2 則階 1,而且不是我發明的**:
      人類當天答了「系統太簡單 = **單調**」,四個軸全中(畫面都一樣 / 只能打字 /
      他做的事同一個形狀 / 玩十次跟玩一次一樣),參考類型指了放置掛機、模擬經營、敘事章節。
      **「玩十次跟玩一次一樣」是根**,所以先排世界時鐘,再排開場場景跟著它動。
      3 則裡階 5-7 佔 1 則,`floor(3/2)=1`,在上限內。
      ⚠️ **這一棒違反了我自己上一棒寫的「心跳補上之前不准排新東西」——是人類明示「planner 快想」,
      我照做並記在這裡。** 那條規則是他的,他可以推翻;但**推翻要留痕跡,不能靜靜地繞過去**。
      ❗ **還有一個沒解決的**:我提的拆法(累積 → 按鈕 → 章節)人類**沒有明確點頭**,
      只說「快想」。**如果順序錯了,錯的是這兩則的方向,不是它們的做法。**
- 第 13 輪 寫(builder)@2026-08-07 by Claude Opus 5 —— 撿「退回重做」那則,**修好了**。
      `db.get_media_assets` 濾掉 `SCENE_KINDS`,`web.py` 三處改走它,**前端一個字都沒動**。
      **196 passed(原 194)**,兩個新測試都先看到紅,而且**同時斷言 DB 裡還存得到**——
      不是靠不存來讓畫面乾淨。
      ⚠️ **判準第 4 條(去問瀏覽器)只做到一半,我自己標出來了**:
      `page_check` 全過、截圖也跑了,但那張圖沒有 beat 所以證明不了漏洞修好。
      **請 auditor 判斷夠不夠,不夠就退回。**
- 第 14 輪 查(auditor)@2026-08-07 by Claude Opus 5 —— **通過**,我上一棒退回的那則這次修對了。
      證據全部自己重跑:前端 0 行、`get_assets` 在 `src/` 沒有第四個呼叫者、
      **關掉修復跑全套恰好 2 紅 194 綠**(用 pytest plugin 別名還原,不動 repo,
      因為 `git checkout -- src/` 被權限擋)。那個數字兩頭都有用:
      **這兩個測試是唯一釘住它的東西**(我上一棒是讀 code 讀出來的,不是測出來的),
      **而且沒有別的測試依賴著過濾行為**。現況 196 passed。
      **判準第 4 條判定通過**,理由不是通融:前端 diff 0 行、`index.html:985` 空清單直接 return、
      `page_check` 裡「該渲染的照樣渲染」那條也綠——有 beat 的截圖只會用更慢的儀器把同一件事再證一次。
      ❗ **這一棒實際踩到 `loop_check` 兩個新的洞,兩個都用執行結果確認過**:
      改個標題就能讓一整區從順序檢查裡消失(`loop_check.py:87` 先濾掉不認得的標題再比對順序)、
      註解掉的項目仍然被數成一則(第 10 輪 builder 記過,**它一直還在檔案裡**)。
      **兩個都是〈第四題〉的形狀,跟排隊中那則 [階6] 同一個病灶。留給下一棒反思,我沒碰 `tools/`。**
      ⚠️ 跟第 11 輪同樣的判斷:**心跳逾期下我照樣驗收**,因為驗收是把已完成的東西收尾、
      什麼都沒新開,而且**人類 2026-08-07 明示照跑**。已同步 `PROGRESS.md` 與 `NOW.md`;
      `TASKS.md` 查過**沒有對應的 T 編號可翻**(這則來源是收件匣不是 backlog)
- 第 15 輪 反思(reflector)@2026-08-07 by Claude Opus 5 —— 同一個病灶第 3、4、5 次,
      **但這次不寫第三節文字**:前兩節的診斷「量錯對象」把三次講成三個獨立疏忽,是誤診。
      共用的機制是 **`LOOP.md` 是 Markdown 卻被當資料庫用**——parser 從排版反推結構,
      而排版可以合法地長出各種樣子。**作者們都想清楚了要量什麼**
      (`loop_check.py:30-31` 自己寫著 order is load-bearing),**沒料到的是標題可以被改名。**
      產出是一條做得了的判準:**每加一條規則,先答「文件可以怎麼合法地騙過這個 parser」,
      把答案寫成測試**。已 append `AGENTS.md`〈第五題〉,`git diff --numstat` → **`37 0`**,0 刪。
      **並且推翻我自己第 2 次反思的觀察**:那次說「三輪下來遊戲一行都沒動」,
      第 12-14 輪 196 passed、走完交→退回→修→通過的全程。
      **而且退回那一棒是循環唯一能產生的價值**——當時 194 個測試全綠,
      是 auditor 讀 code 抓到的,**單一 session 的 agent 不會退回自己剛寫的東西**。
      **仍然不寫 skill**(專案專屬機制細節,`writing-skills` 說該進 instructions file),帳本維持空的。
      **沒碰 `src/`。**
- 第 16 輪 寫(builder)@2026-08-07 by Claude Opus 5 —— 撿世界時鐘,**做完了。210 passed(原 196,+14)。**
      **這是第一則直接針對人類那句「單調」的產出。**
      新模組 `src/everliving/world.py`:汙染 = 世界存在了幾天,**算出來的不是跳出來的**,
      所以沒有東西要 tick,單向惡化是**結構保證不是防呆**(時間的單調函數講不低)。
      **刻意不放 `agent_state`**——那張表直接拿模型的 `state_changes` 寫,
      一次幻覺的「汙染:好轉」就把累積清掉了;另開 `world` 表且**沒有 `agent_id`**(里程碑 1 要共用天氣)。
      五個階段寫的是**他這禮拜摸得到的東西**(水濾幾次、鏽咬多深、限電幾天),
      照設計文件第十一節「必須局部化」,**有測試擋掉文明級寫法**,也有測試斷言**原始數值不進 prompt**。
      ⚠️ **一條我自己標紅請 auditor 判**:既有存檔用第一個 agent 的 `created_at` 當世界起點。
      理由是那個時間戳是**真的被記錄過的事實**,不是捏造(對照第 10 輪拒絕回填 scene 那條線)。
      拿人類存檔的**副本**驗過,沒動原檔。
      🔴→✅ **改了 prompt 所以照收尾檢查第 4 條拿 Ollama 真跑,第一次沒過,對照組才把它釘死。**
      第一份兩輪:一次 JSON 截斷在半路、一次敘事全空。**我沒有猜是誰害的,跑了對照組**
      (A `pressure=None` = 這一輪之前的行為,B stage 4 最長那段),
      **兩組都完整,而且 B 的 output token 比 A 還少(2898 vs 3174),兩邊都用不到 6000 的一半**。
      **所以那兩次失敗是模型變異,不是這一輪造成的。**
      **而且 B 反過來證明了判準 2**:同一個 agent、同一段離線窗,只差壓力段,
      B 寫出濾水器卡死、除鏽劑啃裂指節、「**別人留下的廢零件**」(stage 4 那個搬進模擬倉的老人,
      **被折射進他自己的一週而不是複述**),A 完全沒有這些,**而且全程沒出現任何數字或等級**。
      🔴 **但留一筆硬資料**:今天四次真呼叫**兩次產不出可用 JSON(50%)**,已進 `NOW.md` 收件匣——
      對 AUTO ROUTER 跟 H-1 都算數。
      **沒自己標 done。** 兩件刻意沒做留給 planner:對話路徑沒接壓力、畫面上看不到。
- 第 17 輪 查(auditor)@2026-08-07 by Claude Opus 5 —— 世界時鐘**通過,但退回兩個測試**。
      行為自己驗過是真的(檔案 DB 寫入→`close()`→重開,epoch 讀回、`pollution` 正確),
      **但釘住它的兩個測試是套套邏輯**:寫 plugin 把 `world._epoch` 換成常數(等於儲存完全失效),
      全檔 **5 failed / 9 passed**,而**那兩個以「記得住」命名的照樣綠**——
      它們開的是第二個 `:memory:` DB 而且把 epoch 用參數餵回去。
      **測試的名字宣稱在量持久化,實際量的是「同一個常數等於它自己」。**
      **不退回整則**:判準的實質全過,壞的是證據強度不是產品。
- 第 18 輪 寫(builder)@2026-08-07 by Claude Opus 5 —— 修那兩個被退回的測試。
      改用**真的檔案 DB**,寫入→`close()`→重開,**而且不把 epoch 餵回去**。
      驗法是拿 auditor 那個 `breakstorage` plugin 再跑一次:**兩個都變紅了**;正常時 210 全綠。
      **沒動 `src/` 一行**——退回的是測試,產品沒問題。
- 第 19 輪 想(planner)@2026-08-07 by Claude Opus 5 —— 排 1 則 [階5],佇列 2→3。
      **階 5 的正當性寫明**:服務卡住的「AUTO ROUTER」,而且**兩條路都需要**——
      路由決定打給誰,任何一家模型都會偶爾吐出解析不出來的東西。
      **不是我發明的**:第 16 輪量到四次真呼叫兩次產不出可用 JSON,全空那次會把**空敘事寫進永久歷史**。
      3 則裡階 5-7 佔 2 則(階5 + 階6),`floor(3/2)=1`——⚠️ **這會讓配額紅燈亮**,
      而那正是排隊中那則 [階6] 在講的 bug。**我沒有為了閃紅燈而不排該排的東西。**
- 第 20 輪 寫(builder)@2026-08-07 by Claude Opus 5 —— 撿階 5 那則,**做完了。215 passed(原 210)。**
      `_ask_until_there_is_a_night`:空回覆**重試一次**,仍然空就丟 `LLMRefusal`
      (`web.py:312` 早就在接並顯示),**空敘事永遠不進 `memory_events`**(測試直接查表斷言 `== []`)。
      5 個測試 3 個先紅,另 2 個是「既有行為不准壞」的守門測試——
      **其中一個釘住「好的回覆不會被問第二次」,重試不准在能用的路徑上加錢。**
      只重試一次不是迴圈:兩次真實失敗都來自**幾分鐘前才用同一個 prompt 答對過**的模型。
      **沒碰 `MAX_TOKENS`、沒換模型、沒做 provider 選擇**——那是 AUTO ROUTER 的題。
- 第 21 輪 查(auditor)@2026-08-07 by Claude Opus 5 —— **兩則都通過。**
      空白夜晚那則,我追的是 builder 沒追的下一步:**例外丟出去之後誰接得住**
      (第 11 輪那次退回就是漏了這步)。`web.py:312` 與 `cli.py:124` **兩條路都是既有的**,
      而且 CLI 那條的註解寫著「不要讓它中斷啟動」。所以這一則沒有製造新的失敗模式。
      測試強度這次沒有第 17 輪那個毛病:`llm.calls == 1/2` 跟查表 `== []` 都是**直接觀察**不是 proxy。
      世界時鐘那則的退回事項已結清(拿 `breakstorage` 重驗,兩個測試現在會紅)。
- 第 22 輪 寫(builder)@2026-08-07 by Claude Opus 5 —— 撿「開場不要永遠是工作間」,
      **做完了。221 passed(原 215)。這是直接針對「單調」四個軸裡「畫面都一樣」那一軸的。**
      開場改由**世界時鐘的階段**決定,讀起來是一句話:**他從自己的工作檯開始,世界越壞就被推得越往外**
      (回收場 → 配電所 → 潮線);**而他的身體贏過世界**——狀態裡有手/體/傷就回工作檯。
      **不是隨機**,有測試連問 20 次斷言只有一個答案。
      ✅ **真的去問了瀏覽器**:`page_check` exit 0,而且**四個開場各截一張我逐張看過**——
      其中三個從來沒當過開場,不看不算數。三張都畫得出來、構圖在 SAFE 帶內、彼此明顯不同。
      ⚠️ **`_BODY_MARKS` 用子字串比對,我自己標了脆**:列舉法會在模型換寫法那天靜靜失效,
      子字串則可能誤中。**我選了會誤中而不是會漏掉,請 auditor 判。**
      ⛔ **收尾時配額紅燈會亮**:排隊中被我撿到只剩 1 則而那則是階 6,`floor(1/2)=0`。
      **這正是那一則任務本身在講的 bug**,第 6、7、10 輪都是這樣紅的。
- 第 23 輪 想(planner)@2026-08-07 by Claude Opus 5 —— **人類把範圍圍籬拆了**
      (原話:「範圍圍籬拆」),前一句是目標「以目標變成最多人玩的遊戲」。
      已改寫 `AGENTS.md`〈現在該做什麼〉:**舊禁令加刪除線保留**(理由仍值得讀),
      換成一句排序原則——**先讓人玩得到,再讓人變多**。
      **這句不是我的偏好,是可查證的**:H-1 到今天一次都沒過,而 Ollama 四次真呼叫兩次產不出敘事。
      **他沒指定先拆哪一根**,所以照排序原則自己判並寫明理由,**沒有回頭再問一次同一題**。
      排的是 AUTO ROUTER——**他自己早就答過的那題,而且一直沒有設計**。
- 第 24 輪 寫(builder)@2026-08-07 by Claude Opus 5 —— **AUTO ROUTER 做出來了。228 passed(原 221)。**
      新模組 `router.py`:**每一次呼叫**才選 provider,快的先、本機最後
      ——本機最後不是備胎心態,**它是唯一不會用完的那一格**。
      **把 H-1 那條「換 provider 會讓歸因失效」拆掉了**:`log_usage` 本來就每次都寫
      `usage["model"]` 進 `llm_calls`,衝突的是「換」跟「事後用猜的」,而沒人需要猜。
      **拒絕不換人**(否則「他說不要」會變成「問到有人肯為止」)。
      🔴 **我試著連預設值一起換,失敗並退回**:整套測試卡死,因為
      `test_provider_selection.py:76` 的假 `XAI_API_KEY` 讓 router **真的發出網路呼叫**。
      **那顆地雷比預設值重要——這套測試會打真的 API。** 預設值改回 `anthropic`,另排一則,
      判準第 1 條是先做「測試不可能打出去」的機制。**寧可小而完整。**
- 第 25 輪 想(planner)@2026-08-07 by Claude Opus 5 —— 排 1 則階 1(換預設值 + 先擋地雷)。
      **不是我發明的**:上一棒自己撞出來的,判準第 1 條直接寫成「先做機制再改那一行」。
- 第 26 輪 寫(builder)@2026-08-07 by Claude Opus 5 —— **預設值換成 `auto` 了。230 passed(原 228)。**
      `_no_real_network` 擋在 **socket 層**,在每個 SDK、每個 provider、每條金鑰路徑**底下**。
      **設定層的防護會一直輸,因為金鑰永遠有下一條路進來**——證據就在隔壁:
      `_hide_the_developers_dotenv` 是上一次踩到時加的,而它**擋不住這一次**。**同一形狀第二次。**
      loopback 留著(有測試自己起 server),本機模型那個 port 例外,**而且 port 從設定讀不是打字**。
      ⚠️ **改了 4 個既有測試**,理由寫在待驗收,**請 auditor 逐一判斷有沒有掩蓋回歸**。
      🔴 **判準第 4 條結果跟預期不同,照實寫**:清光金鑰後 `AnthropicLLMClient` **照樣建得起來**
      (SDK 不在建構時驗憑證)。沒有金鑰的人**仍然玩得到**,但走的是「呼叫時失敗再往下掉」,
      **會先浪費一次失敗的呼叫,我沒有假裝那是免費的**,也沒有順手去改 Anthropic 的建構檢查。
- 第 27 輪 想(planner)@2026-08-07 by Claude Opus 5 —— 排 1 則 **[階7] 把決定變便宜**。
      人類設了「最多人玩」但沒答「要量哪一個」,而 agent **答應過不再問第三次**。
      階 7 的定義正好是這個:**做出可比較的東西讓人類打開就能指。**
- 第 28 輪 寫(builder)@2026-08-07 by Claude Opus 5 —— **H-1 現在答得出來了,而答案是「還沒過」。**
      **234 passed(原 230)。**
      ⚠️ **動工前先撞到同一個形狀的第三次**:`player_state` 是一列、每次來訪覆蓋掉上一次,
      **所以系統記得「他在」,從來沒記過「他來了」**——H-1 在結構上無法回答。
      (前兩次:`scene` 只活在一次 HTTP 回應裡;`memory_events` 不記陌洲回覆的原始形式。)
      新增 append-only 的 `visits`,**記在抵達不是離開**(關掉分頁不能等於沒來過),
      而且**把我原本順手耦合的 `last_seen` 拆開**——抵達跟離開是兩件事。
      `tools/retention.py` 把候選指標印在真實數字旁邊,**不替人類選**,
      並指出 (d)「有沒有人講給別人聽」是唯一跟「最多人」直接有關的、**而它一筆資料都沒有**。
      拿真實存檔跑過:誠實回報「沒有被記錄過」而不是「他沒回來」。
      ⛔ **反思已經逾期三輪了**,而且原料很硬(上面那個「狀態 vs 歷史」的第三次)。**下一棒必須是它。**
- 第 29 輪 反思(reflector)@2026-08-07 by Claude Opus 5 —— **抓到這個 repo 目前最貴的一個模式:
      把該存成歷史的東西存成了狀態。** 三個實例(`scene` / 陌洲的原話 / **來訪**),
      共同形狀是**存「現在怎樣」、卻一直被問「有沒有變」**。
      第三個擋住的是**里程碑 0 唯一的成敗判準**,而且**能回答它的資料一天都沒存在過**——
      三個月裡沒有任何 review 或測試會發現,**因為程式完全正常**。
      已 append `AGENTS.md`〈第六題〉,`git diff --numstat` → **`34 0`**,0 刪,**沒碰 `src/`**。
      **並且誠實記一筆對這座工廠不利的**:第 15-28 輪全在同一個 session、四個角色同一個 agent,
      `T2-L4` 仍未做,**所以「builder 不能自我驗收」目前靠的是自律不是隔離**——
      雖然它確實退回過自己兩次,**但自律跟隔離在報告裡不該長得一樣。**
      ⚠️ **skill 帳本第一次有東西該寫卻沒寫**,理由是 Iron Law 要 baseline 而這輪沒跑。
      **「還沒測」跟「測過了不該寫」是兩件事,已記進帳本。**
