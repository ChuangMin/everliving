# NOW.md — 醒來先讀這一份

> **這份是「現在」,不是歷史。** 歷史在 `PROGRESS.md`,那份只增不改;這份**每個 loop 結束前整份重寫**,
> 永遠只描述當下。這正好是這個專案自己的規則套在自己身上:
> `PROGRESS.md` 是 append-only 的來源,`NOW.md` 是壓縮後的視圖(見設計文件〈記憶磨損〉)。
>
> **順序:先讀這份 → 要細節再去 `TASKS.md` → 要理由才去 `PROGRESS.md` / 設計文件。**
>
> **如果你是被當成四種角色之一叫起來的**:改讀 `LOOP.md` 的「現在輪到」,契約在 `AGENTS.md`〈四種角色〉。

**最後更新**:2026-08-08 by Claude Opus 5(`LOOP.md` 第 38 輪 auditor)

## ✅ 場景圖驗收通過了,而這次的證據是伺服器的請求記錄,不是截圖

**272 passed / `node tests/page_check.js` exit 0 / `python tools/loop_check.py` 只剩心跳一盞紅。**

三則一起通過:**場景圖管線**、**場景圖真的畫出來了**、**CSP 修復 `d218c4e`**。

**病因回顧**:`index.html` 的 `Content-Security-Policy` 寫著 **`img-src data:`**
——這個頁面只准載入 data: URI 的圖片,所以 `/scenes/…` 被瀏覽器**在發出請求之前**就擋掉。
**每一層都是對的**:檔案對、路由對、payload 對、標記對,
**唯一沒有人讀的是那條政策,而被擋掉的圖跟壞掉的圖長得一模一樣。**

**auditor 沒有採信任何宣稱,做的是會失敗的對照組**:

- 政策改回 `img-src data:` → 新測試**變紅**,而且真瀏覽器**整份請求記錄裡一個 /scenes/ 都沒有**
- 政策是 `img-src 'self' data:` → `GET /scenes/工作間.webp 200`,**畫面上是工作檯**
- 世界時鐘真的換圖:DB 推到第 20 天(stage 2)→ 瀏覽器要的是 `工作間-s2.webp`

⚠️ **為什麼不用 headless 截圖當主要證據**:這一輪栽在它上面兩次
(測試圖是漸層而故障是放大 → 那個檢查不可能失敗;第二次是頁面 65ms 就關掉)。
**一個不可能失敗的儀器不是證據。**

## 🔴 新發現:載不到的圖不會安靜地讓路,它會蓋出一團模糊的光

`src/everliving/static/index.html:324-325` 的註解寫著「圖 404 時 `<image>` 畫不出東西,
底下的向量圖自己透出來,**退路是免費的**」。**只對了一半**,兩個對照組:

| 情況 | 畫面 |
|---|---|
| 伺服器**根本不送 URL**(沒有那張圖) | ✅ 向量場景完整畫出來,退路是真的 |
| 伺服器**送了 URL 而瀏覽器要不到**(404 / 被政策擋 / 載入失敗) | 🔴 **一團模糊的光** |

**這就是人類看到的那個畫面,也是六輪誤診的一半來源。**
**重現只要一行**:`web.scene_image_url = lambda scene, stage: "/scenes/missing.webp"`。
**驗收仍判通過**(那一則自己的三條判準全中,而這是它沒被要求做的事),**但問題沒解決**——
**要 planner 排一則。**

## 🎨 給人類:畫面現在等的是圖,不是程式

**丟一張圖進 `src/everliving/static/scenes/`,重新整理就換掉了。** 不用重開伺服器、不用改任何 code。
**檔名規則、尺寸、以及六個場景的生圖 prompt 全部寫在那個資料夾的 `README.md`。**

現在資料夾裡有三張程式構圖的 `工作間{,-s2,-s4}.webp`(`tools/draw_scenes.py`),
**2026-08-08 16:10 另一個 session 用 ambientCG 的 CC0 灰階材質重算過一次**(commit `88c1f5d`)。
**那一版還沒有被驗收**——上面那三則驗的是它之前的圖,結論(管線、CSP、時鐘換圖)不受影響,
**但「新的材質版好不好看」沒有人判過,那本來就要人自己看。**

⚠️ **agent 這邊沒有生圖工具,畫不出真正的美術。** 管線是通的、端到端驗過,**缺的是圖本身。**

---

## ⚠️ 人類 2026-08-08 又給了兩件事(原文照抄,不要改寫成別的意思)

### 一、方向:**「盡量讓人感覺是真實的世界 天氣 陽光 物質 物理」**,以及一個字:**「風」**

**這句話立刻抓到一個剛做出來的退步**:向量場景本來跟著**真實時間**換天色、
跟著**真實潮汐**升降水位、會下雨(`index.html` 的 `skyFor(hour)` / `tideAt` / `rain`)。
**貼上靜態場景圖之後,那些全部被蓋掉了**——畫面凍在算圖那一夜。
**加圖換到了細緻度,賠掉的是「這個世界在動」,而後者正是這句話要的。**

**已排進處理**:場景圖的窗戶開成透明,讓底下的天空/潮水/雨透出來
——**室內是細緻的圖,窗外仍然是真實時間**。

「物質」那一半已經動了:`tools/textures/` 三張 CC0 灰階材質(鏽/金屬/混凝土)。
**「物理」還沒有任何東西**——目前世界只有一條單向的汙染曲線,
潮汐是畫面效果、不進模擬,東西不會壞、不會被用完。**這條沒人排,也沒人裁定要做到哪。**

### 二、卡住的一題:**「每個人如果進程都不一樣 之後怎麼連載一起?」**

**現況**:`world.started_at` **每個存檔一份**,所以你的世界第 20 天、別人的第 3 天,
**兩邊沒有共同時間,連載不起來。**

**但這題比看起來好解**,因為第 16 輪的決定是「汙染是**算出來的**不是跳出來的」:
**只要起點是同一個,所有人算出來的就是同一天,不需要同步、不需要伺服器。**

三條路,**agent 不替人類選**:

| 路 | 連載 | 代價 |
|---|---|---|
| **全域常數起點** | 免費 | 兩年後加入的人一開場就是 stage 4,**永遠看不到 stage 0** |
| **分季(season)** | 同季可以 | 要有季的概念,跨季接不起來 |
| **兩個時鐘**(天氣全域 / 關係個人) | 可以 | 要分開兩種進度,工作量最大 |

**agent 的建議是第三條,而且理由是現成的不是發明的**:`world` 表**刻意沒有 `agent_id`**,
設計文件寫「里程碑 1 會有好幾個 agent 站在**同一場天氣**裡」——
**天氣本來就被設計成共用的,只是現在共用範圍是一個存檔。**

⚠️ **改起點會把所有既有存檔重新定年,不可逆。** 所以這題必須人類拍板。

---

## ⚠️ 人類 2026-08-07:目標 **「以目標變成最多人玩的遊戲」**,而且 **「範圍圍籬拆」**

**兩句原文照抄,不要改寫成別的意思。**

**圍籬已經拆了,`AGENTS.md`〈現在該做什麼〉已改寫**——舊禁令(不要做多 agent / 世界地圖 /
金流 / 帳號系統 / 前端框架 / 向量檢索)**加刪除線保留**,因為那些理由是關於
「提前做會浪費額度」,不是關於「那些東西不好」,仍然值得讀。

**但拆掉圍籬不等於全部一起開工。排序原則現在是一句話:**

> ### 先讓人玩得到,再讓人變多。

**理由可查證,不是偏好**:**H-1(一個人隔天想不想再打開)到今天一次都沒過**,
而 2026-08-07 量到 Ollama 六次真呼叫**四次產不出可用敘事**、一次要等 5-7 分鐘。
**一個一個人都留不住的東西,再多人來也會走。**

**他沒有指定先拆哪一根。** 沒指定的時候照上面那句自己判斷並寫明理由,
**不要回頭再問一次同一題**,但也不要假裝他指定了某一根。

---

## 一句話:現在最重要的是什麼

> **🔴 遊戲那一側這幾天真的動了**:世界時鐘在走、開場會跟著它變、空白的夜晚有下場了、
> `--provider auto` 是預設、`visits` 記得住「他來過」、陌洲的原話存得下來、
> **而且畫面上終於有一張真的圖。**
>
> **但開發循環那一側正在悄悄失效,而且這是連續第二輪查到同一件事:**
> **`待驗收` 現在 9 則(第 38 輪一度長到 11,清掉 2 則之後又回到 9),
> 而 `loop_check` 對那一區一道檢查都沒有。**
> 「進行中」有上限 1、「排隊中」有上限 3 加階數加判準三道,**「待驗收」零道**
> (`tools/loop_check.py:36` 是它唯一出現的地方,在順序名單裡)——
> 所以「查」這一棒可以無限期落後,**而沒有任何東西會叫**。
> 第 22、24、26、28、30、31 輪交的東西到今天仍然沒被驗過。
>
> **同一個病灶還有兩個**:`LOOP.md` 表頭的「本輪次」漂了兩輪(寫 36,記錄已到 37);
> **CSP 那一輪根本沒有自己的輪次記錄**。**都沒有回填**——不在場的輪次編出來就是造假。

## 🔴 你這台機器上 AUTO ROUTER 根本沒在動

**`.env` 裡有 `EVERLIVING_PROVIDER=groq`,它會蓋掉預設的 `auto`**
(`src/everliving/web.py:412` 讀得到這個環境變數)。

**後果**:groq 一旦撞到那個每日 200k tokens 的牆,你看到的是錯誤,
**不會自動掉到 ollama**——而「不會用完的那一格」正是 AUTO ROUTER 存在的理由。

**要走 router 就明寫**:

```
python -m everliving.web --provider auto
```

**這不是 bug,是設定優先序**(命令列 > 環境變數 > 預設值,而 `.env` 是環境變數那一層)。
**要不要把 `.env` 那一行拿掉是你的題**,agent 沒有動它。

## 下一個 loop 該做什麼(照順序)

1. **[下一棒,已逾期很久] 反思(reflector)。** 上次是第 29 輪,規則是每 3 輪。
   **原料現在很硬,而且是同一個形狀的三個實例**:
   **`待驗收` 沒有上限**、**`輪次記錄` 漏了第 32、33 輪跟 CSP 那一輪**、
   **表頭的「本輪次」自己會漂**。加上 `PROGRESS.md` 曾經漏掉第 15-33 輪——
   **四個都是「同步綁在落後的那一棒上」**
2. **[積壓,要「查」那一棒清] `待驗收` 還有 9 則沒驗過。** 最舊的是第 22 輪的開場場景。
   **不要一次全驗**——一棒驗一到兩則、附證據,才是這一棒的價值
3. **[要 planner 排] 載不到的圖不會退回向量圖**(見上面那節)。
   **先寫一個會紅的測試,而且不能用截圖當儀器。**
4. **[排隊中,builder 撿] 重試是白花的一次呼叫。** 判準是「重試必須跟第一次不同,否則不准重試」。
   ⚠️ **它要真模型跑到至少一次成功才算完成**,而 2026-08-07 三次長時間執行都被中止。
   **這是它至今沒被撿走的真正原因**
5. **[要人開別的 session] T2-L4 空跑一輪** —— 新蓋的循環唯一的驗收,**到現在仍然沒做**。
   **啟動指令**(開新 session 貼這一句,不要多講;Claude Code 可改打 `/turn`):
   ```
   讀 LOOP.md,照它說的做這一輪。做完更新「現在輪到」交棒。
   ```
   **不能由寫它的 session 自己扮演**。**最好開 Codex / Qwen**——
   `AGENTS.md` 號稱廠商中立,沒被別家驗過就只是宣稱
6. **[設計,不進 `src/`] 第十八節:玩家的身體** ← 人類已裁定要做,見〈已裁定但還沒動工〉

## 這幾輪做了什麼(第 15-38 輪,2026-08-07 ~ 08-08)

**196 → 272 passed。這是循環第一次連續多輪真的在推遊戲。**

- **世界時鐘**(第 16 輪):汙染 = 世界存在了幾天,**算出來的不是跳出來的**,所以沒有東西要 tick。
  真模型對照組證明敘事讀得出壓力、**全程不提數字**(`playtests/2026-08-07-world-clock-control.txt`)
- **開場不再永遠是工作間**(第 22 輪):由世界階段決定,**而他的身體贏過世界**
- **空白的夜晚有下場**(第 20 輪):重試一次、仍然失敗丟 `LLMRefusal`,**空敘事永遠不進 `memory_events`**
- **AUTO ROUTER**(第 24 輪)+ **預設值換成 `auto`**(第 26 輪),
  `tests/conftest.py` 的 `_no_real_network` 擋在 **socket 層**
- **`visits`**(第 28 輪)+ **量測鏈走過一次**(第 30 輪):記在**抵達**不是離開
- **配額假警報修掉**(第 31 輪):只在「上一棒是想」時檢查
- **陌洲的原話存得下來**(第 33 輪):`llm_replies`,零 migration,**剝掉場景標籤之前就存**
- **世界時鐘上畫面**(第 35 輪):壓力段 + `●●●○○` + 世界第 N 天,`.scene` 改 sticky
- **場景圖管線 + 第一批場景圖 + CSP 修復**(第 36-37 輪,第 38 輪驗收通過):見最上面

## 已裁定但還沒動工(人類 2026-08-06 決定)

**人類要:登入時能做「臉部設定 / 身體設定 / 聲音設定」,而且明確選了「玩家自己的化身」。**
選項當下就把代價寫在畫面上(「離線敘事退化成執行報告」),他仍然選了,**所以這是他的決定,照做**。

但照做之前要把連鎖攤開,不能假裝沒看到:

- **推翻的是第十二節〈定案建議:委託模式〉。** 那節的理由是機制不是偏好:
  你能直接命令他 → 回來讀到的只是自己命令的執行報告 → **驚喜沒了,而驚喜是核心賭注的燃料**
- **連帶影響第十七節**:主線的整個位置是「**你是他的外部記憶**」,
  那句話只有在**你是另一個人**時才成立
- **第十一節「玩家是稀缺資源」靠同一個前提**
- **`AGENTS.md` 把帳號系統列為現在不要做**。但**本機角色檔案 ≠ 帳號系統**

**要提的替代形態(提案,不是決定)**:第十二節那張表其實有第三列——**雙角色**
(你有自己的化身 + 他是獨立夥伴,各自行動)。
**那個形態能給你臉、身體、聲音,而不用把驚喜燒掉**。代價是工作量。

**如果堅持純化身**,有一題必須先答:
**「沒有驚喜之後,『想再打開來看它做了什麼』還剩什麼?」**

## 等人類決定(agent 不要自己往下猜)

> **2026-08-07 人類一次答掉三題**(原文照抄):
>
> 1. **provider → 「AUTO ROUTER」** —— **已做完**(而且是預設值)
> 2. **簡體字 → 「A」= 只記 log** —— **這就是現況,結案。**
> 3. **「系統還是太簡單了」→ 「單調,可以參考其他遊戲」** —— **已產出世界時鐘 + 開場場景 + 場景圖**

**下面這些仍然沒有答案:**

- **⚠️ 「最多人玩」的可量測代理是什麼** —— **東西做好了,打開就能指:**
  ```
  python tools/retention.py            # 或 python tools/retention.py play.db
  ```
  報表列了四個候選,**agent 不替你選**。**(a)(b)(c) 都只量得到「這一個人」,
  唯一直接對應「最多人」的是 (d)「有沒有人講給別人聽」,而那條線現在一筆資料都沒有。**
- **雙角色 還是 純化身?**——**現在是最擋路的一題**
- **H-1 playtest** —— **里程碑 0 唯一的成敗判準**,到今天一次都沒過
- **T0-18 懸念要不要「過期成後果」**
- **第十七節主線劇情是提案**(而且**化身決定會影響它**)
- **`everliving.db` 要不要清乾淨**:所有驗證與 demo 都跑在別的 DB 或副本,**沒動過原檔**
- **`.env` 那行 `EVERLIVING_PROVIDER=groq` 要不要拿掉**
- **人類心跳逾期**:`loop_check` 說 31 輪,**實際更多**(`輪次記錄` 漏了三輪)。
  **你 2026-08-08 真的打開過畫面並回報「沒看到工作台」**——那句話是 CSP 那個病因的唯一來源,
  **但「輪次記錄」還沒有那一行**。**開完請在 `LOOP.md`「輪次記錄」補一行含 `人類心跳:已開`**,
  計數才會重來。**agent 沒有替你補**:不在場的輪次補了就是回填

## 收件匣

- **[🔴 新,2026-08-08,已驗證] 載不到的圖不會退回向量圖,它會蓋出一團模糊的光。**
  見上面那一節。`index.html:324-325` 的註解是已知錯的。**要 planner 排一則。**
- **[🔴 新,2026-08-08] 材質版場景圖(`88c1f5d`)還沒被驗收。**
  另一個 session 在第 38 輪稽核進行中(16:10)重算了三張 webp 並加了
  `tools/fetch_textures.py` + `tools/textures/`(ambientCG,CC0,`PROVENANCE.md` 有出處)。
  **多 agent 併發是 `AGENTS.md` 明寫的前提**,這裡只是記下「有第二個寫入者在場」。
  **好不好看要人自己看。**
- **[🔴 沒驗到] `.scene` 的 sticky 沒有被瀏覽器證明過。**
  headless Chrome 從 CLI **捲不動頁面**,所以只證明了「頂端渲染正確」。**不要把它記成通過。**
- **[🔴 2026-08-08] `llm_replies` 存下來了,而沒有任何人讀得到。**
  `get_llm_reply` 只有測試在呼叫、`tools/` 零命中。
  對照 `tools/retention.py`:那則同樣是存資料,**但它附了讀的工具**。
  **驗收判通過,但問題沒解決。** 要 planner 排一則:**一個能列出「空的那幾筆」的小工具**
- **[🔴 連續兩輪查到] `待驗收` 沒有上限也沒有任何規則。** 現在 9 則,最舊的來自第 22 輪。
  `tools/loop_check.py:110-135` 對「進行中」「排隊中」都有檢查,**對「待驗收」零檢查**。
  **反思那一棒的原料。**
- **[🔴 同一個病灶] `輪次記錄` 漏了第 32、33 輪跟 CSP 那一輪,而心跳計數是數那一區算的**
  (`tools/loop_check.py:178-186`)。**所以檢查器少報。** 表頭的「本輪次」也漂了兩輪。
  **沒有補寫**——不在場的輪次編出來就是回填。
- **[🔴 更正,證據很硬] 重試是白花的一次呼叫。**
  同一個 prompt 連問兩次,`llm_calls` 記到 **`out=3423` 跟 `out=3423`,一個 token 都不差**。
  **一模一樣的輸出長度 = 那次生成是決定性的。**
  真正的失敗模式是:**這個 prompt 讓 qwen3.6 想了 3423 個 token 然後不作答。**
  **已排進 `LOOP.md`「排隊中」。**
- **[🔴 更正] Ollama 的失敗率是 67% 不是 50%。** 六次真實呼叫,**四次產不出可用敘事**。
  **這對 H-1 是硬資料:三分之二的夜晚他會打開來看到一句「這一夜沒有被記下來」。**
- **[✅ 走完了,但走到失敗那條分支——而擋它的程式做對了]**
  全文 `playtests/2026-08-07-h1-two-day-cycle.txt`。模型連兩次交白卷 → 重試一次 → 丟 `LLMRefusal`。
  **事後直接查 DB:`offline_narrative` 0 筆、`llm_calls` 2 筆。**
- **[還沒排] `loop_check` 剩兩個「量錯對象」的洞**(`AGENTS.md`〈第四題〉):
  1. **改個標題就能讓一整區從順序檢查裡消失**——`tools/loop_check.py:87` 先濾掉不認得的標題再比對順序
  2. **註解掉的項目仍然被數成一則**——parser 只看行首是不是 `- `(**繞法是縮排,已經在用**,
     但那是慣例不是機制)
- **[可觀測了,但不要急著修] 場景/動作跟台詞對不上。** 現在跟敘事一起存進 `story_assets`,
  **但還沒有一筆真實的不一致樣本**——先寫一致性檢查等於憑空猜規則。
- **[還沒排]** **Groq 免費額度是每日 200k tokens**,而且不是午夜整點重置
- **[已答]** **OpenSpec → 不裝**。**真正該考慮換 SQLite 的是 `PROGRESS.md`(150KB+),不是 `LOOP.md`**
- **[已答,結案]** 簡體字 → **只記 log**
- **[已修]** ~~`scene`/`action` 漏到玩家畫面上~~ / ~~代打按了沒反應~~ / ~~工作檯被裁掉~~ /
  ~~鏡頭跳~~ / ~~reduced-motion 是假的~~ / ~~陌洲的原話存不下來~~ / ~~場景圖載不進來(CSP)~~
- **[已答]** three.js → **還不要**(條件:H-1 過了 + Blender 產線真的有 glTF 資產)
- **[已答]** 知識狀態 → 要,但不能是技能樹
- **[已答]** 隨機物品 / 基因崩潰圖鑑 / 山海經 → 物品由事件產生;要「他經手過的人」的檔案

---

## Loop 收尾檢查(每次結束前跑一遍)

1. 測試真的跑過,而且是**這一輪跑的**(`python -m pytest -q`,現況 **272 passed**)
2. 動過 `LOOP.md`:`python tools/loop_check.py` 要 exit 0
   (**現在只剩心跳那則紅,人類明示照跑**——其餘任何一則都要當真)
3. 動過 `static/index.html`:`node tests/page_check.js` **而且**真的看一眼畫面
4. **改了 prompt 或模型行為,測試過不算數**——拿真模型跑一次,結果(含沒驗到的)寫進 `playtests/`
5. **宣稱瀏覽器層級的行為要去問瀏覽器,而且要問對問題**:
   **「伺服器有沒有收到那個請求」比截圖強**,因為截圖看得到的東西可能根本不會失敗。
   **每次都問一次「這個檢查有沒有可能失敗?」——不會失敗的檢查不是證據**
6. **人類回報的問題,先查根因再修**——「沒看到工作台」底下藏的是一條沒有人讀過的 CSP
7. **存下來的東西要往下游追一步問「誰會讀」**——第 11 輪漏的是一頭,第 33 輪漏的是另一頭
8. `TASKS.md` 狀態改對;`PROGRESS.md` append 一則;**重寫這份 `NOW.md`**
9. commit(先 `git pull --rebase`)
