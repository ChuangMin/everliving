# NOW.md — 醒來先讀這一份

> **這份是「現在」,不是歷史。** 歷史在 `PROGRESS.md`,那份只增不改;這份**每個 loop 結束前整份重寫**,
> 永遠只描述當下。這正好是這個專案自己的規則套在自己身上:
> `PROGRESS.md` 是 append-only 的來源,`NOW.md` 是壓縮後的視圖(見設計文件〈記憶磨損〉)。
>
> **順序:先讀這份 → 要細節再去 `TASKS.md` → 要理由才去 `PROGRESS.md` / 設計文件。**

**最後更新**:2026-08-05 by Claude Opus 5

---

## 一句話:現在卡在哪

**沒有技術阻塞,而且不需要人點頭的事已經做完了。161 passed、page checks 全過。**
**剩下的每一條都卡在人身上**,最上面那條 **H-1 是里程碑 0 唯一的成敗判準**——
它沒跑,後面所有東西都是在猜。

## 你可以叫 agent 給你看畫面

```
python tools/shot.py --size 1440x820           # 寬螢幕是會壞的那一種,優先看它
python tools/shot.py --scene 潮線 --action 淹水
```

現成的 Chrome headless,**不用裝擴充功能、不用網路、不花任何 token**。
`tests/page_check.js` 只能證明畫圖的程式跑得動,**看不見畫出來的東西**。

## 上一個 loop 做了什麼(2026-08-05 第十輪)

**鏡頭真的會移過去,而且「會動的東西」真的關得掉(T1-V8)。**
兩件事一起做,因為它們是同一個東西的兩面:**畫面上最大的那個移動**。

- **鏡頭移到事件的位置**:前一輪是讓 `transform-origin` 跟著動作跳,那是**瞬間位移**
  (scale 1.03 下大約跳 10 個單位),是破綻不是運鏡。改成獨立的 `#aim` 圖層,
  6 秒 ease 的 `transition`——**鏡頭轉過去看,不是切鏡頭**。
  只移到焦點與中心距離的 **30%**(移到正中央會讀成切換,而且每個事件會看起來發生在同一個地方),
  並 clamp 在 scale 買得起的餘裕內。`SAFE` 在多出來的 1.09 之後仍然成立,截圖確認過
- **`prefers-reduced-motion` 本來是假的**,而且是我前一輪親手寫下那句宣稱的。
  那條規則是 CSS,而 **SMIL 不聽 CSS**:87 個 `<animate>` 一個都沒被關掉。
  修在 `el()` 一個地方(補 `begin="indefinite"`,那是 SMIL 自己的「永遠不要開始」),
  守衛放在每個會動的元素本來就會經過的地方,不是散在九個呼叫點。
  CSS 那條也補上 `transition:none`——否則畫面上最大的那個移動剛好是唯一漏掉的
- **驗證是拿真瀏覽器數出來的**:`chrome --headless=new --dump-dom [--force-prefers-reduced-motion]`,
  **normal 0/87,reduced 87/87**。
  **這種宣稱不能用單元測試代替**——上一版就是這樣錯的,而且錯了好幾輪沒被發現,
  因為測試從來不會去問瀏覽器。已經寫進下面的收尾檢查
- 順帶修好截圖工具的盲點:`transition` 不是 `animation`,`paused` 碰不到它,
  所以靜圖會停在 6 秒運鏡的隨機一格。工具現在連 `transition` 一起關掉

## 更早那幾個 loop 做了什麼

- **委託機制(T1-C1)**:你開口 → 回覆多一行 `委託:X`(不花第二次呼叫,而且**寫了不代表答應**)
  → 離線期間結算成 `done` / `refused`。第十二節那兩條規則一起做了:
  **拒絕必須給出來自狀態或人設的理由**、**拒絕本身要生線**。
  真模型驗過,而且它自己做出了設計文件預言的落差:他拿到閥了,
  **但順便撞塌廢料堆、掀開一個在漏水的配電艙門**,新的懸念是從那個副作用長出來的。
  **沒驗到拒絕路徑**
- **T1-V7 構圖**:1440×780 下開場畫面本來**工作檯不見、吊燈不見、人只剩半截**。
  訂了量出來的 `SAFE = {top:108, bottom:308}`
- **連續運動(T1-V4/V5/V6)** + `tools/shot.py`
- **T0-17 懸念不再疊**:3/3 不再開重複的,但只有 1/3 標 resolved(→ T0-18)
- **H-1 四步用 AI 代打整條跑通**:第 3 步的承諾**真的長進了第 4 步的離線敘事**。
  成本:**output 是 input 的 3.6 倍**
- **主線劇情 → 設計文件第十七節**:主線只能是**一個會一直生線的位置**;玩家是**他的外部記憶**

## 下一個 loop 該做什麼

**不需要人點頭的清單:空的。**

這不是還沒想到,是**真的做完了**。剩下的每一條都卡在人身上(見下一節),
而且最上面那條 **H-1 是里程碑 0 唯一的成敗判準**——它沒跑,後面所有東西都是在猜。

照 `AGENTS.md`:**「今天沒有事該做」是一個正確的結果,而且經常會是正確的結果。**
為了填滿一輪而發明的任務會製造技術債,並把專案推往還沒被驗證的方向——那比什麼都不做更糟。

**如果要我繼續動手**,請從〈等人類決定〉挑一條(最有價值的是 H-1,其次 T0-18)。

## 等人類決定(agent 不要自己往下猜)

- **H-1 playtest 要不要親自跑**(跑法在 `TASKS.md`)——**這是里程碑 0 唯一的成敗判準,
  而且現在擋著後面所有東西**。當初擋住它的是「連畫面都沒有我不太想打開」;
  現在畫面會動、在你的螢幕上不會被裁掉一半,而且你還能**請他去做事**。
  先看看的話:`python tools/shot.py`、`playtests/` 底下三份記錄
- **T0-18 懸念要不要「過期成後果」**:委託現在有三條出路,**懸念還是只有「被完成」一條**——
  玩家不理它就永遠開著、永遠進 prompt(越來越貴)。
  提案:期限到了沒等到就 resolve 掉,同時產生一件**回不去的事**。
  跟「三座時鐘單向且無解」是同一個形狀。要加欄位,**是新機制,所以先問**
- **第十七節主線劇情是提案**:第一章 premise〈還留著別人的那一片〉要不要照走、
  那三個不可逆選項(擦乾淨 / 留著不用 / 交給機器廠)刻意設計成沒有一個是對的
- **`everliving.db` 要不要清乾淨**:H-1 若要乾淨起點就得清(清前會備份)。所有驗證都跑在另一個 DB
- **`ANTHROPIC_API_KEY` 是壞的**(401)。不換也不擋事,Groq 那把是好的

## 收件匣(人類講過但還沒被排進上面的)

- **[還沒排]** 排練第 1 步出現一次**場景跳到「配電所/淹水」但台詞還在工作間修閥門**——
  兩軸各自合法、合起來跟話對不上。只出現一次
- **[還沒排]** 委託的**拒絕路徑還沒被真模型驗過**。下次跑排練時,刻意用一個他做不到的委託試一次
- **[已修]** ~~工作檯被裁掉~~ / ~~焊接弧光構圖~~(T1-V7)/ ~~鏡頭跳~~(T1-V8)
- **[已做完]** ~~HelloRun 那種流暢~~ → 連續運動(T1-V4)
- **[已答,第十七節]** ~~劇情等級/難度~~ —— 只有「這條線走到哪了」與「有幾件事回不去了」;
  真要難度的話唯一合法形態是**代價變貴**
- **[已答]** three.js → **還不要**。條件:**H-1 過了 + Blender 產線真的有 glTF 資產**
- **[已答]** 知識狀態 → **要,但不能是技能樹**
- **[已答]** 隨機物品 / 基因崩潰圖鑑 / 山海經 → 物品由事件產生不由機率產生;
  不要怪物圖鑑要「他經手過的人」的檔案;山海經融合在人身上

---

## Loop 收尾檢查(每次結束前跑一遍)

1. 測試真的跑過,而且是**這一輪跑的**(`python -m pytest -q`)
2. 動過 `static/index.html` 的話:`node tests/page_check.js` **而且**
   `python tools/shot.py --size 1440x820` 真的看一眼
3. **改了 prompt 或模型行為的話,測試過不算數**——測試只能證明那句話在 prompt 裡。
   拿真模型跑一次,把結果(**包含沒驗到的部分**)寫進 `playtests/`
4. **宣稱瀏覽器層級的行為(無障礙、動畫、CSP…)要去問瀏覽器**,不能只寫測試。
   `chrome --headless=new --dump-dom` 之類的實測,數字寫進 `TASKS.md`。
   `prefers-reduced-motion` 那句就是假了好幾輪才被抓到的
5. `TASKS.md` 狀態改對了(`done` 的東西真的能跑)
6. `PROGRESS.md` 加一則新記錄(**append,不要改舊的**)
7. **重寫這份 `NOW.md`**
8. commit(先 `git pull --rebase`)
