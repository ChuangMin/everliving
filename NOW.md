# NOW.md — 醒來先讀這一份

> **這份是「現在」,不是歷史。** 歷史在 `PROGRESS.md`,那份只增不改;這份**每個 loop 結束前整份重寫**,
> 永遠只描述當下。這正好是這個專案自己的規則套在自己身上:
> `PROGRESS.md` 是 append-only 的來源,`NOW.md` 是壓縮後的視圖(見設計文件〈記憶磨損〉)。
>
> **順序:先讀這份 → 要細節再去 `TASKS.md` → 要理由才去 `PROGRESS.md` / 設計文件。**

**最後更新**:2026-08-05 by Claude Opus 5

---

## 一句話:現在卡在哪

**沒有技術阻塞。151 passed、page checks 全過、六個場景在寬螢幕與窄螢幕都親眼看過。**
但**不等人就能做的事快做完了**:剩下「鏡頭移到事件的位置」那半條。
等人的有四題,其中**新的一題是「委託機制要不要在 H-1 之前做」**。

## 先知道這件事:你可以叫 agent 給你看畫面

```
python tools/shot.py                                   # 開場,取樣動畫週期三個點
python tools/shot.py --scene 潮線 --action 淹水 --size 1440x780
```

用機器上現成的 Chrome headless,**不用裝擴充功能、不用網路、不花任何 token**。
`tests/page_check.js` 只能證明畫圖的程式跑得動,**看不見畫出來的東西**。
這兩輪抓到的三個缺陷,**全部是 stub DOM 永遠看不到的那一類**。

## 上一個 loop 做了什麼(2026-08-05 第八輪)

**本來要做「操控度」,先翻設計文件,發現不能照上一輪寫的做。**
第十二節其實早就把控制模型定案了——**委託模式**:「探索 = 委託 + 讀回報」,
沒有移動、沒有地圖、沒有方向鍵。而它的**顯性形態**(敘事末尾那 2–3 個選項)
就是第十節的 action 層,**那一層明確被 H-1 擋著**。
**我上一輪在這份文件裡寫「操控度不需要任何人點頭」是錯的,已經改掉**,並整理成一題丟回給人。

**改做截圖抓到、明確 in-scope 的那個缺陷,結果它比想像中嚴重:**

- **1440×780(最常見的筆電尺寸)下,開場畫面工作檯完全不見、吊燈不見、人只剩半截。**
  那個「不用一句台詞就說明他是誰」的場景,一樣都沒留下
- 根因不是動畫:viewBox 是 2.38:1、`.scene` 在那個視窗是 4.4:1,`slice` 裁掉 46% 的高度。
  **本來就有的**(拿 `--page` 指到舊版對照確認過),只是在有截圖工具之前**沒有人看得見**
- 修法兩層:`.scene` 高度改成 `max(42vh,26vw)`(讓高度跟著寬度走);
  以及訂一條 **`SAFE = {top:108, bottom:308}`——量出來的不是猜的**,
  所有承載意義的東西一律放進這個帶,天空與水可以溢出去
  (**裁掉天空讀起來是取景,裁掉工作檯讀起來是壞掉**)
- 搬上去才出現的破綻:**欄杆的鏤空**在底部黑水裡看不見,搬到有光的水面上就變成一排亮格子。
  改成實心甲板 + 立柱剪影朝上
- **方法上值得記一句**:這輪改了三次構圖,每次都是「改 → 截圖 → 看 → 再改」,
  三次裡有兩次是**看了才知道要再改**

## 更早那幾個 loop 做了什麼

- **連續運動(T1-V4/V5/V6)**:三層不同速率漂移、水面照一個完整波長無縫流動、240 秒週期極慢推拉。
  **全部用 CSS 不用 SMIL**(`prefers-reduced-motion` 是 CSS 規則,SMIL 會繞過)。
  鏡頭會朝正在發生的事偏過去。離線那一夜也終於能說「在幹嘛」(`action`,`null` 也送,而且沒有 fallback)。
  外加 `tools/shot.py`——踩到的坑:**Chrome 的 `--virtual-time-budget` 不會推進 CSS 動畫**,
  所以工具改用負的 `animation-delay` 把每一格釘住
- **T0-17 懸念不再疊**:`_build_prompts` 規則 2 依有沒有未解懸念分兩種寫法,而且**取代不是並存**。
  真跑三次:3/3 不再開重複的,但只有 1/3 標 resolved(另外兩次模型是對的 → T0-18)
- **H-1 四步用 AI 代打整條跑通**(26 次呼叫零失敗)。第 3 步的承諾**真的長進了第 4 步的離線敘事**。
  成本:**output 是 input 的 3.6 倍**,一則兩三句的回覆記約 2,500 output token
- **主線劇情 → 設計文件第十七節**:主線只能是**一個會一直生線的位置**;玩家是**他的外部記憶**

## 下一個 loop 該做什麼(照順序,做完就從這裡刪掉)

1. **[視覺,剩下半條] 鏡頭移到事件的「位置」** ← **不需要任何人點頭,只剩這條**。
   現在 `FOCUS` 是四個**動作**的固定座標,不是**事件**的座標。
   要真的把鏡頭帶到事件發生的地方,得先有「這個事件在畫面上的哪裡」這個東西。
   `SAFE` 已經訂好了,新的焦點一律要落在裡面
2. **[之後] 語音輸入 / 配音輸出** — 不是現在,但設計時別擋死

**做完第 1 項,不等人就能做的事就沒有了**——那時候「今天沒有事該做」是正確答案(見 `AGENTS.md`)。

## 等人類決定(agent 不要自己往下猜)

- **[新] 委託機制要不要在 H-1 之前做?** 第十二節定案的控制模型是**委託**,
  而**自由輸入本來就沒被鎖死**——所以玩家現在就能「請他去做一件事」,
  排練也證明他**真的會接**(第 3 步的承諾長進了第 4 步的離線敘事)。
  缺的是**機制**:沒有任何地方記著「你請他做了 X」→「他做了 / 他拒絕,理由是什麼」。
  第十二節還訂了兩條規則沒實作:**拒絕必須給出來自 `agent_state` 或人設的理由**、
  而且**拒絕本身要生線**。
  問題是:**這算劇本層(被 H-1 擋)還是算機制(可以先做)?**
  我的判斷是後者(它不需要篇章、stage 或選項 UI),但這是閘門歸屬問題,**不該由 agent 自己認定**
- **H-1 playtest 要不要親自跑**(跑法在 `TASKS.md`)。當初擋住它的是「連畫面都沒有我不太想打開」——
  **現在畫面會動,而且在你的螢幕上不會再被裁掉一半了**。
  想先看:`python tools/shot.py`,或 `playtests/2026-08-05-h1-autoplay.txt`
- **T0-18 懸念要不要「過期成後果」**:現在一條懸念只有「被完成」一條出路,玩家不理它就永遠開著、
  永遠進 prompt(越來越貴)。**提案**:期限到了沒等到就 resolve 掉,同時產生一件**回不去的事**。
  跟「三座時鐘單向且無解」是同一個形狀。要加欄位,**是新機制,所以先問**
- **第十七節主線劇情是提案**:第一章 premise〈還留著別人的那一片〉要不要照走、
  那三個不可逆選項(擦乾淨 / 留著不用 / 交給機器廠)刻意設計成沒有一個是對的
- **`everliving.db` 要不要清乾淨**:H-1 若要乾淨起點就得清(清前會備份)。所有驗證都跑在另一個 DB
- **`ANTHROPIC_API_KEY` 是壞的**(401)。不換也不擋事,Groq 那把是好的

## 收件匣(人類講過但還沒被排進上面的)

- **[還沒排]** 排練第 1 步出現一次**場景跳到「配電所/淹水」但台詞還在工作間修閥門**——
  兩軸各自合法、合起來跟話對不上。只出現一次
- **[已修]** ~~工作檯被裁掉~~ / ~~焊接弧光構圖~~ —— T1-V7 一起處理掉了
- **[已答,第十七節]** ~~劇情等級/難度~~ —— 只有「這條線走到哪了」與「有幾件事回不去了」。
  真要難度的話,唯一合法形態是**代價變貴**
- **[已做完]** ~~HelloRun 那種流暢~~ —— 結論是連續運動(T1-V4)
- **[已答]** three.js → **還不要**。條件:**H-1 過了 + Blender 產線真的有 glTF 資產**
- **[已答]** 知識狀態 → **要,但不能是技能樹**(第十七節有產生器與三種下場)
- **[已答]** 隨機物品 / 基因崩潰圖鑑 / 山海經 → 物品由事件產生不由機率產生;
  不要怪物圖鑑要「他經手過的人」的檔案;山海經融合在人身上

---

## Loop 收尾檢查(每次結束前跑一遍)

1. 測試真的跑過,而且是**這一輪跑的**(`python -m pytest -q`)
2. 動過 `static/index.html` 的話:`node tests/page_check.js` **而且**
   `python tools/shot.py --size 1440x780` 真的看一眼(寬螢幕是會壞的那一種)
3. `TASKS.md` 狀態改對了(`done` 的東西真的能跑)
4. `PROGRESS.md` 加一則新記錄(**append,不要改舊的**)
5. **重寫這份 `NOW.md`**——尤其「下一個 loop 該做什麼」跟「收件匣」
6. commit(先 `git pull --rebase`)
