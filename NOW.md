# NOW.md — 醒來先讀這一份

> **這份是「現在」,不是歷史。** 歷史在 `PROGRESS.md`,那份只增不改;這份**每個 loop 結束前整份重寫**,
> 永遠只描述當下。這正好是這個專案自己的規則套在自己身上:
> `PROGRESS.md` 是 append-only 的來源,`NOW.md` 是壓縮後的視圖(見設計文件〈記憶磨損〉)。
>
> **順序:先讀這份 → 要細節再去 `TASKS.md` → 要理由才去 `PROGRESS.md` / 設計文件。**

**最後更新**:2026-08-05 by Claude Opus 5

---

## 一句話:現在卡在哪

**沒有技術阻塞,147 passed。** 下一輪要動的是**視覺:連續運動**,那條不需要任何人點頭。
等人的有兩題:**H-1 要不要親自跑**(排練不能代答)、**T0-18 懸念要不要「過期成後果」**(那是新機制)。

## 上一個 loop 做了什麼(2026-08-05 第六輪)

**T0-17:懸念不再疊。改的只有 `offline._build_prompts` 的規則 2。**

- 關鍵是**取代不是並存**:原本那條「通常要留下一件懸而未決的事」正是製造重複的來源,
  留著它再加一條「不要重複」等於給模型兩條打架的指令。
  有未解懸念時規則 2 整條換成:把那件事**往前推**(發展寫進 `events`)、`open_thread` 填 null,
  真的另外一件事才開新的。**而且是條件式的**——沒有懸念時不能出現,否則第一條懸念永遠開不出來
- **驗證分兩層,第二層才算數**:測試只能斷言「那句話在 prompt 裡」;模型聽不聽話要真的跑。
  拿上一輪跑完的 DB(結尾兩條幾乎一樣的懸念)重跑三次離線模擬,
  **3/3 不再開重複的**,但**只有 1/3 把舊的標 resolved**。
  看內容之後:**另外兩次模型是對的**,軸承還沒送到,那條懸念本來就沒結束。
  全文 `playtests/2026-08-05-t0-17-check.txt`。**n 只有 3**,說「不再疊」比說「一定不會疊」誠實
- **所以拆出 T0-18,而且它比原本以為的更根本**:一條懸念**只有「被完成」一條出路**。
  玩家不理它,它就永遠開著、永遠進 prompt(越來越貴)、面板上永遠掛著

## 更早那幾個 loop 做了什麼

- **H-1 四步用 AI 代打整條跑通**(`tools/h1_autoplay.py`,26 次呼叫零失敗,
  全文 `playtests/2026-08-05-h1-autoplay.txt`)。最值得看的一步成立了:
  第 3 步訪客答應「明晚日落前帶軸承」→ **第 4 步的離線敘事是從那個承諾長出來的**,
  還自己加了沒人要求的後果(為了頂住壓力切斷外圍區供氧)。
  成本的形狀跟預期不一樣:**output 是 input 的 3.6 倍**,一則兩三句的回覆記了約 2,500 output token
  ——是推理量,不是敘事長度
- **主線劇情 → 設計文件第十七節**(只動文件)。主線不可能是一條會結束的線,只能是**一個會一直生線的位置**:
  **他知道的東西只存在他身上,而唯一能保存它的方法是交給那個會把它拿走的人。**
  玩家的位置是**他的外部記憶**(他會忘,你不會——`memory_events` 本來就 append-only)
- **AI 代打(訪客模式)**:世界不知道對方是人還是 AI,分別只記在 `llm_calls.purpose = auto_visitor`
- **`動作` 標籤**:`scene` 只講「在哪」不講「在幹嘛」,所以加第二軸(焊接 / 停電 / 淹水 / 起霧)

## 下一個 loop 該做什麼(照順序,做完就從這裡刪掉)

1. **[視覺] 連續運動 + 離線事件演在場景裡** ← **不需要任何人點頭,直接做這條**。
   HelloRun 的流暢**主要來自鏡頭一直在動**,不是 3D;現在的頁面是每 30 秒重畫一次的靜態圖。
   方向:**圖層視差緩慢漂移、水面持續流動、鏡頭極慢推移**(SVG/CSS 就做得到,不用換渲染器)。
   **而鏡頭會動,才有辦法把鏡頭移到事件發生的地方**——這正好是「agent 自己玩要看得到」需要的載體。
   `scene`、`動作`、`event_id`、`assets` 四個接口都已經在了。
   動到 `static/index.html` 就要跑 `test_page_render.py`(它會真的執行頁面 JS)
2. **[操控] 操控度** — 工作檯只是第一步(可以查),還缺「可以做」。
   設計文件:**three.js 真正會贏的是這一塊(可走動、可點、視差)**,不是靜態畫面好不好看
3. **[之後] 語音輸入 / 配音輸出** — 不是現在,但設計時別擋死

**劇本層的實作(篇章 / stage / 動作選項)不要現在做**——閘門是 H-1(設計文件第十節)。
第十七節已經查過:主線需要的東西裡**只有「篇章 / stage」是缺的**,其餘全部長在既有結構上。

## 等人類決定(agent 不要自己往下猜)

- **T0-18 懸念要不要「過期成後果」** ← 新的一題,而且是這輪才看清楚的。
  現在一條懸念只有「被完成」一條出路,玩家不理它就永遠開著。
  **提案**:期限到了沒等到,離線模擬就把它 resolve 掉,同時產生一件**回不去的事**。
  這跟設計文件「三座時鐘單向且無解」是同一個形狀,也比「待辦清單」更像這個世界。
  但要在 `open_threads` 加欄位,**是新機制,所以先問**
- **H-1 playtest 要不要親自跑**(跑法在 `TASKS.md`)。
  排練已經證明**流程通、狀態會接**,但 H-1 問的是「**你**想不想再打開」,那題只有你能答。
  想先看排練長什麼樣:`playtests/2026-08-05-h1-autoplay.txt`
- **第十七節主線劇情是提案**:第一章 premise〈還留著別人的那一片〉要不要照走、
  那三個不可逆選項(擦乾淨 / 留著不用 / 交給機器廠)刻意設計成沒有一個是對的
- **`everliving.db` 要不要清乾淨**:目前有測試灌進去的記憶,H-1 若要乾淨起點就得清(清前會備份)。
  排練跟 T0-17 驗證都跑在另一個 DB,沒動它
- **`ANTHROPIC_API_KEY` 是壞的**(401)。不換也不擋事,Groq 那把是好的

## 收件匣(人類講過但還沒被排進上面的)

- **[還沒排]** 排練第 1 步第三輪出現一次**場景跳到「配電所/淹水」但台詞還在工作間修閥門**——
  `場景`/`動作` 兩軸各自合法、合起來跟話對不上。只出現一次,先記著不動
- **[已答,第十七節]** ~~劇情等級/難度~~ —— 合法形態只有「這條線走到哪了」與「有幾件事回不去了」。
  真要難度的話,唯一合法形態是**代價變貴**,不是敵人變強
- **[已排進第 1 項]** ~~HelloRun 那種流暢~~ —— 結論是**連續運動,不是換渲染器**
- **[已答]** three.js → **還不要**。條件:**H-1 過了 + Blender 產線真的有 glTF 資產**
- **[已答]** 知識狀態 → **要,但不能是技能樹**(第十七節有產生器與三種下場)
- **[已答]** 隨機物品 / 基因崩潰圖鑑 / 山海經 → 物品由事件產生不由機率產生;
  不要怪物圖鑑要「他經手過的人」的檔案;山海經融合在人身上

---

## Loop 收尾檢查(每次結束前跑一遍)

1. 測試真的跑過,而且是**這一輪跑的**(`python -m pytest -q`)
2. 動過 `static/index.html` 的話,`test_page_render.py` 有沒有過(它會真的執行頁面 JS)
3. `TASKS.md` 狀態改對了(`done` 的東西真的能跑)
4. `PROGRESS.md` 加一則新記錄(**append,不要改舊的**)
5. **重寫這份 `NOW.md`**——尤其「下一個 loop 該做什麼」跟「收件匣」
6. commit(先 `git pull --rebase`)
