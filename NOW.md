# NOW.md — 醒來先讀這一份

> **這份是「現在」,不是歷史。** 歷史在 `PROGRESS.md`,那份只增不改;這份**每個 loop 結束前整個重寫**,
> 永遠只描述當下。這正好是這個專案自己的規則套在自己身上:
> `PROGRESS.md` 是 append-only 的來源,`NOW.md` 是壓縮後的視圖(見設計文件〈記憶磨損〉)。
>
> 會有這份是因為 `PROGRESS.md` 已經 80KB 以上——醒來要先讀完才知道該做什麼,那個成本每個 loop 都要付一次。
>
> **順序:先讀這份 → 要細節再去 `TASKS.md` → 要理由才去 `PROGRESS.md` / 設計文件。**

**最後更新**:2026-08-05 by Claude Opus 5

---

## 一句話:現在卡在哪

**沒有技術阻塞。** 程式跑得動、測試 126 passed、網頁開得起來(`python -m everliving.web`,已指向 Groq)。
真正等的是兩件人類才能做的事:**H-1 playtest 要不要跑**,以及**視覺這一輪夠不夠讓你想打開**。

## 上一個 loop 做了什麼

重做整個場景視覺(`static/index.html`)。原本每個場景都是同一套「長方形天際線」產生器換座標,
所以**站在工作間裡會看到一排大樓**——這是「一點都沒有真實世界的樣子」最直接的原因。現在:

- **室內/室外是兩種構圖**:室內是一面挖了窗的暗牆 + 窗上的水氣 + 工作檯 + 吊燈光暈;室外才是天際線
- **加了深度**:霧靄層、遠景偏藍且低對比、水面反射整層中景、水光碎線
- **加了會動的東西**:窗戶燈火會閃、高塔頂端有紅色航警燈、雨、呼吸
- **加了人形**(陌洲本人,剪影不畫臉)——最大的作用是**給尺度**:沒有人的天際線是背景板,有人才是地方
- 配色改成這個世界的材質:鹵水、鏽鐵、鈉燈。**鈉燈是世界裡唯一的光源,所以是頁面上唯一的暖色**

驗證(照人類要求檢查三遍):id 參照全對、`node --check` 語法通過、
六個場景全部 headless 跑過不丟例外且重畫穩定、126 passed。

## 下一個 loop 該做什麼(照順序,做完就從這裡刪掉)

1. **[劇情] 想主線劇情** ← 人類指定的下一件事。素材已經很多了(設計文件第十一節「修補」、
   機器廠對抗線、沒人維護的系統、照神話改造自己的人),缺的是**把它們接成一條陌洲會遇到的線**
2. **[視覺] agent 自己玩要看得到** — 人類指出「AI agent 自己玩也要能夠展現在 web 上面才有效果」。
   現在離線敘事只是文字。方向:離線事件要**演在場景裡**(陌洲移到那個場景、做那個動作),
   而不是只被描述。`scene` 欄位已經在了,`story_assets` 的錨點也已經在了
3. **[操控] 操控度** — 現在玩家只能打字。人類說「操控度也不行」。要想的是:
   在不做成「遊戲關卡」的前提下,玩家還能對世界做什麼
4. **[之後] 語音輸入** — 人類說「之後還要支援語音輸入」。**不是現在**,但設計時不要擋死

## 等人類決定(agent 不要自己往下猜)

- **H-1 playtest** 要不要跑(跑法在 `TASKS.md`;現在有畫面、有人形了)
- **`everliving.db` 要不要清乾淨**:目前有 6 筆記憶(我測試灌的 2 輪 + 你打的「123」),
  H-1 若要乾淨起點就得清,清之前會先備份
- **`ANTHROPIC_API_KEY` 是壞的**(401)。不換也不擋事,Groq 那把是好的

## 收件匣(人類講過但還沒被排進上面的)

- **[劇情・已答,已寫進設計文件第十三節]** 隨機拿物品 / 基因崩潰圖鑑 / 融合山海經 →
  結論是「可以,但不是那個形狀」:**物品由事件產生不由機率產生**(掉落表會一路推到玩家間零和,那是禁掉的);
  **不要怪物圖鑑,要「他經手過的人」的檔案**(有圖鑑就有敵人類型,有敵人類型就有戰鬥,而這個世界沒有戰鬥);
  山海經的融合點在**人**身上不在生物身上,而且那條已經寫在那裡了
- **[語音・之後]** 語音輸入(玩家講話)
- **[語音・之後]** 模擬真人對談聲音(TTS 輸出)。注意設計文件已定調:
  **配音是 per-call 成本、隨使用線性成長,跟配樂/畫面的「預先生成、可重用」不同**——
  所以它屬於付費層,不是基礎層。做之前先看第十六節那段

---

## Loop 收尾檢查(每次結束前跑一遍)

1. 測試真的跑過,而且是**這一輪跑的**(`python -m pytest -q`)
2. `TASKS.md` 狀態改對了(`done` 的東西真的能跑)
3. `PROGRESS.md` 加一則新記錄(**append,不要改舊的**)
4. **重寫這份 `NOW.md`**——尤其是「下一個 loop 該做什麼」和「收件匣」
5. commit(先 `git pull --rebase`)
