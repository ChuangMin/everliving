# NOW.md — 醒來先讀這一份

> **這份是「現在」,不是歷史。** 歷史在 `PROGRESS.md`,那份只增不改;這份**每個 loop 結束前整份重寫**,
> 永遠只描述當下。這正好是這個專案自己的規則套在自己身上:
> `PROGRESS.md` 是 append-only 的來源,`NOW.md` 是壓縮後的視圖(見設計文件〈記憶磨損〉)。
>
> **順序:先讀這份 → 要細節再去 `TASKS.md` → 要理由才去 `PROGRESS.md` / 設計文件。**

**最後更新**:2026-08-05 by Claude Opus 5

---

## 一句話:現在卡在哪

**沒有技術阻塞。135 passed,`python -m everliving.web` 開得起來(已指向 Groq)。**
等的是人:**H-1 要不要跑**。而且視覺這幾輪改完後,H-1 要問的問題比之前更乾淨了。

## 上一個 loop 做了什麼(2026-08-05 第三輪)

- **AI 代打(訪客模式)**:頁面下方「讓 AI 代打」按鈕,一個訪客 agent 坐到玩家的位子跟陌洲對話。
  **有硬上限**(預設 6 輪,`--auto-turns` 可調)——每輪**兩次** LLM 呼叫而且迴圈自己會轉,
  沒上限等於把 denial-of-wallet 對準自己的 key。實測跑過真的一輪,對話與場景都正常。
  **世界不知道對方是人還是 AI**(統一居民介面那條架構預留第一次真的被用到),
  區別只記在 `llm_calls.purpose = auto_visitor` ——**分別在帳上,不在故事裡**。
  **但它答不了 H-1**:H-1 問的是「**你**想不想再打開」,模型說好玩不算數
- **`動作` 標籤(修「畫面跟故事很不一樣」)**:人類回報即使場景對了,畫面仍跟文字不符——
  他描述焊槍藍光燒紅閥面,畫面卻是一般的工作間。**根因是 `scene` 只講「在哪」不講「在幹嘛」**,
  而那次的「在哪」本來就是對的。所以加了第二軸(同樣封閉值域):
  **焊接**(藍白電弧閃爍 + 火星,會把整個畫面打冷)、**停電**(全暗只剩紅色緊急燈)、
  **淹水**(水位蓋過前景)、**起霧**。四個都改變光,所以隔著螢幕就看得出來。
  標籤照樣會被剝掉、不進記憶、認不得就當沒有

## 更早那個 loop 做了什麼

人類實際玩了一輪,回報**「網頁背景跟文字對不上、圖文不符」**(陌洲說他在工作檯邊拉防潮簾,畫面卻沒動)。

- **根因**:`scene` **只有離線敘事會更新**,對話永遠不動畫面。
  現在 `respond()` 回傳 `Turn(reply, scene, event_id)`,模型在回覆後另起一行寫 `場景:X`(封閉值域),
  **那一行會被剝掉,不進畫面也不進記憶**(否則下一輪會被當成他講過的話餵回去)。
  認不得的地點 → `scene=None` → **畫面原地不動**,不會跳到錯的地方
- **工作檯面板**(人類要的「可以點進去看狀態」):右下角「工作檯」按鈕,抽屜式面板。
  顯示 **他獨自過的夜 / 發生過的事 / 你們說過的話 / 了結的事**、在哪、手上的東西與狀態、還沒完的事。
  後端多了 `Session.ledger()`,只給計數不給內容——**面板是查狀況的地方,不是故事的第二份副本**
- **影片與故事的預留變成活的**(不再只寫在設計文件裡):
  對話節點現在也有 `event_id` 錨點(原本只有離線敘事有),`/api/say` 與 `/api/open` 都會帶 `assets`,
  頁面**真的會渲染** asset 欄位——而且是**貼在文字旁邊,永遠不取代文字**(設計文件第五節那條界線)
- **`tests/page_check.js` + `test_page_render.py`**:用 stub DOM **真的執行頁面的 JS**——
  六個場景全畫一遍、工作檯開關填空、錯誤 payload 不炸。
  Python 測試看不到瀏覽器端的 typo,而「測試過了但東西跑不起來」這個專案已經吃過一次虧

## 下一個 loop 該做什麼(照順序,做完就從這裡刪掉)

1. **[劇情] 想主線劇情** ← 一直是下一件事,而且現在有兩條新理由必須先做它:
   **知識狀態那一欄的產生器是劇情**(先做會得到一張永遠空的表),
   **「agent 自己玩要看得到」也要有劇情才有東西可演**
2. **[視覺] agent 自己玩要看得到** — 離線那段現在仍然只是文字。方向:
   離線事件要**演在場景裡**(陌洲移到那個場景、做那個動作)。`scene`、`event_id`、`assets` 三個接口都已經在了
3. **[操控] 操控度** — 工作檯只是第一步(可以查)。還缺「可以做」。
   注意設計文件剛記的:**three.js 真正會贏的是這一塊(可走動、可點、視差),不是靜態畫面好不好看**
4. **[之後] 語音輸入 / 配音輸出** — 不是現在,但設計時別擋死

## 等人類決定(agent 不要自己往下猜)

- **H-1 playtest** 要不要跑(跑法在 `TASKS.md`;現在有畫面、有人形、有工作檯了)
- **`everliving.db` 要不要清乾淨**:目前有測試灌進去的記憶,H-1 若要乾淨起點就得清(清前會備份)
- **`ANTHROPIC_API_KEY` 是壞的**(401)。不換也不擋事,Groq 那把是好的

## 收件匣(人類講過但還沒被排進上面的)

- **[待做]** **劇情等級/難度設定還沒設計**(人類:「劇情等級設定好像也沒有設定 先記著」)。
  先記著,不要急著做——**它會撞到已定案的兩條**:這個世界**沒有勝利條件**、
  而且**零和只發生在角色之間不發生在玩家之間**。所以「等級」如果長成難度或戰力就違反設定;
  比較可能成立的形態是**「這條線走到哪了」**(進度)或**「這件事有多不可挽回」**(代價),
  而不是「敵人多強」。設計時從第十六節那句尺度出發:「能不能保住這一個人」

- **[參考,待做]** 人類舉了 <https://helloenjoy.itch.io/hellorun> 說「畫面看起來就很流暢」。
  拆開來看,那種流暢**主要不是來自 3D,是來自「鏡頭一直在動」**——連續運動比多邊形數量重要得多。
  而現在這個頁面是**每 30 秒重畫一次的靜態圖**,只有閃爍/雨/呼吸這些局部動作,整體是不動的。
  **所以下一輪視覺該做的是連續運動,不是換渲染器**:圖層視差緩慢漂移、水面持續流動、鏡頭極慢推移。
  這在 SVG/CSS 就做得到,而且它同時是「agent 自己玩要看得到」的載體(鏡頭可以移到事件發生的地方)。
  **要真的長成 HelloRun 那樣仍然需要資產,那條路還是 Blender → glTF**
- **[已答,寫進設計文件]** three.js 要不要用 → **還不要**。根因是構圖不是渲染器,
  而且 three.js 是渲染器不是美術(沒資產的 3D = 沒貼圖的方塊)。
  換的條件很明確:**H-1 過了 + Blender 產線真的有 glTF 資產**。座標預留早就寫好了,不會白做
- **[已答,寫進設計文件]** 知識狀態要不要 → **要,但不能是技能樹**。條目會遺失、用來辨認不是解鎖、
  他親歷出來的。**產生器是劇情,所以排在劇情後面**
- **[已答,寫進設計文件]** 隨機物品 / 基因崩潰圖鑑 / 山海經 → 物品由事件產生不由機率產生;
  不要怪物圖鑑要「他經手過的人」的檔案;山海經融合在人身上

---

## Loop 收尾檢查(每次結束前跑一遍)

1. 測試真的跑過,而且是**這一輪跑的**(`python -m pytest -q`)
2. 動過 `static/index.html` 的話,`test_page_render.py` 有沒有過(它會真的執行頁面 JS)
3. `TASKS.md` 狀態改對了(`done` 的東西真的能跑)
4. `PROGRESS.md` 加一則新記錄(**append,不要改舊的**)
5. **重寫這份 `NOW.md`**——尤其「下一個 loop 該做什麼」和「收件匣」
6. commit(先 `git pull --rebase`)
