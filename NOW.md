# NOW.md — 醒來先讀這一份

> **這份是「現在」,不是歷史。** 歷史在 `PROGRESS.md`,那份只增不改;這份**每個 loop 結束前整份重寫**,
> 永遠只描述當下。這正好是這個專案自己的規則套在自己身上:
> `PROGRESS.md` 是 append-only 的來源,`NOW.md` 是壓縮後的視圖(見設計文件〈記憶磨損〉)。
>
> **順序:先讀這份 → 要細節再去 `TASKS.md` → 要理由才去 `PROGRESS.md` / 設計文件。**

**最後更新**:2026-08-05 by Claude Opus 5

---

## 一句話:現在卡在哪

**沒有技術阻塞。145 passed,`python -m everliving.web` 開得起來(已指向 Groq)。**
等的是人:**H-1 要不要跑**。而且主線劇情剛寫完,所以「有畫面、有人形、有工作檯、也知道要演什麼」了。

## 上一個 loop 做了什麼(2026-08-05 第四輪)

**寫完主線劇情 → 設計文件新增第十七節。只動文件,沒動 `src/`**
(第十節那道閘門管的是劇本層的**實作**,設計不受它限制)。

- **最重要的一步不是編故事,是先證明故事不能是那個形狀**:沒有勝利條件 × 沒有戰鬥 ×
  三座時鐘全部單向且無解 → 傳統主線(有目標、有高潮、有結局)直接被推翻。
  所以 **主線不是一條會結束的線,是一個會一直生線的位置**,而那個位置早就寫死了:
  **他知道的東西只存在他身上,而唯一能保存它的方法,是交給那個會把它拿走的人。**
  於是**機器廠不必對付陌洲,只要等他的容量用完**
- **玩家的位置也定了:你就是他的外部記憶**。**他會忘,你不會**——而這句話**在程式裡本來就是真的**
  (`memory_events` append-only、永不磨損;磨損的是角色記得的壓縮視圖)。
  界線:**不可以變成考試**,答不出來要生線不是扣分
- **三條軸全部是既有對手,沒有新增反派**:A 封裝(機器廠)、B 沒人維護的系統(不需要玩家,照走)、
  C 他經手過的人。**每一章至少要有一條 B**——「世界在活著」只來自不需要你的線
- **第一章提案〈還留著別人的那一片〉**:回收場撿到一片擦不乾淨的晶片。
  不可逆節點三選一(擦乾淨 / 留著不用 / 交給機器廠),**沒有一個是對的**。
  **美術成本零**,只用既有的四個場景、兩個動作
- **順帶關掉兩題**:知識狀態那一欄的來源(每修好一個沒人修過的故障 = 一條,
  三種下場:留著 / 掉了 / 被封裝 → **主線同時是科技發展線的推進器**);
  以及**劇情等級**(合法形態只有「走到哪了」與「有幾件事回不去了」,不是敵人多強)
- **新訂一條界線**:**主線只能發生在畫得出來的地方**(六個 `scene`、四個 `動作`)。
  寫得出來但畫不出來的橋段,在網頁版等於沒有發生

## 更早那個 loop 做了什麼

- **AI 代打(訪客模式)**:一個訪客 agent 坐到玩家的位子跟陌洲對話,**有硬上限**(預設 6 輪)。
  世界不知道對方是人還是 AI,區別只記在 `llm_calls.purpose = auto_visitor`——**分別在帳上,不在故事裡**。
  **但它答不了 H-1**:H-1 問的是「**你**想不想再打開」
- **`動作` 標籤**:修「畫面跟故事很不一樣」。根因是 `scene` 只講「在哪」不講「在幹嘛」,
  所以加了第二軸(焊接 / 停電 / 淹水 / 起霧,四個都改變光)
- 再更早:`scene` 改成對話也會更新(那一行會被剝掉、不進記憶)、工作檯面板、
  `assets` 從紙上變成活的、`tests/page_check.js` 用 stub DOM 真的執行頁面 JS

## 下一個 loop 該做什麼(照順序,做完就從這裡刪掉)

1. **[視覺] 連續運動 + 離線事件演在場景裡** ← 現在是同一件事,所以合成一條。
   收件匣那條已經拆清楚了:HelloRun 的流暢**主要來自鏡頭一直在動**,不是 3D;
   而現在的頁面是每 30 秒重畫一次的靜態圖。方向:**圖層視差緩慢漂移、水面持續流動、鏡頭極慢推移**
   (SVG/CSS 就做得到,不用換渲染器)。**而鏡頭會動,才有辦法把鏡頭移到事件發生的地方**——
   這正好就是「agent 自己玩要看得到」需要的載體。`scene`、`動作`、`event_id`、`assets` 四個接口都已經在了
2. **[操控] 操控度** — 工作檯只是第一步(可以查),還缺「可以做」。
   注意設計文件:**three.js 真正會贏的是這一塊(可走動、可點、視差),不是靜態畫面好不好看**
3. **[之後] 語音輸入 / 配音輸出** — 不是現在,但設計時別擋死

**劇本層的實作(篇章 / stage / 動作選項)不要現在做**——閘門是 H-1(設計文件第十節)。
第十七節已經查過:主線需要的東西裡**只有「篇章 / stage」是缺的**,其餘全部長在既有結構上。

## 等人類決定(agent 不要自己往下猜)

- **H-1 playtest** 要不要跑(跑法在 `TASKS.md`)
- **第十七節主線劇情是提案**,尤其這兩點想聽人的意見:
  - **第一章的 premise**〈還留著別人的那一片〉要不要照這樣走
  - **那三個不可逆選項**(擦乾淨 / 留著不用 / 交給機器廠)——刻意設計成沒有一個是對的
- **`everliving.db` 要不要清乾淨**:目前有測試灌進去的記憶,H-1 若要乾淨起點就得清(清前會備份)
- **`ANTHROPIC_API_KEY` 是壞的**(401)。不換也不擋事,Groq 那把是好的

## 收件匣(人類講過但還沒被排進上面的)

- **[已答,寫進設計文件第十七節]** ~~劇情等級/難度設定~~ —— 合法形態只有兩個:
  **「這條線走到哪了」**(arc 的 `stage`)與**「有幾件事回不去了」**(不可逆節點)。
  「敵人多強 / 玩家幾級」否決(同時撞沒有勝利條件、沒有戰鬥、零和只發生在角色之間)。
  真要難度的話,唯一合法形態是**代價變貴**,不是敵人變強
- **[已排進第 1 項]** ~~HelloRun 那種流暢~~ —— 結論是**連續運動,不是換渲染器**;
  要真的長成那樣仍然需要資產,那條路還是 Blender → glTF
- **[已答,寫進設計文件]** three.js 要不要用 → **還不要**。換的條件很明確:
  **H-1 過了 + Blender 產線真的有 glTF 資產**。座標預留早就寫好了,不會白做
- **[已答,寫進設計文件]** 知識狀態要不要 → **要,但不能是技能樹**。第十七節已經給了產生器與三種下場
- **[已答,寫進設計文件]** 隨機物品 / 基因崩潰圖鑑 / 山海經 → 物品由事件產生不由機率產生;
  不要怪物圖鑑要「他經手過的人」的檔案;山海經融合在人身上

---

## Loop 收尾檢查(每次結束前跑一遍)

1. 測試真的跑過,而且是**這一輪跑的**(`python -m pytest -q`)
2. 動過 `static/index.html` 的話,`test_page_render.py` 有沒有過(它會真的執行頁面 JS)
3. `TASKS.md` 狀態改對了(`done` 的東西真的能跑)
4. `PROGRESS.md` 加一則新記錄(**append,不要改舊的**)
5. **重寫這份 `NOW.md`**——尤其「下一個 loop 該做什麼」和「收件匣」
6. commit(先 `git pull --rebase`)
