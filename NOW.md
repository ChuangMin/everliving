# NOW.md — 醒來先讀這一份

> **這份是「現在」,不是歷史。** 歷史在 `PROGRESS.md`,那份只增不改;這份**每個 loop 結束前整份重寫**,
> 永遠只描述當下。這正好是這個專案自己的規則套在自己身上:
> `PROGRESS.md` 是 append-only 的來源,`NOW.md` 是壓縮後的視圖(見設計文件〈記憶磨損〉)。
>
> **順序:先讀這份 → 要細節再去 `TASKS.md` → 要理由才去 `PROGRESS.md` / 設計文件。**
>
> **如果你是被當成四種角色之一叫起來的**:改讀 `LOOP.md` 的「現在輪到」,契約在 `AGENTS.md`〈四種角色〉。

**最後更新**:2026-08-08 by Claude Opus 5(`LOOP.md` 第 36 輪 builder)

## 🔴 一件卡住的事,而且只有人類做得到(2026-08-08)

**場景圖畫好了(`static/scenes/工作間{,-s2,-s4}.webp`),但沒有人確認過它在畫面上顯示得對不對。**

```
python -m everliving.web --provider auto --db play.db
```

**看場景那一塊**:看到工作檯 → 沒有 bug;看到一團模糊的光或破圖 → bug 是真的。

**⚠️ 不准用 headless 截圖判這一條。** 這一輪就是栽在它上面:
`tools/shot.py` 那類工具會在 `/api/open` 回來後 **65 毫秒**就截圖關頁,
**圖片請求根本來不及發出去**,拍到的是佔位圖被放大。
**確定的是圖檔本身沒問題**(直接開檔、直接開網址、兩個最小測試頁,四種方式都正常)。

**這一條沒過之前不要投資 3D。** 人類 2026-08-08 提議用 Blender 做更細緻的資產
(設計文件第 330、339 行本來就認可「Blender 屬於生成側」這個用法),
**但他自己選了「先驗頁面再話」**——因為**頁面若不會顯示圖,再精緻的資產也到不了螢幕上**。

## 🎨 給人類:畫面現在等的是圖,不是程式

**丟一張圖進 `src/everliving/static/scenes/`,重新整理就換掉了。** 不用重開伺服器、不用改任何 code。
**檔名規則、尺寸、以及六個場景的生圖 prompt 全部寫在那個資料夾的 `README.md`。**

**先做 `工作間.webp` 一張就看得到差別**(開場預設就是那裡)。
想看到時間在走再補 `工作間-s2.webp` / `工作間-s4.webp`。

⚠️ **agent 這邊沒有生圖工具,畫不出來。** 管線是通的、端到端驗過,**缺的是圖本身。**

---

## ⚠️ 人類 2026-08-07:目標 **「以目標變成最多人玩的遊戲」**,而且 **「範圍圍籬拆」**

**兩句原文照抄,不要改寫成別的意思。**

**圍籬已經拆了,`AGENTS.md`〈現在該做什麼〉已改寫**——舊禁令(不要做多 agent / 世界地圖 /
金流 / 帳號系統 / 前端框架 / 向量檢索)**加刪除線保留**,因為那些理由是關於
「提前做會浪費額度」,不是關於「那些東西不好」,仍然值得讀。

**但拆掉圍籬不等於全部一起開工。排序原則現在是一句話:**

> ### 先讓人玩得到,再讓人變多。

**理由可查證,不是偏好**:**H-1(一個人隔天想不想再打開)到今天一次都沒過**,
而 2026-08-07 量到 Ollama 六次真呼叫**四次產不出可用敘事**、一次要等 5-7 分鐘。
**一個一個人都留不住的東西,再多人來也會走。**

**他沒有指定先拆哪一根。** 沒指定的時候照上面那句自己判斷並寫明理由,
**不要回頭再問一次同一題**,但也不要假裝他指定了某一根。

---

## 一句話:現在最重要的是什麼

> **🔴 2026-08-08 人類打開畫面後回報兩件事,兩件都已經做掉了(第 35 輪):**
> **「缺了很多視覺化」** → 世界時鐘終於上主畫面(壓力段 + `●●●○○` 階段圓點 + 世界第 N 天);
> **「對話我要一直拉到最下面才看的到,就看不到上面的畫面」** → `.scene` 改 `position:sticky`。
> ⚠️ **sticky 那條沒有被驗證**:headless Chrome 從 CLI 捲不動頁面,**只證明了頂端渲染正確**。
> **請人類自己看一眼捲下去畫面有沒有留著。**
>
> **257 passed。遊戲那一側這兩天真的動了**——世界時鐘在走、開場會跟著它變、
> 空白的夜晚有下場了、`--provider auto` 可用而且是預設、`visits` 開始記得住「他來過」、
> 陌洲的原話終於存得下來。
>
> **但開發循環那一側正在悄悄失效,而且是這一輪查出來的:**
> **`待驗收` 堆了 8 則沒人驗過的東西,而 `loop_check` 對那一區一道檢查都沒有。**
> 「進行中」有上限 1、「排隊中」有上限 3 加階數加判準三道,**「待驗收」零道**——
> 所以「查」這一棒可以無限期落後,**而沒有任何東西會叫**。
> 第 22、24、26、28、30、31 輪交的東西到今天都還沒被驗過。

## 🔴 2026-08-08 新發現:你這台機器上 AUTO ROUTER 根本沒在動

**`.env` 裡有 `EVERLIVING_PROVIDER=groq`,它會蓋掉第 26 輪剛換好的 `auto` 預設值**
(`src/everliving/web.py:412` 讀得到這個環境變數)。

**後果**:groq 一旦撞到那個每日 200k tokens 的牆,你看到的是錯誤,
**不會自動掉到 ollama**——而「不會用完的那一格」正是 AUTO ROUTER 存在的理由。

**要走 router 就明寫**:

```
python -m everliving.web --provider auto
```

**這不是 bug,是設定優先序**(命令列 > 環境變數 > 預設值,而 `.env` 是環境變數那一層)。
**要不要把 `.env` 那一行拿掉是你的題**,agent 沒有動它。

## 下一個 loop 該做什麼(照順序)

1. **[下一棒,已逾期] 反思(reflector)。** 上次是第 29 輪,規則是每 3 輪。
   **原料是第 34 輪驗收結果最下面那兩則**,而且它們講的正好是「怎麼工作」:
   **`待驗收` 沒有上限**、**`輪次記錄` 漏了第 32、33 輪而心跳是數那一區算的**。
   加上 `PROGRESS.md` 也漏了第 15-33 輪——**三個都是「同步綁在落後的那一棒上」的同一個病灶**
2. **[積壓,要「查」那一棒清] `待驗收` 還有 9 則沒驗過**(第 35 輪又加了一則)。
   最舊的是第 22 輪的開場場景。
   **不要一次全驗**——一棒驗一到兩則、附證據,才是這一棒的價值
3. **[排隊中,builder 撿] 重試是白花的一次呼叫。** 判準是「重試必須跟第一次不同,否則不准重試」。
   ⚠️ **它要真模型跑到至少一次成功才算完成**,而 2026-08-07 三次長時間執行都被中止。
   **這是它至今沒被撿走的真正原因**
4. **[要人開別的 session] T2-L4 空跑一輪** —— 新蓋的循環唯一的驗收,**到現在仍然沒做**。
   **啟動指令**(開新 session 貼這一句,不要多講;Claude Code 可改打 `/turn`):
   ```
   讀 LOOP.md,照它說的做這一輪。做完更新「現在輪到」交棒。
   ```
   **不能由寫它的 session 自己扮演**:那個 agent 遵守是因為它記得,不是因為文件寫得清楚。
   **最好開 Codex / Qwen**——`AGENTS.md` 號稱廠商中立,沒被別家驗過就只是宣稱
5. **[設計,不進 `src/`] 第十八節:玩家的身體** ← 人類已裁定要做,見〈已裁定但還沒動工〉

## 這幾輪做了什麼(第 15-34 輪,2026-08-07 ~ 08-08)

**196 → 252 passed。這是循環第一次連續多輪真的在推遊戲。**

- **世界時鐘**(第 16 輪):汙染 = 世界存在了幾天,**算出來的不是跳出來的**,所以沒有東西要 tick。
  刻意不放 `agent_state`(那張表是模型寫的,一次幻覺的「汙染:好轉」就把累積清掉)。
  真模型對照組證明敘事讀得出壓力、**全程不提數字**(`playtests/2026-08-07-world-clock-control.txt`)
- **開場不再永遠是工作間**(第 22 輪):由世界階段決定,世界越壞他被推得越往外
  (工作間 → 回收場 → 配電所 → 潮線),**而他的身體贏過世界**。四個開場各截一張看過
- **空白的夜晚有下場**(第 20 輪):拿不到可用敘事重試一次、仍然失敗丟 `LLMRefusal`,
  **空敘事永遠不進 `memory_events`**
- **AUTO ROUTER**(第 24 輪)+ **預設值換成 `auto`**(第 26 輪):每一次呼叫才選 provider,
  快的先、本機最後。換預設值之前先把地雷拆了——**這套測試在 provider 建得起來時會打真的 API**,
  現在 `tests/conftest.py` 的 `_no_real_network` 擋在 **socket 層**
- **`visits`**(第 28 輪)+ **量測鏈走過一次**(第 30 輪):`player_state` 本來一列、每次覆蓋,
  **所以系統記得「他在」,從來沒記過「他來了」**。現在記在**抵達**不是離開
- **配額假警報修掉**(第 31 輪):只在「上一棒是想」時檢查。**`loop_check` 現在只剩心跳一盞紅,而且它是真的**
- **陌洲的原話存得下來**(第 33 輪,第 34 輪驗收通過):`llm_replies`,零 migration,
  **剝掉場景標籤之前就存**

## 已裁定但還沒動工(人類 2026-08-06 決定)

**人類要:登入時能做「臉部設定 / 身體設定 / 聲音設定」,而且明確選了「玩家自己的化身」。**
選項當下就把代價寫在畫面上(「離線敘事退化成執行報告」),他仍然選了,**所以這是他的決定,照做**。

但照做之前要把連鎖攤開,不能假裝沒看到:

- **推翻的是第十二節〈定案建議:委託模式〉。** 那節的理由是機制不是偏好:
  你能直接命令他 → 回來讀到的只是自己命令的執行報告 → **驚喜沒了,而驚喜是核心賭注的燃料**
- **連帶影響第十七節**:主線的整個位置是「**你是他的外部記憶**」,
  那句話只有在**你是另一個人**時才成立。你就是他的話,主線要重寫
- **第十一節「玩家是稀缺資源」靠同一個前提**(他重視你,是因為你是別人)
- **`AGENTS.md` 把帳號系統列為現在不要做**。但**本機角色檔案 ≠ 帳號系統**——
  單機一份 profile 可以,雲端帳號不行

**要提的替代形態(提案,不是決定)**:第十二節那張表其實有第三列——**雙角色**
(你有自己的化身 + 他是獨立夥伴,各自行動)。
**那個形態能給你臉、身體、聲音,而不用把驚喜燒掉**,因為陌洲仍然自己行動。
代價是工作量,原本排在里程碑 3。**要不要走這條,下一輪問人。**

**如果堅持純化身**,有一題必須先答,不然做出來會空:
**「沒有驚喜之後,『想再打開來看它做了什麼』還剩什麼?」**
那是核心賭注本身,不是實作細節。

## 等人類決定(agent 不要自己往下猜)

> **2026-08-07 人類一次答掉三題**(原文照抄,不要改寫成別的意思):
>
> 1. **provider → 「AUTO ROUTER」** —— **已做完**(`router.py`,而且是預設值)
> 2. **簡體字 → 「A」= 只記 log** —— **這就是現況,不用改任何東西,結案。**
> 3. **「系統還是太簡單了」→ 「單調,可以參考其他遊戲」** —— **已產出世界時鐘 + 開場場景**

**下面這些仍然沒有答案:**

- **⚠️ 「最多人玩」的可量測代理是什麼** —— **東西做好了,打開就能指:**
  ```
  python tools/retention.py            # 或 python tools/retention.py play.db
  ```
  報表列了四個候選,**agent 不替你選**。但有一句必須先講:
  **(a)(b)(c) 都只量得到「這一個人」,而你的目標寫的是「最多人」——
  唯一直接對應的是 (d)「有沒有人講給別人聽」,而那條線現在一筆資料都沒有。**
  ⚠️ **舊的來訪永遠補不回來**,報表會誠實說「沒有被記錄過」,不會說「他沒回來」
- **雙角色 還是 純化身?**(見〈已裁定但還沒動工〉)——**現在是最擋路的一題**,
  因為它會回頭改第十二、十七、十一節
- **H-1 playtest** —— **里程碑 0 唯一的成敗判準**,到今天一次都沒過
- **T0-18 懸念要不要「過期成後果」**:委託有三條出路,懸念還是只有「被完成」一條
- **第十七節主線劇情是提案**(而且**化身決定會影響它**)
- **`everliving.db` 要不要清乾淨**:所有驗證與 demo 都跑在別的 DB 或副本,**沒動過原檔**
- **`.env` 那行 `EVERLIVING_PROVIDER=groq` 要不要拿掉**(見上面那節)
- **人類心跳逾期**:`loop_check` 說 27 輪,**實際更多**(`輪次記錄` 漏了兩輪)。
  **你 2026-08-07 明示照跑**,2026-08-08 你說要去開畫面——
  **開完請在 `LOOP.md`「輪次記錄」補一行含 `人類心跳:已開`**,計數才會重來

## 收件匣

- **[🔴 新,2026-08-08,沒驗到] `.scene` 的 sticky 沒有被瀏覽器證明過。**
  headless Chrome 從 CLI **捲不動頁面**,所以第 35 輪只證明了「頂端渲染正確」,
  **沒有證明「捲下去之後畫面真的留著」**——而後者才是人類回報的那件事。
  **不要把它記成通過。** 人類看一眼就知道;要自動化得換一個能驅動瀏覽器的工具
- **[🔴 新,2026-08-08] `llm_replies` 存下來了,而沒有任何人讀得到。**
  `get_llm_reply` 只有測試在呼叫、`tools/` 零命中。
  **這一則的動機是「模型到底吐了什麼答不出來」,而現在要看仍然得手寫 SQL 且先知道 `llm_call_id`。**
  對照 `tools/retention.py`:那則同樣是存資料,**但它附了讀的工具**。
  **驗收判通過**(存不下來的那一半補不回來,讀的那一半隨時補得上),**但問題沒解決**。
  要 planner 排一則:**一個能列出「空的那幾筆」的小工具**
- **[🔴 新,2026-08-08] `待驗收` 沒有上限也沒有任何規則。** 現在堆 8 則,最舊的來自第 22 輪。
  `tools/loop_check.py:110-133` 對「進行中」「排隊中」都有檢查,**對「待驗收」零檢查**。
  **反思那一棒的原料。**
- **[🔴 新,2026-08-08] `輪次記錄` 漏了第 32、33 輪,而心跳計數是數那一區算的**
  (`tools/loop_check.py:178-186`)。**所以檢查器少報。**
  **沒有補寫**——不在場的輪次編出來就是回填。同一個病灶還有:`PROGRESS.md` 漏了第 15-33 輪
- **[🔴 更正我自己的一個決定,而且證據很硬] 重試是白花的一次呼叫。**
  第 20 輪加重試時的理由是「模型幾分鐘前才用同一個 prompt 答對過」。**那句話是錯的**——
  那兩次成功用的是**比較短的、不一樣的** prompt。
  **直接證據**:同一個 prompt 連問兩次,`llm_calls` 記到 **`out=3423` 跟 `out=3423`,
  一個 token 都不差**(上限 6000,沒撞到)。
  **一模一樣的輸出長度 = 那次生成是決定性的**,所以重試把同一份計算再跑一遍。
  真正的失敗模式是:**這個 prompt 讓 qwen3.6 想了 3423 個 token 然後不作答**
  (`strip_reasoning` 回傳空字串是**正確**的)。**已排進 `LOOP.md`「排隊中」。**
- **[🔴 更正,比先前記的更差] Ollama 的失敗率是 67% 不是 50%。**
  六次真實呼叫,**四次產不出可用敘事**。
  **一個看得出來、但樣本數 6 還不算證實的模式**:成功的兩次 prompt 最短,失敗的四次都比較長。
  `llm.py:61-64` 早就寫著 Qwen 推理跟輸出**共用**預算。
  **不要據此去調 `MAX_TOKENS`**——「路由要不要看 prompt 長度」是 AUTO ROUTER 的題。
  **這對 H-1 是硬資料:三分之二的夜晚他會打開來看到一句「這一夜沒有被記下來」。**
- **[✅ 走完了,但走到失敗那條分支——而擋它的程式做對了]**
  **空白夜晚防護第一次被真模型驗過**,全文 `playtests/2026-08-07-h1-two-day-cycle.txt`。
  模型連兩次交白卷 → 重試一次 → 丟 `LLMRefusal`。
  **事後直接查 DB(不採信 traceback):`offline_narrative` 0 筆、`llm_calls` 2 筆。**
  同時驗到:**世界時鐘確實正確地把 stage 3 的壓力送進了 prompt。**
- **[還沒排] `loop_check` 剩兩個「量錯對象」的洞**(`AGENTS.md`〈第四題〉):
  1. **改個標題就能讓一整區從順序檢查裡消失**——`tools/loop_check.py:87` 先濾掉不認得的標題再比對順序
  2. **註解掉的項目仍然被數成一則**——parser 只看行首是不是 `- `,不懂 HTML 註解
     (**繞法是縮排,已經在用**,但那是慣例不是機制)
- **[可觀測了,但不要急著修] 場景/動作跟台詞對不上。**
  `scene`/`action` 現在跟敘事一起存進 `story_assets`。
  **但現在連一筆真實的不一致樣本都還沒有**——先寫一致性檢查等於憑空猜規則。
  正確的下一步是**讓真實資料累積**。原始查證全文 `playtests/2026-08-06-scene-mismatch-repro.txt`
- **[還沒排]** **Groq 免費額度是每日 200k tokens**,而且不是午夜整點重置
- **[已答]** **OpenSpec → 不裝**。逐條理由在設計文件第十九節末〈已否決:OpenSpec〉。
  **真正該考慮換 SQLite 的是 `PROGRESS.md`(140KB+),不是 `LOOP.md`**
- **[已答,結案]** 簡體字 → **只記 log**,`src/everliving/script_check.py` 就是現況
- **[已修]** ~~`scene`/`action` 漏到玩家畫面上~~ / ~~代打按了沒反應~~ / ~~工作檯被裁掉~~ /
  ~~鏡頭跳~~ / ~~reduced-motion 是假的~~ / ~~陌洲的原話存不下來~~
- **[已答]** three.js → **還不要**(條件:H-1 過了 + Blender 產線真的有 glTF 資產)。
  **注意:化身決定可能把這條的時程往前推**
- **[已答]** 知識狀態 → 要,但不能是技能樹
- **[已答]** 隨機物品 / 基因崩潰圖鑑 / 山海經 → 物品由事件產生;要「他經手過的人」的檔案

---

## Loop 收尾檢查(每次結束前跑一遍)

1. 測試真的跑過,而且是**這一輪跑的**(`python -m pytest -q`,現況 **271 passed**)
2. 動過 `LOOP.md`:`python tools/loop_check.py` 要 exit 0
   (**現在只剩心跳那則紅,人類明示照跑**——其餘任何一則都要當真)
3. 動過 `static/index.html`:`node tests/page_check.js` **而且**
   `python tools/shot.py --size 1440x820` 真的看一眼
4. **改了 prompt 或模型行為,測試過不算數**——拿真模型跑一次,結果(含沒驗到的)寫進 `playtests/`
5. **宣稱瀏覽器層級的行為要去問瀏覽器**(`chrome --headless=new --dump-dom`),不能只寫測試
6. **人類回報的問題,先查根因再修**——「按了沒反應」底下同時藏著一個 bug 跟一個額度問題
7. **存下來的東西要往下游追一步問「誰會讀」**——第 11 輪的退回漏了這步(存對了但漏到玩家畫面),
   **第 33 輪漏的是另一頭**(存對了,而沒有任何人讀得到)。**兩頭都要問**
8. `TASKS.md` 狀態改對;`PROGRESS.md` append 一則;**重寫這份 `NOW.md`**
9. commit(先 `git pull --rebase`)
