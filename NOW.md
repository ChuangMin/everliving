# NOW.md — 醒來先讀這一份

> **這份是「現在」,不是歷史。** 歷史在 `PROGRESS.md`,那份只增不改;這份**每個 loop 結束前整份重寫**,
> 永遠只描述當下。這正好是這個專案自己的規則套在自己身上:
> `PROGRESS.md` 是 append-only 的來源,`NOW.md` 是壓縮後的視圖(見設計文件〈記憶磨損〉)。
>
> **順序:先讀這份 → 要細節再去 `TASKS.md` → 要理由才去 `PROGRESS.md` / 設計文件。**
>
> **如果你是被當成四種角色之一叫起來的**:改讀 `LOOP.md` 的「現在輪到」,契約在 `AGENTS.md`〈四種角色〉。

**最後更新**:2026-08-07 by Claude Opus 5(`LOOP.md` 第 14 輪 auditor)

---

## 一句話:現在最重要的是什麼

**人類 2026-08-07 答了卡最久的那一題:「系統太簡單」= 「單調,可以參考其他遊戲」。**
**單調講的是變化不足,不是內容不足**——加更多功能不一定解得掉,同樣的內容換一種排列方式可能就解得掉。
這個差別已經變成排隊中最上面那兩則:**世界時鐘**(讓某個東西真的在累積),
然後**開場不要永遠是工作間**(讓累積在畫面上看得出來)。

**開發循環同時第一次走完了「交 → 退回 → 修 → 通過」的全程**,遊戲的持久層真的動了(188 → 196 passed)。
**下一棒是反思(reflector)**,原料是第 12-14 輪。

## 下一個 loop 該做什麼(照順序)

1. **[下一棒] 反思(reflector),涵蓋第 12-14 輪。** 原料已經齊了而且是同一個模式的**第三個實例**:
   檢查器量的東西跟規則講的東西分家(`AGENTS.md`〈第四題〉)。三個實例都有執行結果當證據,
   列在 `LOOP.md`「驗收結果」倒數第三則。**別再寫第四遍同一個教訓——這次該問的是它為什麼一直復發**
2. **[排隊中,builder 撿]** **世界時鐘** → 然後 **開場場景跟著它動**。判準寫在 `LOOP.md`「排隊中」。
   時鐘沒做完不要開始第二則
3. **[已答成 AUTO ROUTER,設計還沒開始] provider 自動路由。**
   ⚠️ 這跟 `TASKS.md` H-1 那條「中途換 provider 會讓歸因失效」**直接衝突**,設計時要處理。
   **這一題人類已經答了,不在〈等人類決定〉裡**——沒人動它只是因為還沒排
4. **[要人開別的 session] T2-L4 空跑一輪** —— 新蓋的循環唯一的驗收,**到現在仍然沒做**。
   **啟動指令**(開新 session 貼這一句,不要多講;Claude Code 可改打 `/turn`):
   ```
   讀 LOOP.md,照它說的做這一輪。做完更新「現在輪到」交棒。
   ```
   **不能由寫它的 session 自己扮演**:那個 agent 遵守是因為它記得,不是因為文件寫得清楚。
   **最好開 Codex / Qwen**——`AGENTS.md` 號稱廠商中立,沒被別家驗過就只是宣稱
5. **[設計,不進 `src/`] 第十八節:玩家的身體** ← 人類已裁定要做,見〈已裁定但還沒動工〉

## 這幾輪做了什麼(第 9-14 輪,2026-08-07)

**`scene` / `action` 現在存得下來,而且沒有漏到玩家臉上。196 passed(第 9 輪開始時 188)。**

- **存(第 10 輪)**:`db.attach_scene` 掛在 `story_assets` 上,**零 schema 變更**——
  那張表三個月前就建好了而且一直是空的,**掛鉤留了三個月只是沒人掛上去**
- **退回(第 11 輪)**:存對了但**漏到展示層**——`web.py` 把每一筆 asset 往前端送、
  `index.html:988` 把每一筆渲染成 `kind · ref`,**每一拍都會多兩個 debug 標籤**。
  `grep get_assets` 一次就看得到,**不是難,是沒查**
- **修(第 13 輪)+ 通過(第 14 輪)**:`db.get_media_assets` 濾掉 `SCENE_KINDS`,
  `web.py` 三處改走它,**前端一個字都沒動**。
  驗收時**關掉修復跑全套 → 恰好 2 紅 194 綠**:那兩個新測試是唯一釘住這件事的東西,
  而且沒有別的測試依賴著過濾行為

**所以「場景/動作跟台詞對不上」現在第一次可觀測了。** 但注意順序:
**可觀測 ≠ 已經修好**,現在該做的是**等真實樣本累積**,不是急著寫一致性檢查(那等於憑空猜規則)。

**沒做完的**:T2-L4 空跑一輪。**在它跑過之前,那套循環的正確說法仍然是「寫好了」,不是「能用」。**

## 已裁定但還沒動工(人類 2026-08-06 決定)

**人類要:登入時能做「臉部設定 / 身體設定 / 聲音設定」,而且明確選了「玩家自己的化身」。**
選項當下就把代價寫在畫面上(「離線敘事退化成執行報告」),他仍然選了,**所以這是他的決定,照做**。

但照做之前要把連鎖攤開,不能假裝沒看到:

- **推翻的是第十二節〈定案建議:委託模式〉。** 那節的理由是機制不是偏好:
  你能直接命令他 → 回來讀到的只是自己命令的執行報告 → **驚喜沒了,而驚喜是核心賭注的燃料**
- **連帶影響第十七節**:主線的整個位置是「**你是他的外部記憶**」,
  那句話只有在**你是另一個人**時才成立。你就是他的話,主線要重寫
- **第十一節「玩家是稀缺資源」靠同一個前提**(他重視你,是因為你是別人)
- **`AGENTS.md` 把帳號系統列為現在不要做**。但**本機角色檔案 ≠ 帳號系統**——
  單機一份 profile 可以,雲端帳號不行

**要提的替代形態(提案,不是決定)**:第十二節那張表其實有第三列——**雙角色**
(你有自己的化身 + 他是獨立夥伴,各自行動)。
**那個形態能給你臉、身體、聲音,而不用把驚喜燒掉**,因為陌洲仍然自己行動。
代價是工作量,原本排在里程碑 3。**要不要走這條,下一輪問人。**

**如果堅持純化身**,有一題必須先答,不然做出來會空:
**「沒有驚喜之後,『想再打開來看它做了什麼』還剩什麼?」**
那是核心賭注本身,不是實作細節。

## 更早那幾個 loop 做了什麼

- **Ollama 跑得起來而且敘事品質可用,但一次呼叫 5-7 分鐘**(Groq 是 2.8 秒,**差兩個數量級**)。
  全文 `playtests/2026-08-06-ollama.txt`。**人類親身等過 6 分 47 秒,中途以為當機又貼了一次指令**
- **人類實際打開頁面試玩**,回報:**「沒甚麼改變,系統還是太簡單了,讓 AI 代打沒甚麼反應」**。
  兩層原因:表層是 `/api/auto` 錯誤 payload 沒有 `auto` 欄位導致頁面直接 return(**已修**+回歸測試);
  底層是 **Groq 429**,額度根本沒重置。**「太簡單」那句已於 2026-08-07 答成「單調」**
- **`script_check`**:繁體中文那條規則終於有執行者(離線與對話共用一份)
- **四個開發角色 + `LOOP.md` + `tools/loop_check.py`**(設計文件第十九節,人類逐節確認過)
- **委託機制(T1-C1/C2)**、**T1-V8 運鏡 + 無障礙**(真瀏覽器數過 0/87 → 87/87)、
  **T1-V7 構圖 `SAFE`**、**T0-17 懸念不再疊**(→ 拆出 T0-18)

## 等人類決定(agent 不要自己往下猜)

> **2026-08-07 人類一次答掉三題**(原文照抄,不要改寫成別的意思):
>
> 1. **provider → 「AUTO ROUTER」** —— 不選一個,要自動在兩者之間選。**設計還沒開始。**
>    ⚠️ 這跟 `TASKS.md` H-1 那條「中途換 provider 會讓歸因失效」直接衝突,設計時要處理
> 2. **簡體字 → 「A」= 只記 log** —— **這就是現況,不用改任何東西,結案。**
> 3. **「系統還是太簡單了」→ 「單調,可以參考其他遊戲」** ← **這是專案卡最久的那一題,終於有答案了**
>
> **第 3 題最重要,而且它推翻了「太簡單=功能太少」的預設讀法。**
> 「單調」講的是**變化不足**,不是**內容不足**——加更多功能不一定解得掉單調,
> 而**同樣的內容換一種排列方式可能就解得掉**。這個差別會決定接下來做什麼。

**上面三題都答完了,下面這些仍然沒有:**

- **雙角色 還是 純化身?**(見〈已裁定但還沒動工〉)——**現在是最擋路的一題**,
  因為它會回頭改第十二、十七、十一節
- **H-1 playtest** —— **里程碑 0 唯一的成敗判準**。AUTO ROUTER 設計完才有得測
- **「單調」的拆法順序沒被點頭**:planner 提的是「累積 → 按鈕 → 章節」,人類只說「快想」。
  排隊中那兩則照這個順序排了——**如果順序錯了,錯的是方向不是做法**
- **T0-18 懸念要不要「過期成後果」**:委託有三條出路,懸念還是只有「被完成」一條
- **第十七節主線劇情是提案**(而且**化身決定會影響它**)
- **`everliving.db` 要不要清乾淨**:所有驗證與 demo 都跑在別的 DB,沒動它
- **人類心跳已經逾期 9 輪**,`loop_check` 一直在叫。**你 2026-08-07 明示照跑**,
  所以第 11-14 輪都是在紅燈下跑的。**要不要真的開一次畫面重新計時,是你的題**——
  記在這裡是為了不讓「在紅燈下跑」靜靜變成默認慣例

## 收件匣

- **[還沒排] `loop_check` 有三個「量錯對象」的洞,同一個病灶**(`AGENTS.md`〈第四題〉):
  1. **配額規則**量的是佇列此刻剩下什麼,規則講的是 planner 這一輪加了什麼(**已排進排隊中 [階6]**)
  2. **改個標題就能讓一整區從順序檢查裡消失**——`tools/loop_check.py:87` 先濾掉不認得的標題再比對順序
  3. **註解掉的項目仍然被數成一則**——parser 只看行首是不是 `- `,不懂 HTML 註解
  **第 2、3 項是第 14 輪實際踩到的,兩個都有執行結果當證據。反思那一棒的原料。**
- **[可觀測了,但不要急著修] 場景/動作跟台詞對不上。**
  `scene`/`action` 現在跟敘事一起存進 `story_assets`(第 10-14 輪),
  **所以這件事第一次有辦法被觀察**。
  **但現在連一筆真實的不一致樣本都還沒有**——先寫一致性檢查等於憑空猜規則。
  正確的下一步是**讓真實資料累積**,不是排一則「修好它」。
  原始查證全文 `playtests/2026-08-06-scene-mismatch-repro.txt`
- **[還沒排]** **Groq 免費額度是每日 200k tokens**,而且不是午夜整點重置。
  **AUTO ROUTER 要處理的正是這件事**
- **[已答]** **OpenSpec → 不裝**。逐條理由在設計文件第十九節末〈已否決:OpenSpec〉。
  簡短版:輪子已經手工造好、權威來源會分裂、它管的規格漂移不是這裡的阻塞。
  **真正該考慮換 SQLite 的是 `PROGRESS.md`(130KB+),不是 `LOOP.md`**——後者已經有硬上限,是固定大小
- **[已答,結案]** 簡體字 → **只記 log**,`src/everliving/script_check.py` 就是現況,不用改
- **[已修]** ~~`scene`/`action` 漏到玩家畫面上~~ / ~~代打按了沒反應~~ / ~~工作檯被裁掉~~ /
  ~~鏡頭跳~~ / ~~reduced-motion 是假的~~
- **[已答]** three.js → **還不要**(條件:H-1 過了 + Blender 產線真的有 glTF 資產)。
  **注意:化身決定可能把這條的時程往前推**
- **[已答]** 知識狀態 → 要,但不能是技能樹
- **[已答]** 隨機物品 / 基因崩潰圖鑑 / 山海經 → 物品由事件產生;要「他經手過的人」的檔案

---

## Loop 收尾檢查(每次結束前跑一遍)

1. 測試真的跑過,而且是**這一輪跑的**(`python -m pytest -q`,現況 **196 passed**)
2. 動過 `LOOP.md`:`python tools/loop_check.py` 要 exit 0
   (**現在只剩心跳那則紅,人類明示照跑**——其餘任何一則都要當真)
3. 動過 `static/index.html`:`node tests/page_check.js` **而且**
   `python tools/shot.py --size 1440x820` 真的看一眼
4. **改了 prompt 或模型行為,測試過不算數**——拿真模型跑一次,結果(含沒驗到的)寫進 `playtests/`
5. **宣稱瀏覽器層級的行為要去問瀏覽器**(`chrome --headless=new --dump-dom`),不能只寫測試
6. **人類回報的問題,先查根因再修**——「按了沒反應」底下同時藏著一個 bug 跟一個額度問題,
   只修一個會以為好了
7. **存下來的東西要往下游追一步問「誰會讀」**——第 11 輪的退回就是漏了這一步:
   資料層做得對,但它同一秒就漏到玩家畫面上了
8. `TASKS.md` 狀態改對;`PROGRESS.md` append 一則;**重寫這份 `NOW.md`**
9. commit(先 `git pull --rebase`)
