# NOW.md — 醒來先讀這一份

> **這份是「現在」,不是歷史。** 歷史在 `PROGRESS.md`,那份只增不改;這份**每個 loop 結束前整份重寫**,
> 永遠只描述當下。這正好是這個專案自己的規則套在自己身上:
> `PROGRESS.md` 是 append-only 的來源,`NOW.md` 是壓縮後的視圖(見設計文件〈記憶磨損〉)。
>
> **順序:先讀這份 → 要細節再去 `TASKS.md` → 要理由才去 `PROGRESS.md` / 設計文件。**

**最後更新**:2026-08-05 by Claude Opus 5

---

## 一句話:現在卡在哪

**沒有技術阻塞。161 passed、page checks 全過、委託機制接上真模型跑通了。**
**不等人就能做的只剩一條**(鏡頭移到事件的位置)。做完那條之後,
「今天沒有事該做」就會是正確答案——閘門全部在人身上,見〈等人類決定〉。

## 你可以叫 agent 給你看畫面

```
python tools/shot.py --size 1440x820           # 寬螢幕是會壞的那一種,優先看它
python tools/shot.py --scene 潮線 --action 淹水
```

現成的 Chrome headless,**不用裝擴充功能、不用網路、不花任何 token**。
`tests/page_check.js` 只能證明畫圖的程式跑得動,**看不見畫出來的東西**。

## 上一個 loop 做了什麼(2026-08-05 第九輪)

**委託機制(T1-C1)。** 人類裁定「可以先做」,所以把上一輪丟回去的閘門問題落地成實作。
**被 H-1 擋住的仍然是 action 層那 2–3 個選項的 UI,不是委託本身**——
第十二節寫明「不鎖死自由輸入」,所以自由輸入就是委託的第一個介面,而它早就在了。

- **記錄不花第二次呼叫**:對話回覆多一行 `委託:X`,跟 `場景`/`動作` 同一個機制。
  而且 prompt 講明**寫了不代表答應**
- **新的 `delegations` table,不併進 `open_threads`**:方向是反的
  (懸念是**他在等你**,委託是**你在等他**),而且委託有 `outcome`
- **離線結算**,第十二節那兩條規則一起做了:**拒絕必須給出來自狀態或人設的理由**、
  **拒絕本身要生線**。認不得的 status 一律當 done——留著會永遠進 prompt
- 工作檯多一區「你請他做的事」

**真模型驗過,而且它自己做出了設計文件預言的那件事**
(`playtests/2026-08-05-delegation-check.txt`):
他把玩家的話收斂成自己的一句話、閒聊那輪沒有誤記成委託、隔天的離線敘事把它結掉——
**但順便撞上了別的事**(廢料堆塌陷、掀開一個在漏水的配電艙門),
而新的懸念是從那個副作用長出來的。第十二節說那個落差是「委託模式白送的」,**它真的是白送的**。
**沒驗到拒絕路徑**(這次他答應了),只有 fake 測試蓋著。

## 更早那幾個 loop 做了什麼

- **T1-V7 構圖**:1440×780 下開場畫面本來**工作檯不見、吊燈不見、人只剩半截**
  (本來就有的,只是在有截圖工具之前沒人看得見)。訂了量出來的 `SAFE = {top:108, bottom:308}`
- **連續運動(T1-V4/V5/V6)**:三層不同速率漂移、水面無縫流動、240 秒週期極慢推拉,
  **全部用 CSS 不用 SMIL**(`prefers-reduced-motion` 是 CSS 規則)。離線那一夜也能說「在幹嘛」了。
  外加 `tools/shot.py`
- **T0-17 懸念不再疊**:3/3 不再開重複的,但只有 1/3 標 resolved(另外兩次模型是對的 → T0-18)
- **H-1 四步用 AI 代打整條跑通**:第 3 步的承諾**真的長進了第 4 步的離線敘事**。
  成本:**output 是 input 的 3.6 倍**
- **主線劇情 → 設計文件第十七節**:主線只能是**一個會一直生線的位置**;玩家是**他的外部記憶**

## 下一個 loop 該做什麼

1. **[視覺,剩下半條] 鏡頭移到事件的「位置」** ← **不需要任何人點頭,只剩這條**。
   現在 `FOCUS` 是四個**動作**的固定座標,不是**事件**的座標。
   要真的把鏡頭帶到事件發生的地方,得先有「這個事件在畫面上的哪裡」這個東西。
   新的焦點一律要落在 `SAFE` 裡

**做完這條,不等人就能做的事就沒有了。** 那時候正確行為是**停下來說明狀況**,不是找事做
(`AGENTS.md`:「今天沒有事該做」是一個正確的結果)。

## 等人類決定(agent 不要自己往下猜)

- **H-1 playtest 要不要親自跑**(跑法在 `TASKS.md`)——**這是里程碑 0 唯一的成敗判準,
  而且現在擋著後面所有東西**。當初擋住它的是「連畫面都沒有我不太想打開」;
  現在畫面會動、在你的螢幕上不會被裁掉一半,而且你還能**請他去做事**。
  先看看的話:`python tools/shot.py`、`playtests/` 底下三份記錄
- **T0-18 懸念要不要「過期成後果」** ← **這輪之後更值得看一眼**:
  委託現在有三條出路(done / refused / 認不得也當 done),**懸念還是只有「被完成」一條**。
  提案:期限到了沒等到就 resolve 掉,同時產生一件**回不去的事**。要加欄位,**是新機制,所以先問**
- **第十七節主線劇情是提案**:第一章 premise〈還留著別人的那一片〉要不要照走、
  那三個不可逆選項(擦乾淨 / 留著不用 / 交給機器廠)刻意設計成沒有一個是對的
- **`everliving.db` 要不要清乾淨**:H-1 若要乾淨起點就得清(清前會備份)。所有驗證都跑在另一個 DB
- **`ANTHROPIC_API_KEY` 是壞的**(401)。不換也不擋事,Groq 那把是好的

## 收件匣(人類講過但還沒被排進上面的)

- **[還沒排]** 排練第 1 步出現一次**場景跳到「配電所/淹水」但台詞還在工作間修閥門**——
  兩軸各自合法、合起來跟話對不上。只出現一次
- **[新]** 委託的**拒絕路徑還沒被真模型驗過**。下次跑排練時,刻意用一個他做不到的委託試一次
- **[已修]** ~~工作檯被裁掉~~ / ~~焊接弧光構圖~~(T1-V7)
- **[已做完]** ~~HelloRun 那種流暢~~ → 連續運動(T1-V4)
- **[已答,第十七節]** ~~劇情等級/難度~~ —— 只有「這條線走到哪了」與「有幾件事回不去了」;
  真要難度的話唯一合法形態是**代價變貴**
- **[已答]** three.js → **還不要**。條件:**H-1 過了 + Blender 產線真的有 glTF 資產**
- **[已答]** 知識狀態 → **要,但不能是技能樹**
- **[已答]** 隨機物品 / 基因崩潰圖鑑 / 山海經 → 物品由事件產生不由機率產生;
  不要怪物圖鑑要「他經手過的人」的檔案;山海經融合在人身上

---

## Loop 收尾檢查(每次結束前跑一遍)

1. 測試真的跑過,而且是**這一輪跑的**(`python -m pytest -q`)
2. 動過 `static/index.html` 的話:`node tests/page_check.js` **而且**
   `python tools/shot.py --size 1440x820` 真的看一眼
3. **改了 prompt 或模型行為的話,測試過不算數**——測試只能證明那句話在 prompt 裡。
   拿真模型跑一次,把結果(包含沒驗到的部分)寫進 `playtests/`
4. `TASKS.md` 狀態改對了(`done` 的東西真的能跑)
5. `PROGRESS.md` 加一則新記錄(**append,不要改舊的**)
6. **重寫這份 `NOW.md`**
7. commit(先 `git pull --rebase`)
