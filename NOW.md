# NOW.md — 醒來先讀這一份

> **這份是「現在」,不是歷史。** 歷史在 `PROGRESS.md`,那份只增不改;這份**每個 loop 結束前整份重寫**,
> 永遠只描述當下。這正好是這個專案自己的規則套在自己身上:
> `PROGRESS.md` 是 append-only 的來源,`NOW.md` 是壓縮後的視圖(見設計文件〈記憶磨損〉)。
>
> **順序:先讀這份 → 要細節再去 `TASKS.md` → 要理由才去 `PROGRESS.md` / 設計文件。**

**最後更新**:2026-08-05 by Claude Opus 5

---

## 一句話:現在卡在哪

**沒有技術阻塞。145 passed,H-1 那四步剛用 AI 代打整條跑通(26 次呼叫,零失敗)。**
等的是人:**H-1 要不要親自跑**——排練不能代答那一題。

## 上一個 loop 做了什麼(2026-08-05 第五輪)

**把上一輪只寫下來沒跑的 H-1 排練跑完了。** 工具 `tools/h1_autoplay.py`,
全文留在 `playtests/2026-08-05-h1-autoplay.txt`(groq / `qwen/qwen3.6-27b`)。

- **做法照上一輪定好的,沒有重想**:每一步**新建一個 `web.Session`**(網頁按鈕的同一條路徑;
  `Session.opened` 是 per run,共用會靜靜跳過離線模擬),`offline_hours = None / 24 / 0 / 24`,
  每步三輪 `auto_turn()`。跑在另一個 DB,`everliving.db` 沒動
- **最值得看的一步成立了**:第 2 步留下懸念(扳手斷了、備用泵密封圈炸了、要軸承、撐兩天)→
  第 3 步訪客答應「明晚日落前帶軸承」→ **第 4 步的離線敘事是從那個承諾長出來的**
  (鐵台掃乾淨、硬幣罐推到你手邊),還自己加了沒人要求的後果:**為了頂住壓力切斷外圍區供氧**
- **抓到一個真的問題(已開 T0-17)**:**懸念會疊,不會收**。第 4 步是「再開一條講同一件事的新懸念」,
  不是把舊那條標 resolved,跑完有兩條幾乎一樣的、`resolved` 全程 0。
  機制本來就有(`resolved_thread_ids`),是模型沒用 → 修法在 prompt 層
- **成本的形狀跟預期不一樣**:input 17,098 / **output 61,322(3.6 倍)**。
  一則玩家只讀到兩三句的回覆,那次呼叫記了約 2,500 output token——**是推理量,不是敘事長度**。
  設計文件第四節要量的兩個數字之一有第一個真實值了
- **這不是 H-1 的答案**,而且它的弱點是已知的:訪客的 prompt 不會「答應他一件事」那種份量,
  所以第 4 步的力道比人類自己跑弱一截

## 更早那幾個 loop 做了什麼

- **主線劇情 → 設計文件第十七節**(只動文件)。主線不可能是一條會結束的線,只能是**一個會一直生線的位置**:
  **他知道的東西只存在他身上,而唯一能保存它的方法是交給那個會把它拿走的人。**
  玩家的位置是**他的外部記憶**(他會忘,你不會——`memory_events` 在程式裡本來就 append-only)。
  第一章提案〈還留著別人的那一片〉,**美術成本零**
- **AI 代打(訪客模式)**:訪客 agent 坐到玩家的位子,有硬上限(預設 6 輪)。
  世界不知道對方是人還是 AI,**分別只記在 `llm_calls.purpose = auto_visitor`**——在帳上,不在故事裡
- **`動作` 標籤**:`scene` 只講「在哪」不講「在幹嘛」,所以加第二軸(焊接 / 停電 / 淹水 / 起霧)
- 再更早:`scene` 對話也會更新、工作檯面板、`assets` 從紙上變成活的、`tests/page_check.js` 真的執行頁面 JS

## 下一個 loop 該做什麼(照順序,做完就從這裡刪掉)

1. **[修] T0-17 懸念會疊,不會收** ← 排練當場抓到的,而且三個層面同時中招
   (面板顯示重複、對話 prompt 每次變長=錢、「解決了」永遠不會發生)。
   動的是 `offline._build_prompts`,**不是新機制**:既有懸念還沒解決時,新發展寫進 `events`,
   不要再開一條同義的。改之前先看測試怎麼斷言 prompt 內容
2. **[視覺] 連續運動 + 離線事件演在場景裡** — HelloRun 的流暢**主要來自鏡頭一直在動**,不是 3D;
   現在的頁面是每 30 秒重畫一次的靜態圖。方向:**圖層視差緩慢漂移、水面持續流動、鏡頭極慢推移**
   (SVG/CSS 就做得到,不用換渲染器)。**而鏡頭會動,才有辦法把鏡頭移到事件發生的地方**——
   這正好是「agent 自己玩要看得到」需要的載體。`scene`、`動作`、`event_id`、`assets` 四個接口都在了
3. **[操控] 操控度** — 工作檯只是第一步(可以查),還缺「可以做」。
   設計文件:**three.js 真正會贏的是這一塊(可走動、可點、視差)**,不是靜態畫面好不好看
4. **[之後] 語音輸入 / 配音輸出** — 不是現在,但設計時別擋死

**劇本層的實作(篇章 / stage / 動作選項)不要現在做**——閘門是 H-1(設計文件第十節)。
第十七節已經查過:主線需要的東西裡**只有「篇章 / stage」是缺的**,其餘全部長在既有結構上。

## 等人類決定(agent 不要自己往下猜)

- **H-1 playtest 要不要親自跑**(跑法在 `TASKS.md`)。
  排練已經證明**流程通、狀態會接**,但 H-1 問的是「**你**想不想再打開」,那題只有你能答。
  想先看看排練長什麼樣再決定的話:`playtests/2026-08-05-h1-autoplay.txt`
- **第十七節主線劇情是提案**,尤其這兩點想聽人的意見:
  - **第一章的 premise**〈還留著別人的那一片〉要不要照這樣走
  - **那三個不可逆選項**(擦乾淨 / 留著不用 / 交給機器廠)——刻意設計成沒有一個是對的
- **`everliving.db` 要不要清乾淨**:目前有測試灌進去的記憶,H-1 若要乾淨起點就得清(清前會備份)。
  排練沒有動它,跑在另一個 DB
- **`ANTHROPIC_API_KEY` 是壞的**(401)。不換也不擋事,Groq 那把是好的

## 收件匣(人類講過但還沒被排進上面的)

- **[新,還沒排]** 第 1 步第三輪出現一次**場景跳到「配電所/淹水」但台詞還在工作間修閥門**——
  兩軸各自合法、合起來跟話對不上。只出現一次,先記著不動
- **[已答,寫進設計文件第十七節]** ~~劇情等級/難度設定~~ —— 合法形態只有兩個:
  **「這條線走到哪了」**與**「有幾件事回不去了」**。真要難度的話,唯一合法形態是**代價變貴**,不是敵人變強
- **[已排進第 2 項]** ~~HelloRun 那種流暢~~ —— 結論是**連續運動,不是換渲染器**
- **[已答]** three.js 要不要用 → **還不要**。條件:**H-1 過了 + Blender 產線真的有 glTF 資產**
- **[已答]** 知識狀態要不要 → **要,但不能是技能樹**(第十七節有產生器與三種下場)
- **[已答]** 隨機物品 / 基因崩潰圖鑑 / 山海經 → 物品由事件產生不由機率產生;
  不要怪物圖鑑要「他經手過的人」的檔案;山海經融合在人身上

---

## Loop 收尾檢查(每次結束前跑一遍)

1. 測試真的跑過,而且是**這一輪跑的**(`python -m pytest -q`)
2. 動過 `static/index.html` 的話,`test_page_render.py` 有沒有過(它會真的執行頁面 JS)
3. `TASKS.md` 狀態改對了(`done` 的東西真的能跑)
4. `PROGRESS.md` 加一則新記錄(**append,不要改舊的**)
5. **重寫這份 `NOW.md`**——尤其「下一個 loop 該做什麼」和「收件匣」
6. commit(先 `git pull --rebase`)
