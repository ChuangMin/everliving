# NOW.md — 醒來先讀這一份

> **這份是「現在」,不是歷史。** 歷史在 `PROGRESS.md`,那份只增不改;這份**每個 loop 結束前整份重寫**,
> 永遠只描述當下。這正好是這個專案自己的規則套在自己身上:
> `PROGRESS.md` 是 append-only 的來源,`NOW.md` 是壓縮後的視圖(見設計文件〈記憶磨損〉)。
>
> **順序:先讀這份 → 要細節再去 `TASKS.md` → 要理由才去 `PROGRESS.md` / 設計文件。**

**最後更新**:2026-08-05 by Claude Opus 5

---

## 一句話:現在卡在哪

**沒有技術阻塞。151 passed、`node tests/page_check.js` 20 項全過、畫面現在會動而且我看得到它。**
下一輪要動的是**操控度**,不需要任何人點頭。等人的三題:**H-1 要不要親自跑**、
**T0-18 懸念要不要「過期成後果」**、**第十七節主線提案**。

## 先知道這件事:你現在可以叫 agent 給你看畫面

```
python tools/shot.py                          # 開場場景,取樣動畫週期的三個點
python tools/shot.py --scene 潮線 --action 淹水
```

用機器上現成的 Chrome headless 截圖,**不用裝擴充功能、不用網路、不花任何 token**。
`tests/page_check.js` 只能證明畫圖的程式跑得動,**看不見畫出來的東西**——
什麼被裁掉、誰蓋在誰上面、會動的東西會不會動到框外,它一律無感。
**動過 `static/index.html` 就順手跑一次 `shot.py` 看一眼**,已經加進下面的收尾檢查。

## 上一個 loop 做了什麼(2026-08-05 第七輪)

**畫面從「每 30 秒換一張靜態圖」變成「鏡頭一直在動」(T1-V4/V5/V6)。**

- **連續運動**:三層以不同速率漂移(近的走最多、遠的最少)、水面照**一個完整波長**無縫流動、
  整個框架用 240 秒週期極慢推拉。**全部寫成 CSS 不是 SMIL**——
  `prefers-reduced-motion` 是 CSS 規則,SMIL 會繞過它,而這層才是真的會讓人不舒服的那層。
  每個漂移層都帶一點 scale,否則橫移會露出框外。水面反光改成 seeded(它是最後一個會跳的東西)
- **鏡頭會朝正在發生的事偏過去**(`transform-origin` 跟著 `動作` 走)。
  漂移本身只是裝飾,**能把視線帶到事件上才是它存在的理由**
- **離線那一夜本來說不出「在幹嘛」**:`動作` 只長在對話路徑上,所以「整層樓的燈都滅了」
  那種夜晚畫面照樣開在燈火通明的工作間。現在 `action` 補上了,`null` 也送。
  跟 `scene` 不同,**它沒有 fallback**——憑空生出敘事沒提過的天氣比場景平靜更糟
- **`tools/shot.py`**(見上一節)。踩到一個值得記的坑:
  **Chrome 的 `--virtual-time-budget` 不會推進 CSS 動畫**(角落的鐘會走,畫面逐像素相同),
  所以工具改用負的 `animation-delay` + `paused` 把每一格**釘**住,反而更快也可重現

## 更早那幾個 loop 做了什麼

- **T0-17 懸念不再疊**:`_build_prompts` 的規則 2 依有沒有未解懸念分兩種寫法,而且是**取代不是並存**。
  真跑三次驗證:**3/3 不再開重複的,但只有 1/3 標 resolved**——另外兩次模型是對的,
  軸承還沒送到,那條懸念本來就沒結束(→ 拆出 T0-18)
- **H-1 四步用 AI 代打整條跑通**(`tools/h1_autoplay.py`,26 次呼叫零失敗)。
  第 3 步訪客答應「明晚日落前帶軸承」→ **第 4 步的離線敘事是從那個承諾長出來的**。
  成本:**output 是 input 的 3.6 倍**,一則兩三句的回覆記約 2,500 output token(推理量,不是敘事長度)
- **主線劇情 → 設計文件第十七節**:主線只能是**一個會一直生線的位置**——
  **他知道的東西只存在他身上,而唯一能保存它的方法是交給那個會把它拿走的人。**
  玩家的位置是**他的外部記憶**(`memory_events` 本來就 append-only)
- **AI 代打(訪客模式)**:世界不知道對方是人還是 AI,分別只記在 `llm_calls.purpose`

## 下一個 loop 該做什麼(照順序,做完就從這裡刪掉)

1. **[操控] 操控度** ← **不需要任何人點頭,直接做這條**。
   工作檯只是第一步(可以查),還缺「可以做」。
   設計文件:**three.js 真正會贏的是這一塊(可走動、可點、視差)**,不是靜態畫面好不好看
2. **[視覺,剩下半條] 鏡頭移到事件的「位置」** — 現在 `FOCUS` 是四個動作的固定座標,
   不是事件的座標。要真的把鏡頭帶到事件發生的地方,得先有「事件在畫面上的哪裡」這個東西
3. **[之後] 語音輸入 / 配音輸出** — 不是現在,但設計時別擋死

**劇本層的實作(篇章 / stage / 動作選項)不要現在做**——閘門是 H-1(設計文件第十節)。

## 等人類決定(agent 不要自己往下猜)

- **H-1 playtest 要不要親自跑**(跑法在 `TASKS.md`)。排練已經證明**流程通、狀態會接**,
  但 H-1 問的是「**你**想不想再打開」,那題只有你能答。
  現在**畫面也會動了**,所以當初擋住這個測試的「連畫面都沒有我不太想打開」那道門檻更低了。
  想先看:`playtests/2026-08-05-h1-autoplay.txt`,或直接 `python tools/shot.py`
- **T0-18 懸念要不要「過期成後果」**:現在一條懸念只有「被完成」一條出路,玩家不理它就永遠開著、
  永遠進 prompt(越來越貴)。**提案**:期限到了沒等到就 resolve 掉,同時產生一件**回不去的事**。
  跟設計文件「三座時鐘單向且無解」是同一個形狀。但要在 `open_threads` 加欄位,**是新機制,所以先問**
- **第十七節主線劇情是提案**:第一章 premise〈還留著別人的那一片〉要不要照走、
  那三個不可逆選項(擦乾淨 / 留著不用 / 交給機器廠)刻意設計成沒有一個是對的
- **`everliving.db` 要不要清乾淨**:H-1 若要乾淨起點就得清(清前會備份)。
  排練跟後續驗證都跑在另一個 DB,沒動它
- **`ANTHROPIC_API_KEY` 是壞的**(401)。不換也不擋事,Groq 那把是好的

## 收件匣(人類講過但還沒被排進上面的)

- **[新,截圖之後才看得到]** **工作間的工作檯在矮視窗下會被裁掉**:`.scene` 是 42vh,
  `slice` 會把 y>364 切掉,而工作檯在 346–420。**這是本來就有的,不是連續運動造成的**
  (前後對照確認過),但以前沒人看得見。工作檯是工作間的招牌細節,值得處理
- **[新,同上]** 焊接的弧光落在窗框下緣那條暗帶裡,構圖有點怪。一樣是本來就有的
- **[還沒排]** 排練第 1 步出現一次**場景跳到「配電所/淹水」但台詞還在工作間修閥門**——
  兩軸各自合法、合起來跟話對不上。只出現一次
- **[已答,第十七節]** ~~劇情等級/難度~~ —— 只有「這條線走到哪了」與「有幾件事回不去了」。
  真要難度的話,唯一合法形態是**代價變貴**
- **[已做完]** ~~HelloRun 那種流暢~~ —— 結論是連續運動,已完成(T1-V4)
- **[已答]** three.js → **還不要**。條件:**H-1 過了 + Blender 產線真的有 glTF 資產**
- **[已答]** 知識狀態 → **要,但不能是技能樹**(第十七節有產生器與三種下場)
- **[已答]** 隨機物品 / 基因崩潰圖鑑 / 山海經 → 物品由事件產生不由機率產生;
  不要怪物圖鑑要「他經手過的人」的檔案;山海經融合在人身上

---

## Loop 收尾檢查(每次結束前跑一遍)

1. 測試真的跑過,而且是**這一輪跑的**(`python -m pytest -q`)
2. 動過 `static/index.html` 的話:`node tests/page_check.js` **而且** `python tools/shot.py` 真的看一眼
3. `TASKS.md` 狀態改對了(`done` 的東西真的能跑)
4. `PROGRESS.md` 加一則新記錄(**append,不要改舊的**)
5. **重寫這份 `NOW.md`**——尤其「下一個 loop 該做什麼」跟「收件匣」
6. commit(先 `git pull --rebase`)
