# NOW.md — 醒來先讀這一份

> **這份是「現在」,不是歷史。** 歷史在 `PROGRESS.md`,那份只增不改;這份**每個 loop 結束前整份重寫**,
> 永遠只描述當下。這正好是這個專案自己的規則套在自己身上:
> `PROGRESS.md` 是 append-only 的來源,`NOW.md` 是壓縮後的視圖(見設計文件〈記憶磨損〉)。
>
> **順序:先讀這份 → 要細節再去 `TASKS.md` → 要理由才去 `PROGRESS.md` / 設計文件。**
>
> **如果你是被當成四種角色之一叫起來的**:改讀 `LOOP.md` 的「現在輪到」,契約在 `AGENTS.md`〈四種角色〉。

**最後更新**:2026-08-06 by Claude Opus 5

---

## 一句話:現在最重要的是什麼

**Ollama 跑起來了,而且離線敘事的品質是好的——但一次呼叫要 5-7 分鐘。**
Groq 是 2.8 秒。**差兩個數量級**,所以 Ollama 解得掉「額度」,解不掉「打開來玩」:
人類上次抱怨的是「代打沒甚麼反應」,而五分鐘的等待在體感上跟沒反應是同一件事。
**H-1 要用哪個 provider 現在是一題要人回答的問題**,不是實作細節。

開發循環跑過三輪(想→寫→查)並通過驗收,但**那三輪都在同一個 session 裡**,
所以 **T2-L4 仍然沒被驗證**——寫它的人自己跑不算數。

## 下一個 loop 該做什麼(照順序)

1. **[✅ 已做,2026-08-06] 用 Ollama 讓它真的能玩** —— 三步全過,全文 `playtests/2026-08-06-ollama.txt`。
   `/api/open` 0.004s → `/api/say` **305s** → `--offline-hours 24` **404s**。
   敘事、events、state_changes、open_thread 全部非空,而且**在角色裡**。
   **接下來的問題不是「能不能跑」,是「五分鐘的等待要不要拿去做 H-1」**——見〈等人類決定〉
2. **[要人開別的 session] T2-L4 空跑一輪** —— 新蓋的循環唯一的驗收。
   **啟動指令**(開新 session 貼這一句,不要多講;Claude Code 可改打 `/turn`):
   ```
   讀 LOOP.md,照它說的做這一輪。做完更新「現在輪到」交棒。
   ```
   **不能由寫它的 session 自己扮演**:那個 agent 遵守是因為它記得,不是因為文件寫得清楚。
   要測的正好是「一個沒有這段記憶的 agent 讀了會不會照做」。**最好開 Codex / Qwen**——
   `AGENTS.md` 號稱廠商中立,沒被別家驗過就只是宣稱
3. **[還沒答] 「系統還是太簡單了」到底指什麼** —— 這句話沒被拆開之前,
   任何「再加一個功能」都是猜的。候選:對話太短?沒有可以按的東西?世界不會自己動?看不出有進展?
   **不要自己選一個去做**
4. **[設計,不進 `src/`] 第十八節:玩家的身體** ← 人類已裁定要做,見下一節

## 這一輪蓋了什麼(2026-08-06)

**四個開發角色 + 一份 `LOOP.md`,設計在設計文件第十九節,全部由人類逐節確認過。**

- **`tools/loop_check.py` + 14 個測試**(177 passed,原 163)——十條規則,
  把「每個角色只寫自己那一區」從自律變成機制
- **`AGENTS.md` 改寫**:〈最重要的一條規則〉整條換掉(見下),〈兩種角色〉→〈四種角色〉
- **四個 `.claude/agents/` 薄包裝**,契約一個字都沒複製過去

**⚠️ `AGENTS.md` 第 25 行那條規則已經被人類推翻了。**
舊版是「『今天沒有事該做』是一個正確的結果」,現在是
**「每一輪都要交出東西,但每一則都要說得出它是從哪裡來的」**——靠七階取材梯 + 階 5-7 標記與配額擋技術債。
**唯一仍然不准的是替人類拍板。**

**沒做完的**:T2-L4 空跑一輪。**在它跑過之前,那套循環的正確說法是「寫好了」,不是「能用」。**

## 已裁定但還沒動工(人類 2026-08-06 決定)

**人類要:登入時能做「臉部設定 / 身體設定 / 聲音設定」,而且明確選了「玩家自己的化身」。**
選項當下就把代價寫在畫面上(「離線敘事退化成執行報告」),他仍然選了,**所以這是他的決定,照做**。

但照做之前要把連鎖攤開,不能假裝沒看到:

- **推翻的是第十二節〈定案建議:委託模式〉。** 那節的理由是機制不是偏好:
  你能直接命令他 → 回來讀到的只是自己命令的執行報告 → **驚喜沒了,而驚喜是核心賭注的燃料**
- **連帶影響第十七節**:主線的整個位置是「**你是他的外部記憶**」,
  那句話只有在**你是另一個人**時才成立。你就是他的話,主線要重寫
- **第十一節「玩家是稀缺資源」靠同一個前提**(他重視你,是因為你是別人)
- **`AGENTS.md` 把帳號系統列為現在不要做**。但**本機角色檔案 ≠ 帳號系統**——
  單機一份 profile 可以,雲端帳號不行

**要提的替代形態(提案,不是決定)**:第十二節那張表其實有第三列——**雙角色**
(你有自己的化身 + 他是獨立夥伴,各自行動)。
**那個形態能給你臉、身體、聲音,而不用把驚喜燒掉**,因為陌洲仍然自己行動。
代價是工作量,原本排在里程碑 3。**要不要走這條,下一輪問人。**

**如果堅持純化身**,有一題必須先答,不然做出來會空:
**「沒有驚喜之後,『想再打開來看它做了什麼』還剩什麼?」**
那是核心賭注本身,不是實作細節。

## 更早那幾個 loop 做了什麼

- **人類實際打開頁面試玩**,回報:**「沒甚麼改變,系統還是太簡單了,讓 AI 代打沒甚麼反應」**。
  兩層原因:表層是 `/api/auto` 錯誤 payload 沒有 `auto` 欄位導致頁面直接 return(**已修**+回歸測試);
  底層是 **Groq 429**,額度根本沒重置
- **`--db` 旗標**加進 `python -m everliving.web`;**訪客 agent 現在會委託**
- **委託機制(T1-C1/C2)**:回覆多一行 `委託:X` → 離線期間結算成 `done` / `refused`。
  真模型驗過:他拿到閥了**但順便撞塌廢料堆、掀開一個在漏水的配電艙門**;拒絕 3/3 理由都落在狀態上
- **T1-V8 運鏡 + 無障礙**:`prefers-reduced-motion` 本來是假的(SMIL 不聽 CSS),真瀏覽器數過 **0/87 → 87/87**
- **T1-V7 構圖**訂了 `SAFE`;**連續運動(T1-V4/V5/V6)** + `tools/shot.py`
- **T0-17 懸念不再疊**(→ 拆出 T0-18);**主線劇情 → 設計文件第十七節**

## 等人類決定(agent 不要自己往下猜)

> **2026-08-07 人類一次答掉三題**(原文照抄,不要改寫成別的意思):
>
> 1. **provider → 「AUTO ROUTER」** —— 不選一個,要自動在兩者之間選。**設計還沒開始。**
>    ⚠️ 這跟 `TASKS.md` H-1 那條「中途換 provider 會讓歸因失效」直接衝突,設計時要處理
> 2. **簡體字 → 「A」= 只記 log** —— **這就是現況,不用改任何東西,結案。**
> 3. **「系統還是太簡單了」→ 「單調,可以參考其他遊戲」** ← **這是專案卡最久的那一題,終於有答案了**
>
> **第 3 題最重要,而且它推翻了「太簡單=功能太少」的預設讀法。**
> 「單調」講的是**變化不足**,不是**內容不足**——加更多功能不一定解得掉單調,
> 而**同樣的內容換一種排列方式可能就解得掉**。這個差別會決定接下來做什麼。

- **[已答成 AUTO ROUTER,設計未開始] H-1 要用哪個 provider?** 現在兩邊的數字都有了:
  **Groq 2.8 秒但額度會撞牆**(每日 200k,上次就是這樣掛的);
  **Ollama $0、不用網路、品質可用,但一次 5-7 分鐘**。
  五分鐘的等待很可能會**重現你上次抱怨的那個感覺**(「代打沒甚麼反應」)。
  **agent 不會自己選**——這題會決定 H-1 測到的是敘事還是耐心
- **「系統還是太簡單了」指的是什麼?** ← 仍然是最重要的一題,而且只有你能答
- **雙角色 還是 純化身?**(見〈已裁定但還沒動工〉)
- **H-1 playtest** —— **里程碑 0 唯一的成敗判準**。換 Ollama 之後才有得測
- **T0-18 懸念要不要「過期成後果」**:委託有三條出路,懸念還是只有「被完成」一條
- **第十七節主線劇情是提案**(而且**化身決定會影響它**)
- **`everliving.db` 要不要清乾淨**:所有驗證與 demo 都跑在別的 DB,沒動它

## 收件匣

- **[已答]** **OpenSpec → 不裝**。逐條理由在設計文件第十九節末〈已否決:OpenSpec〉。
  簡短版:輪子已經手工造好、權威來源會分裂、它管的規格漂移不是這裡的阻塞。
  **真正該考慮換 SQLite 的是 `PROGRESS.md`(80KB+),不是 `LOOP.md`**——後者已經有硬上限,是固定大小
- **[還沒排]** **Groq 免費額度是每日 200k tokens**,而且不是午夜整點重置。要嘛等,要嘛升級,**要嘛用 Ollama(建議)**
- **[偵測已做,處置等你決定]** **簡體字混進繁中敘事。**
  `src/everliving/script_check.py` 現在會抓,`offline.py` 會記一行 WARNING。
  真的那份 Ollama 記錄跑過:**只抓到 `没见过` 三個字,八十行繁體零假陽性**。
  **但它只報不擋**——敘事照樣交給玩家。**要不要擋是你的題**,而且不是小事:
  重生一次在 Ollama 上是再等五分鐘,在 Groq 上是多燒一次額度。
  三個選項:(a) 維持只記 log;(b) 偵測到就重生一次,失敗才放行;(c) 事後靜默改寫成繁體
  (最省但等於竄改模型輸出,而這個專案的賭注建立在「他說的話是他說的」)
- **[已查明,換成一件更小的事]** 場景/動作跟台詞對不上——**無法重現,而且是結構上查不到**。
  `scene`/`action` 只活在一次 HTTP 回應裡:`memory_events` 沒這欄、`story_assets` 0 筆、
  `llm_calls` 只有 token 數(三個 DB 含備份都查了)、`everliving.log` 零命中,
  連 `--debug` 都只記 prompt 跟玩家訊息、**不記模型的回覆**(`llm.py:168-169`)。
  **這個系統記玩家說了什麼,不記陌洲說了什麼的原始形式**——`memory_events` 存的是
  `split_scene_tag` 剝掉場景標籤之後的文字。
  **所以先做的應該是:把 `scene`/`action` 跟敘事一起存下來。**
  存了之後這件事才第一次可觀測——現在連「修好了沒」都無法驗證。
  全文 `playtests/2026-08-06-scene-mismatch-repro.txt`
- **[已修]** ~~代打按了沒反應~~ / ~~工作檯被裁掉~~ / ~~鏡頭跳~~ / ~~reduced-motion 是假的~~
- **[已答]** three.js → **還不要**(條件:H-1 過了 + Blender 產線真的有 glTF 資產)。
  **注意:化身決定可能把這條的時程往前推**
- **[已答]** 知識狀態 → 要,但不能是技能樹
- **[已答]** 隨機物品 / 基因崩潰圖鑑 / 山海經 → 物品由事件產生;要「他經手過的人」的檔案

---

## Loop 收尾檢查(每次結束前跑一遍)

1. 測試真的跑過,而且是**這一輪跑的**(`python -m pytest -q`)
2. 動過 `LOOP.md`:`python tools/loop_check.py` 要 exit 0
3. 動過 `static/index.html`:`node tests/page_check.js` **而且**
   `python tools/shot.py --size 1440x820` 真的看一眼
4. **改了 prompt 或模型行為,測試過不算數**——拿真模型跑一次,結果(含沒驗到的)寫進 `playtests/`
5. **宣稱瀏覽器層級的行為要去問瀏覽器**(`chrome --headless=new --dump-dom`),不能只寫測試
6. **人類回報的問題,先查根因再修**——「按了沒反應」底下同時藏著一個 bug 跟一個額度問題,
   只修一個會以為好了
7. `TASKS.md` 狀態改對;`PROGRESS.md` append 一則;**重寫這份 `NOW.md`**
8. commit(先 `git pull --rebase`)
