# AGENTS.md — 給任何開發這個專案的 AI agent(不限廠商)

這個專案預期會有**多個不同來源的 AI agent**(Claude Code、Codex、Qwen、Kimi、Grok 等)輪流或同時對這個 repo 做開發。這份文件是所有 agent 共用的入口,不管你是哪一家模型都先讀這份。

## 這是什麼

`everliving`(AI大世界)。完整設計脈絡讀 `AI大世界_設計文件.md`——決策理由都在裡面,不要重複踩已經討論過的問題。

## 現在該做什麼

只做**里程碑 0**,在 `AI大世界_設計文件.md` 第四節〈里程碑 0〉:

- 一個 Agent、一個玩家
- 記憶存 SQLite(不用 vector DB)
- 關掉,隔天打開
- 一次 LLM 呼叫生成「這段時間我做了什麼」
- 讀到那段敘事

> ### ⚠️ 人類 2026-08-07 把這一節的圍籬拆了
>
> 他先設了目標:**「以目標變成最多人玩的遊戲」**,然後在被問「範圍圍籬拆不拆」時答:**「範圍圍籬拆」**。
>
> **所以下面那句「不要做…」不再是禁令。** 原文保留在下面加了刪除線的地方,因為
> **它的理由仍然值得讀**——那些理由是關於「提前做會浪費額度」,不是關於「那些東西不好」。
>
> **但拆掉圍籬不等於全部一起開工。** 現在的排序原則換成一句話:
>
> **先讓人玩得到,再讓人變多。**
>
> 理由是可以查證的,不是偏好:**里程碑 0 的成敗判準 H-1(一個人隔天想不想再打開)到今天一次都沒過**,
> 而 2026-08-07 量到 Ollama 上四次真呼叫**兩次產不出可用敘事**、一次要等 5-7 分鐘。
> **一個一個人都留不住的東西,再多人來也會走。**
>
> **他沒有指定先拆哪一根。** 沒有指定的時候,agent 照上面那句排序原則自己判斷並寫明理由,
> **不要回頭再問一次同一題**——但也不要假裝他指定了某一根。

~~**不要**做世界地圖、多角色、前端框架、向量檢索、Unity、金流、帳號系統——這些都是後面里程碑的事,提前做是在浪費額度。里程碑 1 才開始做多 agent(見設計文件第四節〈里程碑 1〉),現在還不是時候。~~

技術選型:Python + SQLite,能跑就好,不用學新框架。**這一條沒有被拆**——
拆的是「做什麼」的範圍,不是「用什麼」的節制。要換技術仍然要有理由,而且理由不能只是「規模會變大」。

## 最重要的一條規則

**每一輪都要交出東西,但每一則都要說得出它是從哪裡來的。**

這個專案曾經靠「沒事做就停下來」來擋住技術債(舊版規則:「今天沒有事該做」是一個正確的結果)。人類 2026-08-06 推翻了它——理由是那條規則會讓專案停在原地。現在改成靠**取材階梯**擋:

| 階 | 取材來源 | 標記 |
|---|---|---|
| 1 | `NOW.md`「下一個 loop 該做什麼」 | 免標 |
| 2 | `TASKS.md` 的 `todo` | 免標 |
| 3 | `LOOP.md` 的「退回重做」 | 免標 |
| 4 | `NOW.md` 收件匣裡還沒排進去的 | 免標 |
| 5 | **繞過阻塞**:卡住的決定**兩條路都需要**的共同前置 | **要標** |
| 6 | 降債:測試覆蓋、真模型驗證、文件跟 code 對不對得上 | **要標** |
| 7 | **把決定變便宜**:做出可比較的原型,讓人類打開就能指 | **要標** |

**從第一階往下找,找到就停。** 用到第 5-7 階的任務必須在 `LOOP.md` 標上 `[階N]`,而且一輪內不得超過半數(無條件捨去)。

擋的東西沒變:**為了填滿輪次而發明的任務,會製造技術債並把專案推往還沒被驗證的方向。** 標記就是為了讓這種任務**看得見**——人類掃一眼就知道這一輪有幾則是 agent 自己想出來的。

**唯一不准的仍然是:替人類拍板。** 卡住的決定(見 `NOW.md`〈等人類決定〉)可以做前置工作,不能代為決定。排階 5 的任務要寫明「服務哪個卡住的題,而且兩條路都需要」。

完整理由見設計文件第十九節。

## 四種角色

每個角色綁死一組**檔案所有權**——這是輪流交棒不互相蓋掉的機制,不是禮貌約定。**動之前和收尾都跑一次 `python tools/loop_check.py`。**

- **想(planner)**:**只准寫** `LOOP.md` 的「排隊中」。只讀 code,不改。照上面的取材階梯排,每則要帶**完成判準**與**階數標記**。「排隊中」上限 3 則——**滿了不代表你沒事做,那一棒的工作是重排跟砍,不是再加**。
- **寫(builder)**:**只准寫** `LOOP.md` 的「進行中」→「待驗收」。只准碰 `src/`、`static/`、`tests/`。一次只做一則,「退回重做」優先於「排隊中」。**不准自己標 `done`**——做完移到「待驗收」交棒。
- **查(auditor)**:**只准寫** `LOOP.md` 的「驗收結果」與「退回重做」。只讀 code,**不准順手修、不准寫 feature**。每則發現要附**證據**(`檔案:行號` 或指令輸出),沒有證據的意見不算發現。驗收通過的那一刻**由它**同步回 `TASKS.md` / `PROGRESS.md` / `NOW.md`——它是最後一棒,所以那三份永遠只有一個寫入者。
- **反思(reflector)**:**每 3 輪**跑一次,原料是那 3 輪的「驗收結果」與「退回重做」——**失敗紀錄才是原料**。
  **人類 2026-08-06 授權它派 subagent**,用途限定在 `superpowers:writing-skills` 的 Iron Law 要求的
  baseline 測試(先觀察沒有 skill 時 agent 怎麼失敗,再寫)。**沒跑過 baseline 就不准寫 skill,
  而且 baseline 顯示「沒有 skill 也不會失敗」時,正確結果是不寫。**
  只准寫 `LOOP.md` 的「反思」與「skill 帳本」、`.claude/skills/`,以及**用 append 的方式**往這份 `AGENTS.md` 加教訓。**不准修改這份文件已經存在的行**(要改就寫成提案等人類點頭),**不准碰 `src/`**。同一個模式出現 ≥2 次才准寫成 skill;網路抄來的先標 `unverified`,真的用過一輪才准轉正。

**交接板是 `LOOP.md`**,「現在輪到」那一行就是交棒動作。輪流跑,同一時間只有一個角色在動檔案。

## 工作流程(給自動排程 / 自主 session)

> **如果你是上面四種角色之一被叫起來的**:先讀 `LOOP.md` 的「現在輪到」那一行確認是不是你,
> 然後只做你那一區的事。下面這套流程是給**單獨跑一輪**的 session 用的(沒有在循環裡的時候)。

0. **先讀 `NOW.md`。** 那份是「現在」——上一輪做完什麼、這一輪該做什麼、卡在哪、
   以及人類講過但還沒排進去的事。**它會被整份重寫,永遠只描述當下**,所以讀它比翻歷史快得多
1. 需要細節再去 `TASKS.md`,需要「為什麼」才去 `PROGRESS.md` 最新一則跟設計文件
   (`PROGRESS.md` 已經 80KB 以上,每個 loop 都從頭讀一次是純粹的浪費)
2. 認領前把 `TASKS.md` 該任務狀態改 `in-progress` 並填上你是哪個 agent/模型(claimed-by)
3. 做一小塊有意義的進展(寧可小而完整,不要大而半成品)
4. 跑得動、測過的東西才算完成——沒辦法本機驗證的功能不要宣稱做完。能寫自動測試就寫(mock 掉真的 LLM API 呼叫,這裡沒有遊戲要用的 LLM key)
5. 任務做完把 `TASKS.md` 狀態改 `done`,在 `PROGRESS.md` 加一則新記錄(日期、用的是哪個 agent/模型、做了什麼、下一步、任何卡住的地方)
6. **收尾一定要重寫 `NOW.md`**(不是往下加,是整份重寫)——尤其「下一個 loop 該做什麼」跟「收件匣」。
   這是下一個 agent 醒來唯一會先看的東西,沒更新等於下一輪要重新摸索一次
7. 如果卡住需要人決定的問題,寫進 `NOW.md` 的「等人類決定」,不要自己猜著往下做

### `PROGRESS.md` 太大時怎麼辦

**超過 3MB 就把舊的部分搬到 `PROGRESS-archive-<年份>.md`**,`PROGRESS.md` 只留最近的。

這**不違反 append-only**:append-only 管的是「不修改已經寫下的內容」,搬家沒有改任何一個字。
規則是**整則整則搬,不摘要、不刪改**——一旦開始濃縮舊記錄,那就是在重寫歷史了。
搬完在 `PROGRESS.md` 開頭留一行指到 archive。

## 給 Claude Code 的補充(其他廠商工具略過即可)

如果重複性工作值得固化成一個可重用的流程(例如「開發-驗證-記錄」的固定步驟、跑測試的固定方式),可以自行在 `.claude/skills/` 底下建立專案級 Skill(例如 `development`、`testing`),不用每次都重新描述一遍。建了就照 repo 慣例 commit 進去,讓下一個 Claude Code session 也能用。

上面〈四種角色〉在 `.claude/agents/` 底下有四個對應的檔案(`planner` / `builder` / `auditor` / `reflector`),Claude Code 可以直接叫。**那四個檔案是薄包裝,只負責把你指回這份文件**——角色契約的內容一個字都沒複製過去,因為複製了就會有兩份、兩份就會漂,而其他廠商的 agent 只讀得到這一份。**要改角色的規則,改這裡,不要改那四個檔案。**

## commit 慣例

不要加任何特定廠商的具名貢獻標記(例如 `Co-Authored-By: <某家模型>`)——這個 repo 是多廠商協作,commit 記錄保持廠商中立。

## 多 agent 併發安全(重要)

會有其他 agent(可能是別家模型)同時或交錯在動這個 repo:

- **commit 前一定先 `git pull --rebase`**,避免蓋掉別人剛推上去的東西
- **commit 切小塊**,不要憋一個巨大 commit——衝突範圍越小越好處理
- 遇到真的解不開的 merge conflict:不要用蠻力二選一蓋過去,在 `PROGRESS.md` 記下衝突狀況然後停手,留給人或下一個 session 處理
- `PROGRESS.md` 是唯一的交接窗口,不管你是哪一家 agent——動手前一定要先讀最新記錄

## 寫下一條規則的時候,同時決定誰執行它

> reflector 於 2026-08-06 append(第 1 次反思,原料是 `LOOP.md` 第 1-3 輪)。

這個 repo 反覆出現同一種失敗:**規則被寫下來,然後沒有任何東西在執行它。** 已知五個實例:

1. `prefers-reduced-motion` 要生效——上線後拿真瀏覽器數才發現 0/87
2. 「所有文字一律用繁體中文」寫在 `src/everliving/offline.py:259`——被違反,沒有任何測試在管
3. 「人類心跳」寫進設計文件——差點沒有任何任務在實作它
4. 「階 5-7 不得超過半數」——文字說進位、程式碼說捨去,兩份漂開
5. 佇列不能無限長大——**根本沒被寫下來**,直到人類問「一個一直塞 task 進去」

**每寫下一條「一律要 / 一律不准」的約束,當場回答三題:**

1. **誰會檢查?**(自動測試 / `tools/loop_check.py` / 人)
2. **什麼時候檢查?**(commit 前 / 每一輪 / 只有人打開的時候)
3. **檢查不過會怎樣?**(測試紅 / exit 1 / 只是留個紀錄)

**三題有任何一題答不出來,就在那條規則旁邊標「未執行」。** 標了不會讓它生效,
但至少下一個人看得到那是一句願望而不是一個機制——**上面五條裡有四條,當初都以為自己寫的是機制。**

## 第四題:那個執行者量的,是規則講的那件事嗎?

> reflector 於 2026-08-06 append(第 2 次反思,原料是 `LOOP.md` 第 5-7 輪)。
> **這節是上一節的續集,不是重複**——上一節管「有沒有執行者」,這節管「執行者量對了沒」。

上一節寫完之後,同一個檢查器兩次因為**規則自己的形狀不對**而報錯,兩次都不是使用者做錯:

1. **「現在輪到」只接受四種角色** —— 它假設永遠有一棒在跑,所以 loop 被心跳擋住的時候,
   「暫停」這個狀態在格式裡**無法表達**。想誠實記錄狀態,反而違規
2. **「階 5-7 不得超過半數」** —— 這條的用意是限制 planner **這一輪加了什麼**,
   但檢查器量的是佇列**此刻剩下什麼**。佇列在「寫」耗盡、要到兩棒後的「想」才補,
   所以整個「查」都坐在紅燈上,**而沒有任何人做過任何決定**

**所以三題不夠,還有第四題:**

4. **那個執行者量的,是規則講的那件事嗎?** 特別要問:
   - 它量的是**某個人的決定**,還是**時間流逝的殘留**?(配額那條踩的就是這個)
   - 規則假設的狀態是不是**永遠成立**?(暫停那條踩的就是這個)

**沒有執行者的規則是一句願望;量錯對象的規則是一個會說謊的儀表。**
願望至少誠實地什麼都不做,儀表會讓人相信它——**然後在真的出事時,它正在對別的事情叫。**

紅燈太便宜就會被無視,而被無視的檢查器跟沒有檢查器是同一回事。

## 第五題:文件可以怎麼合法地騙過這個 parser?

> reflector 於 2026-08-07 append(第 3 次反思,原料是 `LOOP.md` 第 12-14 輪)。
> **這一節是前兩節的收斂,不是第三層疊加。** 前兩節問「有沒有執行者」「量的對不對」,
> 這一節問更前面的一題:**量得到嗎。**

同一個檢查器又壞了兩次,加上原本那次共三次。**但把它們講成三個獨立的疏忽是誤診**——
三次共用同一個機制:

> **`LOOP.md` 是一份 Markdown 文件,卻被當成資料庫用。**
> `tools/loop_check.py` 必須從**排版**(行首是不是 `- `、標題那串字)反推**結構**
> (這是一則任務、這是哪一區),而排版是人手寫的、可以合法地長出各種樣子。
> **parser 的結構模型是一個猜測,而文件可以在不違反任何 Markdown 規則的前提下,
> 悄悄違反那個猜測。**

三個實例,全部有執行結果:

| 文件做了什麼(完全合法) | parser 因此以為 |
|---|---|
| 把做完的項目用 `<!-- -->` 註解掉 | 它還在佇列裡(只看行首 `- `,不懂註解) |
| 標題寫成 `## 待驗收(第 13 輪 builder 交)` | **這一區不存在**(`loop_check.py:87` 的 `if h in SECTIONS` 先濾掉不認得的標題,再比對順序) |
| 佇列被 builder 撿到只剩 1 則 | planner 這一輪違反了配額 |

**為什麼寫兩節文字擋不住?** 因為那兩節教的是「想清楚要量什麼」,而這三次的作者**都想清楚了**——
`loop_check.py:30-31` 自己寫著「order is load-bearing, not cosmetic」。
**它知道順序重要,它只是沒料到標題可以被改名。這不是思慮不周,是介面選錯了。**

**所以第五題是一條可以當場做的動作,不是又一句心法:**

5. **每加一條 `loop_check` 規則,先回答「文件可以怎麼合法地騙過這個 parser?」——然後把答案寫成測試。**

三次裡有兩次(註解掉的 `- `、加後綴的標題)**都是一行測試就抓得到**,
而且**寫規則的當下就想得到**,不需要等它在真實輪次裡炸掉才發現。

**如果一條規則的答案是「騙法太多、寫不完」,那訊號不是再多寫幾個測試,
是那件事不該用文字檔的排版來承載。**

## 第六題:這張表要答的是「現在怎樣」,還是「有沒有變」?

> reflector 於 2026-08-07 append(第 4 次反思,原料是 `LOOP.md` 第 15-28 輪)。
> **前三節都在講規則跟檢查器,這一節講資料**,而它擋住的東西比前三節加起來都貴。

三個實例,形狀完全一樣:

| 存了什麼 | 答不了什麼 |
|---|---|
| `scene`/`action` 只活在一次 HTTP 回應裡 | 「場景跟台詞對不上」過去發生過幾次 |
| `memory_events` 存的是剝掉場景標籤**之後**的文字 | 陌洲**原本**說了什麼 |
| `player_state` 一列、每次來訪覆蓋掉上一次 | **他有沒有再回來** |

**存的都是「現在怎樣」,而每一個真正想問的問題都是「有沒有變」。**

第三個最貴:「他隔天想不想再打開」是 **里程碑 0 唯一的成敗判準**,寫在 `TASKS.md` 最上面,
而**能回答它的資料一天都沒有存在過**。不是沒人去算,是**算不出來**。
三個月的開發裡,沒有任何一次 code review、任何一個測試、任何一條規則會發現這件事——
因為程式**完全正常**:它確實正確地記住了「他上次來是什麼時候」。

**所以寫任何一張表之前,先答這一題:**

6. **這張表要回答的問題,是「現在怎樣」還是「有沒有變」?**
   **只要有任何一個人會問第二種,它就必須是 append-only 的,而且現在就要是。**

**為什麼不能等到需要的時候再說**:狀態沒有歷史可以回推。
`visits` 加上去的那一天,**在那之前的每一次來訪都已經永遠不見了**——
補不回來,而且**編一個出來就是造假**(這條線第 10 輪拒絕回填 `scene` 時已經劃過)。

**便宜的判斷法**:如果你正在寫 `ON CONFLICT DO UPDATE`、或是覆蓋一個欄位、
或是「只留最新的一筆」,**停下來問一次上面那題**。
大部分時候答案是「現在怎樣」而覆蓋是對的。**但這個專案賭的是時間累積出來的東西,
所以它問「有沒有變」的頻率,遠比一般的 CRUD 系統高。**
