# TASKS.md — 任務分解看板

> 給任何 agent(不限廠商)認領工作用。狀態:`todo` / `in-progress` / `done`。
> 認領規則:動手前把狀態改成 `in-progress` 並填 claimed-by(寫你是哪個 agent/模型),完成後改 `done`,連同程式碼一起 commit。
>
> **「沒有任務可做」是一個正常且正確的結果。** 這份看板不保證隨時有貨——如果所有 `todo` 都清空、而下一步卡在人類決定(見下方「目前卡在哪」),正確的行為是**什麼都不做並說明原因**,不是自己發明工作。發明出來的任務會變成技術債,而且會把專案推進還沒被驗證的方向。

## 目前卡在哪(動手前先看這裡)

**2026-08-03 更新:H-1 沒能跑起來,而且原因是門檻,不是內容。**

人類(這個專案唯一的第一測試者)要跑 H-1 時說「連畫面都沒有我是不太想打開」,並確認是**必須有畫面才有動力**,而不是「純文字讀了覺得普通」。

**這件事要記清楚,以免之後誤讀:**

- ❌ **這不代表核心賭注被推翻。** H-1 要問的「離線敘事會不會讓你想再打開」**根本還沒被回答**——它被一個更前面的門檻擋住了
- ✅ 它推翻的是**里程碑排序**:視覺化原訂第四階段,現在必須前移,否則驗證做不下去
- ⚠️ 加了畫面之後 **H-1 仍然要跑**,而且要保留「能單獨判斷敘事好壞」的方法,否則兩個變數一起動就無法歸因

**現在的 in-scope 範圍:**

- ✅ **最小視覺外殼**(T1-V 系列)——本機網頁 + 風格化 SVG 場景。**不接圖片 API、不做 Unity、不做 3D、不用前端框架**
- ✅ 原本就允許的:降低 playtest 摩擦、成本量測、測試覆蓋、修 bug、文件
- ❌ 仍然不要:多 agent、世界地圖、金流、帳號系統、向量檢索

依據:設計文件第八節〈週末專案的現實提醒〉「每階段結束要真的檢視,不要自動往下走」——**這次檢視的結論就是排序要改**。那條規則的目的是防止在未驗證的方向上加碼;當規則本身讓驗證做不下去時,該被檢視的是規則。

## 里程碑 0.5 — 最小視覺外殼(讓 H-1 跑得起來)

> 2026-08-03 由 Claude Opus 5 完成。`python -m everliving.web` 起一個只綁 127.0.0.1 的本機頁面,
> 重用既有的 `respond` / `simulate_offline_period`,CLI 完全沒動。104 passed(原 92)。
> **接下來是 H-1(網頁版):帶畫面重跑 playtest。**

- [x] `done` **T1-V1 本機網頁介面** — Python 內建 `http.server` + 單檔 HTML,無框架、無外部資源。
      重用既有的 `agent_loop.respond` 與 `offline.simulate_offline_period`,不複製邏輯。CLI 保留不動。
- [x] `done` **T1-V2 風格化場景視覺** — SVG/CSS,**不接圖片 API**。潮位與天色跟現實時間連動
      (對應設計文件「時間與潮汐」與「美術風格向抽象/風格化靠」)。潮汐會動本身就是「世界在活著」的視覺證據。
- [x] `done` **T1-V3 `scene` 欄位** — 離線 JSON 多一個封閉值域的 `scene`,驅動場景切換
      (規格見設計文件第五節架構預留)。

- [x] `done` **T1-V4 連續運動** — 2026-08-05 完成(claimed-by: Claude Opus 5)。
      收件匣那條「HelloRun 那種流暢」的結論是**連續運動,不是換渲染器**,這條就是它。
      畫面本來是每 30 秒換一張靜態圖;現在**鏡頭一直在動**:三層以不同速率漂移(近的走最多、
      遠的最少,這就是視差的全部)、水面照一個完整波長無縫流動、整個框架用 240 秒的週期極慢推拉。
      **全部寫成 CSS 而不是 SMIL**,因為 `prefers-reduced-motion` 那條是 CSS 規則,SMIL 會直接繞過它。
      每個漂移層都帶一點 scale,否則橫移會露出框外。
      順帶把水面反光改成 seeded——它是最後一個每次重畫都在跳的東西。
      **鏡頭還會朝正在發生的事偏過去**(`FOCUS`,`transform-origin` 跟著 `動作` 走):
      漂移本身只是裝飾,能把視線帶到事件上才是它存在的理由。

- [x] `done` **T1-V5 離線那一夜也能說「在幹嘛」** — 2026-08-05 完成(claimed-by: Claude Opus 5)。
      `動作` 這一軸本來只長在對話路徑上,離線 JSON 只有 `scene`——所以「整層樓的燈都滅了」
      那種夜晚,畫面照樣開在一個燈火通明的工作間。現在 `OfflineResult.action` 也在了,
      而且 `web` 把它送到 payload 頂層(頁面就是在那裡讀的),**null 也送**——
      「沒有特別在發生什麼」是畫面必須能回去的真實狀態。
      跟 `scene` 不同的是**它沒有 fallback**:認不得的值退化成 null,
      因為憑空生出敘事沒提過的天氣,比把場景畫得平靜更糟。

- [x] `done` **T1-V6 看得到畫面的測試工具** — 2026-08-05 完成(claimed-by: Claude Opus 5)。
      `tests/page_check.js` 能證明畫圖的程式跑得動,但**看不見畫出來的東西**——
      什麼被裁掉、誰蓋在誰上面、會動的東西會不會動到框外,它一律無感。
      `python tools/shot.py` 用機器上現成的 Chrome/Edge 跑 headless 截圖,不用擴充功能、不用網路。
      兩個關鍵:頁面加了 `?scene=&action=` 的**開發用鉤子**(不然畫面只能靠玩才看得到);
      以及**動畫時間軸是釘住的不是播出來的**——實測 Chrome 的 `--virtual-time-budget`
      會推進 `setInterval`(角落的鐘真的會走)但**不會推進 CSS 動畫**,
      所以改用負的 `animation-delay` + `paused` 把每一格釘在指定秒數,順便還比等它播更快也可重現。
      `--page` 可以指到 `git show HEAD:…` 存下來的舊版做前後對照。

- [x] `done` **T1-V7 構圖在寬螢幕下會被裁掉一半** — 2026-08-05 完成(claimed-by: Claude Opus 5)。
      T1-V6 做出截圖工具的第一天就抓到:**1440×780(最常見的筆電尺寸)下,開場畫面
      工作檯完全不見、吊燈不見、人只剩半截**。根因是 viewBox 是 1000×420(2.38:1)
      而 `.scene` 在那個視窗是 4.4:1,`slice` 直接裁掉 46% 的高度。
      **這是本來就有的,不是連續運動造成的**(拿 `--page` 指到舊版對照確認過),
      只是在有截圖工具之前沒有人看得見。
      修法兩層:`.scene` 的高度改成 `max(42vh,26vw)`(讓高度跟著寬度走,一般視窗形狀零成本);
      以及訂一條 `SAFE = {top:108, bottom:308}`——**量出來的,不是猜的**,
      所有承載意義的東西(工作檯、吊燈、配電盤那排燈、人、近景邊緣)一律放進這個帶裡,
      天空與水可以溢出去,因為裁掉那些讀起來是取景不是損失。
      順帶修掉一個搬上去才出現的破綻:**欄杆的鏤空**在畫面底部的黑水裡看不見,
      一搬到有光的水面上就變成一排亮格子。改成實心甲板 + 立柱剪影朝上。

- [x] `done` **T1-V8 鏡頭真的會移過去,而且會動的東西真的關得掉** — 2026-08-05 完成(claimed-by: Claude Opus 5)。
      兩件事一起修,因為它們是同一個東西的兩面:**畫面上最大的一個移動**。
      - **鏡頭移到事件的位置**:上一輪是讓 `transform-origin` 跟著動作跳,那是**瞬間位移**
        (在 scale 1.03 下大約跳 10 個單位),是破綻不是運鏡。改成獨立的 `#aim` 圖層,
        `transform` 帶 6 秒 ease 的 `transition`——**鏡頭轉過去看,不是切鏡頭**。
        只移到焦點與中心距離的 30%(移到正中央會讀成切換,而且每個事件看起來會發生在同一個地方),
        並 clamp 在 `#aim` 的 scale 買得起的餘裕內(±40 / ±14)。
        `SAFE` 在多出來的 1.09 scale 之後仍然成立,截圖確認過
      - **`prefers-reduced-motion` 本來是假的**:那條規則是 CSS,而 **SMIL 不聽 CSS**——
        87 個 `<animate>` 一個都沒被關掉。頁面一直在宣稱尊重這個偏好而實際上無視它。
        修在 `el()` 一個地方:不 OK 就補 `begin="indefinite"`(那是 SMIL 自己的「永遠不要開始」)。
        CSS 那條也補上 `transition:none`,否則畫面上最大的那個移動剛好是唯一漏掉的
      **驗證方式(可重現)**:
      `chrome --headless=new --virtual-time-budget=2000 --dump-dom [--force-prefers-reduced-motion] <file>`
      然後數 SVG 裡的動畫元素。**normal 0/87,reduced 87/87**——
      這種宣稱不能用測試代替,上一版就是這樣錯的。

## 里程碑 0 — 驗證核心賭注

設計依據:`AI大世界_設計文件.md` 第四節〈里程碑 0〉。

- [x] `done` **T0-1 SQLite schema** — agent 人設 table + 事件記憶 table(時間戳、內容、來源)
- [x] `done` **T0-2 單一 agent 人設** — 寫死一個原創角色的 persona,存進 DB 初始化腳本
- [x] `done` **T0-3 互動迴圈** — CLI 對話:玩家輸入 → agent 回應(persona + 近期記憶)→ 存成記憶事件
- [x] `done` **T0-4 離線時間追蹤** — 記錄玩家上次離開的時間戳;下次啟動時算經過多久
- [x] `done` **T0-5 離線敘事生成** — 一次 LLM 呼叫產生「這段時間我做了什麼」,存成記憶事件
- [x] `done` **T0-6 上線讀敘事** — 重新啟動時先印出離線敘事,再進入互動迴圈
- [x] `done` **T0-7 LLM 呼叫可測試化** — 可注入/可 mock 的介面 + 自動測試(不用真的打 API)
- [x] `done` **T0-9 playtest 加速** — `--offline-hours N` 假裝已離開 N 小時,不用真的等一天就能測核心體驗
- [x] `done` **T0-10 成本量測** — `llm_calls` table 記錄每次呼叫的 token,`--cost-report` 印每日用量。對應設計文件第四節〈必須量出的兩個數字〉之一
- [x] `done` **T0-8 本機執行說明** — README 補上安裝依賴、API key、執行方式
- [x] `done` **T0-12 離線模擬要有最小間隔** — 原本只檢查「有沒有 elapsed」,所以離開後立刻重開就會再跑一次
      完整的離線模擬:多算一次 LLM 帳、憑空生出一段離線期間與一條新懸念。三個層面同時中招——成本
      (上線後是 denial-of-wallet 管道)、敘事(「你離開了不到一分鐘」也會生出一整段)、以及 **H-1**
      (playtest 中途離開再進來就會讀到假敘事)。加 `MIN_OFFLINE_GAP = 30 分鐘`。
      注意:原本有個測試正好在**斷言這個 bug 的行為**(立刻重開就該看到敘事),已改成把 `last_seen`
      往前調再測,這樣測的是真實路徑而不是旗標。

- [x] `done` **T0-15 開場場景不再是港城** — 2026-08-05 完成(claimed-by: Claude Opus 5)。
      人類問「可以不要放港城嗎?剛起點就是港城嗎?」——查出來**開場是港城不是選的,是撞的**:
      `DEFAULT_SCENE` 同時兼兩份不相干的工作(模型講出畫不出來的地點時的退路、+ 第一次打開的開場鏡頭)。
      拆成 `DEFAULT_SCENE = 港城`(退路不動,最寬、永遠說得通)與 `OPENING_SCENE = 工作間`(開場)。
      人類選了工作間:陌洲靠修老舊維生系統維生,**工作間是他自己的地方,不用一句台詞就說明他是誰**。
      頁面初始值一起改掉,免得先閃一下港城再跳。

- [x] `done` **T0-14 全程記錄(troubleshooting)** — 2026-08-04 完成(claimed-by: Claude Opus 5)。
      人類指出「系統應該要能記錄一切,才troubleshoot得動」。當時 `python -m everliving.web`
      連啟動那行都看不到(stdout 不是 TTY 時會被 buffer 住),401 只以瀏覽器裡一行紅字存在,
      跑完什麼證據都沒留下。現在每趟寫 `everliving.log`(UTF-8):啟動的 provider/model/port、
      每個請求的耗時、每次 LLM 呼叫的 model/秒數/token、失敗的完整原因與 traceback。
      **key 永遠不進 log;玩家講的話要 `--debug` 才記**(內容是玩家的)。
      順手補上 web 的 catch-all:沒預期到的例外以前會逸進 request thread 把它弄死、
      玩家看到一個停住的頁面而檔案裡什麼都沒有,現在回 500 並寫下 traceback。

- [x] `done` **T0-13 本機 Ollama provider** — 2026-08-04 完成(claimed-by: Claude Opus 5)。
      `.env` 裡的 `ANTHROPIC_API_KEY` 實際打出去是 **401 invalid**,H-1 第 1 步直接開不起來——
      這是第二次被「門檻」而不是「內容」擋住,跟當初沒有畫面是同一類問題。
      `--provider ollama` 走本機 Ollama:**不用 key、不用網路、每次呼叫 $0**,
      所以帳號問題再也不能擋住 playtest。實作只是 `OPENAI_COMPATIBLE` 多一筆
      (`key_env: None` 代表免金鑰),沒有新的 client 類別——用的是既有的可替換邊界。

- [x] `done` **T0-11 離線期間產生後果** — 離線模擬改成結構化輸出:狀態變化寫進 `agent_state`、
      懸念寫進 `open_threads`、事件各存一筆記憶;下次對話會把狀態與懸念帶進 prompt,
      agent 會主動提起在等玩家的事。JSON 解析失敗時退化成純敘事(不會弄丟玩家要讀的東西)

- [x] `done` **T0-16 H-1 流程排練(AI 代打)** — 2026-08-05 完成(claimed-by: Claude Opus 5)。
      `python tools/h1_autoplay.py` 把 H-1 那四步用訪客 agent 跑完:每一步**新建一個 `web.Session`**
      (跟網頁按鈕同一條路徑,不是複製一份;`Session.opened` 是 per run,共用會靜靜跳過離線模擬),
      `offline_hours` 依序 `None / 24 / 0 / 24`,每步三輪 `auto_turn()`,最後印帳。
      預設跑在另一個 DB(`everliving.db` 要不要清是人類的事),記錄寫 `h1_autoplay.log`。
      全文留在 `playtests/2026-08-05-h1-autoplay.txt`。
      **這不是 H-1 的答案**,H-1 問的是「你想不想再打開」——見「人類專屬」那一節。

- [x] `done` **T0-17 懸念會疊** — 2026-08-05 完成(claimed-by: Claude Opus 5)。
      T0-16 跑出來的:第 4 步的離線敘事把第 3 步答應的事接住了,但它是**再開一條講同一件事的新懸念**,
      而不是把舊那條標 resolved。修法在 `offline._build_prompts`:**已經有未解懸念時,規則 2 整條換掉**——
      預設是把那件事往前推(發展寫進 `events`),`open_thread` 填 null,只有真的另一件事才開新的。
      原本那條「通常要留下一件懸念」正是製造重複的來源,所以是**取代**不是並存(測試兩個方向都斷言)。
      **驗證**:拿 T0-16 跑完的 DB(結尾兩條幾乎一樣的懸念)重跑三次離線模擬,
      **3/3 不再開重複的**(`open_thread` 每次都是 null)。全文 `playtests/2026-08-05-t0-17-check.txt`。
      **只修好了「疊」,沒修好「收」** → 拆出 T0-18。

- [ ] `todo` **T0-18 懸念沒有失效路徑** — T0-17 驗證時看清楚的:三次裡只有 1 次把舊懸念標 resolved,
      而另外兩次**模型是對的**——軸承還沒送到,那條懸念本來就沒結束。
      問題不在模型不聽話,在於**一條懸念只有「被完成」一條出路**:
      玩家不理它,它就永遠開著,永遠進 prompt(越來越貴),面板上永遠掛著。
      **提案(還沒定案,要人點頭)**:讓它**過期成後果**而不是一直等——
      期限到了沒等到,離線模擬就把它 resolve 掉,同時產生一件回不去的事。
      這跟設計文件「三座時鐘單向且無解」是同一個形狀,而且比「待辦清單」更像這個世界。
      需要的欄位(`open_threads` 加期限/後果)是新機制,所以**先問人再動手**。

## 操控 — 委託機制(設計文件第十二節)

> 人類 2026-08-05 裁定:**這算機制不算劇本層,可以先做**(當時 agent 查出來
> 「操控度」的顯性形態是第十節的 action 層、被 H-1 擋著,把閘門歸屬問題丟回去問)。
> **被擋住的仍然是 action 層那 2–3 個選項的 UI**,不是委託本身——
> 第十二節寫明「不鎖死自由輸入」,所以自由輸入就是委託的第一個介面。

- [x] `done` **T1-C1 委託會被記下來,而且在離線期間有下場** — 2026-08-05 完成(claimed-by: Claude Opus 5)。
      控制模型是**委託**:玩家不移動任何人,只開口,結果在**離線期間**產生。
      - **記錄不花第二次呼叫**:對話回覆多一行 stage direction `委託:X`(跟 `場景`/`動作` 同一個機制)。
        **寫了不代表答應**——他有沒有真的去做,是離線那段時間才會知道的
      - **新的 `delegations` table**,不併進 `open_threads`:方向是反的
        (懸念是他在等你,委託是你在等他),而且委託有 `outcome`,懸念沒有
      - **離線結算**:pending 委託進 prompt,回來時每一件都要有 `done` / `refused`。
        第十二節那兩條規則一起實作了:**拒絕必須給出來自狀態或人設的理由**、
        而且**拒絕本身要生線**(refused 的 outcome 直接變成一條懸念)
      - 幻覺 id 不會碰 DB;認不得的 status 一律當 done(留著會永遠進 prompt)
      - 工作檯多一區「你請他做的事」——委託只在你不在時結算,所以在那之前它只存在在這裡
      **真模型驗過**(`playtests/2026-08-05-delegation-check.txt`):他把玩家的話
      **收斂成自己的一句話**、閒聊那輪**沒有**誤記成委託、隔天的離線敘事把它結掉,
      而且**順便撞上了別的事**(廢料堆塌陷、掀開一個在漏水的配電艙門)——
      那正是第十二節說「委託模式白送的」那個落差。
      **拒絕路徑後來也補驗了**(見 T1-C2)。

- [x] `done` **T1-C2 拒絕路徑補驗,並修掉兩條打架的 prompt 規則** — 2026-08-05 完成(claimed-by: Claude Opus 5)。
      灌一個會頂撞差事的狀態(手傷 + 對機器廠的積怨)再委託他去扛重物,跑三次。
      **3/3 拒絕,而且每一次的理由都同時抓住兩個狀態**,讀起來像角色不像系統失敗
      (其中一次是「手廢了誰來修你的維生系統?」——用角色本身反問)。
      **順帶抓到我自己上一輪留下的 prompt bug**:沒有既有懸念時,
      規則 2「通常要留下一件懸念」會跟委託規則「拒絕本身就是懸念」**打架,而模型兩條都聽**
      → 產生兩條同義的懸念,正是 T0-17 那個缺陷從我自己開的另一扇門走回來。
      修法:**任何能生出懸念的東西都要排在那條預設之前**,所以規則 2 現在有三個分支
      (有既有懸念 / 有待辦委託 / 都沒有)。修完重跑 **2/2 只剩一條**。
      全文 `playtests/2026-08-05-refusal-check.txt`。
      **第 3 次沒跑完:撞到 Groq 免費額度上限(每日 200k tokens)。**
      錯誤處理照 T0-14 設計的樣子運作——429 被翻成 `LLMUnavailable`,
      log 留下 provider/model/秒數,沒有任何東西默默壞掉。

## 設計任務(只寫文件,不進 code)

> 劇本層的**實作**閘門是 H-1(設計文件第十節),但**設計**不受那道閘門限制——
> 先想清楚,H-1 過了才不會邊寫邊編。這一類任務不動 `src/`,所以也不會在未驗證的方向上加碼。

- [x] `done` **D-1 主線劇情** — 2026-08-05 完成(claimed-by: Claude Opus 5)。
      設計文件新增**第十七節〈主線劇情〉**。結論不是編一個故事,是先證明**這個世界的主線不可能是一條會結束的線**
      (沒有勝利條件 × 沒有戰鬥 × 三座時鐘單向),所以主線只能是**一個會一直生線的位置**——
      而那個位置已經被寫死了:**他知道的東西只存在他身上,而唯一能保存它的方法是交給那個會把它拿走的人。**
      玩家的位置也跟著定了:**你就是他的外部記憶**(他會忘,你不會——這在程式裡本來就是真的,
      `memory_events` 是 append-only)。附第一章具體提案〈還留著別人的那一片〉,**美術成本零**
      (只用既有的四個場景與兩個動作)。順帶答掉「知識狀態那一欄從哪來」與「劇情等級」兩題。

## 人類專屬(agent 不要認領,也不能代為判定通過)

- [ ] **H-1 真人 playtest** — 這是里程碑 0 唯一的成敗判準。
      **2026-08-03 起改用網頁版跑**——純 CLI 的門檻讓這個測試根本開始不了(見上方「目前卡在哪」)。
      **2026-08-04:`ANTHROPIC_API_KEY` 是 401 invalid,已改用 Groq(跑 `qwen/qwen3.6-27b`)。**
      `.env` 已設 `EVERLIVING_PROVIDER=groq`,所以**下面的指令照原樣打就好,不用加旗標**
      (實測 2.8 秒一輪,繁體中文品質是三個選項裡最好的)。
      不想依賴任何帳號的話,加 `--provider ollama` 走本機、$0,但慢很多。

      每一步跑完就關掉瀏覽器分頁、在終端機 Ctrl-C,然後跑下一行:

      1. `python -m everliving.web` — 聊幾句。**答應它一件事**,第 4 步才好判斷
      2. `python -m everliving.web --offline-hours 24` — 讀敘事、看狀態變化、看它在等你什麼
      3. `python -m everliving.web --offline-hours 0` — 回應它等你的那件事
      4. `python -m everliving.web --offline-hours 24` — 看它有沒有接住你的回應
      5. `python -m everliving.cli --cost-report` — 順便看這四步花了多少 token

      要回答的問題:**你想不想再打開來看它做了什麼?**
      特別注意第 4 步——如果「我上次回應的事真的有後續」這件事有打中你,那核心賭注就是成立的。

      **歸因提醒**:現在同時有畫面和敘事兩個變數。判斷時請分開問自己——
      「是那段文字打中我,還是只是介面比較好開?」如果是後者,敘事本身仍未被驗證。

- [x] `done` **H-2 讓測試變公平** — 已於 T0-11 處理:離線期間現在會產生實際狀態變化與懸念。
      H-1 現在測的是「有後果的離線」,不是「無狀態的散文」。
