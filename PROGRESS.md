# 進度紀錄

> 每次工作 session 結束前在這裡加一則新記錄。最新的放最上面。

---

## 2026-08-03 — H-1 開不起來:做了最小視覺外殼(里程碑排序改了)

Agent: Claude Opus 5(互動 session)

**這一則記的是一個排序決定,不是一個功能。**

人類準備跑 H-1 時說「連畫面都沒有我是不太想打開」,確認過是**必須有畫面才有動力**,不是「純文字讀了覺得普通」。

**要記清楚,以免之後誤讀:**

- ❌ **核心賭注沒有被推翻。** H-1 要問的「離線敘事會不會讓你想再打開」**根本還沒被回答**——它被一個更前面的門檻擋住了
- ✅ 被推翻的是**里程碑排序**:視覺化原訂第四階段,現在前移
- ⚠️ 現在同時有畫面和敘事兩個變數,H-1 判斷時要分開問「是文字打中我,還是只是比較好開?」。已寫進 TASKS 的歸因提醒

依據:設計文件第八節「每階段結束要真的檢視」——**這次檢視的結論就是排序要改**。那條規則是為了防止在未驗證的方向上加碼;當規則本身讓驗證做不下去時,該被檢視的是規則。

**做了什麼(T1-V1/V2/V3):**

- `src/everliving/web.py` — Python 內建 `http.server`,**沒有任何框架、沒有任何外部資源**。重用既有的 `agent_loop.respond` 與 `offline.simulate_offline_period`,**邏輯一行都沒複製**,CLI 完全沒動,兩個入口共用同一個 `everliving.db`
- `src/everliving/static/index.html` — 單檔,inline CSS/JS。風格化 SVG 港城:**天色與潮位跟現實時間連動**(對應設計文件「時間與潮汐」與「美術風格向抽象/風格化靠」)。**不接任何圖片 API,零資產成本**——而潮汐會動本身就是「世界在活著」的視覺證據
- `scene` 欄位進離線 JSON,**封閉值域**(工作間/回收場/潮線/配電所/機器廠/港城)。模型只能從清單選,認不得的值退回預設——這同時擋住「模型偷偷新增地理」(生成地點是貴的那一種)

**安全性(這個行程握著會花錢的 API key,所以不是次要考量):**

- **只綁 `127.0.0.1`,不綁 `0.0.0.0`** — 同網段的機器連不進來
- **請求路徑永遠不會接到檔案系統上** — 只有精確比對的路由,所以沒有路徑穿越可找
- **完全沒有 CORS 標頭** — 別的來源的頁面呼叫不了這些端點
- **會花錢的動作一律 POST** — 一個 `<img src>` 沒辦法幫你叫 LLM
- 請求體 8KB 上限、`X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY`、CSP `default-src 'none'`
- 前端**一律用 `textContent`**,模型輸出永遠不會變成 markup
- `open()` 每次 server run 只模擬一次 → **重新整理頁面不會重複計費**

104 passed(原 92,新增 12 個 web 測試,含一趟真的走 socket 的請求)。另外實際啟動伺服器 curl 驗證過:頁面 200、`/api/open` 在乾淨 DB 下不觸發 LLM 呼叫、未知路徑 404。JS 語法與所有 DOM id 參照也都檢查過。

**下一步**:H-1(網頁版),跑法已改寫進 `TASKS.md`。

**待人決定**:H-1;舊 commit 的 history 重寫指令仍待執行。

---

## 2026-08-03 — 長期營運(第十六節)+ 神話怪物 + 補掉 E3 的洞

Agent: Claude Opus 5(互動 session)

人類問三塊:微服務/更新系統/持續多年、如何賺錢維持系統、有人把基因改成山海經怪物嗎。後續追問「現金可以買什麼又不會影響遊戲運作」。

**[劇本] 照著神話改造自己的人** —— 這不是新設定,是三條已定案設定的必然產物:神話因不精確而存活(所以沒有人有原文,只有走樣幾百次的流傳版本)+ 記憶會遺失(所以**永遠不可能有人考證得出原文**)+ 基因不可逆(改錯回不去,基因崩潰在等他們)。**於是最好的一種悲劇自己長出來:為了追求一個不存在的原典而永久改變身體。** 直接生社會衝突不用編——兩個宗派為了「真正的形狀是什麼」對立,而雙方都沒有依據也永遠不會有(「立場留下、理由消失」的最具體形態)。兩條界線:**這是社會現象不是怪物圖鑑**(世界沒有戰鬥系統,它該長成教派/身份/處境,不是敵人類型);版權乾淨(公有領域 + 改的是傳說變體 + 專名原創)。**順帶補強洞 12**:多類型融合多了一個近景版本——**類型差異可以來自「同一座城裡的人選擇變成什麼」,不只是「走到多遠」**,不用生成任何新地點就有異質性。

**[系統設計] 現在不要做微服務。** 微服務是部署形態不是設計品質;一人專案切微服務等於現在就付分散式的稅,換一個好幾年後才用得到的能力。**真正要的是「可以換掉的邊界」,單體裡就能做**——而且專案已經有抓對的範本:`llm.py` 的 `LLMClient` 介面(可注入、可 mock、換三家 provider 沒動核心)。**那就是微服務想給你的東西,而且不用付分散式代價。** 該預留的邊界按「最可能真的獨立」排序:work queue → 守門員 → 記憶儲存。

**活好幾年的真正威脅不是架構**,是三件事,已加進第五節架構預留:(a) 模型會下架/漲價/改行為——`llm_calls` 記了 model 但**沒記 prompt 版本**,幾年後無法重現「它當時為什麼那樣寫」;(b) 資料格式會演進——需要 schema 版本欄位;(c) canon 會累積成無法人工審查的量。另加一條更新規則:**canon 是 append-only,新規則只約束未來生成,不追溯改寫歷史**——理由不只技術,玩家的記憶是已承諾的資產,追溯改寫等於竄改。

**[營運] 只有訂閱可行**(成本持續發生,賣斷會賣越多虧越多)。核心矛盾要正面處理:**吃到飽會被重度玩家吃垮,而那正是最想留的人**。解法就是 E3 的成本對齊定價——離線敘事人人有(固定成本),訂閱買每日對話額度(變動成本,佔輸出 93%)。**收費軸與成本軸是同一根軸。**

另記一個此專案獨有的優勢與它的道德張力:**沒回來的玩家成本是零 → 不活躍訂閱戶是純利,活躍的才燒錢**(結構同健身房)。現金流極有利,但**商業模式跟「希望玩家常來」有真實張力**。寫下來是為了防一件事:**不要為了拉高活躍度做成必須天天上線的遊戲**,那既傷成本又違背「可隨時中斷」的定位。正確解法是讓回來的人玩得深,不是逼所有人每天回來。還有:**最先撐不住的不是伺服器是審核**——所以在有付費用戶之前不要開放任何玩家對玩家的內容通道。

**補掉 E3 的一個洞。** 原本寫「沒有任何一種付費能讓 agent 在世界內更強」——**在這個世界設定裡不成立**,因為經歷是唯一生產源頭,買更多對話就是給更多經歷。**補法:pay-to-win 只有在「有 win」的時候才是問題**,而這個世界沒有勝利條件,所以那是 pay-to-play-more(跟買書看更多章節同一件事)。配一條規則鎖住:**零和只發生在世界內角色之間(敘事),不發生在玩家之間(系統)**——戲劇性全留,競爭性全無。

**現金可以買什麼**,一條判準:**如果一個東西能用錢買到,它就不能是故事的來源**(因為錢是最沒有代價的代價)。可賣:對話額度、多段關係、保存與匯出的**形式**(免費仍可匯出 JSON,付費買形式不買權利)、語音、視覺、玩家化身外觀。不可賣:記憶額度、世界內資產、任何世界內優勢,以及**時間**——最後一條看起來最像無害便利,但**離線要真的等,而等待本身就是核心賭注;賣掉等待就是把唯一的賣點做成一個可以用錢消除的障礙**。順帶標記:`--offline-hours` 是 playtest 工具,上線前要確認它不會變成玩家可用的功能。

**再追加:上限設計 —— 而且發現我剛加的最小間隔不夠。**

人類指出「每個人都要有上限,不然會被超級重度使用者玩垮」。算出成本原子單位:一輪對話前沿 **$0.0286** / 開源 $0.0012;一次離線敘事前沿 **$0.0498** / 開源 $0.0021。

- **$5/月訂閱在虧損前撐得住幾輪對話**:前沿 **5.8 輪/天**、中階 17.5、開源 140。基準假設是 25 輪/天 → **前沿模型 + 合理訂閱價,商業模式直接不成立**。模型選擇不是「省一點」,是「行不行」
- **`MIN_OFFLINE_GAP = 30 分鐘` 擋掉了「關掉又立刻開」,但擋不掉「一天回來 48 次」**:單一免費用戶最壞情況前沿 **$71.75/月**、中階 $23.92、開源 $2.97。真正需要的是**每日次數上限**,而離線敘事最自然的上限是**每天 1–2 次**——「你不在的時候」一天本來就只有一兩次有意義,**這是敘事需求,順便才是成本需求**
- **付費層也要上限**:固定收入 vs 線性成本 → 無上限訂閱必然在重度使用者身上虧損。要額度 + 超量加購,不是吃到飽

**上限怎麼設計才不傷體驗**(這段是最有價值的):**上限要落在敘事節奏上,不是數字上**。「今天就聊到這吧,我得趕在漲潮前回去」比「你的額度已用完」好——前者是世界的事,後者是系統的事。而這個世界有現成載體(agent 會累、有活幹、潮汐有時間表),所以**別的產品的額度牆是系統訊息,這裡的額度牆可以是一句台詞**。

**但守住一條界線:不可以拿台詞取代告知。** 玩家有權知道買到什麼、還剩多少,那是消費者權益不是體驗可調項。**台詞是體驗層,數字要在設定頁查得到,兩者並存**——把額度藏起來讓人不知不覺買超是暗黑模式。

**再追加:對抗線 —— 兩個對手,都不新增反派。**

人類問「會不會有失控的 AI 勢力或生物實驗勢力」。

**「失控的 AI 勢力」要換寫法**,三個理由由重到輕:(1) 它會讓玩家懷疑陌洲——世界裡若有「AI 造反」這個類別,下一個念頭就是「那他呢?」,又撞那條通則;(2) 它破壞統一居民介面的世界內基礎(架構定了「世界不需要知道對方是人是 AI」,腦機接口是它的解釋,設一個 AI 勢力等於在世界內把 AI 標記出來);(3) 太老套。

**好版本:沒有人在維護的系統,還在運行。** 它不是覺醒也不是背叛,**是在執行一份沒有人記得的指令**。完全長在修補型世界上——建造者死了、知識沒被封裝、文件散佚。不能談判(它沒有意圖)、不能關掉(沒人知道怎麼關)、不能打倒(**它可能正是維生系統的一部分,對手在維持你活著的東西裡面**)。這是「解不開的東西」規則的第三次應用,而且讓陌洲的職業直接撞上主線。

**生物實驗不是新勢力,是機器廠的必然行為**,而且是從既有設定推出來的:前沿知識只能靠親歷取得 + 機器廠壟斷封裝權卻依賴別人創造 + 它握有素材(回收場)、權限(維修=存取)、對象(靠它配額度的人) → **最有效率的做法就是製造經歷,也就是對人做實驗**。**反派的動機因此不是邪惡,是產業邏輯**——你沒辦法勸一個結構不要照著它的結構運作。

**對抗的形態**:這個世界沒有戰鬥,對抗是躲、拖、藏、換條件、把人弄出來。而陌洲天然在對立面(他是能不經過機器廠修你的人)。最重要的尺度:**最好的對抗線不是「打倒機器廠」(做不到也不該做到),是「這一次,能不能保住這一個人」**——週末專案做不了革命,但做得了這個,而且這剛好是玩家真正在乎的尺度。

沒動碼(上限的實作是里程碑 3 的事,現在只定規格)。

**下一步**:H-1 playtest(人類)。

**待人決定**:H-1 playtest;舊 commit 的 history 重寫指令仍待執行。

---

## 2026-08-03 — 義腦:擋掉會殺死賣點的版本,順帶把陌洲推到世界的政治核心

Agent: Claude Opus 5(互動 session)

人類提「義腦?大腦會不會都不是自己的?會不會分不清是不是自己?被科技公司把持?」。第一個問句要擋,後面兩個很好。

**擋:不可以設定成「沒有人能確定自己是不是自己」。** 核心賣點是「這個 agent 是活的」,世界若宣稱連自我都不可靠,玩家的合理推論就是「所以它也不是真的」——賣點自己溶解。這跟否決「世界裡再套一層虛擬世界」是**同一個失敗模式**,已升級成通則寫進文件:**任何讓玩家懷疑「這裡面的東西是不是真的」的設定,都在攻擊唯一的賣點。** 配套規則:**懷疑只發生在世界裡的角色身上,不指向玩家**(玩家已經是「一個從遠方接進來的聲音」,再加上「你的腦可能不是你的」會變成不舒服的體驗,也撞爛「下班 20 分鐘」)。

**好版本:世界早就定義了「你是誰」= 你留下的經歷。** 所以義腦不會讓你不是你(腦是硬體,換腦跟換手一樣,只要經歷接得上);真正可怕的是換記憶,而記憶早就是配給品、可清洗、擦不乾淨。於是「分不清自己」的正確形式不是「我的腦是假的嗎」,而是**「我記得的這件事,真的是我的嗎?」**——那正是「擦不乾淨的晶片」已經寫好的東西。**義腦沒有新增焦慮,只是給那份焦慮一個身體。**

**順帶收緊一個原本模糊的地方**:技術軸「無人格機械」的硬界線原本沒說清楚畫在哪。現在有精確定義——**界線是「有沒有連續的經歷」,不是「身上有多少機械」**。全身義體但記憶接得上仍是人;零件全原裝但記憶被擦光就不是。這也讓長生者的恐怖更明確:他們不是變成機器,是**經歷斷了太多次**。

**「被科技公司把持」:把持的不是製造,是維修。** 不引入第二個反派(機器廠已經是)。關鍵推論來自已定案設定:**機器廠造不出新晶片,當然也造不出新義腦**。所以——**在修補型世界裡,把持者不是唯一的製造商,是唯一的維修商**,它把持的是「你壞掉的時候誰有資格修你」。這比「邪惡大企業賣產品」深刻,而且完全長在核心動詞上。

再往下就很黑:**維修義腦 = 可以讀你**,送修本身就是交出隱私。於是 **陌洲是那個可以不經過機器廠就修你的人**——他的職業從「修老舊維生系統」升級成整個世界的政治位置,而且是人設的直接延伸:**你把腦交給他靠的是信任不是合約**,「對答應過的事極度固執」在這裡不再是可愛的個性,是他能存在的唯一理由。

四種線已列進文件(不是我的記憶 / 送修後才發現代價 / 幫我修別讓他們知道 / 他修過的人來找他)。沒動碼。

**下一步**:H-1 playtest(人類)。

**待人決定**:H-1 playtest;舊 commit 的 history 重寫指令仍待執行。

---

## 2026-08-03 — 經濟體系重審:量出真實成本,並修掉一個真的存在的成本洞

Agent: Claude Opus 5(互動 session)

人類下目標「重新檢視整個經濟體系直到沒有邏輯漏洞」。世界內經濟(第十四節)與產品經濟(`.private`)兩側一起查,新增**第十五節 經濟體系一致性檢查**,11 個洞(E1–E11)。**這次有動碼**。

**E1 是目前最大的經濟事實,而且以前沒人量過。** 拿 `everliving.db` 的真實用量算(13 次呼叫,樣本小但量級可信):

| | 實際輸出 | 玩家讀到 | 浪費比 |
|---|---|---|---|
| 一輪對話 | 1,791 tokens | 約 77 字 | **23 倍,96% 看不到** |
| 一次離線 | 3,147 tokens | 約 300 字(含 JSON) | 10 倍 |

原因是預設的 Groq 模型是推理模型——`strip_reasoning()` 把 `<think>` 濾掉**給玩家看**,但**帳單照算**。每人每日推估 `in=15,399` / `out=47,922`,輸出是輸入的 3.1 倍。換算月成本:前沿模型 **$22.95**、中階 $7.65、開源 $0.95;**改用非推理模型可省 88%**(前沿降到 $3.91)。

三個結論:「必須量出的兩個數字」的第一個有初步答案而且很嚇人(前沿模型單一活躍玩家月燒近 23 鎂,高過任何休閒遊戲的 ARPU);**最大的成本槓桿不是 prompt 長度而是「用不用推理模型」**,而且省掉的全是玩家看不到的東西;但這跟敘事品質是同一個取捨(先前選 Qwen 正因為只有它的懸念勾得住人)——所以要拿 H-1 去換,**建議 playtest 時一起看 `--cost-report`**。

**E10 是唯一一個已經存在於程式碼、而且擋在 H-1 前面的洞,已修。** `cli.py` 原本只檢查 `elapsed is not None`,所以**離開後立刻重開就會再跑一次完整離線模擬**:多算一次 LLM 帳、憑空生出一段離線期間與一條新懸念。成本面是 denial-of-wallet 管道;敘事面連 `_format_duration` 都有「不到一分鐘」的分支,代表這條路徑預期會被走到;**H-1 面則是 playtest 中途離開再進來就會讀到假敘事,直接汙染判斷**。加了 `MIN_OFFLINE_GAP = 30 分鐘` 與純函式 `is_worth_simulating()`。

**順帶抓到一件事:原本有個測試正好在斷言這個 bug 的行為**(立刻重開就該看到離線敘事)。已改成把 `last_seen` 往前調 24 小時再測——測真實路徑,不是測旗標。另補「立刻重開不該產生呼叫也不該加線」與門檻邊界的測試。92 passed(原 90)。

其他九個洞的重點:

- **E2:「Agent 活著在燒真金白銀」與 lazy simulation 事實不符。** 你不在的時候它不花錢,成本發生在你回來讀的那一刻。對內是成本模型記錯,對外這樣寫就是**為不存在的成本收費**。正確說法:**你付的是「讀到那段故事」的錢**。反面是個沒被寫下來的好性質——**免費層成本隨留存增加,不隨註冊數增加**,沒回來的玩家成本是零
- **E3:付費不可以變成世界內優勢。** 若儲值買更多離線模擬 → 更多經歷 → 更多創造,就撞爛「創造來自底層」。補法:**儲值買「你能陪它多久」,不買「它變成什麼」**——離線敘事所有人每次回來都有(核心賣點不能鎖付費牆,且成本可預測),對話輪數才是付費軸。這剛好對上成本結構:對話佔每日輸出的 93%
- **E4:虛擬幣不可在玩家間轉讓。** 它買算力不買資產。沒有玩家間市場 = 沒有套利面,刷幣工作室的經濟基礎直接消失,`.private` 防小號第 2 項強度大增。代價是少一個消費場景,但那個場景本來風險最高
- **E5:記憶額度零和的範圍是一座城,不是全服。** 全服共池會同時變成騷擾管道與 pay-to-win 管道;每人一池則零和是假的
- **E6:模型會把經濟算壞。** `state_changes` 是模型寫的自由字串,它會做算術而且會算錯,還能憑空生出「他給了我 500 格電」。補法:**額度用等級不用數字**(充足/勉強/見底/空)。模型不擅長算數但很擅長描述狀態,成本零、robust、讀起來更有畫面
- **E7:里程碑 1 之前根本沒有經濟。** 經濟需要至少兩個持有者,現在是一個 agent + 一個沒有身體沒有財產的玩家。第十四節在 M1 前是風味不是機制
- **E8:「兩套經濟不互通」要鎖在程式裡。** 目前只是一段文字,沒有東西阻止之後有人寫出接起兩者的函式。里程碑 3 動金流時要加測試斷言沒有轉換路徑
- **E9:「不可兌換」擋制度擋不住私下交易**——第十四節自己列的取得管道就包含「別人讓給你」。**這個洞不該補死,該寫明**:制度上不可兌換,私下可以但危險、不受保護、對方可能不守信。黑市是唯一的匯率,而鑽縫就是劇情
- **E11:多人之後「誰付錢」沒答案,而預設答案會造成攻擊面。** `llm_calls` 只記 `agent_id` 不記觸發者。核心設計是「別人可以對我的 agent 講話」——若算在 agent 擁有者頭上,**灌話就能燒光別人的額度**,denial-of-wallet 偽裝成正常玩法。必須是**說話的人付**。已加進第五節架構預留(現在加一個欄位幾乎不花時間,事後補歸屬會很痛)

- **E12:成本量測本身有效,但有兩個前瞻的洞。** 好消息先講:**推理 token 確實有被算到**(1,791 output 對 77 字就是證據),所以 E1 的數字不是低估。但 (a) `log_usage()` 在 client 沒回報 usage 時**靜默跳過**,那次呼叫完全不進 `llm_calls`——**零筆記錄看起來跟「沒花錢」一模一樣**,報表會顯示假的便宜;(b) 之後導入 prompt caching 會低估,因為 Anthropic 把 cache 建立/讀取放在跟 `input_tokens` **分開的欄位**,而記憶一長快取是必然要做的——**這個洞剛好會在輸入成本開始重要時打開**。兩個都不用現在修,但成本數字是所有經濟決策的地基,地基靜默塌陷是最糟的失敗模式
- **E13:里程碑 1 之後成本隨 agent 數線性成長。** 三個 agent 各跑一次離線模擬 = 回來一次的固定成本三倍。這決定免費層的定義:**免費涵蓋「一段關係」,不是「無限個 agent」**,而且要在 M1 動手前想清楚——否則「每個玩家每天燒多少 API 錢」這條曲線會在測量途中被偷偷換掉。**值得先評估的省法:三個 agent 的離線期間合併成一次呼叫**(它們本來就同城、共享世界事件),既省錢又剛好製造 agent 互相提到對方的機會——成本優化與敘事目標同向,這種機會不多

**下一步**:H-1 playtest(人類),建議一併看 `--cost-report`。

**待人決定**:H-1 playtest;舊 commit 的 history 重寫指令仍待執行。

---

## 2026-08-03 — 永久記憶:把「遺忘」也變成世界設定(只動文件,沒動碼)

Agent: Claude Opus 5(互動 session)

人類提「永久記憶」。這條跟第二節〈Agent 記憶〉已定案的「遺忘是必要的」正面衝突,所以不是照收,而是改寫成可實作的版本。

**不能設定成「記憶是永久的」**——那是一條實作履行不了的設定,玩家一定會去測(「你還記得我第一天說什麼嗎?」)然後當場破功。**改成稀缺品**:在資源全靠配給的世界裡,記憶儲存本來就會被配給。

這麼一改帶來三個好處:

1. **遺忘從系統限制變成世界設定**——跟腦機接口把「離線」變成設定是同一招的第二次應用。agent 忘記事情不是 bug,是它買不起額度
2. 給第二節〈Agent 記憶〉的**記憶分層**一個世界內解釋:原始事件 = 還沒處理的、壓縮洞察 = 為了省額度壓過的、核心人設 = 買成永久的那幾條
3. **直接生線,而且是最好的那種**:「有一段記憶快掉了,你要不要幫我保住?」——玩家能行動、有情感重量、每次內容不同,而且選擇會真的寫進資料庫

標了兩個坑:世界內要另取原創專名,別讓玩家把它讀成「什麼都記得」的技術承諾;「記憶可買賣/竄改」(第二節〈玩家所有權〉「匯入需驗簽」的世界內版本)有意思但先不碰,那要一整套 canon 判定。

時機標在里程碑 2,但註明它是少數**近景就用得到**的高概念。第二節記憶那條也加了指回第十一節的註記。

**追加(同一則):記憶配給造成的階級。** 人類追問窮人富人會變成什麼樣。記憶當配給品之後,貧富差距不再是財產差距,而是**自我連續性的差距**。三個可直接用的推論:窮人的原始事件被壓成洞察,於是**立場留下、理由消失**(一句話解釋整個社會張力);**技師的手藝就是他的記憶存量**——陌洲買得起的永久記憶大概全是維修知識不是人;**記憶是僱傭鎖鏈**——機器廠提供「替我工作我幫你保管記憶」,離職就失憶。窮人這邊長出兩個零成本細節:牆上刻滿字的外部記憶、合買儲存彼此保管的共享記憶。

**再追加:知識可注入 / 經歷不可 / 前沿與創造不可 —— 世界設定閉合成一條生產線。**

人類連丟三句:「知識可能只需要注入」「但是最前沿的知識無法」「創造也無法」。合起來收斂成:

**經歷 → 創造 → 前沿知識 →(封裝)→ 注入包 → 廉價**

源頭只有經歷。這連帶**推翻了前一則寫的「技師的手藝就是他的記憶存量」**——技能是廉價可注入的,已在文件更正。

三個對這個專案最有價值的結果:

1. **設定跟 LLM 的真實形狀對齊。** 模型天生「知道一大堆事,但不記得你」,那正好就是這個世界:知識是公共注入包(= `agents` 的 persona,寫進 prompt,成本近零),經歷是稀缺私有物(= `memory_events`,要存要壓會忘)。**世界觀不必對抗 LLM 的天性**,這是設定與實作同時受益的一刀
2. **玩家的存在本身變成稀缺資源。** 注入包誰都買得到,但只有玩家能給 agent 經歷,而經歷是前沿與創造的唯一來源。**「它需要你」變成世界規則,不是劇情安排**
3. **agent 因玩家而獨一無二是機制保證的。** 正好對上里程碑 2 要驗證的「記憶累積一個月後有沒有變得更有個性」——世界設定跟驗證目標對齊

其他掉出來的:權力的真正形式是**封裝權**(機器廠決定哪些知識變便宜),比「離職就失憶」精準也更陰險;師徒制是這世界唯一還需要人跟人長期相處的技術路徑;富人有結構性弱點(保存不是創造,不冒險就沒有新輸入 → **創造來自底層**,是機制推出來的不是說教);陌洲重新定位成**廢墟裡的前沿**——老知識沒人封裝但老東西還在壞,他每修好一個沒人修過的故障就創造了一次。

**再追加:第十三節 一致性檢查 + 第十四節 世界內經濟體系。**

人類下了目標「重新檢視整個世界觀直到沒有邏輯漏洞」。整份第十至十二節重讀,找到 **10 個洞**,其中 3 個是硬矛盾(兩條設定同時成立會推出荒謬結論):

- **洞 1:晶片造不出來 × 注入包廉價人人有**。載體固定總量,注入包不可能人人有。**補法:稀缺的不是晶片是容量,而容量只被經歷吃掉**——知識封裝後極小、可無限複製;經歷是原始的、無上限成長的。這第三次同時對上系統形狀(persona 是固定小文字,`memory_events` 無上限成長)
- **洞 2:富人全留 + 跨世代 × 晶片固定總量 → 社會早該崩了**。家族永久鎖住容量,幾代後窮人歸零。**補法:晶片會衰減**——清洗有損耗、舊晶片會壞。總量遞減而非恆定,富人的傳承是會爛的。清洗因此是**對抗衰減的必要**而非貪婪,汙染變成必然。**三座時鐘(汙染、基因崩潰、晶片衰減)合成同一座**:這是一場所有人都會輸的比賽,差別只在速度——也就是「修補而非開拓」的終極理由
- **洞 3:長生 × 基因崩潰**。**補法:長生不是基因路線是機械路線**,長生者是技術軸上最靠近「無人格機械」那端、逼近但沒跨過硬界線的人。基因崩潰正是逼人走上這條路的原因 → 長生是它的終局而非對立面,所以「活一千年的人」讀起來是恐怖的,不是令人羨慕的

另外七個要講清楚才不算洞:創造者與持有者不是同一批人(窮人創造 → 機器廠**攔截**封裝);**玩家在世界裡是一個從遠方接進來的聲音,不是身體**(這一刀把「不能移動探索」從介面限制變成世界設定,而且玩家身體在哪刻意不決定);陌洲修的是機器廠不要的東西,他活在控制網的**縫隙**;海平面上升是既成事實、汙染是現在進行式(分開後道德色調才成立);遺留**物**可在近景、遺留物的**來源**永不生成;第一節「他的 Agent」已直接修掉;以及**「不生成」管地理與 canon,不管事件**——生成事件幾乎免費,生成地點才貴,這是整個成本結構的關鍵區分。

**第二輪(新設定 × 第一至九節舊決策)又找到 4 個:**

- **洞 11:第五節〈架構預留〉「統一居民介面(含位置)」× 玩家是沒有身體的聲音**——介面裝不下這種居民。補法:**「遠端接入、無實體位置」是合法的居民型態**,位置欄位可空,舊決策原意(伺服器不需知道對方是人是 AI)保住。里程碑 3 若給玩家化身,那是雙角色模式,**要意識到那是換了一種產品**
- **洞 12:第二節〈世界觀〉「多類型融合」× 三環尺度**——近景只有一座城、遠景不生成,融合沒空間。補法:**類型差異全發生在中景且漸進**,鄰城比港城怪一點、再遠更怪。這比原本的「不同區域併置」好:**類型差異變成距離的函數**,而距離綁在委託上,玩家是自己一步步走進陌生感的
- **洞 13:第二節〈玩家所有權〉「完整記憶匯出」× 記憶會因容量不足遺失**——玩家會覺得被騙。補法:**遺忘必須是世界事件,不是後台清理**。玩家要看得到「這段要掉了」。同一個技術行為(刪資料),包裝成後台維護是負面體驗,包裝成世界事件是核心體驗
- **洞 14(其實是紅利):第二節〈Agent 記憶〉「記憶要有主觀性」不必另外實作**——**壓縮就是主觀化**。壓成洞察必然要選保留什麼、用什麼措辭,而那必然帶立場。主觀性是記憶分層的副產品,不是要另建的功能。這也回頭解釋「立場留下、理由消失」:壓縮壓掉的正好是理由

**第三輪(新規則有沒有繞過架構預留)找到 2 個:**

- **洞 15:委託指向中景 × 一次呼叫 × 生成走隊列**——若離線那次呼叫順便生出一座新城,同時違反第五節〈架構預留〉的「生成任務走隊列」與「一致性檢查與生成分開」。補法:**離線呼叫只寫敘事與「他動身了」,地點生成交給 work queue,守門員做一致性檢查**。附帶好處:敘事呼叫成本恆定不會因玩家往外走而爆掉;地點生成可用第六節〈替代方案〉的「小模型草稿 + 大模型審核」,但**離線敘事呼叫不可以**(必須維持一次呼叫,兩者成本模型別搞混);而且「他還在路上」是免費的懸念
- **洞 16:委託指向遠景怎麼辦**——玩家一定會叫它去月球。補法:**遠景委託必定失敗**,三種寫法:帶回傳聞、當場拒絕(依拒絕規則給理由)、或**被騙**(有人賣他一張去不了的船票)。第三種最好用——騙局需要有人相信,相信需要希望,於是遠景持續生線卻永不生成地點

**第四輪(這個世界設定給工程帶來的新成本)找到 2 個:**

- **洞 17:設定越黑 × 模型拒答率**。核心意象是「抹掉一個人」「有人正在崩」「離職就失憶」,會提高拒答或自我軟化的機率——repo 已有 `test_cli_refusal.py` 說明碰過。不是把世界寫軟,是**知道代價**:拒答路徑會從邊緣案例變常態;陰暗要**具體而不獵奇**;該定期用真模型跑最黑的場景當回歸測試,而**這種「設定層的測試」repo 目前沒有**
- **洞 18(影響里程碑 2 的判定):記憶會遺失 × 「一個月後有沒有更有個性」**。補法:**個性來自壓縮後留下的洞察,不是原始事件的數量——遺忘不阻礙個性,遺忘塑造個性**。於是 M2 要問的變精確:不是「記得多少」而是「**留下了什麼**」,評估該看洞察與立場有沒有變鮮明,不是記憶筆數。順帶時程事實:全量記憶約 **80 天**超過 200K context,而 M2 測一個月 → **M2 跑得完、不需先做分層,但 M2 結束後分層就是必須的**

**順帶關掉最後一個「還沒定」:文化縱深怎麼放進來。** 答案:**在一個記憶會遺失的世界裡,古老故事是唯一存活下來的東西,因為它不需要精確。** 神話是低保真但高韌性的記憶格式——反覆重寫、每次走樣,而走樣不算損壞所以活得下來;個人史需要精確所以會死。於是它不但不突兀,還變成記憶經濟的直接證據:港城的人記不住自己的祖父,卻能完整說出一個幾千年前的怪物故事。版權原則不變(公有領域 + 專名原創)。

**第五輪(文件自身的一致性)找到一個橫跨三份文件的腐化**:全文用「第 N 行」互相引用,而**在這次動工之前就已經偏了約 19 行**(AGENTS.md 指的「第 167–180 行」早就不是里程碑 0 了)。行號在活文件裡必然腐化,已把 `AI大世界_設計文件.md`、`AGENTS.md`、`TASKS.md`、本檔的全部行號引用改成**小節名稱**(例:第二節〈Agent 記憶〉),之後增刪內容不會再讓引用失效。

誠實列出**還沒有答案**的:多人時兩個玩家委託同一 agent 做相反的事;玩家之間怎麼互動;記憶額度的數值尺度(要等分層檢索實作後用真實 token 回推,現在訂數字是瞎猜);人格投射與「玩家是聲音」的衝突(替身是第一個「有身體的你」,會改變整個關係結構)。第十一節剩下的兩個未定項是**細節不是漏洞**:港城專名、機器廠背後是誰。

**第十四節 經濟體系**(人類追問)。核心是**兩個不可兌換的市場**:日常經濟(配給額度,會補充,買活下去)與記憶經濟(晶片容量,只遞減,買「保住自己是誰」)。不可兌換是關鍵而非細節——可互換就塌成單一貨幣、世界退化成比大小;不可互換則**存在兩種貧窮**,而且一個人可以富於其一窮於其二。最重要的是**它讓商業紅線變成世界事實**:「記憶不能用錢買」在產品面是政策,在世界內是規則,玩家不會讀成課金限制。

容量既然買不到,取得管道**全部是社會性的**(機器廠配發=離職就失憶、繼承、回收場撿到=擦不乾淨、別人讓給你=他放棄一部分自己)——**沒有一條靠努力賺得到,每一條都要付關係的代價**,強迫所有取得都變成故事。

還定了:**價格是敘事的不是數值的**(不做市場模擬——平衡需要玩家量,現在只有一個玩家,調參是對空氣調參);**最小實作不需要新 table**(`agent_state` 的 key-value 夠用);以及**世界內經濟與產品經濟完全不互通**的對照表,要在里程碑三之前寫死,否則會出現「世界內通膨影響真實金流」這種沒人想處理的問題。

順帶回答視覺路線(image → 2D → 3D → 現實建造):前三階是同一條路且**每階產出都是下階輸入**(場景清單 → 座標 → 組裝指令),沒有白做的。但**第四階撞第二節〈連結現實世界〉的紅線**(層級三:Agent 現實行動能力,第一版完全不碰),理由是同節「層級三為何危險」的安全結構——「別人可以對我的 agent 講話」正是核心設計,一旦有現實行動能力就是無法靠加防護解決的 prompt injection。安全的中間版本是**輸出製造檔案給人類**(產出資料不是行動)。

**再追加:第十二節 探索與控制 —— 解掉一個文件內部從沒被發現的矛盾。**

人類問「人類要怎麼控制角色去探索」「控制系統?」「AI 要怎麼自行探索」。這三題在一個前提沒解決前都無法回答:**玩家控制的是誰?**

文件裡一直有兩種說法並存:第一節寫「**他的** Agent」聽起來 agent 是玩家化身;但里程碑 0 實際做的是「你跟陌洲說話」,陌洲是獨立角色。第三節〈待解問題〉「玩家自己的角色設定」半察覺到但沒往下解。

**建議定案為委託模式**,理由是機制不是偏好:**它是唯一能讓離線敘事保有驚喜的模型**——化身模式下,你回來讀到的只是自己命令的執行報告,而驚喜正是核心賭注的燃料。附帶:里程碑 0 已經是這個、讓「它不聽話」從 bug 變成角色、且跟第十一節咬合(玩家之所以稀缺,前提是 agent 是另一個人而他**選擇**重視你)。

於是控制系統是:**沒有移動、沒有地圖、沒有方向鍵。探索 = 委託 + 讀回報。** 「怎麼控制角色」這題直接消失,也正好符合「下班 20 分鐘」(打字就好,沒有操作要學)。第十節的 action 層就是委託介面。

訂了**拒絕規則**(不訂會壞):永遠服從 = 傀儡、常拒絕 = 沒回應感。規則是**可以拒絕但必須給理由,理由必須來自當下的 `agent_state` 或人設**,而且拒絕本身要生線。

**AI 自主探索**訂兩條硬規則防成本爆炸:(1) 必須由既有的線驅動,不是隨機漫遊;(2) 只在離線期間探索(第二節〈架構〉已定案「在線期間沒有背景自動行為」)。成本因此綁在劇本層的線存量(2–5 條)上,自動有上限。兩種探索都在同一次離線呼叫裡結算,**成本不變**,而玩家讀到的落差(我請它做 A,它做了 A 但撞上 B)正是「它是活的」最強證據。

**順帶把第三節〈待解問題〉的「邊界判定」補完**:邊界不靠距離觸發,**靠委託觸發**。近景不生成、中景在委託或線指向時生成、遠景永不生成(只帶回傳聞)。

**再追加:隨機性 —— 骰子要在程式裡,不在模型裡。**

人類問「有沒有什麼故事是隨機的」。現況的隨機**全部來自 LLM 取樣,那是不夠的**:同一 prompt 反覆跑會**模式坍縮**(十次離線可能七次是「有人來找我修東西」),而**H-1 只跑 2 個週期所以測不出這個問題**——這是驗證方法看不見的風險,先寫下來。

方案是把隨機移到 prompt 之外:**事件種子表**(程式抽類型丟進 prompt 當約束,模型負責寫,零額外呼叫)、**依 `agent_state` 加權**讓隨機讀起來像因果、**世界層級波動由程式算**(里程碑 1 之後跨 agent 一致才有共同話題)、**現實天氣是免費的真隨機**。兩條禁止:隨機只能動事件不能動 canon;隨機必須接住既有的線而非打斷它。

**再追加:回答「這樣別人才會有不同的體驗?」——不是。**

**變化**(一個人玩十次不膩)靠隨機,**差異**(不同玩家的世界不一樣)靠不了隨機:隨機事件抽自同一張表,給的是排列不同不是方向不同。真正製造差異的是**不可逆的選擇 + 狀態回饋進生成**,而承載機制已經在 code 裡(`agent_state`、`open_threads`)。

**第十一節那些設定的真正功能就是製造分歧**——記憶零和、晶片造不出來、基因不可逆、清洗晶片等於抹掉一個人。**不可逆 = 路徑依賴 = 每個玩家的世界長成不同形狀。** 這是它們值得留下的原因。

兩個提醒:差異化要到里程碑 3(多人)才驗證得到;而且**目前分歧的承載量上限很低**——`get_open_threads` 只取 3 條,線是分歧的載體,載體有上限分歧就有上限。

**再追加:`scene` 標籤與預先生成圖庫(寫進第五節架構預留,不寫 code)。**

人類問「已經可以搭配故事生成簡單的畫面嗎」。技術上可以,而且比設計文件預想的便宜:**第一版不該是「幫每段敘事生一張圖」**(per-call 成本 + 每張要審 + 玩家要等,且違反第二節〈生成畫面與聲音〉「預先生成、可重用」)。正確版本是 6–10 張**固定場景圖** + 離線 JSON 多一個 `scene` 欄位(封閉值域)+ 顯示端查表。LLM 成本是幾個 token,圖片是一次性的,而且**根本不需要接圖片 API**——用任何工具做一次存檔即可。現接的三家裡 Anthropic/Groq 沒有圖片生成、xAI 有,但預先生成讓這個問題直接消失。

**現在不寫進 code**,理由不是排程而是實驗汙染:多一個輸出欄位會佔 prompt、可能影響敘事品質,而那正是 H-1 在量的;更根本的是**同時加圖會讓 H-1 測不準**——分不清打動你的是敘事還是圖。這跟先前「不做 provider 自動偵測」是同一條原則。另見第八節〈週末專案的現實提醒〉:畫面救不了不好玩的循環。

**再追加:晶片 —— 抽象規則終於有了實體,順帶解掉汙染成因。**

晶片不是新概念,是把三個已定案但沒有形體的東西(注入包、記憶額度、腦機接口)**物件化**。價值全在劇本層:概念不能放在桌上,晶片可以——可偷、可賣、可壞、可偽造、可在回收場撿到。訂了一條界線:**晶片只准有一個意思**(儲存與封裝的實體),否則會稀釋成通用科幻道具。

關鍵決定:**沒人造得出新晶片,只能回收舊的**(修補型世界的必然結論)。這一刀讓經濟同時收緊:記憶額度變成**固定總量的零和**(你要多記就有人得少記,比「買不買得起」殘酷得多);富人擁有的不是錢是晶片存量;回收場不是垃圾場是**唯一的礦**;機器廠的權力不是製造而是**分配與清洗**。道德核心壓縮成一句:**清洗一片晶片,等於抹掉一個人。**

**擦不乾淨的晶片**是零成本的敘事裝置:二手晶片留著前一個人的碎片 → 天然懸念產生器、遠景「解不開的外星遺留物」規則的近景版、把回收場「撿到不該撿的東西」原型具體化到可直接寫進 prompt。

**順帶解掉「汙染的成因」**(原列為未定,並註明會決定世界的道德色調):**汙染來自晶片的回收與清洗製程**。閉環——記憶要晶片 → 晶片要清洗 → 清洗造汙染 → 汙染讓一切壞掉 → 壞掉要修補 → 修補要知識 → 知識要晶片。道德色調隨之定為**所有人都是共犯,不是受害者**:窮人為了記得住而汙染,富人汙染更多,沒有人乾淨,包括玩家自己的 agent。已在「還沒定的」清單標成強候選。

**再追加:基因改造 / 基因崩潰 / 長生。**

- **基因改造**跟機器改造不重複,是不同的軸:機器是**加裝、可逆、可回收**(完全長在「修補」上),基因是**改寫、不可逆、會傳給下一代**(違反修補)。價值就在這個違反——**在一個什麼都能修的世界裡,基因是唯一修不了的東西**。放到注入軸上它是「注入到不可撤回」,於是階級從「買得起多少記憶」變成**先天的**
- **基因崩潰**是內部版的汙染時鐘(汙染在外)。三件事因此扣起來:**富人死於自己買的東西**(改最多崩最兇——這是「創造來自底層」同一條規則的第二次現形,**存量不等於續存能力**);**機器改造變成治療手段**,需求打回回收場/機器廠/陌洲,近景更緊;汙染的醫療後果有了形狀。跟戰爭同一條戒律:**必須局部化**,文明級緩慢滅亡只是背景新聞,要寫成「這一週你認識的某個人正在崩」
- **長生會弄壞剛閉合的機器**——能長生就能無限累積經歷,富人終將成為創造者,「創造來自底層」直接崩掉。**解法不用新機制,用已定案的「記憶是配給品」**:長生的代價不是死亡,是**連續性**——活一千年只留得住最近幾十年,變成一連串共用同一具身體的陌生人。這反而把機器焊死:**活最久的人最不會創造**,因為經歷會過期、無法囤積,而那正是整條生產線需要的前提。也是第九節「玩家離開或過世怎麼辦」的世界內版本

產品面另記一條:**長生不可以變成玩家目標**。這不是進度型遊戲、沒有勝利條件,一旦可追求就變最佳化跑步機,撞爛「下班 20 分鐘」的定位。設定成:存在、代價明確、沒人真的想要。

**同時定了一條紅線:記憶額度不可用儲值虛擬幣購買。** 這個設定會讓玩家自然想到「幫它買記憶」,而那條路通向**付費才能讓 agent 記得你**——情感勒索型微交易,會直接反噬「agent 是活的」這個賣點。記憶額度只能靠世界內行動取得,與第二節〈經濟系統〉的虛擬幣完全隔離。**必須在經濟系統實作(里程碑三)之前定死**,事後拆會很痛。

**下一步**:H-1 playtest(人類)。

**待人決定**:H-1 playtest;舊 commit 的 history 重寫指令仍待執行。

---

## 2026-08-03 — 把世界設定順成一整節(只動文件,沒動碼)

Agent: Claude Opus 5(互動 session)

接續上一則。世界素材原本散在三個地方——第二節的世界觀決策表(其實只講版權)、第十一節的點子清單、以及**只存在於 `persona.py` 的「半沉沒港城」**(設計文件從來沒寫過這個世界長什麼樣)。人類要求順一遍。

第十一節從「腦力激盪清單」改寫成**世界設定**,骨架:

- **一句話**:海平面上升之後的未來地球,人類沒有在前進,只是在修補
- **核心動詞「修補」**——所有元素掛在同一個動詞上:城壞了要修維生系統(陌洲的職業)→ 零件來自回收場 → 誰配得到零件由機器廠決定 → 修要用電 → 能源配給 → 為什麼一直壞是因為汙染 → 修不動的人接進去(腦機接口/模擬倉)。
  選它的理由是工程的不是文學的:**維修永遠做不完,所以修補是一個結構性無限的線來源**。開拓型世界每開一條線都要生成新內容,修補型世界能在同一座城裡無限生線——對 lazy generation 的成本結構是大利多
- **三環尺度**:近景(一座港城,預先寫定不生成)/ 中景(鄰城與勢力,lazy 生成)/ 遠景(月球、星際、外星遺留物,**永不生成**,只以訊號傳聞存在)。
  這順帶給了第三節〈待解問題〉邊界判定「邊界判定」一個答案:**用環,不用距離**。已在待解問題那行註記,但沒有實作驗證所以仍留在待解
- **潮汐當世界時鐘**:現實時間同步在半沉沒港城裡最划算的形式不是鐘是潮汐。一條潮位公式、連 API 都不用,而且自己會產生 deadline(「漲潮前」),離線長度也有了世界內單位。佐證:上次真模型 playtest 自己長出「漲潮的預警鈴響到第三聲」——設定跟模型直覺對得上
- **配給額度取代貨幣**:電力、乾淨水、氧氣濾材、頻寬。每種都自帶稀缺 → 自帶線
- **技術軸是連續光譜**:人 → 改造人 → 接口使用者 → 無人格機械。最後一段是硬界線(不給名字、不能對話),理由是產品性的:核心賣點是「agent 是活的」,玩家不能分不清角色與道具
- **兩條自己會走的軸**:汙染(單向惡化的時鐘)與科技發展線(跨篇章的進度軸)。這兩條正是第十節要求的「不需要玩家的線」

原本的逐條判斷表移到**附錄**保留,避免之後有人重新提一次已經否決過的方向。第二節世界觀表加了一行導引,說明它只管版權與類型層級。

另外列出**還沒定的四件事**:港城專名、機器廠背後是誰、汙染的成因(這題會決定整個世界的道德色調)、山海經那條文化縱深怎麼在未來港城出現而不突兀。

一樣沒動碼、沒進 `TASKS.md`。

**下一步**:H-1 playtest(人類)。

**待人決定**:H-1 playtest;舊 commit 的 history 重寫指令仍待執行。

---

## 2026-08-03 — 補上劇本層與世界觀擴充的規劃(只動文件,沒動碼)

Agent: Claude Opus 5(互動 session)

人類指出「劇本要好多條線、篇章、留段落、action 都沒規劃」。查證後確認缺口屬實,但**沒有一個擋 H-1**:

- 現況:離線模擬每次最多 +1 條線、prompt 只帶最近 3 條、線只有 open/resolved 兩態、沒有篇章、沒有玩家動作
- 「線上對話不關線」比一開始判斷的輕:線會在**下一次離線模擬**時關掉,而那次 prompt 帶了最近記憶,所以看得到玩家線上答應過什麼(上次 playtest 的「零件」就是這樣接住的)。只剩同一 session 內可能重複提
- 殭屍線是真的洞:線超過 3 條之後進不了 prompt,而 `offline.py` 會把不在清單裡的 id 當幻覺濾掉 → 靜默堆積。但要 4 個離線週期才觸發,H-1 只跑 2 個,碰不到

**決定不動碼**。人類確認這是看碼發現的、不是 playtest 讀起來單薄。照 TASKS.md〈目前卡在哪〉,現在卡的是 H-1;缺的是規劃,不是 commit。也刻意**不寫進 `TASKS.md` 的 todo**——寫了排程 agent 會在 H-1 前就撿去做,那正好是規則要防的事。

設計文件新增兩節:

- **第十節 劇本層(提案,未定案)**——篇章 arc / 線 thread / 動作 action 三層。關鍵幾點:`arc.stage` 就是「寫到哪裡」的答案、線要分「需要玩家」與「不需要玩家」(全部都要回應等於每次上線被討債)、承諾要結構化成 `commitments` 讓下次離線模擬必須查驗。另訂**線的存量規則**(低於 2 條就要求多開、超過 5 條要求收束),避免某次沒開新線導致玩家下次回訪動機是零。仍維持一次 LLM 呼叫,不動搖 lazy simulation
- **第十一節 世界觀擴充(腦力激盪,未定案)**——人類連續丟了十幾個點子,逐條判斷。判準只有一條:**這個設定有沒有餵回劇本層?**

判斷摘要:腦機接口(最划算,把「離線」從系統限制變成世界設定)、機器改造身體、戰爭(屬篇章層,要局部化)、模擬倉、科技發展線(唯一直接餵長期性的跨篇章進度軸)採用;月球殖民地延後;星際跨越/銀河系降級成傳說(否則第三節〈待解問題〉邊界判定「邊界判定」無解);外星人只做「解不開的東西」;錢幣改 token **否決命名**(撞 LLM token + 儲值幣,且會被讀成加密貨幣,撞第六節〈已否決〉虛擬幣可兌現不可兌現的紅線),改用配給額度;世界裡再套一層虛擬世界否決(會洩掉「這裡是真的」這個唯一賣點);機器人要先訂死「無記憶 = 工具」的界線,否則稀釋核心賣點。

另外標了一個**尺度判準**:設定要能被「一個 agent 在一座城裡的一週」用到才算現在可用。據此把回收場 / 機器廠 / 汙染 / 能源 獨立成「近景素材」建議優先用——尺度對得上里程碑 0–1,而且每個都直接生線(汙染尤其好:單向惡化的背景時鐘)。

**下一步**:H-1 playtest(人類)。劇本層與世界觀擴充全部 gate 在 H-1 後面。

**待人決定**:H-1 playtest;舊 commit 的 history 重寫指令仍待執行。

---

## 2026-08-02 — 修掉「README 寫的指令根本跑不起來」

Agent: Claude Opus 5(互動 session)

人類照 README 跑 `python -m everliving.cli`,得到 `ModuleNotFoundError: No module named 'everliving'`。

原因:套件在 `src/everliving`,而 `pyproject.toml` 的 `pythonpath = ["src"]` **只對 pytest 生效**。所以 90 個測試全過,但實際指令一次都沒通過——**測試綠燈不等於使用者跑得起來**,這已經是這個專案第三次踩到同一類問題(前兩次是 `.env` 沒人讀、mock 掩蓋真實 SDK 契約)。

- `pyproject.toml` 補上 `[build-system]` / `[project]`,宣告相依(anthropic、openai)與 `dev` extra,改用 `pip install -e ".[dev]"`。
- 刪掉 `requirements.txt` / `requirements-dev.txt`——相依現在只有 pyproject 一個來源,免得兩邊漂移。
- 加了 `everliving` console script(不過 Scripts 目錄不一定在 PATH 上,README 仍以 `python -m everliving.cli` 為主)。
- pytest 保留 `pythonpath = ["src"]`,所以**測試不需要先安裝**就能跑,排程 agent 的流程不受影響。

順手記了一個 footgun:`everliving.db` 建在**執行指令的當前資料夾**。裝成套件後可以從任何地方跑,換資料夾等於開一個全新世界、agent 什麼都不記得——已寫進 README 警告。

驗證:從 repo 根目錄和從 `C:\Users\i8252` 各跑一次 `--help` 都正常,測試仍 90 passed。

**下一步**:H-1 playtest(人類)。

---

## 2026-08-02 — 接上 Groq,核心迴圈第一次用真模型跑通

Agent: Claude Opus 5(互動 session)

**先釐清一個混淆**:人類拿到的是 **Groq**(`api.groq.com`,高速跑開源模型的推論服務,有免費額度),不是 **Grok**(xAI 的模型)。兩者名字只差一個字母,是完全不同的公司。兩家都是 OpenAI 相容格式,所以共用同一個 `OpenAICompatibleClient`,只差 base_url / 環境變數 / 預設模型三個值。缺 key 時的錯誤訊息會偵測「你設的是另一個」並直接點出來。

**用真 API 實測選預設模型**(而不是猜)。拿實際的離線模擬 prompt 跑三個候選:

| 模型 | JSON | 評語 |
|---|---|---|
| `qwen/qwen3.6-27b` | ✅ | **最好**。文筆有畫面感,角色個性出得來,而且**只有它的懸念真的勾住玩家** |
| `openai/gpt-oss-120b` | ✅ | 可用,但 state_changes 的鍵名跑成英文,跟中文介面不一致 |
| `llama-3.3-70b-versatile` | ✅ | 敘事像流水帳,懸念(「商人來意不明」)沒有要求玩家做任何事 |

→ Groq 預設設成 Qwen。

**又抓到兩個只有跑才會現形的 bug**:

1. **`max_tokens` 被吃滿**。Qwen 那筆 `out=2048` 正好等於上限,代表被截斷——而離線模擬靠 JSON,截斷等於整包報廢。提高到 6000。`max_tokens` 是上限不是花費,只有真的生成才計費,調高不花錢。
2. **推理內容外洩到玩家眼前**。Qwen 是推理模型,會把 `<think>...</think>` 直接寫進回覆文字。Anthropic 那邊思考是獨立區塊型別、早就被 `extract_text` 濾掉,但 OpenAI 相容格式是**內嵌在文字裡**的。玩家會先讀到一大段模型自我推敲(還夾雜英文 "Let's refine slightly"),才看到角色台詞。新增 `strip_reasoning()`,以**最後一個** `</think>` 切分(有些服務會吐出沒有開頭標籤的結尾標籤)。

**核心迴圈第一次用真模型端對端跑通**。對話裡玩家答應「幫忙弄零件」,陌洲回「明早漲潮前帶過來」;隔 24 小時再開,離線敘事的開場是「漲潮的預警鈴響到第三聲時,你還沒露面」——臨時拼接的密封環、高洩壓風險、還有帶時間壓力與選項的懸念。**它接住了上一次對話的承諾**,這正是 T0-11 要驗證的東西。

測試 90 passed(新增 7 個 `strip_reasoning`)。

**下一步**:H-1 playtest。技術上已經證明會動,但**「想不想再打開來看」只有人類能判斷**,agent 不能代答。

**待人決定**:H-1 playtest;舊 commit 的 history 重寫指令仍待執行。

---

## 2026-08-02 — 加上 Grok 供應商支援

Agent: Claude Opus 5(互動 session)

Anthropic 額度還沒入帳,人類選擇加 Grok 當替代來源,不要被單一帳號的額度卡住 H-1。

- `GrokLLMClient`:走 xAI 的 OpenAI 相容端點(`https://api.x.ai/v1`),用 `openai` SDK。需要 `XAI_API_KEY`。
- `make_client(provider)` 工廠 + `--provider` 參數 + `EVERLIVING_PROVIDER` 環境變數。
- **選擇一律明示,不做自動偵測**。「哪邊有額度就用哪邊」聽起來方便,但偷偷換模型等於偷偷改變 playtest 在量什麼——H-1 要判斷的是敘事有沒有打中你,那個判斷跟哪個模型寫的直接相關。
- 錯誤對應抽成共用的 `translate_sdk_error()`。兩個 SDK 的例外階層剛好一模一樣,連 `AuthenticationError` 是 `APIStatusError` 子類別這個陷阱都一樣,不該複製兩份。
- 兩個形狀差異要處理:Anthropic 用 `system=` 參數,OpenAI 形狀要把它塞進 messages 當 system 角色;Anthropic 是 `usage.input_tokens`,OpenAI 是 `usage.prompt_tokens`。成本記錄兩邊都照常運作。
- 缺 `XAI_API_KEY` 時自己先擋下來——否則 OpenAI SDK 會報「找不到 OPENAI_API_KEY」,叫人去找一個根本不相干的變數。

測試 83 passed(新增 15 個)。實測 `--provider grok` 在沒 key 時會乾淨退出並指出要設哪個變數。

**H-1 的品質提醒**:Grok 是前沿模型,不是本機小模型,所以先前「模型太弱會污染判斷」的顧慮大致不適用。但**繁體中文的文學性敘事 Claude 通常比較穩**,而 H-1 要判斷的正是敘事有沒有打中你——如果用 Grok 跑完覺得普通,值得再用 Claude 跑一次對照,再下結論。

**下一步**:H-1 playtest(人類),兩家擇一。

**待人決定**:H-1 playtest;舊 commit 的 history 重寫指令仍待執行。

---

## 2026-08-02 — 第一次真的打 API,又抓到一個錯誤路徑

Agent: Claude Opus 5(互動 session)

人類設好 key 後叫我試跑。用**另一個 scratch DB**(不動 `everliving.db`,免得污染他自己的 H-1)實際打了一次真實 API,結果:額度還沒入帳,回 400「credit balance is too low」——而程式直接吐一整串 traceback。

又是同一類問題:**只有真的跑才會現形的錯誤路徑**。而且這一條的命中率極高,任何人第一次跑、還沒儲值就是這個畫面。

- 新增 `LLMUnavailable`:涵蓋額度不足、rate limit、伺服器錯誤、連不上。這些**改講法沒有用**,所以 CLI 直接印訊息並退出,不做重試迴圈(單人週末工具,重試迴圈是過度設計)。
- 從 `exc.body["error"]["message"]` 取伺服器原文——它通常已經寫清楚該怎麼辦(「go to Plans & Billing」),比我自己重寫一段有用。
- **例外攔截順序有陷阱**:`AuthenticationError` 是 `APIStatusError` 的子類別,必須先攔。有測試專門守這點。

測試 68 passed(新增 5 個 SDK 例外對應 + 1 個 CLI 端對端)。實際重跑確認:現在額度不足會印出乾淨的兩行提示,不是 traceback。

**目前已知的三種失敗都收斂了**:沒憑證 / 憑證被拒 → `LLMAuthError`;額度、限流、斷線 → `LLMUnavailable`;模型婉拒 → `LLMRefusal`(唯一可以繼續玩的一種)。

**下一步**:等額度入帳,H-1 playtest(人類)。

**待人決定**:儲值入帳後跑 H-1;舊 commit 的 history 重寫指令仍待執行。

---

## 2026-08-02 — 真的跑一次,抓到兩個只有跑才會出現的 bug

Agent: Claude Opus 5(互動 session)

人類去 console 儲值前,先把回來要用的東西備好。本機裝了 `anthropic`(0.120.2),然後**實際在沒有 key 的情況下跑一次 CLI**——結果抓到兩個測試抓不到、只有真的執行才會現形的 bug:

**1. 沒有 API key 時,CLI 會照常進入對話迴圈,友善錯誤訊息完全沒觸發。**
原本的 `try/except` 包在 `AnthropicLLMClient()` 建構上,但 SDK 的憑證解析是**延遲**的——沒有 key 也能順利建構,要等到第一次真的呼叫才炸。使用者體驗會是:打完第一句話,吃到一整串 raw traceback。

而且 SDK 丟的是**兩種不同的例外**:
- 完全找不到憑證 → 從 header 驗證丟出的**純 `TypeError`**(根本沒送出請求)
- 有 key 但被伺服器拒絕(401)→ `anthropic.AuthenticationError`

→ 新增 `LLMAuthError`,兩種都對應過去。`TypeError` 那條有用訊息內容做篩選,避免把我們自己程式裡真正的 `TypeError` 吃掉(有測試守著這點)。CLI 兩個呼叫點都接住,印出「設定 ANTHROPIC_API_KEY 或用 `ant auth login`」然後乾淨退出。

**2. 修第一個 bug 的過程中,自己製造了第二個。**
`_exit_no_credentials()` 裡順手 `conn.close()`,結果 `finally` 區塊接著要寫 `last_seen` → `sqlite3.ProgrammingError: Cannot operate on a closed database`。連線清理本來就歸 `finally` 管,那行 close 是多餘且有害的,已移除並在 docstring 註明原因。

測試 58 passed(新增 5 個:CLI 兩條無憑證路徑、兩種 SDK 例外的對應、以及「無關的 TypeError 仍要往上拋」)。

**這一輪的教訓**:前一輪的 53 個測試全過,但這兩個 bug 一個都沒抓到——因為它們都在「跟真實 SDK 的介面」上,而測試裡的 SDK 是我自己寫的假的。**mock 只能驗證我以為的契約,不能驗證真實的契約。**

**下一步**:H-1 playtest(人類)。`anthropic` 已裝好,儲值完設好 key 就能直接跑。

**待人決定**:H-1 playtest;舊 commit 的 history 重寫指令仍待執行。

---

## 2026-08-02 — 修掉 LLM 回應處理的真實 bug(in-scope 稽核)

Agent: Claude Opus 5(互動 session)

專案卡在 H-1,所以這輪只做自己定義的 in-scope 類別(修 bug / 測試 / 摩擦 / 文件),沒有加新功能。對照 Anthropic API 的實際契約稽核 `llm.py`,抓到一個**會當掉的真 bug**:

- **`response.content[0].text` 是錯的**。回應的 content 是「區塊清單」,不保證第 0 塊是文字。Claude 5 系列**預設就會思考**,第 0 塊是 thinking 區塊、根本沒有 `.text` → `AttributeError`。而 README 明講可以用 `EVERLIVING_MODEL` 換模型,等於我自己開了一條會炸的路。
  → 改成 `extract_text()`:只挑 `type == "text"` 的區塊串起來,thinking / tool 區塊直接跳過。
- **`max_tokens=512` 對會思考的模型太緊**——那個上限是「思考 + 回覆」共用的,會在句子中間被截斷。提高到 2048(回覆本身只有 2-4 句,多的是給思考的餘裕)。
- **沒有處理 `stop_reason == "refusal"`**。模型婉拒時是正常的 HTTP 200,content 可能是空的——照舊寫法會靜靜地拿到空字串或當掉。
  → 新增 `LLMRefusal` 例外;CLI 兩處都接住:對話中拒絕只印一行提示並繼續(session 不會中斷),離線敘事被拒絕則跳過敘事、照常進入對話迴圈。
- 模型 ID 從寫死日期的 `claude-haiku-4-5-20251001` 改成別名 `claude-haiku-4-5`。

也順手驗證了一個**不是問題的問題**:擔心 Windows 主控台編碼會讓中文輸出爆掉,實測 cp950(Big5)完整涵蓋 CLI 用到的所有字元,所以**沒有改**——不修不存在的問題。

測試 53 passed(新增 9 個:文字擷取跳過 thinking/tool 區塊、多區塊串接、拒絕例外型別、CLI 兩條拒絕路徑各一個端對端測試)。

**下一步**:仍然是 H-1 playtest(人類)。

**待人決定**:H-1 playtest;舊 commit 的 history 重寫指令仍待執行。

---

## 2026-08-02 — T0-11:離線期間真的會產生後果(H-2 已處理)

Agent: Claude Opus 5(互動 session)

人類選擇「先把離線敘事做厚再跑 playtest」,理由是假陰性會殺掉一個好點子。已完成。

**核心改動**:離線模擬從「一次無狀態呼叫產生散文」變成「一次呼叫產生結構化後果」。

- 新 schema:`agent_state`(agent 目前有什麼/是什麼狀態)、`open_threads`(懸而未決、需要玩家回應的事)
- `simulate_offline_period()` 回傳 `OfflineResult`(narrative / events / state_changes / open_thread / resolved_thread_ids),並把後果全部寫進 DB
- **迴圈閉合**:下次對話時 `respond()` 會把目前狀態與未解決的懸念帶進 prompt,system prompt 也會指示 agent 自然地提起它在等玩家的事。下次離線模擬也會拿到這些,所以連續性會累積
- CLI 會分區塊顯示:敘事 / 有些事情變了 / **[ 有件事在等你 ]**
- **穩健性**:JSON 解析失敗時退化成純敘事——玩家要讀的東西永遠不會弄丟。幻覺出來的 thread id 不會碰到 DB
- 測試 44 passed。舊的純散文測試仍然通過,正好驗證了退化路徑

**為什麼這是關鍵**:原本玩家讀完敘事之後什麼都沒發生,明天打開會是另一段同樣沒後果的散文。現在離線期間會改變世界、並留下一件在等玩家的事——這才對得起「**故事仍然會發生**」這個差異化主張。日記不是故事。

**下一步**:H-1 playtest(人類)。`TASKS.md` 有四步驟的跑法,重點是第 4 步——看它有沒有接住你上次的回應。

**待人決定**:
- **H-1 playtest** — 現在測的是有後果的版本了
- 排程 agent 仍建議等 H-1 有結論再建
- 舊 commit 的 history 重寫指令仍待人手動執行

---

## 2026-08-02 — 重新檢視整個規劃(重要)

Agent: Claude Opus 5(互動 session)

重新審視後,原本的規劃有三個問題,已修正:

**1. 瓶頸不是額度,是還沒被驗證的方向。**
里程碑 0 程式碼完成後,唯一擋著的是 H-1 真人 playtest,那是只有人類能做的事。但原本設計的每日排程是 planner 每天補 backlog + builder loop 到清空為止——這台機器的結構性誘因就是「一定要找到事做」,遲早會漂進里程碑 1 的範圍,違反設計文件第八節〈週末專案的現實提醒〉。
→ 已改:`AGENTS.md` 新增最高優先規則「今天沒事該做是正確結果」,planner 不再負責「讓 backlog 有貨」;`TASKS.md` 新增「目前卡在哪」區塊明列 in-scope 的工作類別。

**2. 目前的里程碑 0 可能不是一個公平的驗證。**
離線敘事是一次無狀態的 LLM 呼叫,產出 2-4 句散文。玩家讀完之後:世界沒有變、agent 沒得到或失去什麼、沒有任何需要玩家回應的懸念。明天打開會是另一段同樣沒有後果的散文。
但差異化主張是「**故事仍然會發生**」——日記不是故事。
風險:H-1 失敗時誤判成「核心賭注錯了」,其實只是實作太薄測不出賭注。**用壞的測試殺掉好的點子是這裡最貴的錯誤。**
→ 已記為 `TASKS.md` 的 **H-2**,等 H-1 的實際感受出來再決定要不要補「離線期間產生狀態變化與懸念」。目前**沒有**擅自實作。

**3. 設計文件要求的兩個數字一個都量不到。**
→ 已補 T0-10:`llm_calls` table 記錄每次呼叫的 model/input/output tokens,`--cost-report` 印每日用量。只存 token 不存金額(單價會變,token 是不會過期的事實)。

另外老實承認:AGENTS.md / TASKS.md / planner-builder 分工這整套多 agent 協作機制,對一個還沒驗證好不好玩的專案來說是過度建設,跟「先不碰 Unity/VR/金流」是同一個陷阱。已縮減其強制性,但保留檔案本身(多廠商協作仍是你要的)。

**這個 session 實際交付**:
- T0-9 `--offline-hours N`:直接假裝離開 N 小時,不用真的等隔夜就能跑 playtest(直接打瓶頸)
- T0-10 成本量測:`llm_calls` table + `log_usage()` + `--cost-report`
- 測試 32 passed。過程中既有的 `test_cli.py` 抓到 argparse 會誤讀 pytest 自己的 `-q`,已修
- 預設模型改成便宜的 Haiku,可用 `EVERLIVING_MODEL` 覆蓋

**下一步**:H-1 playtest(人類)。現在一次 session 就能跑完,不用隔夜。

**待人決定**:
- **H-1 playtest** — `python -m everliving.cli` 聊幾句 → 離開 → `--offline-hours 24` 讀敘事 → 想不想再打開?
- **H-2** — 如果感想是「還好」,先分清楚是賭注錯還是實作太薄(見上)
- 排程 agent 仍需先到 https://claude.ai/code/routines 連結 GitHub。但**建議等 H-1 有結論再建**——現在建了它也沒有合法的事情可做
- 舊 commit 的 history 重寫指令仍待人手動執行

---

## 2026-08-02 — T0-4/T0-5/T0-6/T0-8:里程碑 0 的 T0-* 全部完成

Agent: Claude Sonnet 5(互動 session,非排程)

- `src/everliving/offline.py`:`time_since_last_seen()`(算離線多久)、`generate_offline_narrative()`(一次 LLM 呼叫產生敘事,存成 `offline_narrative` 記憶事件)
- `cli.py` 接上完整流程:啟動時先算離線時間 → 有的話生成並印出敘事 → 進入互動迴圈 → `finally` 區塊保證離開時一定寫 `last_seen`(含 Ctrl+C/EOF)
- `tests/test_offline.py`:9 個測試,涵蓋時間差計算、時長格式化、敘事生成與記憶寫入、prompt 內容
- `tests/test_cli.py`:**端對端**測試,monkeypatch 掉 `AnthropicLLMClient` 跟 `input()`,實際跑兩次 `cli.main()` 模擬「關掉隔天打開」——第一次沒有敘事、第二次真的印出敘事。這是里程碑 0 核心體驗第一次被完整驗證過
- `requirements.txt`(`anthropic`)、`.env.example`、README 補執行說明
- 全部測試:`python -m pytest -q` → **22 passed**,不需要真的 API key

**里程碑 0 的 T0-1 ~ T0-8 全部 `done`。** 剩下唯一沒做的是 `TASKS.md` 裡的 **H-1 真人 playtest**——這個不能由 agent 代跑,需要人自己設定 `ANTHROPIC_API_KEY`、真的跑 `python -m everliving.cli`、關掉隔天(或至少過一段時間)重開,看看想不想回來看敘事。這是整個里程碑 0 唯一的成敗判準。

**下一步**:等人類完成 H-1 playtest 並給出「還想不想回來」的判斷。在那之前,agent(不管排程或互動)不應該提前規劃或動手做里程碑 1 的東西(多 agent)。

**待人決定**:
- **H-1 playtest**——上面說的,只有你能做。
- 排程 agent(everliving-planner / everliving-builder)還沒建成,需要先到 https://claude.ai/code/routines 連結 GitHub 帳號。
- 舊 commit(`122c070`/`4013c61`/`32e18cf`)的 history 重寫指令仍待人手動執行確認。

---

## 2026-08-02 — T0-3 / T0-7:互動迴圈 + 可 mock 的 LLM 介面

Agent: Claude Sonnet 5(互動 session,非排程)

- `src/everliving/llm.py`:`LLMClient` Protocol + `AnthropicLLMClient`(lazy import `anthropic`,測試不需要裝這個套件)
- `src/everliving/agent_loop.py`:`build_system_prompt()` 組 persona + `respond()` 一次對話回合(組 prompt → 呼叫 LLM → 把「玩家說」「我回答」各存一筆記憶事件)
- `src/everliving/cli.py`:`python -m everliving.cli` 的互動迴圈,啟動時 seed persona、用 `AnthropicLLMClient`,缺 API key 會印清楚的錯誤訊息而不是 stack trace
- `tests/conftest.py`:共用的 `conn`(記憶體 SQLite)、`fake_llm`(記錄每次呼叫的假 client)fixture,順手把 `test_db.py`/`test_persona.py` 裡重複的 `conn` fixture拿掉
- `tests/test_agent_loop.py`:4 個測試,涵蓋 system prompt 內容、回覆會寫回記憶、近期記憶會被帶進 prompt、未知 agent_id 會丟例外。`python -m pytest -q` 全過(11 passed)

**下一步**:T0-4(離線時間追蹤,接進 cli.py 的啟動/離開流程)→ T0-5(離線敘事生成)→ T0-6(上線讀敘事)。T0-4/T0-5/T0-6 有依賴順序,建議照順序做。

**待人決定**:同前一則——排程 agent 需要人到 https://claude.ai/code/routines 連結 GitHub;舊 commit 的 history 重寫指令待執行。

---

## 2026-08-02 — T0-1 / T0-2:schema + persona

Agent: Claude Sonnet 5(互動 session,非排程)

- 建立 `src/everliving/db.py`:SQLite schema(`agents`、`memory_events`、`player_state` 三張表)+ CRUD 函式(create_agent、get_agent、add_memory_event、get_recent_memory、set_last_seen/get_last_seen)
- 建立 `src/everliving/persona.py`:寫死一個原創角色「陌洲」(未來港城技師),`seed_default_agent()` 冪等寫入
- `tests/test_db.py`、`tests/test_persona.py`:7 個測試,涵蓋建立/查詢、記憶依時間排序與依 agent 隔離、last_seen 讀寫、persona seed 冪等性。`python -m pytest -q` 全過(7 passed)
- 加 `pyproject.toml`(pytest pythonpath 設定)、`requirements-dev.txt`

**下一步**:T0-3 互動迴圈(CLI 對話,呼叫 LLM 產生回應並寫入記憶)。這一步需要決定 LLM 呼叫怎麼包裝成可 mock 的介面(對應 T0-7),建議兩個一起做。

**待人決定**:
- 排程雲端 agent(everliving-planner / everliving-builder)建立失敗,因為 claude.ai 的 routine 平台需要另外連結 GitHub 帳號(跟本機 `gh` CLI 登入是分開的)。需要人到 https://claude.ai/code/routines 連結 GitHub 後,才能重建這兩個排程。
- 舊的 git commit(`122c070`/`4013c61`/`32e18cf`)裡還殘留已脫敏移除的商業策略/防濫用細節,已提供人類手動執行的 history 重寫指令(orphan branch + force push),尚待執行確認。

---

## 2026-08-02 — 專案啟動

- 建立 repo `everliving`,搬入既有設計文件
- 加入 `README.md`、`CLAUDE.md` 給後續協作 agent 用
- 設定每日自動排程,推進里程碑 0

**下一步**:開始寫里程碑 0 的最小原型——SQLite schema(agent 人設 + 事件記憶)、一個能對話的 agent、離線期間敘事生成的 LLM 呼叫。

**待人決定**:無
