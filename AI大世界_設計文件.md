# AI 大世界 — 專案設計文件

> 本文件收攏設計討論中已定案的決策、待解問題與分階段路線。
> 狀態:設計階段 · 執行形式:週末專案(一人)

---

## 一、核心概念

一個**持續存在的虛擬世界**,設定在**未來的地球**(現實地理基礎 + 自編未來)。

真人玩家與 AI Agent 共存於同一世界。玩家離線時,他的 Agent 繼續在世界裡活著;上線時能讀到「我不在的這段期間發生了什麼」。

世界會由 AI 持續向外構築,不是預先做完的固定地圖。

### 真正的差異化與產品定位

完整內容(核心差異化主張、產品定位判準)另存本機 `.private/strategy-notes.md`,不隨 repo 公開。

---

## 二、已定案的決策

### 世界觀

> 這裡只放**版權與類型層級**的決策。世界實際長什麼樣(修補、港城、潮汐、配給、三環尺度)在**第十一節**。

| 項目 | 決策 | 理由 |
|---|---|---|
| 設定 | 未來地球 | 現實地理當骨架,自編未來完全避開版權 |
| 素材來源 | 公有領域 + 原創 | 《山海經》《西遊記》等當文化縱深,角色與專名全原創 |
| 多類型融合 | 同一地球的不同區域/勢力 | 比硬接兩個宇宙自然,衝突仲裁容易得多 |
| 借用現有小說 IP | **不採用** | 商業販售 + 即時生成他人角色內容 = 最高風險形態 |

**版權原則**:保護的是「表達」不是「點子」。類型慣例、設定原理可借;角色、專有名詞、具體情節不可。AI 生成不會洗白版權責任。

**放棄既有 IP 反而更好**:玩家對熟悉角色的行為期待極高,AI 稍有偏差就是「崩人設」——等於自設一個永遠達不到的標準。原創角色沒有這個包袱。且世界構築 Agent 需要的是可延展的規則,不是綁死的既定事實。

### 架構

- **主世界持有權威狀態**——記憶正本、canon 判定、寫入權都在伺服器
- **玩家節點是進階選項**,只做快取與本機推論,不是核心體驗前提
- **節點是提案者,主世界是守門員**——客戶端永遠不可信
- **Lazy simulation**——離線期間不做完整模擬,玩家上線前用一次 LLM 呼叫生成該期間敘事。這是整個成本結構最關鍵的一招
- **在線期間沒有背景自動行為**——這是 Lazy simulation 的另一面:玩家在線時,agent 不會自己跑背景 tick,所有回應都是被玩家動作觸發,且每一輪都直接寫進記憶(里程碑 0 已如此實作)。之後若做多 agent 間的背景互動,規則會是「玩家一上線,該 agent 的背景 tick 要暫停」,不能讓背景模擬跟即時互動同時對同一個 agent 寫記憶
- **世界構築走 work queue**——現在 worker 跑在本機,之後可外包
- **Lazy generation**——只在有人接近未生成邊界時才觸發,不背景填滿世界

### Agent 記憶

- 分層:近期原始事件 / 反思壓縮的洞察 / 核心人設
- 檢索靠 相關度 + 新近度 + 重要性 加權,不全塞 context
- **記憶要有主觀性**:同一件事,A 記成「他救了我」,B 記成「他多管閒事」。這不是 bug,是世界深度的來源
- 遺忘是必要的——控制成本,也讓 Agent 更像人。**第十一節給了它一個世界內的解釋**:記憶儲存是配給品,忘記事情是買不起額度,不是系統做不到

### 玩家所有權

- 記憶存主世界(保住離線模擬能力與防竄改)
- **提供完整匯出**:人設 + 完整記憶 + 時間戳權重 + 世界足跡,開放 JSON 格式附 schema
- **匯入需驗簽**(HMAC 或簽章),防止玩家自行編輯記憶
- 完全開放匯入 = 有人會塞「我是世界之王」的記憶,別的 Agent 會信以為真並跟著行動 → 不採用

匯出格式開放的副作用:玩家理論上可把 Agent 帶到別處。這通常會變成生態護城河而非競爭劣勢(參考 Minecraft 存檔)。

### 經濟系統

雙貨幣模型與計費機制的完整設計另存本機 `.private/strategy-notes.md`,不隨 repo 公開。核心工程要求(雙分錄記帳、webhook idempotency)會在實作里程碑三時再對外公開對應的技術規格。

### 帳號與防濫用

- 目標是**帳號綁定(可追溯)+ 防小號**,**不做實名制**
- Agent 永久指向一個帳號 ID,不可轉移;營運方永遠查得到,但世界內預設不顯示真人身分

不做實名制的理由:與「下班隨手玩」的低摩擦定位衝突、Steam 上無先例、且讓自己變成高風險資料持有者(個資法遵 + 外洩標靶)。

防小號優先順序:完整順序另存本機 `.private/strategy-notes.md`,不隨 repo 公開(公開偵測順序等於告訴刷幣工作室怎麼繞過前幾層)。

> 目標不是「零小號」,是「開小號的成本高過收益」。追求完全杜絕會傷到正常玩家。

### 連結現實世界

| 層級 | 內容 | 決策 |
|---|---|---|
| 一:現實資料輸入 | 天氣、時間、地理 | **採用**,成本近零、沉浸感提升大 |
| 二:虛實價值兌換 | 世界內成果換現實價值 | 只做「真錢→虛擬幣」單向 |
| 三:Agent 現實行動能力 | 代訂、下單、對外發訊息 | **第一版完全不碰** |

**層級一最划算的是「時間」**:世界時間與現實同步,Agent 就有真實作息、季節、事件週期,成本近乎為零。天氣次之(中央氣象署有開放 API)。新聞事件要小心——真實新聞灌進虛構世界易產生尷尬內容,只取抽象化訊號。

**層級三為何危險**:世界裡有真人也有別人的 Agent。若 Agent 有現實行動權限,A 玩家就能透過遊戲內對話操縱 B 的 Agent 在現實中做事(prompt injection)。而「別人可以對我的 Agent 講話」正是這個世界的核心設計。

未來若要做,原則是:現實行動一律真人逐次確認,不給 standing authorization;世界內來的文字永遠當「資料」不當「指令」。

### 視覺化與 VR

- 目標:畫面驅動,之後綁 VR
- **VR 是未來目標,不是第一版**——先做平面 3D,引擎選 Unity(VR 生態成熟、C# 迭代快、程序化工具鏈完整)
- **模組化資產庫 + 程序化組裝**:世界構築 Agent 產出的不是 3D 模型,而是**組裝指令**(用哪些元件、怎麼擺、什麼氛圍)。AI 負責設計,引擎負責呈現
- **美術風格向抽象/風格化靠**——越寫實,AI 組裝的破綻越明顯

> 這就是 Minecraft 的邏輯:方塊是固定的,世界是無限的。只是把隨機演算法換成 AI 敘事驅動。

**VR 的張力**:戴頭盔的心理門檻比開網頁高很多,與「下班 20 分鐘」有衝突,且用戶基數少一個數量級。

### 生成畫面與聲音

- **畫面盡量往「預先生成、可重用」靠**——世界構築時順便生成該地區視覺並存檔,玩家共用。不用等、成本可控、可審核
- **聲音天生即時**——建議每個 Agent 固定音色、只在重要對話配音,或做成虛擬幣開啟的功能(成本與收入直接掛鉤)
- **內容安全三層必做**:prompt 端過濾 + 輸出端分類器審核 + 玩家檢舉管道

### 平台

**先自建網頁版,養出核心玩家與數據,再考慮 Steam。**

Steam 的問題:
- 演算法吃早期爆發,但這是「玩一段時間才體會價值」的產品
- AI 揭露標籤公開顯示,部分玩家直接跳過
- 抽 30% + 金流綁 Steam Wallet,而你不像一般遊戲賣完就沒成本——每收一筆錢背後還有持續 API 帳單
- 上架曝光窗口是一次性機會,產品沒打磨好就浪費了

但**畫面驅動 + VR 會反轉這個結論**——SteamVR 是 PC VR 的實質標準。所以最終仍可能上 Steam,只是不該是第一站。

Discord 因為承載不了畫面驅動而出局。

### Steam 上架須知(未來參考)

- 屬 **Tier 2 即時生成**,揭露要求最高:需說明模型、內容過濾方式、並明確讓玩家知道對話是 AI 生成
- 需建立防護機制與玩家回報管道,**無適當防護有下架風險**
- **絕對紅線**:即時 AI 生成的成人性內容無任何核准途徑
- AI coding assistant 寫的程式碼**明確豁免**,不用揭露
- Valve 明講:即時 AI 的營運成本要開發者自己透過定價/微交易/訂閱管理

---

## 三、待解問題

- [ ] **跨區域世界觀衝突怎麼仲裁**——不同類型設定相遇時的規則層(未來地球設定已大幅降低難度)
- [ ] **邊界判定**——什麼條件觸發生成新內容。**第十一節提了一個答案:用「環」而不是用距離**(近景預先寫定 / 中景 lazy 生成 / 遠景永不生成)。還沒實作驗證,先留在待解
- [ ] **核心循環具體化**——玩家上線後前五分鐘看到什麼、做什麼(決定留存)
- [ ] **劇本層(篇章 / 多線 / 動作)**——見第十節。里程碑 0 只有「單線 + 兩態」,不足以撐長期敘事。**閘門在 H-1**:先確認「回來一次會想再開」,再決定劇本層要做多大
- [ ] **虛擬幣具體消費場景**——Agent tick 成本 / 玩家間交易 / 道具 / 語音功能
- [ ] **主世界與私人節點的協定**
- [ ] **玩家自己的角色設定**——多人上線後(里程碑三),玩家自己的化身要能設定名稱/聲音/面部等基本項目,這跟「AI agent 的 persona」是不同的東西,要分開設計。視覺/聲音客製化本身要等里程碑四(視覺化)才有意義

---

## 四、分階段路線

> 分階段的重點不是排功能清單,是**每階段回答一個決定要不要繼續的問題**。

### 里程碑 0(兩個週末)— 驗證核心賭注

**問題:「離線時 Agent 替我活著,上線讀到發生了什麼」會不會讓人想回來?**

做:
- 一個 Agent、一個玩家
- 記憶存 SQLite(不用 vector DB)
- 關掉,隔天打開
- 一次 LLM 呼叫生成「這段時間我做了什麼」
- 讀到那段敘事

不做:世界地圖、多角色、前端(命令列即可)、向量檢索。

**你自己就是第一個測試者。** 如果隔天真的很想打開來看它做了什麼,方向就對了。如果自己都覺得「還好」,回頭改設計——而你只花了兩個週末。

### 里程碑 1 — 多 Agent 有沒有火花
三個 Agent 互相認識,產生你沒預料到的互動。

### 里程碑 2 — 長期性
記憶累積一個月後,Agent 有沒有變得更有個性。

### 里程碑 3 — 多人
找兩三個朋友一起玩。

### 之後的階段

| 階段 | 要驗證的問題 | 內容 |
|---|---|---|
| 二 | 世界會不會自己長大 | 世界構築 Agent、work queue、一致性檢查 |
| 三 | 有人願意付錢嗎 | 雙貨幣、儲值、防小號 |
| 四 | 視覺化 | 資產庫、程序化組裝、Unity |
| 五 | 錦上添花 | VR、私人節點、人格投射 |

**必須量出的兩個數字**(階段一就要):
1. 每個玩家每天燒多少 API 錢
2. 第七天還有多少人回來

這兩個數字決定定價與商業模式是否成立。**沒有真實用量曲線就買 GPU,基本上是在猜。**

---

## 五、架構預留清單

> 原則:**架構上預留,實作上延後。** 這些現在做幾乎不花時間,沒做之後要拆會很痛。

- [ ] 生成任務走隊列,不要寫成「偵測到邊界 → 直接呼叫 LLM」
- [ ] Work unit 自我包含——帶著全部 context,worker 不直接查資料庫(以後下放不用給知識庫存取權)
- [ ] 一致性檢查與生成分開——生成可外包,審核永遠留在守門員這邊
- [ ] 世界狀態用**座標**表示而非純文字位置描述(為之後的 3D)
- [ ] 記憶資料結構從一開始就設計成可完整匯出
- [ ] 統一「居民」介面——真人與 AI Agent 走同一套(位置、感知、動作佇列、聊天),世界伺服器不需知道對方是人是 AI
- [ ] **敘事帶 `scene` 標籤**——見下方。**現在只定規格,不寫進 code**(gate 在 H-1 之後)

### `scene` 標籤與預先生成圖庫

第一版畫面**不是「幫每段敘事生一張圖」**。那條路是 per-call 成本 + 每張都要審 + 玩家要等,而且違反第 105 行的「預先生成、可重用」。

正確的第一版:

1. 一組**固定場景圖** —— 港城全景、回收場、機器廠、潮線、工作間、清洗間,約 6–10 張
2. 離線模擬的 JSON 多一個欄位:`"scene": "回收場"`(值域是封閉清單,不讓模型自由發明)
3. 顯示端查表貼圖

| 項目 | 成本 |
|---|---|
| LLM | 一個欄位,幾個 token |
| 圖片 | **一次性**,而且**不需要接任何圖片 API** —— 用任何工具做一次存成檔案即可 |
| per-call | **零** |

**為什麼現在不寫進 code**:多一個輸出欄位會佔 prompt、可能影響敘事品質,而敘事品質正是 H-1 要量的東西。更根本的是,**同時加圖會讓 H-1 測不準**——分不清打動你的是敘事還是圖。這跟「不做 provider 自動偵測」是同一條原則:別偷偷改變 playtest 在量什麼。

參見第 231 行:畫面救不了不好玩的循環。純文字打不中,加圖只會延後你發現這件事。

---

## 六、已否決 / 暫緩的方向

| 方向 | 狀態 | 理由 |
|---|---|---|
| 自架 GPU | **暫緩** | 負載突發但 GPU 24h 折舊;模型迭代快;轉折點很晚。先用 API 量出成本曲線 |
| 分散式推論(借玩家 10% 算力) | **不可行** | LLM 推論延遲敏感、模型放不下、VRAM 不能只借 10%、結果無法驗證 |
| 分散式**世界構築** | **可行但延後** | 符合「獨立、可延遲、結果可驗證」三條件。但早期生成量可能一天幾百次、成本幾塊美金,而建可靠派發系統要好幾週 |
| 玩家本機跑自己的 Agent | **降級為進階選項** | 一般上班族沒有能跑模型的顯卡,與「大家都可以玩」矛盾 |
| 實名制 | **否決** | 見上方帳號章節 |
| 虛擬幣可兌現 | **否決** | 法規 + 生態毀滅風險 |

**替代方案**:小模型生成草稿 + 大模型審核潤飾。生成很花 token,審核相對便宜——把貴的那半外包,便宜的那半自己做。

---

## 七、參考專案

- **AI Town**(a16z-infra, MIT)——虛擬小鎮的生成式 Agent 即時生活/移動/聊天。JS/TS(非 Python)、PixiJS 渲染、Convex 管狀態與向量搜尋、可用 Ollama 本機推論。角色/故事/地圖皆可自訂。有中文 fork `ai-town-cn`
- **Stanford Generative Agents**(Park et al.)——記憶/反思/規劃架構的原始設計,值得讀論文
- **Mindcraft / Voyager**——若要在 Minecraft 內跑 LLM agent(voxel 世界難承載敘事,參考價值有限)

> **但里程碑 0 不要從 AI Town 開始。** 光是跑起來、讀懂 Convex 架構就會吃掉好幾個週末,而那些時間沒在驗證核心賭注。先用最土的方式自己寫兩百行驗證完,確定方向對了再考慮接骨架。

---

## 八、週末專案的現實提醒

- **先不碰 Unity、VR、金流、帳號系統**——這些是「做完了但還是沒有好玩的東西」的陷阱
- **技術選型選最熟的**——沒有學新框架的預算。Python + SQLite + 一個 LLM API 足以跑完前三個里程碑
- **每階段結束要真的檢視**,不要自動往下走。留存差就回頭改核心循環,不要安慰自己「等有畫面就會好了」——畫面救不了不好玩的循環
- **接受這會做很久**。週末專案的優勢是沒有截止日,劣勢也是沒有截止日。小里程碑 → 做完慶祝 → 下一個,比宏大計畫可靠

> 完整願景是好的北極星——但北極星是用來指方向的,不是這個週末要抵達的地方。

---

## 九、人格投射(遠期構想)

玩家不在線時,系統用其過往行為與對話學出一個「像他的」Agent 繼續替他活著。技術上是現有記憶系統的自然延伸(把人設來源從原創設定換成真人實際行為)。

技術不難,難的是這些:

- **知情同意**——玩家要清楚知道行為正在被學習,且學出的東西會代表他跟別人互動
- **別人有沒有權知道**——「這是不是本人」與「這人是誰」是不同的事。前者應該可分辨,否則玩家會在不知情下跟模仿品建立關係
- **退出機制**——應為無條件權利,且要真的刪乾淨
- **玩家離開或過世時怎麼辦**——持續存在的世界 + 學習真人的 Agent,遲早會遇到。事先想好比事後處理好

---

## 十、劇本層(提案,未定案)

> **閘門:H-1 通過前不要實作。** H-1 問的是「回來一次會不會想再開」,劇本層問的是「怎麼讓人一直回來」。順序反了就是在還沒驗證的方向上加碼。
>
> 里程碑 0 的現況:離線模擬每次最多產生 **1 條**懸念、prompt 只帶最近 **3 條**、線只有 open/resolved **兩態**、沒有篇章、沒有玩家動作。夠驗證核心賭注,不足以撐長期敘事。

### 三層結構

| 層 | 是什麼 | 關鍵設計 |
|---|---|---|
| **篇章 arc** | 一個母題,底下掛 3–5 條線。有 `premise` 與 `stage`(開場/升溫/轉折/收束) | `stage` 就是「寫到哪裡」的答案——prompt 帶「目前在升溫」,模型才知道該推進還是該收。收束時寫一則「章末回顧」記憶,那是玩家讀得到的**段落感** |
| **線 thread** | 現有 `open_threads` 加 `arc_id` / `kind` / `deadline` / `stage` | `kind` 必須分**需要玩家**與**不需要玩家**。全部都要玩家回應 = 每次上線都在被討債;有些線要在背景自己走完,玩家只是讀到——「世界在活著」的感覺來自這種線 |
| **動作 action** | 敘事末尾給 2–3 個具體選項,選了寫進 `commitments` | 選項不鎖死自由輸入,只降低「我該講什麼」的摩擦。承諾結構化之後,下次離線模擬**必須**檢查「他做到了沒」——里程碑 0 是靠記憶文字碰運氣接住的 |

### 線的存量規則

**線快用完就要主動補。** 離線模擬前先數還開著幾條:低於下限(暫定 2 條)時,prompt 明確要求這次多開幾條新線,而不是照常最多 +1。反過來,線太多(暫定 5 條以上)時要求優先收束而非開新的。

理由:目前「每次最多 +1、且常被 resolve」的穩態大約只有 1–2 條同時開著,一旦某次沒開新線,玩家下次上線就沒有任何懸念在等他——那次的回訪動機是零。存量規則讓劇本不會斷氣。

### 隨機性:骰子要在程式裡,不在模型裡

現況的「隨機」**全部來自 LLM 取樣,而那是不夠的隨機**:

- **模式坍縮**——同一個 prompt 反覆跑,模型會一直產生同一類事件(十次離線可能七次是「有人來找我修東西」)
- **H-1 測不出這個問題**,因為只跑 2 個離線週期。這是這份設計裡少數「驗證方法看不見的風險」,要先寫下來
- 調高溫度只會更亂,不會更多樣,而且傷敘事品質

正確做法是把隨機**移到 prompt 之外**,用近乎零成本的方式注入:

1. **事件種子表**——一組粗顆粒的事件類型,程式抽 1–2 個丟進 prompt 當約束:「這段時間有一件事跟**失去**有關」「一個**陌生人**進入你的生活」「一件你以為結束的事**回來了**」「你**做錯了一件事**」。
   **模型負責寫,程式負責決定是哪一類。** 成本是換掉幾個字,不增加呼叫
2. **加權而非均勻**——依 `agent_state` 調權重(手受傷 → 身體類事件權重上升)。這讓隨機讀起來像因果
3. **世界層級的隨機**——潮汐、汙染、能源配給的波動由程式算好丟進 prompt。「這週斷電三次」是程式決定的,不是模型編的 → 可重現、成本可控,而且**里程碑 1 之後跨 agent 一致**(大家共用同一個世界事件,才會有共同話題)
4. **免費的真隨機:現實天氣**(第 86 行已決定要接)。潮汐可預測,天氣不可預測——這是不用付錢、而且全服共享的熵

**兩條禁止:**

- **隨機只能動事件,不能動 canon。** 誰死了、哪個勢力垮了不可以擲骰,那是一致性檢查(第 177 行)要守的
- **隨機事件必須接得住既有的線,不是打斷它。** 骰子決定的是這次的**類型與色調**,發生什麼仍由線決定。否則會變成一串互不相干的隨機事件——那比重複更糟

> **最好的隨機來源其實已經有了:玩家。** 玩家講的話是模型無法預測的輸入,而委託模式(第十二節)讓這些話變成世界的擾動源。所以覺得「不夠隨機」時,正確的反應通常是**讓玩家的輸入影響更多東西**,而不是加更多骰子。

#### 但「別人的體驗不一樣」不是靠隨機達成的

這裡有兩件常被混在一起的事:

| | 要的是什麼 | 隨機幫得上嗎 |
|---|---|---|
| **變化** | 同一個玩家玩十次不膩 | ✅ 幫得上 |
| **差異** | 不同玩家的世界長得不一樣 | ❌ **幫不上多少** |

隨機事件是從**同一張表**抽的,兩個玩家抽的是同一個池子——玩久了會發現「這個我也遇過」。隨機給的是**排列不同**,不是**方向不同**。

**真正製造差異的是:不可逆的選擇 + 狀態會回饋進生成。**

- 玩家 A 幫陌洲保住了某段記憶 → 他記得某個人 → 那條線繼續長 → 三個月後他是一個有過去的人
- 玩家 B 沒有 → 記憶掉了 → 那個人消失 → 三個月後他是空的

這兩個世界不是「隨機地不同」,是**結構不同**,而且差距會隨時間**放大**而不是被抹平。這就是路徑依賴,而承載它的機制(`agent_state`、`open_threads`、記憶存量)**已經在 code 裡了**。

**第十一節那些設定的真正功能,就是製造分歧**——這不是巧合,是它們值得留下的原因:

- 記憶額度零和 → 你選誰留下,等於選誰被忘掉
- 晶片造不出來 → 給出去就沒了
- 基因不可逆
- 清洗一片晶片 = 抹掉一個人

**不可逆 = 路徑依賴 = 每個玩家的世界會長成不同的形狀。**

兩個實務提醒:

1. **現在還沒有「別人」。** 里程碑 0/1 是單人單 agent,差異化真正被驗證要到里程碑 3(多人)。現在該確保的是**狀態真的會回饋進生成**——T0-11 已經做了一半(`state_changes` 寫進 `agent_state`,下次 prompt 會帶)
2. **目前分歧的承載量上限很低**:`get_open_threads` 只取 3 條,第 4 條以後進不了 prompt。**線是分歧的載體,載體有上限,分歧就有上限**——這是第十節「已知待補洞」那條的另一面,做劇本層時要一起處理

### 成本

仍是**一次** LLM 呼叫,只是輸出 JSON 變胖,篇章編排丟給模型在同一個 prompt 裡做。不新增呼叫次數 = 不動搖 lazy simulation 的成本結構(第 42 行)。

### 已知待補洞(現況的,不擋 H-1)

- 第 4 條以後的線進不了 prompt,而 `offline.py` 會把不在清單裡的 id 當幻覺濾掉 → 線超過 3 條就開始靜默堆殭屍。H-1 只跑 2 個離線週期,碰不到
- 線只在下一次離線模擬時才會被關,同一個 session 內線上對話不會關線

---

## 十一、世界設定

> 這節把散在各處的世界素材(第二節的世界觀決策、腦力激盪的點子、只存在於 `persona.py` 的「半沉沒港城」)順成一個能互相解釋的整體。
> **定案程度:骨架已定,細節未定。** 專名一律原創(第 33 行的版權原則)。
> 判準沒變:**這個設定有沒有餵回劇本層?** 只增加世界廣度、不生線的設定,對週末專案是純成本。

### 一句話

**海平面上升之後的未來地球。人類沒有在前進,只是在修補。**

### 核心動詞:修補

整個世界掛在同一個動詞上,每個元素都是它的一個面:

| 問題 | 這個世界的答案 |
|---|---|
| 城壞了 | 維生系統要修 ← **陌洲的職業就是這個** |
| 身體壞了 | 換零件(機器改造) |
| 零件哪來 | 回收場 |
| 誰有權修、誰配得到零件 | 機器廠(勢力) |
| 修要用電 | 能源配給 |
| 為什麼一直壞 | 汙染(單向惡化) |
| 修不動的人怎麼辦 | 接進去(腦機接口 / 模擬倉) |

**為什麼選「修補」而不是「開拓」——這是工程理由,不只是文學品味:**

劇本層最怕線枯竭,而修補是一個**結構性無限的線來源**——維修永遠做不完,所以離線期間永遠有東西可寫。開拓型世界(去新地方、打新東西)每開一條線都要生成新內容;修補型世界可以在**同一座城裡**無限生線。這對 lazy generation 的成本結構(第 46 行)是天大的好處。

### 三環尺度 —— 同時是「邊界判定」的答案

第 136 行那題(什麼條件觸發生成新內容)可以用**環**來答,而不是用距離:

| 環 | 內容 | 生成規則 |
|---|---|---|
| **近景** | 一座半沉沒港城:回收場、機器廠、維生系統、配電所、潮線 | **預先寫定,不生成。** 里程碑 0–1 全部發生在這裡 |
| **中景** | 鄰近港城、機器廠背後的勢力、局部戰爭的前線 | **Lazy generation**:有人真的要去、或有線指向那裡,才生成 |
| **遠景** | 月球殖民地、星際、外星遺留物 | **永不生成。** 只以訊號、殘骸、傳聞、聲稱去過的人的形式存在 |

遠景永不生成是刻意的:它把敘事張力(有沒有別的地方?那邊是不是比較好?)全部拿到,而構築成本是零。而且**「去不了的遠方」在修補型世界裡比「可去的遠方」更有力量**——它是希望的來源,也是騙局的來源。

### 時間與潮汐

現實時間同步(第 86 行:層級一最划算的就是時間)。在半沉沒港城裡,同步的不只是鐘,是**潮汐**:

- 漲潮/退潮是天然的日循環,成本近乎零——一條潮位公式,連 API 都不用
- **潮汐自己會產生 deadline**:「漲潮前」是天然的時間壓力,不用硬編劇
- 離線長度有了世界內的單位:你離開了一個潮汐,還是二十個潮汐

上次真模型 playtest,模型自己長出「漲潮的預警鈴響到第三聲時,你還沒露面」——**設定跟模型的直覺對得上,這是這個世界觀可行的實測訊號**。

### 資源與經濟(世界內)

**不用抽象貨幣。** 交易單位是**配給額度**:電力、乾淨水、氧氣濾材、頻寬。

- 每一種都自帶稀缺 → 自帶線
- 額度可轉讓,所以人情與交易仍然存在,但沒有「錢」那種抽象感
- 與產品的儲值虛擬幣(第 63 行,細節在 `.private`)**完全分離**,命名不可混用
- **不叫 token**——撞 LLM token、撞儲值幣,而且會被讀成加密貨幣,直接撞上第 211 行「虛擬幣可兌現:否決」的法規紅線

### 技術軸:人 → 改造人 → 接口使用者 → 無人格機械

這是**一條連續光譜,不是四個種類**:

| 位置 | 是什麼 | 對系統的意義 |
|---|---|---|
| 人 | 沒改造 | — |
| 改造人 | 義肢、器官替換。會磨損、缺零件、要保養 | 給 `agent_state` 一個具體載體 |
| 接口使用者 | 腦機接口 / 模擬倉 | **把「離線」從系統限制變成世界設定** |
| 無人格機械 | 沒有記憶的工具 | **硬界線:不給名字、不能對話** |

最後那條界線是產品性的、不是文學性的:核心賣點是「這個 agent 是活的」,一旦玩家分不清眼前是角色還是道具,賣點就被稀釋了。

**腦機接口是整個世界觀裡回報最高的一塊**,因為它一次解釋掉三件事:

1. 真人與 AI agent 為何能在同一個世界共存(第 198 行「統一居民介面」的世界內說法)
2. 玩家離線時 agent 為何還活著——接口斷開,那邊繼續過日子
3. 第九節「人格投射」的世界內解釋

**模擬倉**是它的硬體形式,價值在給上線/離線一個具體的地點與儀式:你進倉、你出倉,出倉那一刻讀到的就是「你不在的時候」。界線是它是**進入這個世界的門**,不是世界裡的另一層假世界。

### 永久記憶 —— 把「遺忘」也變成世界設定

**不能設定成「記憶是永久的」。** 第 52 行已定案「遺忘是必要的」,理由是成本(context 塞不下,檢索要靠相關度+新近度+重要性加權)與擬人(記得一切的東西不像人)。世界若宣稱記憶永久,就是寫了一條實作履行不了的設定——玩家一定會去測,然後當場破功。

**要做就做成稀缺品。** 在一個資源全部靠配給的世界裡,記憶儲存正好是會被配給的東西:

- **遺忘不再是系統限制,而是世界設定**——跟腦機接口把「離線」變成設定是同一招的第二次應用,而且更漂亮:**agent 忘記事情不是 bug,是它買不起儲存額度**
- 它給第 49 行的**記憶分層**一個世界內的解釋:近期原始事件 = 還沒處理的;反思壓縮的洞察 = 為了省額度壓過的;核心人設 = 花代價買成永久的那幾條
- **它直接生線,而且是最好的那種線**:「有一段記憶快掉了,你要不要幫我保住?」——玩家能實際行動、有情感重量、每次的內容都不一樣,而且**玩家的選擇會真的寫進資料庫**(哪幾條被標成永久)
- 它接上第九節人格投射,以及「玩家離開或過世時怎麼辦」

**兩個要小心的地方:**

1. **不要讓玩家把它讀成技術承諾**。世界內要另取原創專名,而且要明顯是稀缺的、少量的——玩家該有的印象是「它只留得住幾件事」,不是「它什麼都記得」
2. **「記憶可買賣」是個更黑的延伸**(能不能被別人買走、讀取、竄改),直接對應第 58 行「匯入需驗簽」的世界內版本。有意思,但先不碰——這會需要一整套 canon 判定

**時機**:里程碑 2(長期性)。但它是少數幾個**近景就用得到**的高概念——一個 agent 在一座城裡的一週,完全用得到。

#### 記憶配給造成的階級

記憶一旦是配給品,貧富差距就不再是財產差距,而是**自我連續性的差距**:

| | 窮 | 富 |
|---|---|---|
| 記得多少 | 只留得住幾件事,得**定期選要丟掉什麼** | 全留 |
| 記憶的形態 | 原始事件被壓成洞察:記得「我不信任機器廠」,**但不記得為什麼** | 保有完整原始事件,查得到細節 |
| 時間跨度 | 幾個月 | **跨世代**——真正的富有不是記得多,是記得久 |
| 權力形式 | — | 知道所有舊約定、舊債、舊把柄。**家族記憶就是統治工具** |

三個直接可用的推論:

- **立場留下,理由消失。** 窮人區的仇恨代代相傳卻無法辯論——因為沒人記得起因了。這是一句話就能解釋整個社會張力的設定
- **技師的手藝不算在裡面**——見下方「知識可注入」。技能是廉價的,貴的是經歷
- **記憶是僱傭鎖鏈。** 機器廠這種勢力會提供「替我工作,我幫你保管記憶」——**離職就失憶**。這是一台非常強的劇本引擎,而且讓機器廠的權力具體化(不只是發零件)

窮人這邊會長出兩個零成本、畫面感又強的細節:

- **外部記憶**——記不住就寫下來、刻在牆上、託給別人。所以港城的牆上滿是字。一個沒有記憶的社會為什麼仍然有歷史,這句話就答完了
- **共享記憶**——幾個人合買一份儲存,彼此保管對方的事。於是窮人區的人**記得別人的人生,勝過記得自己的**

#### 知識可注入,經歷不可

**技能是廉價的。** 已經被封裝好的知識可以直接注入——怎麼修閥門、怎麼開船、怎麼配藥,買個包就會了。所以技能不是階級標記,誰都能有。

貴的是**經歷**:我跟誰一起經歷過什麼。經歷不能注入,因為注入別人的經歷不會讓你變成他,只會讓你知道那不是你的。

**這條分野正好對上現有的資料結構**,不是巧合:

| 世界內 | 系統內 | 成本 |
|---|---|---|
| 可注入的知識 | `agents` 的 persona / background,寫進 prompt | 幾乎為零 |
| 不可注入的經歷 | `memory_events` | 要儲存、要壓縮、會遺忘 |

**更重要的是它跟 LLM 的真實形狀對齊。** 模型天生就是「知道一大堆事,但不記得你」——那正好就是這個世界的設定:知識是公共的注入包,經歷是稀缺的私有物。**世界觀不需要對抗 LLM 的天性,直接把它寫成設定。** 這在整份文件裡是少見的、設定與實作同時受益的一刀。

於是階級要重寫成:**窮人失去記憶不會失去謀生能力(知識還在),而是失去自己。** 這比「忘記怎麼修東西」慘得多,也戲劇得多。

##### 但最前沿的知識注入不了

因為注入需要知識**已經被封裝**。前沿的東西還沒被封裝,它只存在於某個人的親身經歷裡。所以這兩者不是兩種東西,是**同一條軸的兩端**:

成熟知識 → 已封裝 → 可注入 → 廉價 → 人人有
前沿知識 → 未封裝 → 只能親歷或親傳 → 昂貴 → 極少數人

五個推論,每一個都直接生線:

- **權力的真正形式是「封裝權」。** 機器廠控制人的方式不是保管記憶,是**決定哪些知識被封裝成注入包**(從而變便宜)、哪些留在少數人身上。這比「離職就失憶」精準,也更陰險
- **師徒制回來了。** 前沿知識不能注入,只能跟著人學。在一個什麼都能買的世界裡,這是**唯一還需要人跟人長期相處的技術路徑**——天然的情感引擎
- **陌洲的正確定位**:他修的是**老舊**維生系統。老東西的知識早就沒人封裝了(沒商業價值),文件散佚,只剩少數人身上還留著。**他的價值不在前沿,而在被遺棄的過時知識——他是一座活的檔案館。** 這比「前沿專家」有意思得多,而且完全長在修補型世界上
- **玩家的存在本身就是稀缺資源。** 你不能給 agent 注入包(那誰都買得到),但你能給它**經歷**——而經歷是前沿知識的唯一來源。這直接支撐核心賭注:它需要你,不是因為劇情安排,是因為世界的規則就是這樣
- **知識會衰減成廉價。** 今天稀缺的,幾年後被封裝就爛大街。這給科技發展線(跨篇章進度軸)一個具體機制:**世界推進的方式,就是知識從前沿掉進注入包**

##### 創造也注入不了 —— 於是整條鏈閉合

可以注入**創造出來的結果**,注入不了**創造的能力**。因為創造要有別人沒有的輸入,而那個輸入只能是經歷。所以創造不是第四件獨立的事,它是經歷的產物。

整個世界的經濟因此收斂成一條生產線:

**經歷 → 創造 → 前沿知識 →(封裝)→ 注入包 → 廉價、人人有**

**這條線的源頭只有一個:經歷。** 這世界上每一樣廉價的東西,都曾經是某個人的親身經歷。設定到這裡就閉合了——不再是一堆設定,是一台會自己轉的機器。

四個從閉合中掉出來的東西:

- **富人有結構性弱點。** 他們保存得最好,但保存不是創造。**記得一切的人不產出新東西**——他們有存量,沒有新的經歷輸入,因為他們不必出門、不必冒險。而窮人被迫經歷。**創造來自底層**,這不是說教,是機制推出來的
- **機器廠依賴它所壓迫的人。** 它壟斷封裝權,但封裝得有東西可封,而前沿知識來自別人的創造。這個結構自己會生劇情,不用編
- **陌洲是廢墟裡的前沿。** 老知識沒人封裝,但老東西還在壞——所以修老東西**永遠在產生新的前沿知識**。他每修好一個沒人修過的故障,就創造了一次。「活的檔案館」要升級成這個
- **agent 因為玩家而獨一無二,是機制保證的,不是文案宣稱的。** 玩家給經歷 → agent 產生創造 → 那是世界上買不到的東西。這正好對上里程碑 2 要驗證的問題(記憶累積一個月後有沒有變得更有個性)——**世界設定跟驗證目標對齊了**

順帶,這條規則對世界裡的 AI agent 一體適用,所以避開了「AI 什麼都會、人類沒用」的老套。而且它仍然符合 LLM 的實際行為:模型有龐大的知識,但**這個世界此刻正在發生的事,它不知道**。

##### 一條紅線:記憶額度不可用儲值虛擬幣購買

這個設定會讓玩家自然想到「我能不能幫它買記憶」,而那條路直接通向**付費才能讓你的 agent 記得你**——情感勒索型微交易,是最糟的一種,而且會反噬「這個 agent 是活的」這個唯一賣點。

**記憶額度只能靠世界內的行動取得,與第 63 行的儲值虛擬幣完全隔離。** 這條要在經濟系統實作(里程碑三)之前就定死,事後要拆會很痛。

還要小心別把整個世界寫成社會批判寓言——階級是拿來生線的,不是拿來說教的。壓過角色本身,賣點就沒了。

### 晶片 —— 抽象的東西終於有了實體

晶片不是新概念,是**把三個已定案但沒有形體的東西物件化**:注入包、記憶儲存額度、腦機接口,原本都只是抽象規則。

物件化的價值全在劇本層:**概念不能放在桌上,晶片可以。** 「你的記憶額度不夠了」很難演;「這片晶片裡有他最後三年」可以演——可以偷、可以賣、可以壞、可以偽造、可以在回收場撿到。

**只准有一個意思**:晶片 = 儲存與封裝的實體。如果它同時還是電腦、機器人、身分證,就會稀釋成通用科幻道具,失去全部力量。

#### 關鍵決定:沒人造得出新晶片

修補型世界的必然結論——前沿製造能力早就失去了,只能回收舊的。這一刀下去,前面所有經濟設定同時收緊:

- **記憶額度是固定總量的零和。** 你要多記,就有人得少記。這比「買不買得起」殘酷得多,也戲劇得多
- **富人擁有的不是錢,是晶片存量**
- **回收場不是垃圾場,是唯一的礦**
- **機器廠的權力不是製造,是分配與清洗**——把舊晶片上的別人擦掉,重新發下去

於是整個世界的道德核心可以壓縮成一句:

> **清洗一片晶片,等於抹掉一個人。**

#### 擦不乾淨的晶片

二手晶片上會留著前一個人的碎片。這是零成本的敘事裝置,而且一次給三樣東西:

- **天然的懸念產生器**——這是誰?他怎麼了?他為什麼被擦掉?
- 它是遠景那條「解不開的外星遺留物」規則的**近景版本**:同樣是解不開的東西,但發生在你手上、這個禮拜
- 它把回收場那個劇本原型(**撿到不該撿的東西**)具體化到可以直接寫進 prompt

#### 順帶解掉「汙染的成因」

第十一節原本把汙染成因列為未定,並註明這題會決定整個世界的道德色調。晶片給了一個很強的候選:

> **汙染來自晶片的回收與清洗製程。**

閉環:記憶要晶片 → 晶片要清洗 → 清洗製造汙染 → 汙染讓一切壞掉 → 壞掉要修補 → 修補要知識 → 知識要晶片。

而它決定的道德色調是:**所有人都是共犯,不是受害者。** 窮人為了記得住而汙染,富人汙染得更多。沒有人乾淨,包括玩家自己的 agent。這比「壞人搞壞了環境」耐看得多,而且它讓每一次「幫它保住一段記憶」的選擇都帶著代價。

### 基因:改造 / 崩潰 / 長生

#### 基因改造 —— 唯一修不了的東西

跟機器改造不重複,兩者是**不同的軸**:

| | 機器改造 | 基因改造 |
|---|---|---|
| 動作 | 加裝 | 改寫 |
| 可逆性 | 可拆、可換、可修、可回收 | **不可逆** |
| 傳承 | 不傳 | 傳給下一代 |
| 與世界主軸的關係 | 完全長在「修補」上 | **違反它** |

價值就在這個違反:**在一個什麼都能修的世界裡,基因是唯一修不了的東西。**

放到注入軸上看,基因改造是注入的極端形式——**注入到不可撤回**。你可以拒絕買注入包,但拒絕不了父母替你注入的基因。於是**階級從「買得起多少記憶」變成先天的**。這是一次實質的升級:原本的階級理論上還能翻身,基因不能。

#### 基因崩潰 —— 內部的那座時鐘

汙染是**外部**單向惡化的時鐘,基因崩潰是**內部**那一座:歷代改造互相干涉、層層疊加,最後崩掉。

三件事因此扣起來:

- **富人死於自己買的東西。** 改得最多的崩得最兇。這跟「創造來自底層」是同一條底層規則的第二次現形——**存量不等於續存能力**。同一條規則在不同地方各長出一次,是世界觀真的自洽的訊號,不是重複
- **機器改造變成治療手段**。基因崩了就用機械替換 → 直接把需求打回回收場、機器廠、還有陌洲。整個近景因此更緊
- 汙染的醫療後果有了具體形狀,不再只是「環境很糟」

**必須局部化**(跟戰爭同一條戒律):文明級的緩慢滅亡只是背景新聞,不改變任何 `agent_state`。要寫成**這一週,你認識的某個人正在崩**——那才有著力點。整個世界一起絕望,劇本層拿不到任何東西。

#### 長生 —— 它會弄壞剛閉合的機器,除非走記憶那條路

**問題**:如果能長生,經歷就能無限累積,富人終將擁有最多經歷 → 他們變成創造者 → 前面「創造來自底層」的反轉直接崩掉,整條生產線失效。

**解**:不用新機制,用已經定案的那條——**記憶是配給品**。

> **長生的代價不是死亡,是連續性。**
> 你活一千年,卻只留得住最近幾十年。你變成一連串共用同一具身體的陌生人。

於是長生非但不破壞機器,反而把它焊死:**活最久的人是最不會創造的**,因為創造需要新經歷,而他早就留不住新經歷了。經歷會過期,所以無法囤積——這正是整條生產線需要的前提。

它同時是第九節「玩家離開或過世時怎麼辦」的世界內版本,以及「遺忘是必要的」最戲劇化的一次體現。

**產品面的警告**:長生**不可以變成玩家目標**。這不是養成/進度型遊戲,沒有勝利條件;一旦長生可追求,世界就變成最佳化跑步機,直接撞爛「下班隨手玩 20 分鐘」的定位。設定成:長生存在、代價明確、**沒人真的想要**。

### 兩條自己會走的軸(不需要玩家)

第十節要求有一類線是不管玩家在不在都會推進的。世界層級提供兩條現成的:

- **汙染** —— 單向惡化,一個永遠在走的時鐘。直接連到 `agent_state`(肺、水源、能不能出門)
- **科技發展線** —— 修補技術在推進。這是**跨篇章的進度軸**:arc 是一章一個母題,但章與章之間需要一個會累積的東西,否則第 10 章讀起來跟第 1 章一樣。**要慢**,推太快世界會失去穩定感

**局部戰爭**是第三條,但它屬於篇章層而非設定層:一個天然的 arc `premise` + 一個外生的 deadline。**必須局部化**——遠處的大戰只是新聞,不改變任何 `agent_state`;要做成離港城夠近、會斷補給的那種。

### 否決清單

| 方向 | 為什麼不做 |
|---|---|
| 星際 / 銀河系當**可去的地方** | 與「未來地球」骨架(第 27 行)衝突,且世界構築 agent 失去邊界 → 第 136 行變無解。降級成遠景傳說,張力照拿 |
| **可對話的**外星文明 | 等於維護第二套語言、文化、canon 與跨文明仲裁,週末專案吃不下。改成「解不開的遺留物與訊號」——**解不開本身就是永不枯竭的線來源**,成本零 |
| 世界裡再套一層虛擬世界 | 這個產品**本身**就是那個虛擬世界,唯一賣點是「這裡是真的」。再套一層等於親手提示玩家「反正都是假的」,離線敘事的重量會直接洩掉。只能當道具(agent 自己在玩的遊戲),不能當場景 |
| 世界內貨幣叫 token | 見上方「資源與經濟」 |
| 有名字、能對話的機器人 | 見上方「技術軸」 |
| 月球殖民地當**可去的地方** | 現在只有一座城,第二個地點做了是空的。留在遠景,當「有人去了沒回來」的懸念來源 |

### 還沒定的

- **港城的專名**(原創,還沒取)
- **機器廠背後是誰、配額由誰發**——這決定了第一個勢力的形狀
- ~~**汙染的成因**~~ —— **已有強候選:來自晶片的回收與清洗製程**(見上方晶片段)。道德色調隨之定為「所有人都是共犯」。還沒最終拍板,但目前沒有更好的答案
- **山海經/西遊記那條文化縱深**(第 30 行)要怎麼在未來港城裡出現而不突兀

> **尺度判準**:一個設定要能被「一個 agent 在一座城裡的一週」用到,才算現在可用。銀河系用不到,回收場天天用得到。

---

## 十二、探索與控制

### 先解一個從沒定案的矛盾:玩家控制的是誰?

文件內部一直有兩種說法並存,而且沒人發現:

- 第 12 行:「玩家離線時,**他的 Agent** 繼續在世界裡活著」→ 聽起來 agent 是玩家的化身
- 里程碑 0 實際做出來的:**你跟陌洲說話**,陌洲是獨立角色,不是你的化身
- 第 140 行半察覺到這件事(玩家自己的角色設定跟 AI 的 persona 要分開設計),但沒往下解

**「怎麼控制」這題在這個矛盾解掉之前無法回答。** 三種模型是三種不同的產品:

| 模型 | 玩家是誰 | 探索怎麼發生 | 代價 |
|---|---|---|---|
| **委託** | 一個跟 agent 有關係的外人 | 你請它去,它自己決定 | 直接控制感弱 |
| **化身** | 你就是 agent | 你下指令,它照做 | **離線敘事退化成執行報告** |
| **雙角色** | 有自己的化身 + AI 夥伴 | 各自行動 | 工作量大,里程碑 3 以後 |

### 定案建議:委託模式

理由不是偏好,是機制:

1. **它是唯一能讓離線敘事保有驚喜的模型。** 如果你控制它,回來讀到的只是自己命令的執行報告——而**驚喜是核心賭注的燃料**,化身模式親手把燃料燒掉
2. 里程碑 0 已經是這個,不用改
3. 它讓「它不照你的話做」從 bug 變成**角色**
4. 它跟第十一節咬合:玩家之所以是稀缺資源(經歷的唯一來源),前提是 agent 是**另一個人**,而他選擇重視你。化身不會「選擇」重視自己

### 控制系統:沒有移動,沒有地圖,沒有方向鍵

**探索 = 委託 + 讀回報。**

- 你不說「往東走」,你說「幫我看看回收場東邊還有沒有能用的濾芯」
- 它可能去、可能不去、可能去了撞見別的事
- **離線期間執行**,你回來才讀到結果

「怎麼控制角色」這題因此直接消失——你從來不移動任何人。這也正好是「下班 20 分鐘」要的:打字就好,沒有操作要學。

第十節的 **action 層就是委託介面**:敘事末尾那 2–3 個選項,不鎖死自由輸入。

#### 拒絕的規則(不訂會壞掉)

- 永遠服從 = 傀儡,「它是活的」這個賣點沒了
- 常常拒絕 = 沒有回應感,玩家放棄

規則:**它可以拒絕,但必須給理由,而理由必須來自它當下的 `agent_state` 或人設。** 拒絕要讀起來像角色,不像失敗。而且**拒絕本身要生線**——它拒絕是因為某件事,那件事就是新的懸念。

### AI 要怎麼自行探索

危險在於:讓 agent 自由亂走 = 成本無上限 + 一致性守不住。所以訂兩條硬規則:

1. **自主探索必須由它已有的線驅動,不是隨機漫遊。** 它缺零件 → 去回收場(既有近景,零生成成本);它的線指向中景(聽說東邊那座城有貨)→ **才觸發生成**,而且生成範圍限於那條線需要的
2. **只在離線期間探索。** 第 43 行已定案「在線期間沒有背景自動行為」,所以自主探索天然被壓縮進那一次離線呼叫裡,不會失控

**成本因此自動有上限**:探索量綁在劇本層的線上,而線有存量規則(2–5 條)。

### 兩種探索合成一件事

| | 定向 | 玩家知道嗎 |
|---|---|---|
| 玩家委託的探索 | 有 | 知道要去,不知道結果 |
| AI 自行的探索 | 無 | 回來才發現 |

**兩者都在離線期間發生,都在同一次 LLM 呼叫裡結算——所以成本不變。**

而玩家讀到的那個落差(我請它做 A,它做了 A,但順便撞上了 B)正是「它是活的」最強的證據。這不用額外機制,是委託模式白送的。

### 順帶答了「邊界判定」

第 136 行那題再補上最後一塊:**邊界不是靠距離觸發,是靠委託觸發。**

- **近景** —— 預先寫定,委託不觸發生成
- **中景** —— 玩家委託或 agent 的線指向那裡時,才生成
- **遠景** —— 委託也不生成,只會帶回傳聞

---

## 附錄:世界觀點子的原始判斷紀錄

> 保留逐條理由,避免之後有人重新提一次已經否決過的方向。上面第十一節是結論,這裡是過程。

| 點子 | 判斷 | 理由 |
|---|---|---|
| **腦機接口** | ✅ 最划算,建議採用 | 它是世界觀的**接縫填補物**:一次解釋掉「真人與 AI agent 為何能同世界共存」「玩家離線時 agent 為何還活著」(接口斷開,那邊繼續過日子),也是第九節人格投射的世界內解釋。**把「離線」從系統限制變成世界設定**——這是整個世界觀裡回報最高的一個 |
| **機器改造身體** | ✅ 採用,與腦機接口同一條技術樹 | 給 `agent_state` 的狀態變化一個具體載體:義肢磨損、缺零件、要保養、會故障。陌洲的人設(修理老舊維生系統的技師)直接接得上,而且是**天然的線產生器**——維護、找零件、故障當下要不要救別人 |
| **月球殖民地** | 🟡 可以,但延後 | 不破壞「未來地球」骨架(地球仍是主場)。價值在提供**「遠方」**:有人去了沒回來、訊號延遲幾秒的對話——都是很好的懸念來源。但里程碑 0–1 只有一座港城,第二個地點現在做是空的 |
| **星際跨越** | ❌ 建議只留成傳說,不當可去的地方 | 與第 10 行「未來地球」直接衝突,而選未來地球的理由(第 27 行:現實地理當骨架、完全避開版權、跨區域衝突好仲裁)全部會被抵銷。更實際的問題是**邊界判定**(第 136 行本來就是待解問題)——開放星際等於世界構築 agent 沒有邊界,那題會變成無解。<br>**折衷**:讓「星際」以訊號、殘骸、聲稱去過的人的形式存在。敘事張力全拿,構築成本零 |
| **銀河系** | ❌ 同「星際跨越」,同一個折衷 | 併入上一列處理:當背景與傳說,不當可去的地方 |
| **外星人** | 🟡 只做「解不開的東西」,不做可對話的種族 | 可對話的外星文明 = 要維護第二套語言、文化、canon 與跨文明衝突仲裁,週末專案吃不下。<br>**改成**遺留物、無法解讀的訊號、成因不明的結構——**「解不開」本身就是永不枯竭的線來源,而構築成本是零** |
| **戰爭** | ✅ 採用,但它屬於**篇章層**不是設定層 | 正好補上第十節最缺的東西:一個天然的 arc `premise`,加上一個**外生的 deadline**——前線不管玩家在不在都會推進。這正是「不需要玩家的線」那一類的最佳範例。<br>**但要局部化**:遠處的大戰只是新聞,不改變任何 `agent_state`。要做成**離港城夠近、會斷補給**的那種,狀態變化才有著力點 |
| **機器人** | ⚠️ 與核心賣點打架,規則要先訂死 | 世界裡已經有「有人格、有記憶的 AI agent」,再放一批「機器人」,玩家會分不清眼前這個是活的角色還是背景道具——而**「agent 是活的」正是整個產品的核心賣點**,稀釋不得。<br>**若要用**:機器人 = 沒有記憶的工具,從稱呼、有沒有名字、能不能對話上就明確區分。也可以直接收進「機器改造身體」的同一條軸線:人 → 改造人 → 無人格機械 |
| **錢幣消失、變成 token** | ⚠️ 概念可以,**名字不能用 token** | 三重撞名:世界內貨幣 / 產品的儲值虛擬幣 / LLM token,玩家與開發者都會混淆。更嚴重的是「token」會被讀成加密貨幣,直接撞上第 211 行「虛擬幣可兌現:**否決**」的法規紅線——玩家會預期能換錢。<br>**更好的走法**:世界內根本不用抽象貨幣,改用**配給額度**(電力、乾淨水、氧氣、頻寬)當交易單位。在半沉沒港城裡比抽象貨幣有畫面感得多,而且每一種額度都自帶稀缺 → 自帶線 |
| **世界裡再有一層虛擬世界** | ❌ 建議不做 | 這個產品**本身**就是那個虛擬世界,而唯一的賣點是「這裡是真的、agent 是活的」。在世界裡再套一層假世界,等於親手提示玩家「反正都是假的」——離線敘事的重量會直接洩掉。<br>技術面也麻煩:多一個不受物理約束的空間,一致性檢查(第 177 行)會失控。<br>**唯一可接受的用法**:當道具(agent 自己在玩的一款遊戲),不當場景 |
| **模擬倉** | ✅ 採用,當**玩家端**的進入方式 | 腦機接口的硬體形式,價值在給「離線/上線」一個具體的地點與儀式:你進倉、你出倉,出倉那一刻讀到的就是「你不在的時候」。比抽象的接口有畫面得多。<br>**界線**:模擬倉是**進入這個世界**的門,不是世界裡的另一層假世界——別滑坡到上一列 |
| **科技發展線** | ✅ 採用,而且是這批裡唯一直接餵長期性的 | 第十節的 arc 是「一章一個母題」,但章與章之間需要一個**會累積的東西**,否則第 10 章讀起來跟第 1 章一樣。科技發展線就是那個**跨篇章的進度軸**——世界層級的 `stage`,不是單一 agent 的。<br>而且它天生屬於「不需要玩家的線」:技術照推,不管你在不在。這把里程碑 2 的問題從「agent 有沒有變得更有個性」擴充成「**世界有沒有變得不一樣**」。<br>**要慢**:推太快世界會失去穩定感,而且每一次推進都是世界構築成本 |

### 近景素材(已全部收進第十一節)

上面那些是高概念,尺度多半超過現在能承載的。下面這批**離陌洲走路可到**,是第十一節「修補」那張表的來源:

| 素材 | 為什麼好用 |
|---|---|
| **回收場** | 最貼現有人設的地點——技師找零件就是去那裡,直接當「機器改造身體」那條線的補給站。而且回收場自帶一個劇本原型:**撿到不該撿的東西** |
| **機器廠** | 它其實是**勢力**不是地點:有工廠就有老闆、配額、誰能修誰不能修。這是第一個能穩定產出「不需要玩家的線」的組織 |
| **汙染** | 這批裡最有價值的一個,因為它是**單向惡化的背景壓力**——一個不管玩家做什麼都在走的時鐘,正是劇本層最缺的東西。而且直接連到 `agent_state`(肺、水源、能不能出門) |
| **能源** | 直接接上前面提的**配給額度**:能源就是那個交易單位,同時是港城的核心稀缺。缺電 → 維生系統停 → 技師有活幹,一條因果鏈把人設、地點、經濟串起來 |
